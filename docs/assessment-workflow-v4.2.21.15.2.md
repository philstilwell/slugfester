# Slugfester Candidate-Evidence Primary Failure Analysis v4.2.21.15.2

The bounded-evidence monolithic Primary A gate completed no valid debate. Debate 133 placed relevance/burden outside the selected burden tier's legal range; Debate 178 targeted a later selected move; Debate 182 timed out. The two returned proposals still satisfied the outer section, weight, total-move, and per-side-count structure. No retry, repair, Pass B, or score was authorized.

The repeated pattern retires monolithic partition Primary A. One response should not simultaneously curate a global inventory, resolve chronology, build target topology, and apply every rating constraint.

The successor is decomposed. A score-blind inventory-lock pass selects routes, sections, candidate IDs, global move kind, and precise propositions from the complete candidate-evidence bundle; it emits no response topology or rating. The repository then orders the locked inventory and generates schemas for two independent judgment passes. Each move's target enum contains only earlier opposing selected moves. Relevance/burden is authored as a 0–100 position within the already selected burden tier and repository-mapped to the tier's absolute range. The two judgments feed deterministic disagreement extraction, required audio verification, disputed-fields-only adjudication, and a single post-adjudication score pass.
