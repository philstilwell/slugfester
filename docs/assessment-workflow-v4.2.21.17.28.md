# Slugfester Hard-Route Disputed-Fields-Only Adjudication Preparation v4.2.21.17.28

Prepare one isolated third-pass context for each of Debates 51, 63, 90, 153, and 165. Each context contains only material disputed fields, independently anonymized candidate pairs, locked transcript evidence, and the raw verified diarized transcript for a move when audio verification was mandatory. Candidate provenance, Pass A and Pass B identities and rationales, nondisputed fields, aggregate scores, winners, and publication prose remain unavailable.

Every required pair must be selected exactly once. The adjudicator may choose only an entire candidate object; it may not average, interpolate, rewrite, repair, or invent values. Importance remains a judgment field because the score-blind inventory did not fix it. Strict burden-adjustment exclusion remains in force.

The response schema and deterministic validator must require complete dispute coverage, whole dependency-pair selection, legal candidate values, and zero scores. Passing packet preparation authorizes only a frozen adjudication execution manifest.
