# Slugfester Long-Context Partition Diagnostic v4.2.9

This retired diagnostic responds to the v4.2.8 Debate 99 timeout. It does not retry the failed whole-debate primary. Instead, repository code divides the complete compact ledger into two chronological, overlapping chunks with original event IDs and exact source rows preserved. Two isolated 5.6 Sol/low proposers inventory load-bearing candidate moves without scores, section weights, totals, winners, legacy material, publication prose, or access to one another.

The chunks jointly cover every source event and overlap across the boundary so a live exchange is not severed without context. Candidate spans remain bounded by the proposer’s supplied chunk and must pass the original source hashes, 12–100 lexical-token rule, 450-character ceiling, lexical recall, ordered coverage, speaker/side constraints, chronology, and local-response topology. The overlap may create duplicates; repository analysis records them and does not silently select or discard candidates.

This is source discovery, not a scoring pass. A pass authorizes preparation of a separately frozen integrated primary in which repository code supplies all proposals plus deterministic source-context windows around every candidate. Only that later judge may select the final inventory, define routes and weights, make ratings, and emit the v4.2.6 primary contract. The integrated output must still pass the complete original validator and compiler replay.

No retry of either proposer, score derivation, fresh gate, production mutation, or 195-debate run is authorized here. Partition and merge runtime must enter the eventual projection for debates routed through the long-context lane.
