# Slugfester Hard-Route Deterministic Final Ledger v4.2.21.17.29

Assemble one score-blind final raw-judgment ledger for Debates 51, 63, 90, 153, and 165. This stage makes no model or API calls. It revalidates both independent judgments against the complete local transcript source chain, replays deterministic disagreement extraction, rebuilds the anonymized adjudication packet and hidden provenance, validates every third-pass selection, and then applies only those selections to disputed fields.

For a nondisputed scalar field, apply the preregistered rounded mean of the two independent values. If an adjudicated dependency pair owns that scalar, the dependency selection takes precedence and suppresses the mean. Preserve no adjudication rationale in the scoring input; it remains source-audit evidence only. Keep every score, winner, commentary, AI Extension, and publication field absent.

All required audio evidence must replay from the locally saved diarized transcript and match its frozen hash. Ledger validation requires five final raw judgments, 94 disputed moves, 271 adjudicated candidate selections, 287 nondisputed scalar merge opportunities, three audio-verified moves, and zero calculated scores. A passing ledger authorizes exactly one deterministic scoring pass and nothing else.
