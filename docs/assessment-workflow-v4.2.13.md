# Slugfester Compact Finalization Smoke v4.2.13

This retired smoke test measures the downstream finalization stage without reopening participant judgment. It uses Debate 103's already locked v3.8.8 source packet and calculated ledger, which provide a complete realistic contract for participant summaries, score-calibrated critiques, representative quotations, Overall Commentary, and the visibly separate AI Extension.

One fresh 5.6 Sol/low context receives the current workflow, scoring rubric, finalization manual, closed schema, and the complete source packet. The packet already contains all 25 source-grounded moves, every route and section, both audio-verified representative quotations, and the closed calculated ledger, so raw transcript and event files are deliberately not duplicated in this downstream prose context. The participant-judgment stage retains full transcript access. The finalizer receives no legacy scorecard or prior reconstruction. Every displayed score is immutable and copied from the locked packet; no participant score is derived or revised.

The existing reconstruction validator enforces score identity, source move identity, quotation identity, critique word limits, section coverage, Overall Commentary minimums, balanced AI Extension structure, complete novelty mappings, exact AI disclosure, default-collapsed accordion metadata, the required byline, and prohibited-language exclusions.

A valid elapsed time of at most 4.5 minutes authorizes preparation—not execution—of a three-debate retired finalization gate. That threshold would make the current 195-debate conservative projection feasible below 60 hours if replicated. This smoke does not authorize new scoring, a fresh judgment gate, production mutation, or the 195-debate run.
