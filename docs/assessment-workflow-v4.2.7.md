# Slugfester Bounded Score-Blind Primary Correction v4.2.7

This retired diagnostic tests one narrowly bounded AI correction pass for a primary output that passed endpoint shape and source delivery but failed deterministic relational constraints. It does not retry the judgment from scratch.

The immutable v4.2.6 Debate 106 raw output is sorted by repository code into a non-accepted candidate. Deterministic violations report two original ordering inversions and an overloaded section with three pro moves. An isolated 5.6 Sol/low correction judge receives that candidate, the violations, complete compact ledger, packet, rubrics, manual, and schema. It receives no scores, legacy material, other judgments, control status, or publication prose.

The correction must preserve every move and every route. Within moves, only array position and `sectionId` may change; propositions, source spans, attributions, importance, burden contacts, responses, findings, charity, ratings, evidence bases, and confidence are immutable. Section metadata may change so the judge can split or reorganize sections, but section weights must still total 100 and every section must contain one or two moves per side. All other top-level fields are immutable.

The correction receives one attempt and no second correction. Repository code rejects any unauthorized field change and then applies the complete inherited primary validator. The corrected output compiles only after passing. Correction incidence and runtime must enter the eventual 195-debate projection.

A pass authorizes continuation of the remaining retired v4.2.6 contexts under the same bounded-correction policy. It does not authorize scores, legacy comparison, a fresh gate, production mutation, or the 195-debate run.
