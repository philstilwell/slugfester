# Slugfester Narrow Partition Primary Successor v4.2.21.14

This prospective successor changes only the two inner fields implicated by the v4.2.21.13.1 failures. It retains the successful nested section/side contract and makes no model or audio call.

Primary A now authors `moveKind` after seeing the complete global candidate bundle; discovery's locally proposed kind is advisory. The model emits one `specialResponseMode` enum instead of two mutually exclusive booleans. The repository expands the enum to the unchanged v4.2.20 response flags, restores the remaining immutable candidate fields, and applies the same chronology, target, evidence, burden, and rating validators.

All three Primary A contexts must rerun fresh. The valid predecessor output is a fixture only and is unavailable to the successor judges. No Pass B, audio, adjudication, score, publication, production mutation, or 195-debate run is authorized.
