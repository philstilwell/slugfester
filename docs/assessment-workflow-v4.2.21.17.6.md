# Slugfester Deterministic Final Ledger v4.2.21.17.6

The repository replays both independent raw judgments, the field-level disagreement extraction, every anonymous candidate mapping, every third-pass selection, all required audio verification, and every nondisputed rounded mean. No model call occurs during assembly.

An adjudicated dependency pair takes precedence over a nondisputed scalar mean for the same dependent field. Importance, response, charity, assessment confidence, scoring fields, and residual burden adjustments are copied only from the selected original pass. Equal semantic fields retain Pass A's wording solely as a deterministic tie rule.

Each assembled raw judgment must pass the unchanged v4.2.20 full-source validator. Only after all three ledgers replay exactly and validate may one deterministic score pass be run.
