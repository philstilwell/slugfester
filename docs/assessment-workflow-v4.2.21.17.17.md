# Slugfester Retired Transport Canary v4.2.21.17.17

Before any future held-out or production discovery launch, one isolated 5.6 Sol context must validate against the real discovery schema using retired Debate 133 material. The canary exercises a representative 52 KB transcript chunk, dynamic source-ownership bounds, the frozen-interlocutor allowlist, ChatGPT-subscription authentication, API-key removal, structured output, and the unchanged deterministic validator.

The canary receives one attempt, no retry, and a five-minute timeout. Failure stores separate stdout and stderr tails and blocks the held-out launch without consuming fresh evidence. Passing authorizes only a ramped held-out scheduler; it does not authorize judgments, scoring, publication, production mutation, or the 195-debate campaign.
