# Slugfester Hard-Route Held-Out Source Preparation v4.2.21.17.19

The five fresh partition-route debates are converted mechanically into hash-locked source packets, complete compact ledgers, and isolated score-blind discovery chunks. Every transcript event is owned exactly once; boundary context may repeat, but a candidate must begin in its chunk's owned core.

Every generated structured-output schema enforces three safeguards before model execution: candidate start events are bounded to the owned core, candidate end events are bounded to the available context, and candidate speakers are enumerated from the two frozen interlocutors. Moderator and audience material may remain visible as context but cannot be emitted as candidates. The deterministic source, boundary, speaker, and side validators remain in place as defense in depth.

Preparation reads no transcript semantically and makes no model, audio, adjudication, or scoring call. Passing authorizes only a frozen, canary-gated, ramped discovery execution manifest.
