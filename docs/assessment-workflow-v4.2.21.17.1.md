# Slugfester Independent Judgment Schema Failure v4.2.21.17.1

All six independent-judgment contexts exited in under five seconds with code 1 and no output. A bounded diagnostic probe preserved the shared OpenAI response-format error: the `uniqueItems` keyword is not permitted at the burden-adjustment array path.

This is a pre-generation schema-transport failure. No model produced or attempted a debate judgment, no transcript assessment failed validation, and no rating or score exists. The gate records zero valid contexts and does not authorize disagreement extraction.

The successor may remove `uniqueItems` from the model output schema only. Deterministic runtime validation already requires each affected string array to contain unique values, so the correction does not weaken the semantic contract. All other frozen inventory, targeting, response, burden, charity, isolation, and score-order constraints remain unchanged.

Before a successor execution manifest is authorized, the corrected schema must pass an isolated response-format preflight. This is a protocol successor, not a retry or same-protocol rerun.
