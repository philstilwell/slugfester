# Explicitly authorized post-score source correction

This is a recovery mechanism, not standing permission to rescore. The ordinary one-calculation policy remains unchanged. A run must have a recorded source-wording hold, confirmation of the word, an independent source-impact review, and explicit user approval naming the bounded new judgments and exceptional calculation.

Preserve the original source, inventory, judgments, adjudication, score input/output/attestation, prose and interrupted review records byte-for-byte. A separately hashed erratum changes one word without changing time boundaries or ownership. Two fresh isolated Sol/low contexts receive identical corrected source and the frozen rubric, without prior ratings or rationales. They replace only the named clarity value and rationale; confidence, other dimensions, burden adjustments and all other moves remain fixed.

The current implementation carries existing adjudication forward only when re-extraction produces exactly the same dispute population, triggers and options. A changed dispute stops the implementation until a separately authenticated disputes-only stage is available. It never silently keeps a stale decision or relaxes a threshold.

The existing frozen assembler remains unchanged. Its original inventory hash identifies the base evidence; the corrected ledger separately locks the erratum, user authorization, authenticated new reviews and complete corrected inventory. This makes the effective evidence the original source plus the explicit erratum, not a false claim that corrected bytes share the old hash.

If new judgments require a replacement calculation, freeze its input first and invoke the unchanged calculator once. Preserve its raw return separately. A publication copy changes only provenance status and ordinal to identify calculation 2, exceptional replacement 1. Numerical fields are never manually changed. Audit replays verify both original and replacement results against their respective inputs; replays are checks, not new scored attempts.

A fresh one-field critique repair must pass the unchanged content limits. Calculated display values are mapped mechanically. Source and calculation notes disclose the correction. Original prose is independently replayed from its authenticated repair history, and the new revision must equal that prose plus the one corrected critique, derived numbers and factual provenance notes. Every original evidence hash is checked again. Fresh rhetorical reviews use the corrected source and critique; interrupted original reviews remain preserved.

Only the standalone registry activates the separately authenticated revision. The final adapter records both score passes and locks the correction plus the original attestation. All ordinary content, historical, rendering, quality, protected-branch and deployment gates remain required.
