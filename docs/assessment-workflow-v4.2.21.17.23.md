# Slugfester Hard-Route Discovery Mechanical Recovery v4.2.21.17.23

The v4.2.21.17.20 discovery execution remains a failed gate and is never relabeled as passed. This recovery consumes its twenty untouched raw outputs only after the v4.2.21.17.21 ordering-only diagnosis and the v4.2.21.17.22 sixty-three-output retired regression have passed.

Every raw output is hash-checked against the frozen execution ledger and validated with the order-invariant simplified-discovery validator. The validator may sort only an in-memory clone of the candidate array; it cannot change a candidate field or replace raw output. Candidate compilation then uses the existing deterministic source chronology, transports every candidate without semantic deduplication, and creates sparse source context containing every cited span plus twelve flank events.

Recovery passes only if all twenty outputs validate, exactly one requires ordering canonicalization, all five compiled debate inventories contain at least eight candidates with at least four per side, and untouched versus canonically ordered inputs compile identically. Passing authorizes preparation—not execution—of two isolated independent judgment passes. It derives no rating, score, winner, or publication prose and does not authorize production or the 195-debate campaign.
