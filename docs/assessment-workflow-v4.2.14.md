# Slugfester Bounded Finalization Correction v4.2.14

This retired correction follows the fast but mechanically invalid v4.2.13 finalization smoke for Debate 103. It is a separate preregistered context, not a retry. The v4.2.13 output completed in 2.67 minutes with correct locked scores, identities, section coverage, Overall Commentary minimums, AI Extension balance, novelty structure, disclosure, byline, and accordion metadata, but 15 strings violated deterministic word-count limits.

The correction judge receives only those invalid strings and the minimum locked context needed to revise them: the two verified quotations and source moves, nine displayed moves with summaries and scores, and four AI Extension arguments with their existing titles, novelty records, and side-level thesis structure. It emits only replacement strings.

Repository code applies replacements to exactly the two quote-context paths, nine critique paths, and four new-argument text paths. All other fields remain copied from the frozen v4.2.13 output. The complete unchanged v3.8.8 reconstruction validator then rechecks score identity, source identity, section coverage, word limits, quotation identity, Overall Commentary, novelty mapping, balanced AI structure, disclosure, accordion metadata, byline, and prohibited language.

One Sol/low attempt is allowed. The combined v4.2.13 plus v4.2.14 elapsed time must remain at or below 4.5 minutes. A valid combined lane authorizes preparation—not execution—of a three-debate retired finalization gate. It does not authorize new scoring, a fresh judgment gate, production mutation, or the 195-debate run.
