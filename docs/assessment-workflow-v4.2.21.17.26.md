# Slugfester Hard-Route Disagreement and Audio Preparation v4.2.21.17.26

Compare the ten accepted independent judgments field by field without a model call. The score-blind inventory remains authoritative for routes, sections, chronology, candidate selection, proposition, source span, attribution, and move kind. Importance is a performance-judgment field: any A/B difference is disputed.

Response structure is disputed whenever the repository-derived class, legal targets, component contacts, or closed response findings differ. A within-class responsiveness difference greater than five points is disputed. Burden-contact, precision, and calibration finding differences expose their dependent scoring fields. Other scoring dimensions are disputed only when the A/B difference exceeds five points. Smaller scalar differences are retained for a deterministic rounded mean after adjudication. Rationale wording alone does not create a dispute.

Audio verification is mandatory when either independent pass assigns less than high assessment confidence or when the repository-owned attribution confidence is less than high. Derive every work item from the locked source span and locally stored timestamped transcript events. The audio queue must exactly reproduce the confidence queue identified by the independent-judgment gate. Audio must be verified before any adjudication packet containing a queued move is released.

Do not calculate aggregate, move, section, side, or debate scores during extraction. Passing this step authorizes local audio-source preparation only; audio verification, adjudication, score derivation, production mutation, and the 195-debate run remain unauthorized.
