# v3.6.3 correction-smoke assessment

## Outcome

**PASS — STRUCTURAL CORRECTION GATE.** All four isolated, subscription-backed 5.6 Terra contexts completed and passed their closed schemas plus deterministic family validators. There were zero pre-inference schema rejections, model-output retries, same-request stream recoveries, scoring fields, metered API charges, transcriptions, or production mutations. Artifact integrity passed independently.

The result authorizes preregistration only of a small retired semantic-card test with independent Terra and Sol passes. It does not authorize that test's execution, held-out access, numerical scoring, assessment prose, AI Extension generation, model batches, or production mutation.

## Correction effectiveness

- The strengthened target schema was accepted by the remote structured-output service and mechanically prevented the v3.6.2 license/contrary nullability errors.
- The clarified raw-substring rule led the diagnostic pass to expand its inferential cue; the evidence then occurred exactly once and validated.
- Reframe and burden again passed on the first inference.
- All four contexts used copied ChatGPT subscription authentication with API keys removed. Recorded metered cost was **$0**.

## Non-gating semantic monitoring

Derived decision monitoring matched 12 of 14 synthetic assertions. Every diagnostic, reframe, burden, example-boundary, scope, and contrary assertion matched. The two disagreements were both target granularity judgments:

- Terra marked whole-target contact true where the expected card treated the response as component-only contact.
- Terra treated the public-only restriction as an implicit denial of the private component; the expected card required explicit component contact and left that component at `none`.

These disagreements do not invalidate the schema smoke, but they are directly relevant to assessment accuracy. They show that the next retired test must emphasize direct-target versus component contact and must not treat implication alone as explicit component contact.

## Recommended preregistered test

Use a small, balanced retired set containing partial multi-component answers, relevant contrary material, distinct connected examples, a scope shift, defect-without-consequence, linked diagnostic consequences, linked reframes, and both burden-contact directions. Run one isolated Terra pass and one isolated Sol pass per family packet, compare only derived categorical decisions after outputs close, and keep all scores and production artifacts blocked. The test should determine whether Terra is accurate enough for routine card extraction or whether Sol remains necessary.
