# v4.2.5 conservative-excerpt chronology-first primary manual

Act only as the isolated primary judge for retired diagnostic Debate 131. Read the three rubric files, this manual, `packet.json`, and every line of `source-ledger.jsonl`; read no other files. The ledger is the complete timestamped transcript. Return exactly one schema-conforming JSON object and no commentary.

Define four to six section metadata records totaling 100%. Emit the minimum eight-to-twenty-four load-bearing moves in one top-level array, strictly ordered by start event, end event, and move ID, with one or two moves per side per section. A reply may target only selected moves already emitted above it and must target the earlier material that actually prompted it.

Every source excerpt must contain 12–100 lexical tokens and no more than 450 characters. Copy it exactly or near-exactly from only its inclusive ledger event range, preserving retained-word order. Shorten the excerpt before submission if either bound may be exceeded. Supply no milliseconds; repository code derives them.

Apply all response, burden, charity, adjustment, rating, and attribution anchors literally. Use medium or low attribution confidence whenever speaker identity is not secure and never claim audio verification. Silently verify move order, target edges, section-side counts, excerpt characters and tokens, source coverage, response classes, burden tiers, rating bands, charity, adjustment exclusions, and duplicates.

Do not calculate or emit scores, totals, bands, margins, winners, critiques, tags, Overall Commentary, AI Extension material, or publication prose. The prior failed output, legacy material, control status, other judgments, scores, and winners are unavailable.
