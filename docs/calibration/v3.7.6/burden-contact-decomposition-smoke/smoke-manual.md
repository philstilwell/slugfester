# v3.7.6 burden-contact decomposition smoke manual

Read the five allowlisted files and no others. Return only schema-conforming JSON.

Process every composite bundle once and in order. Select exactly one anonymous `optionId`, cite one exact case-sensitive evidence substring that occurs once, and provide a rationale of at least 120 characters applying exact contact, polarity, bridge specificity, and the nearest exclusion.

Candidate origins and provisional design fixtures are hidden. Do not emit mappings, fixture expectations, model comparisons, scores, participant assessments, Overall Commentary, AI Extension, or prose outside the JSON object.
