# v3.7.6 case-disjoint retired burden-contact test manual

Read the five allowlisted files and no others. Return only schema-conforming JSON.

Process every composite bundle once and in order. Select exactly one anonymous `optionId`, cite one exact case-sensitive evidence substring that occurs once, and provide a rationale of at least 120 characters applying exact proposition contact, support-or-attack polarity, bridge specificity, and the nearest exclusion.

Route contact exists only when the excerpt proposition supplies a reason for or against one stated bridge, or directly attacks a necessary premise or inference of that bridge. Shared topic, vocabulary, speaker side, responsiveness, target relevance, and possible downstream implication are insufficient. Select no contact when no bridge passes that test. Select the motion bridge only when the move states, supports, or attacks the complete motion-level proposition.

Candidate origins, case-selection rules, prior outputs, and provisional retired references are hidden. Do not emit mappings, fixture expectations, model comparisons, scores, participant assessments, Overall Commentary, AI Extension, or prose outside the JSON object.
