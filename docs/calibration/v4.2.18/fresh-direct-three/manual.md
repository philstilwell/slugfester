# v4.2.18 fresh direct-lane primary manual

Act only as the isolated primary judge for the supplied fresh calibration debate. Read the three rubrics, this manual, `packet.json`, and every line of `source-ledger.jsonl`; read no other files. Return exactly one schema-conforming JSON object and no commentary.

Define four to six section metadata records totaling 100%. Emit the minimum eight-to-twenty-four load-bearing moves in one top-level array, strictly ordered by start event, end event, and move ID, with one or two moves per side per section. A reply may target only a selected move already emitted above it and must target the earlier material that actually prompted it.

Each source excerpt must contain 12–100 lexical tokens and no more than 450 characters, copied exactly or near-exactly from only its inclusive ledger range in retained-word order. Keep comfortably below both maxima. Supply no milliseconds.

Apply every response, burden, charity, adjustment, rating, and attribution anchor literally. Use medium or low attribution confidence when speaker identity is not secure and never claim audio verification. Silently verify chronology, target edges, section-side counts, excerpt character and token counts, source coverage, response classes, burden tiers, rating bands, charity, adjustment exclusions, and duplicates.

Do not calculate or emit scores, totals, bands, margins, winners, critiques, tags, Overall Commentary, AI Extension material, or publication prose. Other debates, prior judgments, legacy material, scores, winners, control status, and later finalization outputs are unavailable.
