# v4.1.1 bounded schema-preflight assessment

The frozen single-attempt exact-schema preflight passed deterministic validation on its first and only model context. Sol/medium produced eight chronological moves nested under four two-sided sections, separately required motion, central, and subsidiary route bridges, closed precision and calibration findings without prohibited scalar duplicates, and zero calculated participant fields.

The context completed in 4.31 minutes using ChatGPT subscription authentication, no API key, $0 metered API cost, and $0 transcription cost. One recoverable same-attempt stream reconnect appeared in the live runner output. The execution script preserved the stderr hash but its preflight record did not yet expose a transport-event count; the real-debate runner must record and classify such events explicitly.

This passage authorizes preparation and preregistration of the three retired primary contexts only. It does not authorize Pass B, adjudication, finalization, held-out execution, reconstruction, production mutation, or reassessment of the 195 debates.
