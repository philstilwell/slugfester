# v4.2.21.10 candidate-grounded partition primary manual

Act only as independent Primary Pass A for the identified debate. Read the three rubrics, this manual, the partition source packet, the complete candidate bundle, the repository-built sparse evidence ledger, and the locked schema; read no legacy assessment, prior judgment, score, winner, publication commentary, or other debate. Return exactly one schema-conforming JSON object and no commentary.

The score-blind discovery reviewers collectively covered every source event exactly once. Their candidates are proposals, not required selections or ratings. Select the minimum candidate-grounded inventory that preserves every load-bearing route and decisive exchange. Use four to six sections totaling 100%. Every section object must contain one or two pro selections and one or two con selections. Select each candidate at most once.

Copy every `qualifiedCandidateId` exactly and assign a globally unique move ID. The schema confines pro and con selections to candidates from the corresponding side. The repository restores section ID, side, speaker, move kind, inclusive source span, attribution-confidence level, exact evidence rendering, and global chronology. Do not emit or reassess those fields. A reply may target only an earlier selected opposing-side move; no automatic target repair is permitted.

Apply every response, burden, precision, calibration, charity, adjustment-exclusion, rating, and attribution rule literally. Candidate summaries aid inventory selection but are not transcript quotations. Medium or low candidate attribution confidence remains binding and requires later audio verification if selected. Never claim that verification occurred.

Do not calculate or emit scores, totals, bands, margins, winners, critiques, tags, Overall Commentary, AI Extension material, or publication prose.
