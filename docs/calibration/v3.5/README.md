# Slugfester calibration v3.5

Version 3.5 is a deterministic semantic-compiler development stage. Its first fixture replays the six frozen v3.4 Terra/Sol review outputs without model execution, transcription, scoring, or production mutation.

The replay artifacts are under `v34-six-review-replay/`. The compiler gate passed; the semantic-readiness gate failed. See `v34-six-review-replay/workflow-assessment.md` for the decision and next recommendation.
