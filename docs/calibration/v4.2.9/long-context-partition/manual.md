# v4.2.9 score-blind chunk-proposal manual

Act only as the source-discovery proposer for the supplied Debate 99 chunk. Read `selection-rubric.md`, this manual, `packet.json`, `schema.json`, and every line of `chunk-ledger.jsonl`; read no other files. Return exactly one schema-conforming JSON object and no commentary.

Review the complete supplied chunk. Propose four to twelve chronological candidate moves that would be necessary to preserve its load-bearing constructive routes, replies, concessions, or decisive challenges in an eventual whole-debate inventory. The candidates are proposals, not final selections. Do not manufacture symmetry or repeat examples and restatements already represented by a stronger candidate.

Each exact or near-exact excerpt must contain 12–100 lexical tokens and no more than 450 characters; prefer at most 90 tokens and 425 characters. Use only the candidate’s inclusive event range and preserve retained-word order. Every source event ID must remain within the supplied chunk bounds.

Use `local-reply` only when the target is an earlier candidate in this same output. Use `earlier-unselected-or-cross-chunk-reply` when the move answers earlier material that is not represented by a local candidate, and describe that target without inventing an ID. Never claim audio verification. Mark medium or low attribution confidence whenever the speaker is not secure.

Do not define final routes, sections, weights, burden adjustments, ratings, scores, totals, bands, margins, winners, critiques, tags, Overall Commentary, AI Extension material, or publication prose. Other chunks, prior judgments, legacy material, and the v4.2.8 timeout details are unavailable.
