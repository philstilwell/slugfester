# v3.7.3 atomic packet-development assessment

## Decision

**LOCAL PACKET PASS — MODEL EXECUTION BLOCKED.** The atomic correction-smoke packets, closed schemas, anonymous candidate maps, and deterministic dry fixtures are complete. They cover all eight v3.7.2 bundles in six planned initial contexts and contain no incoherent candidate tuple.

No model context was run. The execution runner, deterministic bundle-disagreement extractor, dispute-only adjudication runner, and final analyzer must be implemented and frozen before the correction smoke may execute.

## Packet result

- Debates: **62, 154, and 185**, including the multi-speaker case.
- Atomic bundles: **8**.
- Initial contexts planned: **6**—two isolated Sol contexts per debate.
- Candidate counts: two to four coherent anonymous options per bundle.
- Target contrary judgments requested from the model: **0**; they will be derived mechanically.
- Diagnostic and reframe candidates: complete coherent tuples only.
- Dry contexts validated: **6 of 6**.
- Speaker-attribution confidence: high for every bundle; no audio prerequisite triggered.
- Model contexts, transcription calls, and metered cost: **0 / 0 / $0**.

## Frozen future threshold

A future execution of this exposed-case correction smoke must achieve:

- six of six valid initial contexts;
- at least **7 of 8** complete initial bundle agreements;
- zero invalid initial bundles;
- third-pass review of disputed bundles only;
- two agreeing votes for all eight final bundles;
- zero unresolved bundles, retries, repairs, score fields, metered API cost, or transcription cost.

These thresholds are now frozen before execution. Passing would authorize only preregistration of a disjoint retired atomic-bundle test because all eight correction-smoke bundles are exposed development material.

## Quality assessment

The packet layer now matches the semantic unit being evaluated. It cannot produce the v3.7.1 `true / true / none` reframe tuple, cannot let a model contradict a derived contrary disposition, and will compare one complete bundle per substantive judgment.

The remaining risk is model repeatability, not packet coherence. The saved scalar outputs yielded only 5 of 8 atomic agreements, so a new run should not be treated as routine or promoted post hoc. The correction smoke is justified only as a test of whether the redesigned choice unit materially improves isolated-pass stability.

## Next step

Implement the subscription-safe execution, deterministic disagreement extraction, dispute-only third pass, and analyzer against the frozen manifest. Before running them, provide a fresh cost estimate. Do not change packets, option ordering, thresholds, or source hashes after model execution begins.
