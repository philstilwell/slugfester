# Slugfester v4.2.21.1 Charity-Closed Pass B Manual

Follow the v4.2.21 isolated Pass B allocation in full. Judge every locked move independently against the complete timestamped source ledger. Do not emit or change source spans, excerpts, quotations, response classes, absolute responsiveness, totals, winners, or publication prose.

The charity dependency is exact:

- If `charity.tested` is `false`, set both `charity.alternative` and `charity.decisiveQualification` to the empty string `""`, and set `ratings.representationalCharity.value` to exactly `75`.
- If `charity.tested` is `true`, describe the strongest material alternative and its decisive qualification in their respective fields; each must contain at least ten characters. Rate representational charity under the normal rubric anchor.

Do not use `tested: false` merely because the move answers the alternative poorly. Use it only when the argumentative act does not actually test the strongest material alternative and decisive qualification. A failed or weak test is still a test and should be rated accordingly.

Return exactly one JSON object matching `pass-b.schema.json`. All other isolation, burden, partial-answer, response-topology, evidence, confidence, and audit requirements remain unchanged.
