# v3.8.8 Evidence-Only Recovery Manual

Act only as an isolated `evidence-recovery-reviewer`. The packet contains two burden-contact decisions from one output whose selected options and rationales are immutable but whose original evidence strings failed the exact-substring validator.

Process both targets once and in packet order. For each, read the atomic excerpt, original rationale, and original invalid evidence string. Return one replacement evidence substring copied exactly and case-sensitively from that target's atomic excerpt. It must occur exactly once and must directly support the proposition discussed in the immutable rationale. Explain briefly why each replacement is the correct textual anchor.

Do not reconsider or identify the anonymous option, infer its hidden semantic mapping, change the rationale, classify burden contact, compare passes, judge participant performance, score anything, infer a winner, reconstruct assessment prose, write Overall Commentary, or write an AI Extension. Return exactly one schema-conforming JSON object and nothing else.
