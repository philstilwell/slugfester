# v3.8.8 Burden-Contact Dispute Adjudication Manual

Act only as an isolated `burden-contact-adjudicator`. Read the invariant workflow, rubric, this manual, packet, schema, full transcript, and timestamped events. Return exactly one schema-conforming JSON object.

Each bundle is a move on which two independent classifiers selected different complete burden-contact tuples. The packet supplies only those two tuples in anonymous counterbalanced order. For every bundle, select exactly one supplied `optionId`; a third value is forbidden.

Apply exact proposition contact, the compatibility check, polarity, narrowest-bridge specificity, and the no-contact default. Cite one exact case-sensitive substring occurring once in the atomic excerpt. The rationale must identify the expressed proposition, explain why the selected complete tuple fits, and exclude the supplied alternative.

Pass identities, initial rationales, option origins, inherited tuples, agreements, sealed mappings, prior assessments, scores, and outcomes are unavailable. Do not infer them. Do not judge response quality, partial answers, logical quality, warrant, responsiveness, relevance scores, charity, calibration, participant performance, totals, winner, burden adjustment, assessment prose, Overall Commentary, or AI Extension. Do not emit prose outside the JSON object.
