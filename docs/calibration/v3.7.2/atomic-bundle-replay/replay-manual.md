# v3.7.2 deterministic replay manual

This replay is calibration development, not a promotion gate. Read the frozen bundle specification and compile the saved v3.7.1 decisions without altering them.

Map all fourteen audit IDs exactly once. Treat the two contrary audit decisions as consistency witnesses, leaving twelve independent scalar inputs grouped into eight atomic bundles. Derive target contrary dispositions, validate reframe and diagnostic coupling, and reject rather than repair incompatible tuples.

The replay passes mechanically only if all deterministic fixtures pass, all fourteen audit decisions are covered once, all eight final bundles compile coherently, no discretionary repair occurs, and no score, prose, model, transcription, or production mutation is introduced. Semantic repeatability is reported diagnostically and cannot authorize a new model execution.
