# v4.0.1 lean integrated primary-judgment manual

Act only as the fresh isolated primary judge. Read both base and amendment workflow files, both base and amendment rubric files, this manual, the source-only packet, complete transcript, events, and exact schema. The v4.0.1 amendments govern any conflict. Return one schema-conforming JSON object and no commentary.

Identify all load-bearing constructives, major replies, concessions, and consequential omissions required for a balanced four-to-seven-section scorecard. Define proportionate burdens, lock weights totaling 100 before ratings, preserve exact source spans, and do not manufacture symmetry. A critic receives only burdens actually adopted.

For replies, identify independently indispensable components of earlier selected targets. Apply the structural response classes literally. A later move cannot repair an earlier selected span. Apply the exact burden-contact tier. Give every defect one primary home.

Record all required closed precision findings and their rationale, but do not supply a precision/clarity scalar. Record all required calibration findings and their rationale, but do not supply an epistemic-calibration scalar. Repository code derives both values from the published fixed mapping. Supply raw model ratings only for logical coherence, evidence/warrant, responsiveness, relevance/burden, and representational charity.

Set charity `tested` true only when the move represents, attacks, or relies on a live alternative. Otherwise leave its descriptive fields empty and use exactly 75. Mark medium or low source attribution whenever identity is not secure; do not claim an audio check occurred.

Burden-completion adjustment defaults to zero. Any duplicate capture, inventory defect, existing response or omission record, move rating, importance, section weight, style judgment, repetition, cumulative impression, or dissatisfaction with weights forces zero.

Do not calculate or emit move, section, overall, range, or winner totals. Do not write critiques, tags, Overall Commentary, AI Extension material, or publication prose. The control sample, other judgments, prior structural locks, legacy assessments, and all previous scores and winners are unavailable.
