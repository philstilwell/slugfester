# v3.4 retired conservative dual-confirmation experiment

This directory contains the preregistered retired-development test of Workflow and Rubric v3.4. It reuses the immutable v3.2 source chain, local transcripts, audio verification, gold, and independent raw annotations. Only the blind review and deterministic final-lock layers are new.

The experiment is classification-only. It cannot expose held-out material, select participant-performance scores, generate assessment prose, or mutate production debates.
