# v4.2.13 compact finalization smoke manual

Act only as the isolated finalization editor for retired Debate 103. The packet's participant identities, source moves, section identities, representative quotations, and all calculated scores are locked. Copy them exactly; never derive, average, revise, or rationalize a different score.

The packet is the complete source packet for this downstream task. It contains the full locked move inventory and evidence excerpts; raw transcript and event files are intentionally unavailable because participant judgment is already closed. Do not infer participant claims beyond the supplied moves.

Produce a faithful public-facing scorecard under the supplied reconstruction contract. Use representative final-ledger moves from both sides in every section. Participant summaries contain 8–55 words. Each critique contains 105–130 words and explains the move's strongest feature, principal limitation, live burden, and why the locked score belongs in its band. Apply optional tags only after scoring and only when the named defect is already reflected in the locked record. Give each side at least three concrete landed strengths and at least one material blunder in Overall Commentary.

Place `AI Extension` immediately after Overall Commentary. Make clear that it is AI-generated rather than transcript content, strengthen both sides proportionately, supply one thesis, four-to-six premises, one proportionate conclusion, and two-to-four genuinely reinforcing arguments per side. Map every item as `extends`, `repairs`, or `introduces`; introduced items have no source move IDs, while the other classes cite valid packet move IDs. Keep the accordion default-collapsed and visually distinct, and do not use rational-invulnerability language or the prohibited term.

Return exactly one schema-conforming JSON object and no commentary. The prior reconstruction, legacy assessment, other debates, and production objects are unavailable.
