# v4.0 lean schema-preflight failure

The frozen one-attempt schema preflight failed and cannot be retried or counted toward passage. The Sol endpoint accepted the exact schema and returned a complete eight-move artifact in 3.00 minutes, but deterministic semantic validation rejected move 7 because `warrantFit: slightly-overstated` permits epistemic calibration only from 70–79 while the model supplied 84.

The failure is not a source, transport, attribution, response-component, burden-contact, precision, charity, adjustment, or JSON-schema failure. It exposes a redundant-choice defect in the proposed lean schema: the model selects a closed precision/calibration finding and separately selects a scalar that repository code can derive from that finding. Universal consensus previously masked or adjudicated this kind of internal inconsistency; a lean primary pass should prevent it structurally.

The required amendment is prospective, not a normalization of this output:

1. remove model-supplied precision/clarity and epistemic-calibration scalar values;
2. retain their closed findings and evidence rationales;
3. derive the two scalar values mechanically from a published fixed mapping;
4. update the schema, fixtures, calculator adapter, workflow, and manual under a new protocol revision; and
5. freeze a new one-attempt synthetic preflight before any retired debate context.

This attempt used ChatGPT subscription authentication, no API key, no retry, $0 metered API cost, and $0 transcription cost. It authorizes no real-debate execution, score derivation, reconstruction, held-out gate, or production mutation.
