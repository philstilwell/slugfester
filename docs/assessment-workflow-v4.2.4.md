# Slugfester Screened Chronology-First Fresh-Six Workflow v4.2.4

This prospective gate inherits v4.2.3's compact transport, chronology-first primary topology, judgment anchors, downstream consensus, audio, compression, scoring, and acceptance rules. Its only substantive change is a corrected and tested source-only topic classifier following the rejected v4.2.3 sample.

## Screened fresh selection

The selector and classifier are committed before any candidate ranks or identities are inspected. Every debate previously retired, developed, calibrated, or revealed—including all six rejected v4.2.3 identities—is excluded. Selection uses only source metadata and local manifest duration; legacy assessments, scores, winners, critiques, and prior judgments remain unavailable.

The classifier gives resurrection/history highest priority, then evil/hiddenness, morality/ethics, mind/agency, science/origins, and general theism/religion. Evil terms are evaluated before mind terms, and bare `soul` is not a mind/agency token. Tests require the v4.2.3 false-positive motion to classify as evil/hiddenness and explicit free-will, consciousness, mind, personal-identity, agency, and mental-causation motions to remain eligible for mind/agency.

The frozen tuple has one debate from each substantive family, at least one duration under 90 minutes, one from 90 through 120, and one over 120, with no more than three over 120. A fixed v4.2.4 salt ranks family and debate ID only; deterministic dynamic programming minimizes aggregate within-family rank and then family-order debate ID. A post-freeze source-only screening must pass before packet preparation.

## Judgment and downstream order

The model receives one complete lossless timestamped JSONL ledger plus the three rubrics, consolidated manual, source-only packet, and schema. It receives no duplicate transcript/events, earlier output, legacy material, score, winner, control status, or publication prose. Section metadata is separate; one top-level move array is source-chronological, and replies can target only earlier emitted selected moves. All inherited source, response, burden, charity, adjustment, attribution, and rating validators remain controlling.

Each Sol/low primary gets one attempt, no retry or normalization, and a 30-minute cap. Repository code derives times. Medium/low attribution requires audio before Pass B. Triggered and control debates receive isolated Sol/high review, deterministic disagreement extraction, disputed-field adjudication, universal score-blind compression audit, final-ledger lock, and only then scoring. Legacy comparison follows immutable scores; AI Extension remains outside scoring.

The gate must pass all six contexts, every downstream duty, the inherited legacy thresholds, and the 52-hour central/60-hour conservative projection. A failure is preserved unchanged. A pass authorizes only preparation of a preregistered held-out end-to-end gate; the 195-debate run still requires that held-out pass and explicit editorial promotion.
