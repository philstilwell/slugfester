# Slugfester Held-Out Source Preparation v4.2.21.17.10

All five held-out debates are converted mechanically into hash-locked source packets, complete compact ledgers, and isolated discovery chunks. Direct debates are split into two chunks so the same score-blind candidate-discovery contract applies uniformly to both routes; partition debates use the established event and byte ceilings.

Every source event is owned exactly once, boundary context may repeat, and a candidate's start event must lie in its chunk's owned core. Preparation reads no transcript semantically, makes no model or audio call, and authorizes only a frozen discovery execution manifest after deterministic replay passes.
