# Slugfester Fresh Direct-Lane Primary Execution v4.2.18.2

This stage executes the corrected v4.2.18.1 sample of fresh Debates 102, 107, and 126. Each debate receives one fresh isolated ChatGPT-subscription 5.6 Sol/low primary judgment with the complete lossless timestamped source ledger, locked packet, three current judgment rubrics, consolidated manual, and closed output schema. Other debates, the hidden control identity, legacy assessment material, calculated scores, and publication prose are unavailable.

The response schema does not impose a maximum string length on source excerpts, preventing structured generation from clipping an excerpt mid-word. The manual and unchanged source validator still reject any intact excerpt over 450 characters or outside 12–100 lexical tokens. Every output receives deterministic source-span, chronology, burden, response, rating, charity, adjustment-exclusion, schema, and compiler-replay validation.

All three preregistered contexts are attempted independently. Each receives one attempt, no retry, no correction, no normalization, and a 15-minute timeout. A failure in one context does not suppress the other frozen contexts. The gate requires all three outputs to validate, each context to complete within 10 minutes, and the mean to be at most 7.75 minutes. Transport recovery above the fixed hard limit invalidates that context.

A pass authorizes preparation—not execution—of repository-owned provisional score derivation, deterministic risk-trigger extraction, and audio verification for every medium- or low-attribution move. Debate 107 remains the hidden forced control for later isolated Pass B. A pass does not validate the long-context lane, authorize score publication, mutate production, or authorize the 195-debate run.
