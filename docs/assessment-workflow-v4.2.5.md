# Slugfester Conservative-Excerpt Diagnostic v4.2.5

This retired diagnostic addresses only the v4.2.4 mismatch between the endpoint's 600-character excerpt ceiling and the independent 100-token ceiling. It retains compact transport, chronology-first moves, all judgment anchors, source validation, one attempt, no retry, no normalization, and every score and legacy boundary.

The endpoint schema now limits each excerpt to 450 characters. The required 12–100 lexical-token range, 0.80 lexical recall, 0.80 ordered coverage, inclusive source-event range, and repository-owned timestamp compilation remain unchanged. The system does not truncate or repair an output.

Debate 131 is retired and is the sole model diagnostic. The isolated 5.6 Sol/low judge receives the complete compact ledger, three rubrics, consolidated v4.2.5 manual, packet, and enforced schema. It does not receive the v4.2.4 raw output, failure analysis, legacy content, scores, or other judgments. The context receives one attempt and a 30-minute cap through ChatGPT subscription authentication with API keys removed.

The diagnostic passes only if the output and every inherited deterministic validator pass unchanged and compiled timestamps replay exactly. A pass authorizes preparation of another disjoint fresh-six gate; a failure stops development. No score, winner, legacy comparison, publication prose, AI Extension material, production mutation, or 195-debate execution is authorized.
