import fs from 'node:fs';
import path from 'node:path';
import assert from 'node:assert/strict';
import {execFileSync} from 'node:child_process';
import {debates} from './src/data/debates.js';
import {fileRecord,sha256,serializedJson,STANDALONE_PROTOCOL_ID} from './scripts/lib/assessment-production-standalone-debate-v1.mjs';
import {validateEditorScopedSource} from './scripts/lib/assessment-editor-scoped-source.mjs';
const args=process.argv.slice(2);assert.deepEqual(args.slice(0,2),['--debate','264']);assert.equal(args[2],'--emit');assert(args[3]);
const number=args[1],videoId='BiwLrxPb1fE',cache=`.assessment-cache/captions/${videoId}`;
const registryPath='docs/assessment-production/standalone-debates-v1/registry.json',registry=JSON.parse(fs.readFileSync(registryPath));
const latest=registry.debates.at(-1),precedent=JSON.parse(fs.readFileSync(`${latest.root}/manifest.json`));
assert.equal(Number(latest.debateNumber)+1,Number(number));assert.equal(Math.max(...debates.map(d=>Number(d.number)))+1,Number(number));
const root=`docs/assessment-production/standalone-debates-v1/debate-${number}`;
const git=(...a)=>execFileSync('git',a,{encoding:'utf8'}).trim(),head=git('rev-parse','HEAD');
assert.equal(head,git('rev-parse','main'));assert.equal(head,git('rev-parse','origin/main'));assert.equal(git('branch','--show-current'),`codex/debate-${number}`);
const read=p=>JSON.parse(fs.readFileSync(p)),cm=read(`${cache}/manifest.json`),events=read(`${cache}/events.json`);
assert.equal(cm.videoId,videoId);assert.equal(cm.track.languageCode,'en');assert.equal(events.length,cm.eventCount);
for(const[f,k]of[['captions.srv3.xml','rawCaptionSha256'],['events.json','normalizedEventsSha256'],['transcript.txt','transcriptSha256']])assert.equal(sha256(fs.readFileSync(`${cache}/${f}`)),cm[k]);
assert(events.every((e,i)=>e.text.trim()&&Number.isInteger(e.startMs)&&Number.isInteger(e.durationMs)&&(!i||e.startMs>=events[i-1].startMs)));
const excludedIntervals=[
 {intervalId:'mutual-proof-interruption',startMs:2023200,endMs:2038679,reason:'Exclude the moderator’s claim that neither position can be proved and the immediate procedural response about continuing the point. Dawkins’s following complexity argument resumes his independently developed account of brains and cells; it is not credited as a rebuttal to the host.',dependentRepliesReviewed:true,noCreditOrPenalty:true},
 {intervalId:'mutual-proof-and-training-framing',startMs:2154800,endMs:2216520,reason:'Exclude the host’s repeated mutual-proof assertion, school-quality alternatives, and topic transition. The subsequent descriptions of scientific explanation independently develop the already exchanged organizational and consciousness arguments, and are not credited as answers to the host’s claims.',dependentRepliesReviewed:true,noCreditOrPenalty:true},
 {intervalId:'persuasion-intention-free-will-and-poll',startMs:3030680,endMs:3332960,reason:'Exclude the entire host-led challenge about intentional influence changing opponents’ minds, Chopra’s dependent conditioning and intention reply, both speakers’ brief free-will exchange, the host’s popularity challenge and Dawkins’s response, and the resulting audience poll and transition. This intentionally removes a unique moderator-initiated argumentative branch under the explicit debate-specific approval.',dependentRepliesReviewed:true,noCreditOrPenalty:true},
 {intervalId:'host-science-spirituality-summation',startMs:3933400,endMs:4056079,reason:'Exclude the host’s science-versus-spirituality summation and complete mixed transition into the religion round, conservatively including overlapping boundary captions. The following religious-benefit and harm statements answer the neutral preannounced topic, not the host’s epistemic summation; neither is credited for answering that summation.',dependentRepliesReviewed:true,noCreditOrPenalty:true}
].map(x=>({...x,durationMs:x.endMs-x.startMs}));
const assessedWindow={startMs:773680,endMs:4347520,durationMs:4347520-773680};
const excludedDurationMs=excludedIntervals.reduce((s,x)=>s+x.durationMs,0);
const disclosure='This assessment covers the independent exchanges between Deepak Chopra and Richard Dawkins. By explicit editorial approval, Andrés Roemer’s substantive interventions and their directly dependent replies are excluded, including the moderator-led persuasion and free-will exchange. Neither debater receives credit or penalty for the excluded material; this is not an assessment of every contribution in the recording.';
const scope={protocolId:'editor-approved-moderator-exclusion-v1',explicitDebateSpecificApproval:true,videoId,userApproval:'Proceed. Don’t interrupt to ask for future approvals unless absolutely necessary.',approvedProposal:'Assess the two debaters while explicitly excluding the moderator’s arguments and directly dependent replies.',originalRecordingIsStrictlyDyadic:false,automaticPrimarySpeakerExceptionClaimed:false,readerDisclosureRequired:true,requiredReaderDisclosure:disclosure,assessedWindow,excludedIntervals,excludedDurationMs,excludedShareOfAssessedWindow:excludedDurationMs/assessedWindow.durationMs,scopeLimitation:'Exact conservative mixed-caption exclusions are broader than pure host speaking time. The excluded share is not represented as a measured host-advocacy share. The automatic 5% exception is not claimed.'};
const identity={debateNumber:number,debateId:'chopra-dawkins-consciousness-purpose-spirituality-2013',videoId,canonicalUrl:`https://www.youtube.com/watch?v=${videoId}`,sourceTitle:cm.title,eventYear:2013,title:'Deepak Chopra vs Richard Dawkins — Consciousness, Cosmic Purpose, and Spirituality (2013)',label:'Consciousness, purpose, and spirituality',topicCategory:'mind-consciousness-free-will',motion:'Does spirituality provide knowledge of consciousness, cosmic purpose, and human well-being beyond naturalistic science?',pro:{speaker:'Deepak Chopra',position:'Spiritual nondualism'},con:{speaker:'Richard Dawkins',position:'Scientific naturalism'},moderator:'Andrés Roemer',dyadic:true,editorApprovedScope:scope};
assert(!debates.some(x=>JSON.stringify(x).includes(videoId)||x.id===identity.debateId||(/Chopra/.test(JSON.stringify(x.sides))&&/Dawkins/.test(JSON.stringify(x.sides)))));
const files=new Map(),put=(p,v)=>{const bytes=typeof v==='string'?v:serializedJson(v);if(fs.existsSync(p))assert.equal(fs.readFileSync(p,'utf8'),bytes,`Refusing to overwrite ${p}`);files.set(p,bytes);},rec=p=>{const b=Buffer.from(files.get(p)??fs.readFileSync(p));return{path:p,sha256:sha256(b),bytes:b.length};};
const at='2026-09-19T20:31:55.000Z',model={model:'gpt-5.6-sol',displayLabel:'5.6 Sol',reasoningEffort:'low',authentication:'ChatGPT subscription'};assert.deepEqual(model,precedent.modelSettings);
const controls=Object.fromEntries(Object.entries(precedent.controlLocks).filter(([k])=>k!=='primarySpeakerScopeException').map(([k,v])=>[k,fileRecord(v.path)]));controls.editorScopeValidator=fileRecord('scripts/lib/assessment-editor-scoped-source.mjs');
const recoveryRoot='output/transcribe/debate-264-source';
const recovery={status:'two-successful-calls-no-retries',estimatedCostUsd:0.0494,maximumPossibleEstimatedCostUsd:0.0494,billingConfirmed:false,sourceAudio:fileRecord(`${recoveryRoot}/${videoId}.webm`),sourceAudioIsOriginalNotDubbed:true,fullAudioDurationSeconds:4408.701,attempts:2,retries:0,directListeningPerformed:false,plan:fileRecord(`${recoveryRoot}/preflight-plan.json`),clips:['opening','poll'].map(x=>({response:fileRecord(`${recoveryRoot}/${x}-response.json`),execution:fileRecord(`${recoveryRoot}/${x}-execution-result.json`)})),review:'Recovered Spanish introductions, format, preannounced topics and polling instructions. The later Spanish poll gap is inside the entire editor-excluded branch, so its words are not assessed. Raw paid results remain ignored and are not presented as timed speaker verification.'};
put(`${root}/source/recovery-provenance.json`,recovery);put(`${root}/source/editor-scope.json`,scope);put(`${root}/source/reader-scope-disclosure.json`,{debateNumber:number,debateId:identity.debateId,requiredReaderDisclosure:disclosure});
const authorization={schemaVersion:'1.0-standalone-debate-authorization',protocolId:STANDALONE_PROTOCOL_ID,status:'authorized-and-frozen',authorizedAt:at,userAuthorization:'User approved the disclosed debate-specific moderator exclusion after source eligibility review, and authorized routine assessment, bounded repairs, isolated reviewers and quality-gated publication without repeated basic approvals. Exhausted attempts, score changes and cost-cap expansion remain unauthorized.',repositoryBaseline:{branch:git('branch','--show-current'),head,localMain:head,originMain:head,clean:true,isolationReason:'Clean synchronized main verified before this dedicated worktree was created; preflight source cache retained and unrelated parent work untouched.'},identity,execution:{assessmentModel:model.model,recordedDisplayModel:model.displayLabel,reasoningEffort:model.reasoningEffort,authentication:model.authentication,primaryJudgments:2,primaryContextsFreshAndIsolated:true,adjudicationContextFreshAndIsolated:true,publicationContextFreshAndIsolated:true,oneDeterministicScorePass:true,modelAuthoredTotalsPermitted:false,manualScoreChangesPermitted:false},cost:{publicCaptionAcquisitionCostUsd:0,localAndSubscriptionIncrementalCostUsd:0,paidTranscriptionUsed:true,knownPaidCostEstimateUsd:0.0494,maximumPossiblePaidCostEstimateUsd:0.0494,standingPaidTranscriptionMaximumUsd:1,fullEstimateRequiredBeforePaidCall:true,otherPaidServicesPermitted:false},repairPolicy:{judgmentSubstanceMutableAfterAcceptance:false,resolvedLedgerMutableAfterScorePass:false,calculatedScoresMutable:false,publicationRepairMaximumWritableFields:2,attemptsPerPublicationRepairPacket:1,recursiveAutomaticRetryPermitted:false},historicalBoundary:{standalonePostCampaignAddition:true,campaignBatch:null,batch18Permitted:false,frozenCampaignEvidenceWritable:false,frozenCalibrationEvidenceWritable:false,frozenClosureEvidenceWritable:false},allowedTrackedPaths:[root,`docs/assessment-ledgers/${identity.debateId}.json`,registryPath,'docs/calibration/v2.1/corpus-transcript-audit.json','src/data/debates.js','src/data/interlocutors.js','src/data/interlocutor-bios.js','scripts/lib/assessment-editor-scoped-source.mjs','scripts/audit-assessment-production-standalone-v1.mjs','frozen snapshot of the prior standalone auditor','generated site output for this addition']};
const overlaps=(e,x)=>e.startMs<x.endMs&&e.startMs+e.durationMs>x.startMs;
const excluded=e=>e.startMs<assessedWindow.startMs||e.startMs+e.durationMs>assessedWindow.endMs||excludedIntervals.some(x=>overlaps(e,x));
let runningEnd=0;const gaps=[];for(const e of events){if(e.startMs-runningEnd>10000)gaps.push({startMs:runningEnd,endMs:e.startMs,durationMs:e.startMs-runningEnd});runningEnd=Math.max(runningEnd,e.startMs+e.durationMs);}
assert(gaps.every(g=>g.endMs<=assessedWindow.startMs||excludedIntervals.some(x=>g.startMs>=x.startMs&&g.endMs<=x.endMs)),'Unexplained retained source gap');
const caution='Assess only the two primary speakers within the explicitly approved source scope. Ordinary questions are contextual prompts, not participant arguments. Do not credit either speaker for the moderator’s assertions, assess excluded replies, or infer that exclusion means weakness. The discarded free-will branch is not evidence for either side. Distinguish source ASR errors from speaker mistakes: sentience, names and technical expressions are sometimes garbled. Below-high attribution or material wording uncertainty must be declared for independent audio checking. Dawkins adopts naturalistic explanation but concedes unsolved problems; Chopra accepts Darwinian evolution while defending cosmic consciousness and epistemic limits. Judge the versions actually argued, not generic worldviews or outside scientific correctness. The recording’s Spanish preamble is format/context only, verified separately; it is not scoring evidence.';
const sourceLock={schemaVersion:'1.0-standalone-source-lock',protocolId:STANDALONE_PROTOCOL_ID,status:'complete-and-hash-locked',identity:{canonicalUrl:identity.canonicalUrl,videoId,title:cm.title,channel:cm.channel,eventYear:2013,dateBasis:'Publisher title explicitly identifies CDI 2013; original opening identifies speakers and three topics.',speakerIdentityBasis:'Named moderator turns, direct addressing, continuous primary speeches and independently translated Spanish subtitles; mixed short exchanges remain subject to audio verification.'},acquisition:{method:cm.extractionMethod,language:'en',trackName:cm.track.name,captionKind:cm.track.kind,retrievedAt:cm.retrievedAt,paidTranscriptionUsed:true,supplementalRecovery:rec(`${root}/source/recovery-provenance.json`)},content:{eventCount:events.length,wordCount:cm.wordCount,spokenWordCount:events.map(e=>e.text).join(' ').split(/\s+/).length,firstStartMs:events[0].startMs,maxEndMs:runningEnd,sourceDurationMs:cm.durationSeconds*1000,eventsChronological:true,eventsNonempty:true,timestampCoverageComplete:true,fullRecordingMultilingualTranscriptClaimed:false,retainedSubstantiveWindowComplete:true,gapsOverTenSeconds:gaps,coverageNote:'All retained substantive English passages are covered. Missing Spanish opening context was independently transcribed; the only later material gap is within the wholly excluded persuasion/poll branch. English raw captions remain unchanged, including a 0.970-second terminal overhang outside the assessment.',captionCaution:caution},participants:{pro:identity.pro.speaker,con:identity.con.speaker,moderators:['Andrés Roemer'],substantiveAdvocatesInOriginal:3,substantiveAdvocatesInAssessedScope:2,moderatorsAdvancedIndependentArgument:true,assessedDebateWindowMs:{start:assessedWindow.startMs,end:assessedWindow.endMs},dyadicGatePassed:true,dyadicGateAppliesOnlyToEditorApprovedScope:true,editorApprovedScope:scope},format:{assessedExchange:'Independent two-primary-speaker exchanges with the explicit user-approved moderator/dependent-reply exclusions.',offMotionCoverage:'Opening biographies, rules, audience poll and closing thanks are unscored context.'},positionCaution:caution,hashes:{rawCaptionSha256:cm.rawCaptionSha256,normalizedEventsSha256:cm.normalizedEventsSha256,transcriptSha256:cm.transcriptSha256,captionManifestSha256:sha256(fs.readFileSync(`${cache}/manifest.json`))},duplicateAudit:{productionDebateCountBeforeAddition:debates.length,canonicalUrlUnique:true,videoIdUnique:true,debateIdUnique:true,debateNumberUnique:true,identityDuplicateFound:false}};
put(`${root}/authorization.json`,authorization);put(`${root}/source/source-lock.json`,sourceLock);
const indexed=events.map((e,i)=>`${i}\t${e.startMs}\t${e.startMs+e.durationMs}\t${e.text}`).join('\n')+'\n';
const masked=events.map((e,i)=>`${i}\t${e.startMs}\t${e.startMs+e.durationMs}\t${excluded(e)?'[OUTSIDE APPROVED SCORING SCOPE]':e.text}`).join('\n')+'\n';
put(`${cache}/indexed-transcript.txt`,indexed);put(`${cache}/scoped-indexed-transcript.txt`,masked);
put(`${cache}/score-blind-source.json`,{identity,sourceLock,events:events.map((e,eventIndex)=>({eventIndex,...e,text:excluded(e)?'[OUTSIDE APPROVED SCORING SCOPE]':e.text}))});
const manifest={schemaVersion:'1.0-standalone-debate-manifest',protocolId:STANDALONE_PROTOCOL_ID,status:'inputs-frozen-before-judgment',debateNumber:number,debateId:identity.debateId,videoId,sourceLocks:{rawCaptions:rec(`${cache}/captions.srv3.xml`),events:rec(`${cache}/events.json`),transcript:rec(`${cache}/transcript.txt`),captionManifest:rec(`${cache}/manifest.json`),committedSourceLock:rec(`${root}/source/source-lock.json`),authorization:rec(`${root}/authorization.json`),editorScope:rec(`${root}/source/editor-scope.json`),completeScoreBlindSource:rec(`${cache}/score-blind-source.json`),scopedTranscript:rec(`${cache}/scoped-indexed-transcript.txt`)},controlLocks:controls,modelSettings:model,scoringControls:{dimensionDisagreementThreshold:8,moveScoreDisagreementThreshold:4,burdenAdjustmentDisagreementThreshold:2,oneScorePass:true,scoreProtocolId:'standalone-v1-single-deterministic-score-pass'},inventoryControls:{schemaVersion:'1.2-standalone-score-blind-inventory',uniqueSemanticSectionIdsRequired:true,selectionBalanceAuditRequired:true,publicationCapacityAuditRequired:true,maximumSelectedMovesPerSidePerSection:4,fourthRowPreauthorizationRequired:true,speakerAttributionReviewRequired:true,sourceExcerptTargetMaximumCharacters:2200,longerCompleteArgumentRequiresRationale:true},frozenAt:at};put(`${root}/manifest.json`,manifest);
validateEditorScopedSource({authorization,sourceLock,inventory:{editorApprovedScope:scope,assessedDebateWindowMs:sourceLock.participants.assessedDebateWindowMs,moves:[]}});
const packet={schemaVersion:'1.0-score-blind-inventory-packet',identity,sourceLock,model,indexedTranscriptPath:path.resolve(`${cache}/indexed-transcript.txt`),scopedTranscriptPath:path.resolve(`${cache}/scoped-indexed-transcript.txt`),eventsPath:path.resolve(`${cache}/events.json`),rubric:fs.readFileSync(controls.rubric.path,'utf8'),inventoryValidatorContract:fs.readFileSync('scripts/lib/assessment-production-standalone-debate-v1.mjs','utf8').split('\n').slice(118,450).join('\n'),inventoryControls:manifest.inventoryControls,outputPath:path.resolve(`${root}/inventory/model-output.json`),readingOutputPath:path.resolve(`${root}/inventory/reading-authentication.json`),rules:[
'Read every indexed event and the entire packet visibly in untruncated tool output before selecting or drafting any move. Reading files in a loop while printing only counts is not reading the source. Use separate bounded reads and record their event ranges and source hashes. No external research, biographies, other debates, prior ratings, tags or outcomes.',
'This recording has a specific user-approved moderator exclusion, not the automatic primary-speaker exception. Honor editorApprovedScope exactly. Independently audit source attribution, all excluded intervals, and whether any unenumerated substantive host argument or dependent reply enters retained evidence. If frozen boundaries leave such material, return an explicit source-gate failure, not a forced inventory. Neutral topic questions do not make the subsequent independent exposition an excluded reply; the burden cannot depend on the host’s substantive assertion.',
'Select all load-bearing arguments, major replies and important concessions. Exclude repetitive micro-fragments, logistical remarks and every out-of-scope passage. Make four to seven coherent semantic sections with sectionId, title, weightPercent (positive integers summing 100), rationale. Each section must have at least one selected move per side. Ordinary capacity is three per side; at most four with a frozen source-led fourth-row rationale. Do not manufacture symmetry.',
'Top-level fields: schemaVersion 1.2-standalone-score-blind-inventory, protocolId assessment-production-standalone-debate-v1, status complete-and-frozen (candidate awaiting validation), debateNumber, debateId, assessmentModel 5.6 Sol, motion byte-identical, assessedDebateWindowMs, editorApprovedScope byte-identical, routes, sections, moves, selectionBalanceAudit, publicationCapacityAudit, completeTranscriptCoverage, audit.',
'Each route: side, speaker, positionLabel, bridges (3..7). Each bridge: bridgeId, tier (exactly one motion, at least one central and one subsidiary per route), burden, successCriterion. A critic has no invented positive contrary burden.',
'Each chronological move: semantic lowercase moveId, sectionId, side, speaker, moveKind constructive or reply, claimSummary, reasonSummary, inferenceSummary {premise,inference,conclusion}, importance 1..3, importanceRationale, burdenContact {bridgeId,tier,rationale}, respondsToIds, responseComponents, sourceSpan {startEvent,endEvent}, quoteEligibleExactSpans, attributionConfidence high/medium/low, attributionRationale, materialWordingConfidence high/medium/low, materialWordingRationale. The controller expands exact text and milliseconds from source events. Never include another speaker’s assertion as the chosen speaker’s evidence. Keep minimal complete claim-plus-reason span, normally <=2200 characters; longer requires longerCompleteArgumentRationale.',
'Constructive moves have empty reply arrays. Replies need earlier selected target IDs and responseComponents {targetMoveId,component,rationale}. Do not mislabel a reply to avoid its linkage. Quotes must be exact source substrings, 3..18 words; no repaired ASR spelling. Avoid garbled quotations. Mixed turns and uncertain speaker transitions are not high-confidence merely because their substance seems characteristic; mark honestly for later audio verification.',
'selectionBalanceAudit: thresholds {totalAbsoluteDifference:3,sectionAbsoluteDifference:2}, totals {pro,con,absoluteDifference}, sections [{sectionId,pro,con,absoluteDifference}], rationaleRequired, rationale. Rationale required when total difference>=3 or any section difference>=2. Explain source-led asymmetry, never drop a major move to balance.',
'publicationCapacityAudit: maximumSelectedMovesPerSidePerSection 4, sections [{sectionId,pro,con,withinCapacity,fourthRowRequired,fourthRowAuthorized,rationale}], allSectionsWithinCapacity, allFourthRowsPreauthorized. fourthRowAuthorized=true when not required. Calculate from moves, do not guess; controller independently recalculates.',
'completeTranscriptCoverage must be contiguous blocks {startEvent,endEvent,moveIds,reason} partitioning every event from 0 to final event, with source-specific reasons for all contextual, excluded or repeated material. audit {calculatedTotalsAbsent:true,completeTranscriptReviewed:true,allSpansSourceExact:true,scoreBlind:true,noOutcomePredetermined:true}. No score, tag or predicted winner.',
'One initial output attempt; no rewriting submitted bytes. In-memory schema checks allowed. Use apply_patch to create only outputPath and readingOutputPath. Do not write scripts or other files. In reading authentication include actual source hashes, ranges, completeSourceReadBeforeDraft true only after actual visible reading, and no external evidence used.'
]};put(`${root}/inventory/packet.json`,packet);
put(`${root}/inventory/execution-plan.json`,{schemaVersion:'1.0-frozen-inventory-execution-plan',status:'frozen-before-model',debateNumber:number,debateId:identity.debateId,createdAt:at,model,isolation:'Fresh built-in subagent, fork_turns none, only declared source and packet inputs; no prior assessments.',attempts:1,retries:0,allowedInputs:[rec(`${root}/inventory/packet.json`),rec(`${cache}/indexed-transcript.txt`),rec(`${cache}/scoped-indexed-transcript.txt`),rec(`${cache}/events.json`)],allowedOutputPaths:[packet.outputPath,packet.readingOutputPath],apiKeysMustNotBeReadOrUsed:true,directIncrementalCostUsd:0});
registry.debates.push({debateNumber:number,debateId:identity.debateId,videoId,root,validationProfile:'semantic-balanced-capacity-v2',status:'authorized-source-frozen',editorScopePath:`${root}/source/editor-scope.json`,readerScopeDisclosurePath:`${root}/source/reader-scope-disclosure.json`,productionLedger:{path:`docs/assessment-ledgers/${identity.debateId}.json`}});
// Emit one bounded patch at a time, refusing every differing pre-existing file.
if(args[3]==='list')console.log(JSON.stringify([...files.keys(),registryPath]));
else if(args[3]===registryPath){
 const old=fs.readFileSync(registryPath,'utf8').trimEnd().split('\n');
 const tail=old.slice(-10),entry=serializedJson(registry.debates.at(-1)).trimEnd().split('\n').map(l=>'    '+l);
 assert.equal(tail.at(-3),'    }');assert.equal(tail.at(-2),'  ]');assert.equal(tail.at(-1),'}');
 console.log('*** Begin Patch\n*** Update File: '+path.resolve(registryPath)+'\n@@\n'+tail.slice(0,-3).map(l=>' '+l).join('\n')+'\n-    }\n+    },\n'+entry.map(l=>'+'+l).join('\n')+'\n   ]\n }\n*** End Patch');
}else{
 const p=args[3],v=files.get(p);assert(v!==undefined);assert(!fs.existsSync(p),`Already materialized ${p}`);
 console.log(`*** Begin Patch\n*** Add File: ${path.resolve(p)}\n`+v.replace(/\n$/,'').split('\n').map(l=>'+'+l).join('\n')+'\n*** End Patch');
}
