import fs from 'node:fs';
import path from 'node:path';
import assert from 'node:assert/strict';
import {execFileSync} from 'node:child_process';
import {fileRecord,serializedJson} from './scripts/lib/assessment-production-standalone-debate-v1.mjs';
const args=process.argv.slice(2);assert.deepEqual(args,['--debate','264']);
const read=p=>JSON.parse(fs.readFileSync(p));
const rec=read('docs/assessment-production/standalone-debates-v1/registry.json').debates.find(r=>r.debateNumber===args[1]);assert(rec);
const b=rec.root,inv=read(`${b}/inventory/source-expanded-candidate.json`),output='output/transcribe/debate-264-verification';
assert.equal(rec.videoId,'BiwLrxPb1fE');assert(!fs.existsSync(`${b}/audio/plan.json`));
const media=`output/transcribe/debate-264-source/${rec.videoId}.webm`,sourceLock=read(`${b}/source/recovery-provenance.json`).sourceAudio;assert.equal(fileRecord(media).sha256,sourceLock.sha256);
const selected=inv.moves.filter(m=>m.attributionConfidence!=='high'||m.materialWordingConfidence!=='high'||m.moveId==='chopra-promissory-note-objection');
const ranges=[];
for(const m of selected){const x={startMs:Math.max(inv.editorApprovedScope.assessedWindow.startMs,m.sourceSpan.startMs-4000),endMs:Math.min(inv.editorApprovedScope.assessedWindow.endMs,m.sourceSpan.endMs+4000),moveIds:[m.moveId]};const p=ranges.at(-1);if(p&&x.startMs<=p.endMs+3000){p.endMs=Math.max(p.endMs,x.endMs);p.moveIds.push(...x.moveIds);}else ranges.push(x);}
const clips=[];for(const r of ranges){for(let start=r.startMs;start<r.endMs;start+=180000){const end=Math.min(start+180000,r.endMs);clips.push({clipId:`clip-${String(clips.length+1).padStart(2,'0')}`,startMs:start,endMs:end,durationSeconds:(end-start)/1000,moveIds:r.moveIds.filter(id=>{const m=inv.moves.find(m=>m.moveId===id);return m.sourceSpan.startMs<end&&m.sourceSpan.endMs>start;})});}}
const references=[{speaker:'Deepak Chopra',startMs:798000,endMs:806000,basis:'First uninterrupted answer after the moderator names Chopra; speaker identity independently established in the Spanish opening.'},{speaker:'Richard Dawkins',startMs:980000,endMs:988000,basis:'First direct Dawkins reply following the timed handoff; continuous organism-purpose answer.'},{speaker:'Andrés Roemer',startMs:585000,endMs:593000,basis:'Moderator explains the English format rules before either primary argumentative answer.'}];
fs.mkdirSync(`${output}/clips`,{recursive:true});fs.mkdirSync(`${output}/references`,{recursive:true});
for(const [n,r]of references.entries()){r.path=`${output}/references/speaker-${n+1}.mp3`;}
for(const c of clips)c.path=`${output}/clips/${c.clipId}.mp3`;
for(const c of [...references,...clips]){assert(!fs.existsSync(c.path));execFileSync('ffmpeg',['-hide_banner','-loglevel','error','-n','-ss',String(c.startMs/1000),'-i',media,'-t',String((c.endMs-c.startMs)/1000),'-vn','-ac','1','-ar','16000','-b:a','64k',c.path]);Object.assign(c,fileRecord(c.path));assert(c.bytes<25*1024*1024);}
const seconds=clips.reduce((s,c)=>s+c.durationSeconds,0),refSeconds=24*clips.length;
const estimate=(seconds+refSeconds)/60*0.006,prior=0.0494;assert(prior+estimate<=1);
const plan={schemaVersion:'1.0-frozen-bounded-audio-verification-plan',status:'frozen-before-paid-verification',createdAt:new Date().toISOString(),debateNumber:rec.debateNumber,debateId:rec.debateId,videoId:rec.videoId,inventoryCandidate:fileRecord(`${b}/inventory/source-expanded-candidate.json`),sourceMedia:fileRecord(media),sourceOriginalAudio:true,sourceLanguage:'en',purpose:'Verify all candidate below-high attribution or material-wording spans plus the controller-flagged promissory-note interleaving. Candidate correction remains score-blind and may only narrow these evidence spans; broader clip context is never automatically scoring evidence.',excludedContextNotScoringEvidence:true,selectedMoveIds:selected.map(m=>m.moveId),model:'gpt-4o-transcribe-diarize',responseFormat:'diarized_json',chunkingStrategy:'auto',prompt:null,knownSpeakerReferences:references,clips,pricing:{verifiedDate:'2026-09-19',source:'https://developers.openai.com/api/docs/models/gpt-4o-transcribe-diarize',durationEstimateBasis:'Same displayed input/output token rates as gpt-4o-transcribe; its official pricing table estimates $0.006 per minute. Reference audio is conservatively included for every request.',estimatedRateUsdPerMinute:0.006,inputUsdPerMillionTokens:2.5,outputUsdPerMillionTokens:10,priorSourceRecoveryEstimatedUsd:prior,verificationAudioSeconds:seconds,referenceAudioSeconds:refSeconds,additionalEstimatedUsd:estimate,cumulativeEstimatedUsd:prior+estimate,standingCapUsd:1,estimateReportedBeforeCall:true,billingConfirmed:false},attemptsPerClip:1,retries:0,maximumClipSeconds:180,timeoutSeconds:180,executionOutputRoot:`${b}/audio/responses`,transcribeSkill:'/Users/philstilwell/.codex/skills/transcribe/scripts/transcribe_diarize.py',directListeningPerformed:false};
console.log('*** Begin Patch\n*** Add File: '+path.resolve(`${b}/audio/plan.json`)+'\n'+serializedJson(plan).trimEnd().split('\n').map(l=>'+'+l).join('\n')+'\n*** End Patch');
