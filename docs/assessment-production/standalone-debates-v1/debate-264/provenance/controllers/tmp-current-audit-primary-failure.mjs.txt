import fs from 'node:fs';
import crypto from 'node:crypto';
import assert from 'node:assert/strict';
import path from 'node:path';
import {fileRecord,validateStandalonePrimaryJudgment} from './scripts/lib/assessment-production-standalone-debate-v1.mjs';
const [flag,number,pass,session]=process.argv.slice(2);assert.equal(flag,'--debate');
const read=p=>JSON.parse(fs.readFileSync(p));
const rec=read('docs/assessment-production/standalone-debates-v1/registry.json').debates.find(r=>r.debateNumber===number);assert(rec);
const plan=read(rec.root+'/judgments/execution-plan.json'),assigned=plan.passes.find(p=>p.pass===pass),inv=read(rec.root+'/inventory/inventory.json'),j=read(assigned.outputPath);
const bytes=fs.readFileSync(session),rows=bytes.toString().trim().split('\n').map(JSON.parse),input=r=>r?.payload.input??r?.payload.arguments??'';
const calls=new Map(rows.filter(r=>r.type==='response_item'&&['function_call','custom_tool_call'].includes(r.payload.type)).map(r=>[r.payload.call_id,r]));
const submissions=[...calls.values()].filter(r=>input(r).includes('tools.apply_patch(')),firstSubmission=submissions[0]?.ordinal??Infinity;
// Preserve literal JSON formatting. Parse only actual tool-envelope values.
const unwrap=v=>{if(Array.isArray(v))return v.map(unwrap).join('\n');if(v&&typeof v==='object')return typeof v.output==='string'?unwrap(v.output):typeof v.text==='string'?unwrap(v.text):JSON.stringify(v);if(typeof v!=='string')return String(v);try{const p=JSON.parse(v);if(Array.isArray(p)&&p.every(x=>x&&typeof x==='object'&&(typeof x.text==='string'||typeof x.output==='string')))return unwrap(p);if(p&&typeof p==='object'&&(typeof p.output==='string'||p.type==='text'&&typeof p.text==='string'))return unwrap(p);return v;}catch{return v;}};
const returns=rows.filter(r=>r.type==='response_item'&&['function_call_output','custom_tool_call_output'].includes(r.payload.type)&&r.ordinal<firstSubmission).map(r=>({row:r,call:calls.get(r.payload.call_id),text:unwrap(r.payload.output)}));
const reads=returns.filter(r=>input(r.call).includes('exec_command')&&!input(r.call).includes('validateStandalonePrimaryJudgment'));
const visible=reads.map(r=>r.text).join('\n'),trimmed=new Set(visible.split('\n').map(s=>s.trim()).filter(Boolean));
const inputAuthentication=plan.inputs.filter(p=>p.path.endsWith('.md')||p.path.endsWith('.json')&&!p.path.endsWith('/inventory.json')).map(lock=>{
 assert.equal(fileRecord(lock.path).sha256,lock.sha256);const raw=fs.readFileSync(lock.path,'utf8').trimEnd(),lines=[...new Set(raw.split('\n').map(s=>s.trim()).filter(Boolean))];
 const whole=reads.filter(r=>r.text.includes(raw));const missing=whole.length?[]:lines.filter(l=>!trimmed.has(l));
 return{...lock,complete:!missing.length,expectedUniqueLines:lines.length,missingUniqueLines:missing.length,missingLineExamples:missing.slice(0,5),wholeDocumentCalls:whole.map(r=>r.row.payload.call_id)};
});
const sourcePath='.assessment-cache/captions/'+rec.videoId+'/scoped-indexed-transcript.txt',sourceLines=fs.readFileSync(sourcePath,'utf8').trimEnd().split('\n'),exact=new Set(visible.split('\n'));
const missingEvents=sourceLines.filter(l=>!exact.has(l)).map(l=>Number(l.split('\t')[0]));
const projection=structuredClone(inv);for(const m of projection.moves)delete m.sourceSpan.excerpt;const strings=[];const walk=x=>{if(typeof x==='string')strings.push(x);else if(Array.isArray(x))x.forEach(walk);else if(x&&typeof x==='object')Object.values(x).forEach(walk);};walk(projection);
const missingStrings=[...new Set(strings)].filter(s=>!visible.includes(s)&&!visible.includes(JSON.stringify(s).slice(1,-1)));
const normalize=r=>r.replace(/ This specifically supports the \w+ rating\.$/,'').replace(/^For (?:logical coherence|evidence and warrant|responsiveness|relevance and burden|precision and clarity|calibration and charity), /,'');
const rationaleReview=j.judgments.map(m=>{const reasons=Object.values(m.dimensions).map(x=>normalize(x.rationale));return{moveId:m.moveId,dimensionCount:reasons.length,distinctSubstantiveExplanations:new Set(reasons).size,sharedExplanation:reasons.every(x=>x===reasons[0])?reasons[0]:null};});
const models=rows.filter(r=>r.type==='turn_context').map(r=>({model:r.payload.model,reasoningEffort:r.payload.effort}));assert(models.every(m=>m.model===plan.model.model&&m.reasoningEffort===plan.model.reasoningEffort));
const audit={schemaVersion:'1.0-primary-output-controller-rejection',status:'rejected-dimension-specific-rationales-missing',debateNumber:number,debateId:rec.debateId,pass,output:fileRecord(path.relative(process.cwd(),assigned.outputPath)),session:{path:session,sha256:crypto.createHash('sha256').update(bytes).digest('hex'),bytes:bytes.length},actualModels:models,schemaValidation:validateStandalonePrimaryJudgment(j,inv,{expectedPass:pass,expectedInventorySha256:fileRecord(rec.root+'/inventory/inventory.json').sha256}),inputAuthentication,sourceReading:{path:sourcePath,expectedEvents:sourceLines.length,visibleEvents:sourceLines.length-missingEvents.length,missingEvents,rule:'Credit only exact literal source lines visibly returned by source-reading calls before submission; no credit for programmatic draft construction or truncated-away content.'},inventorySemanticReading:{expectedUniqueStrings:new Set(strings).size,visibleUniqueStrings:new Set(strings).size-missingStrings.length,missingStrings},rationaleReview,repeatedExplanationMoves:rationaleReview.filter(r=>r.distinctSubstantiveExplanations===1).length,controllerFinding:'One argument-level explanation is mechanically copied to all six different scoring criteria, with only the criterion name added. This does not provide criterion-specific reasoning for six differing ratings. Both the live evidence and the rating-specific assessment must be explained; schema validity alone is insufficient.',sourceConcerns:j.sourceReview.checks.filter(c=>!c.attributionSupported||c.materialUncertainty),submissions:submissions.map(r=>({callId:r.payload.call_id,time:r.timestamp,authorizedOutputPathPresent:input(r).includes(assigned.outputPath)})),readCalls:reads.map(r=>({callId:r.row.payload.call_id,commandSha256:crypto.createHash('sha256').update(input(r.call)).digest('hex'),commandPreview:input(r.call).slice(0,300),outputCharacters:r.text.length,aggregateTruncation:/Warning: truncated output|tokens truncated/.test(r.text)})),attempts:1,retries:0,directIncrementalCostUsd:0,accepted:false,aggregateScoreCalculationPerformed:false};
console.log(JSON.stringify(audit,null,2));
