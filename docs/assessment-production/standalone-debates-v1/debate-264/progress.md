# Debate 264 — preserved primary-review checkpoint

Source: https://www.youtube.com/watch?v=BiwLrxPb1fE

Status: primary, audio, adjudication, single-score-pass, publication, and editorial-depth gates passed. Scores remain Deepak Chopra 66, Richard Dawkins 86, with zero additional burden adjustment. The single publication output is accepted for independent rhetorical review, with all 30 cards directly reviewed. Two fresh blind rhetorical reviewers are now completing their source reading. No production integration, commit, push or pull request exists.

## Publication transport stop

The fresh `publication_264` context used the frozen GPT-5.6 Sol / low setting and read all 1,348 scoped source events, the complete final ledger, score output, audio facts, packet and procedural documents. Its interrupted reading remains authenticated in `publication/transport-stop.json`; only part of the protocol library and the publication checker remain unread. The service error occurred at 2026-09-20T01:20:26.854Z. No draft, output file, post-submission change, retry, model switch, extra paid call or score change occurred.

The user authorized one bounded continuation of the same isolated writer, preserving its completed reading and all frozen inputs. The same context resumed successfully, completed the remaining reads, and passed authentication of all 13 original inputs and all 1,348 scoped source events before drafting release. Initial unsaved generator drafts were rejected for repetitive and mechanically spliced prose before any submission. The same writer then authored six literal card portions and the remaining prose, each directly reviewed, without a new model context or transport retry. The final compilation authenticated 22 inputs, including only that writer's own preserved draft portions and mechanical assembly helpers. One quote-context attribution was corrected before submission. The sole final output passed all checks but was saved under a relative path in the parent checkout; the controller validated and moved those exact bytes to the authorized worktree, with both hashes equal and no content edit. The original error, all interventions, draft provenance, reading gates, execution, and relocation proof remain preserved. No further exceptional transport continuation is authorized.

The accepted publication contains 30 cards in six sections: descriptions average 23.8 words, critiques 127.6 words, and AI reinforcing arguments 91.2 words. Critique boilerplate coverage is zero under the six-word test. All 264 published-plus-candidate punctuation profiles were measured with no flags, and the candidate AI prose has no repeated eight-word openings or six-word overlaps with the independent reference window. The 16 rows and 30 moves are justified by locked source coverage; the slightly shorter 20.2-word premise average retains complete inferential steps. Rhetorical metadata has not yet been applied.

The original inventory, rejected mechanical audit, isolated correction output and its authenticated session remain intact. The additional user authorization allowed only deletion of the two wrong-speaker quotations. The accepted inventory contains 30 moves in six sections, 15 per side; its SHA-256 is `2e58f0092f2701d0e6a6d357ca325479491d14f99c025d8529eed7c2b074369c`.

## Completed quotation recovery

Exactly two quotation-list fields were corrected under the user's approval:

1. On `dawkins-cell-responsiveness-does-not-solve-consciousness`, removed `a single cell has siience`. The audio assigns that sentence to Chopra at approximately 33:29; Dawkins's preceding criticism remains supported and the other exact quotation remains usable.
2. On `chopra-promissory-note-objection`, removed `it is a promisory note`. The audio assigns that concession to Dawkins at approximately 49:51; Chopra's objection remains supported and the other exact quotation remains usable.

No other quotation, argument, speaker, weight, burden, response link, section or scope field changed. The separate output, authorization and before/after proof are in `inventory/exceptional-quotation-repair/`; the rejected earlier output was not overwritten. All 27 source/control locks passed at execution.

## Primary-review rejection and recommended recovery

Both reviewers used GPT-5.6 Sol at low reasoning in fresh isolated subscription-backed contexts, made one submission each, and passed the judgment schema. Authentication confirms that each visibly read all 1,348 scoped source events, all 517 distinct inventory semantic strings and the required input documents before submission. Missing source reading is not the reason for rejection.

The substantive defect was identical in both initial passes: all 30 moves had only one distinct explanation for six different criterion ratings. The rejected raw judgments now remain at `judgments/pass-a/failed-initial-output.json` and `judgments/pass-b/failed-initial-output.json`, with authenticated rejection records at each corresponding `controller-audit.json`. Their prior hashes were verified before preservation. The canonical `output.json` paths now contain only the mechanically assembled accepted replacements, authenticated by `judgments/execution.json`.

The user approved the proposed exceptional replacement round: two independent sets of six section-specific judgment contexts, plus one whole-debate burden-completion context per set (14 fresh contexts total, at most two concurrently). Its authorization, immutable reading packets and execution plan are under `judgments/exceptional-replacement/`. Each context must read the complete frozen scoped source and inventory before judging only its assigned fields. Each section's six criterion explanations must be genuinely criterion-specific and source-grounded, with controller review before acceptance. The contexts must not see either rejected output or another review. Preserve all existing evidence, keep the frozen model and scope, allow one submission per assigned context, calculate no totals, and do not recursively retry. Additional direct cost so far: $0.

Both Section 1 contexts authenticated all 20 reading chunks plus their packets before drafting, used the exact frozen model, and submitted once to separate new paths. Both now provide distinct explanations for all six criteria. Pass-a passed semantic review. Pass-b's `precisionClarity` reasons for `chopra-organisms-and-cosmic-organization` and `chopra-evolution-guided-by-awareness` explicitly add garbled captions to the participant's clarity weaknesses. Existing audio-derived findings resolve those recognition issues; they must not reduce a participant's score. The controller cannot simply remove the sentences or adjust the numbers. All raw outputs remain unchanged. The two initial reading-authentication files required a controller correction to recognize a packet read in two ranges; the preserved v2 authentication proves complete reading and is authoritative.

The user approved the narrowly bounded proposal in `judgments/exceptional-replacement/two-field-recovery-proposal.json`. One fresh isolated reviewer independently reassessed only those two pass-b clarity records, without prior ratings, using the complete frozen source/inventory and the relevant score-blind audio-derived facts. Both explanations passed semantic review: they assess actual terms, distinctions and scope without treating recognition errors as participant defects. Authorization, packet, one submitted output and controller review are under `judgments/precision-replacement/`. No prior judgment file was overwritten; the authorized two-field override is applied mechanically only when complete primary passes are assembled. The remaining approved round has resumed with Section 2.

## Audio and visual disposition

All 15 planned verification calls succeeded once; both earlier source-context calls also succeeded. All 15 full responses and their speaker segments were reviewed. Three source-video contact sheets were examined to corroborate Chopra/Dawkins speaking turns where automatic labels inconsistently named Roemer. These images support contextual attribution, not word-by-word lip-reading. No direct listening was performed, and no caption bytes were changed.

The inventory and formal pre-adjudication audio gates pass. Nineteen factual retained-move resolutions are recorded in `audio/retained-move-resolutions.json`, covering the five below-high-attribution triggers and supplementary wording checks. `audio/audio-verification.json` maps every primary source concern to those resolutions. Full notes and pricing reconciliation are under `audio/`. Durable contact sheets are under `source/visual-checks/`.

Cost based on reported token usage at the verified published rates: $0.712495 total for 17 successful calls. There are no failed or transport-uncertain calls, so the maximum possible total on that basis is also $0.712495. Invoice billing remains unconfirmed. The $1 standing cap was not exceeded.

## Resume safely

Keep this work on `codex/debate-264` in the dedicated worktree. Do not rebuild source acquisition, rerun successful paid calls, reset, stash or clean. All submitted outputs and frozen evidence are immutable. Do not overwrite the accepted inventory, judgments, adjudication or score output; do not change the model or scope, reuse the failed ratings, or rerun the score pass. Continue with publication, independent rhetorical review, complete validation and the protected-branch publication sequence.

All source exclusion intervals remain exactly as approved. The excluded block union is 502.158 seconds, 14.05% of the assessed window; that is mixed host/dependent-reply time, not the moderator's own speech percentage.

No temporary browser tabs, servers or media-extraction processes remain open. Source evidence and continuation controllers are retained intentionally for recovery.
