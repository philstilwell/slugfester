import fs from 'node:fs';import assert from 'node:assert/strict';import {createHash} from 'node:crypto';import {checkRhetorical} from './check-rhetorical-draft.mjs';
const n=process.argv[2];assert.match(n,/^\d{3}$/);const read=p=>JSON.parse(fs.readFileSync(p)),hash=b=>createHash('sha256').update(b).digest('hex'),rec=p=>{const b=fs.readFileSync(p);return{path:p,sha256:hash(b),bytes:b.length};},write=(p,o)=>fs.writeFileSync(p,JSON.stringify(o,null,2)+'\n',{flag:'wx'});
const r=read('docs/assessment-production/standalone-debates-v1/registry.json').debates.find(r=>r.debateNumber===n);assert(r);const base=r.root+'/publication/rhetorical-tag-review-1',dir=base+'/adjudication',packet=read(base+'/packet.json'),a=read(r.root+'/authorization.json');
const union=new Map(),mapping=[];
for(const pass of ['pass-a','pass-b']){const auth=read(base+'/'+pass+'/execution.json'),out=read(base+'/'+pass+'/output.json');assert.equal(auth.output.sha256,rec(base+'/'+pass+'/output.json').sha256);checkRhetorical(out,packet);for(const row of [...out.candidateReviews,...out.overallBlunderCandidateReviews]){if(!union.has(row.candidateKey))union.set(row.candidateKey,[]);union.get(row.candidateKey).push({pass,row});}}
const order=[...packet.moves.map(m=>m.moveId),...packet.overallBlunders.map(b=>b.blunderId)],definitionOrder=packet.referenceDefinitions.map(d=>d.type+'|'+d.label);
const candidates=[...union].sort(([a],[b])=>{const x=a.split('|'),y=b.split('|');return order.indexOf(x[0])-order.indexOf(y[0])||definitionOrder.indexOf(x.slice(1).join('|'))-definitionOrder.indexOf(y.slice(1).join('|'));}).map(([key,options])=>{const rows=options.map(o=>({...o,sort:hash(key+'\0'+o.pass)})).sort((a,b)=>a.sort.localeCompare(b.sort));mapping.push({candidateKey:key,options:rows.map((o,i)=>({option:'option-'+(i+1),pass:o.pass}))});const first=rows[0].row;return {candidateKey:key,...(first.moveId?{moveId:first.moveId}:{blunderId:first.blunderId}),type:first.type,label:first.label,url:first.url,anonymousOptions:rows.map((o,i)=>({option:'option-'+(i+1),decision:o.row.decision,context:o.row.context,rationale:o.row.rationale}))};});
fs.mkdirSync(dir);write(dir+'/controller-private-option-mapping.json',{debateNumber:n,debateId:r.debateId,candidates:mapping});
const input={...packet,schemaVersion:'1.0-anonymous-rhetorical-adjudication-packet',candidates,unionCount:candidates.length,reviewRules:[...packet.reviewRules,'Fresh anonymous adjudication: decide every union candidate exactly once in packet order, including candidates only one review considered and candidates both rejected. Reconsider exact definition and full source independently; do not use vote counts. Choose accepted or rejected, and supply your own source-specific rationale and8–35word context. Do not invent additional labels or change frozen critique.'],outputContract:{schemaVersion:'1.0-anonymous-rhetorical-adjudication',status:'complete-candidate-union-adjudication',debateNumber:n,debateId:r.debateId,reviewedCandidateKeys:candidates.map(c=>c.candidateKey),resolutions:'Exactly one candidate row per reviewedCandidateKey in packet order, same identifier/key/type/label/url as input; context8–35words; rationale≥12words; decision accepted or rejected.',audit:{model:a.execution.assessmentModel,reasoningEffort:a.execution.reasoningEffort,attempts:1,retries:0,directIncrementalCostUsd:0,anonymousReviewOptions:true,completeCandidateUnionReviewed:true,scoresUnavailable:true,existingTagsUnavailable:true,judgmentChanges:0,scoreChanges:0,critiqueChanges:0}}};
write(dir+'/input.json',input);
const allowed=[rec(dir+'/input.json'),rec(base+'/catalog-snapshot.json'),rec(packet.completeTimestampedTranscript),rec(packet.audioContextPath),rec('src/data/references.js'),rec('.assessment-cache/check-rhetorical-draft.mjs')];
write(dir+'/execution-plan.json',{schemaVersion:'1.0-isolated-rhetorical-adjudication-execution-plan',status:'frozen-before-model-execution',debateNumber:n,debateId:r.debateId,model:{slug:a.execution.assessmentModel,displayLabel:a.execution.recordedDisplayModel,reasoningEffort:a.execution.reasoningEffort,authentication:a.execution.authentication},allowedInputs:allowed,packetPath:dir+'/input.json',outputPath:dir+'/output.json',readingAuditPath:dir+'/reading-audit.json',candidateCount:candidates.length,arrayMinItems:candidates.length,arrayMaxItems:candidates.length,attempts:1,retries:0,forkTurns:'none',readingGateRequired:true,directIncrementalCostUsd:0});
console.log(JSON.stringify({status:'fresh-anonymous-union-ready',candidateCount:candidates.length,moves:packet.moves.length,overrides:packet.controllerOverrides.length},null,2));
