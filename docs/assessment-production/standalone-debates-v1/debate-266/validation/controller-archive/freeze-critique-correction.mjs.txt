import fs from 'node:fs';import assert from 'node:assert/strict';import {fileRecord} from '../scripts/lib/assessment-production-standalone-debate-v1.mjs';
const n=process.argv[2];assert.match(n,/^\d{3}$/);const read=p=>JSON.parse(fs.readFileSync(p)),r=read('docs/assessment-production/standalone-debates-v1/registry.json').debates.find(x=>x.debateNumber===n);assert(r);const dir=r.root+'/publication/critique-editorial-repair-1',execution=read(dir+'/execution.json'),parity=read(r.contentParity.auditPath);assert.equal(execution.status,'passed-isolated-card-depth-repair');assert.equal(parity.status,'passed-content-parity-audit');assert.equal(fileRecord(r.root+'/publication/rhetorical-tag-review-1/preserved-prior-output.json').sha256,execution.after.sha256);
const o={schemaVersion:'1.0-bounded-publication-depth-correction',status:'applied-once-and-frozen',debateNumber:n,debateId:r.debateId,reason:'Direct editorial review rejected interchangeable critique language despite valid length counts; fresh isolated two-leaf repairs now explain each frozen source inference and its actual assessed limitation.',preservedPriorOutput:execution.before,writableShards:execution.shards.map(s=>({shardId:s.shardId,fields:s.writableFields,attempts:1,retries:0})),evidence:{before:execution.before,after:execution.after,execution:fileRecord(dir+'/execution.json'),contentParityAudit:fileRecord(r.contentParity.auditPath)},audit:{judgmentChanges:0,scoreChanges:0,moveChanges:0,tagChanges:0,descriptionChanges:0,changedLeafFields:execution.audit.changedLeafFields,maximumWritableFieldsPerShard:2,directIncrementalCostUsd:0}};fs.writeFileSync(r.contentParity.correctionPath,JSON.stringify(o,null,2)+'\n',{flag:'wx'});console.log(JSON.stringify({status:o.status,changedLeafFields:o.audit.changedLeafFields}));
