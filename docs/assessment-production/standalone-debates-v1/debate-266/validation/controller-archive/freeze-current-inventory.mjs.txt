import fs from 'node:fs';
import assert from 'node:assert/strict';
import {fileRecord,sha256,validateStandaloneInventory,validatePrimarySpeakerScopeException} from '../scripts/lib/assessment-production-standalone-debate-v1.mjs';
const n=process.argv[2];assert.match(n,/^\d{3,}$/);const read=p=>JSON.parse(fs.readFileSync(p));
const r=read('docs/assessment-production/standalone-debates-v1/registry.json').debates.find(r=>r.debateNumber===n);assert(r);const root=r.root;
const put=(p,v)=>fs.writeFileSync(p,JSON.stringify(v,null,2)+'\n',{flag:'wx'});
const inv=read(root+'/inventory/pre-audio-inventory.json'),plan=read(root+'/audio/plan.json'),notes=read(root+'/audio/controller-resolution.json'),release=read(root+'/inventory/reading-release.json'),review=read(root+'/inventory/pre-audio-mechanical-review.json');
assert.equal(notes.status,'passed-complete-source-review');assert.equal(notes.inventoryCandidate.sha256,fileRecord(root+'/inventory/pre-audio-inventory.json').sha256);assert.equal(plan.inventoryCandidate.sha256,notes.inventoryCandidate.sha256);
assert.equal(release.status,'authenticated-and-released-for-first-submission');assert(release.inputs.every(x=>x.literalCompleteReadAuthenticated));assert.equal(release.outputExistedAtRelease,false);
const events=read('.assessment-cache/captions/'+r.videoId+'/events.json'),validation=validateStandaloneInventory(inv,events);
assert.deepEqual(validation.belowHighAttributionMoveIds,plan.selectedMoveIds);assert.deepEqual(notes.checks.map(x=>x.moveId),plan.selectedMoveIds);
validatePrimarySpeakerScopeException({authorization:read(root+'/authorization.json'),sourceLock:read(root+'/source/source-lock.json'),inventory:inv});
const sessionRaw=fs.readFileSync(release.session.path),sessionRows=sessionRaw.toString().trim().split('\n').map(JSON.parse),contexts=sessionRows.filter(x=>x.type==='turn_context').map(x=>({model:x.payload.model,reasoningEffort:x.payload.effort}));
assert(contexts.length&&contexts.every(x=>x.model==='gpt-5.6-sol'&&x.reasoningEffort==='low'));
let known=plan.pricing.knownCostBeforeCallsUsd;
const clips=plan.clips.map(c=>{const p=plan.executionOutputRoot+'/'+c.clipId+'.json',cp=plan.executionOutputRoot+'/'+c.clipId+'.completion.json',completed=read(cp);assert.equal(completed.status,'succeeded-no-retry');assert.equal(fileRecord(p).sha256,completed.responseSha256);known+=completed.callCostEstimateUsd;return{clipId:c.clipId,startMs:c.startMs,endMs:c.endMs,moveIds:c.moveIds,response:fileRecord(p),completion:fileRecord(cp)};});
assert(known<=plan.pricing.standingCapUsd);
for(const check of notes.checks){assert.equal(check.materialMeaningResolved,true);assert.equal(check.retainedQuotationOwnershipResolved,true);assert(check.resolution.length>=40);assert(check.clipIds.length&&check.clipIds.every(id=>clips.some(c=>c.clipId===id&&c.moveIds.includes(check.moveId))));}
assert.equal(fs.existsSync(root+'/judgments/pass-a/output.json'),false);assert.equal(fs.existsSync(root+'/judgments/pass-b/output.json'),false);assert.equal(fs.existsSync(root+'/score-pass/output.json'),false);
put(root+'/inventory/inventory.json',inv);const inventory=fileRecord(root+'/inventory/inventory.json');
const audio={schemaVersion:'1.0-standalone-audio-verification',protocolId:'assessment-production-standalone-debate-v1',status:'complete',debateNumber:n,debateId:r.debateId,videoId:r.videoId,inventory,plan:fileRecord(root+'/audio/plan.json'),scopeAudioEvidence:fileRecord(root+'/source/scope-audio-evidence.json'),triggeredMoveIds:plan.selectedMoveIds,checks:notes.checks.map(c=>({...c,verificationClips:clips.filter(x=>c.clipIds.includes(x.clipId)),sourceAndInventoryUnchanged:true})),cost:{knownSuccessfulCallCostUsd:known,maximumPossibleTotalCostUsd:known,maximumPossibleTotalCostEstimateUsd:known,directCostUsd:known,uncertainPaidAttempts:0,successfulCalls:clips.length,scopeVerificationSuccessfulCalls:read(root+'/source/scope-audio-evidence.json').calls.length,retries:0,billingDashboardConfirmed:false,basis:'Returned usage at published token prices, including earlier scope-verification calls; no transport-uncertain paid calls. Estimate is not an independently reconciled billing invoice.',frozenEstimateUsd:plan.pricing.estimatedCompleteDebatePaidWorkUsd,capUsd:1},audit:{requiredAttributionChecks:plan.selectedMoveIds.length,completedAttributionChecks:notes.checks.length,unresolvedAttributionChecks:0,paidCallAttempts:clips.length,paidRetries:0,sourceCaptionBytesUnchanged:true,finalInventoryFrozenAfterAudio:true,directListeningPerformed:false}};
put(root+'/audio/audio-verification.json',audio);
put(root+'/audio/score-blind-supplement.json',{schemaVersion:'1.0-score-blind-audio-source-supplement',debateNumber:n,debateId:r.debateId,inventory,sourceVerification:fileRecord(root+'/audio/audio-verification.json'),method:'Independent audio-derived transcription checks, not direct listening. Scope boundaries were verified separately. Raw captions remain unchanged; only the approved eligible source may be scored.',clarifications:notes.checks.map(c=>({moveId:c.moveId,speaker:inv.moves.find(m=>m.moveId===c.moveId).speaker,sourceEventRange:[inv.moves.find(m=>m.moveId===c.moveId).sourceSpan.startEvent,inv.moves.find(m=>m.moveId===c.moveId).sourceSpan.endEvent],resolution:c.resolution})),scopeRule:'No excluded moderator-led branch or audience Q&A is eligible. Do not infer a blanket disagreement over whether confidence can be rational.',frozenBeforePrimaryJudgments:true});
put(root+'/inventory/session-authentication.json',{schemaVersion:'1.0-inventory-session-authentication',status:'authenticated-complete-source-and-first-inventory',debateNumber:n,debateId:r.debateId,session:{path:release.session.path,sha256:sha256(sessionRaw),bytes:sessionRaw.length,kind:'prefix-snapshot-after-source-reviewed-draft'},agentPath:release.agentPath,actualContextModels:contexts,completeUntruncatedSourceReadBeforeSubmission:true,readingRelease:fileRecord(root+'/inventory/reading-release.json'),readingAudit:fileRecord(root+'/inventory/reading-audit.json'),originalPlan:fileRecord(root+'/inventory/execution-plan.json'),sameFirstDraftContext:true,originalDraft:fileRecord(root+'/inventory/draft-parts.json'),sourceReviewedDraft:review.draft,controllerSourceReview:fileRecord(root+'/audio/controller-resolution.json'),output:inventory,isolation:'Fresh context with declared-input protocol isolation; not an operating-system filesystem sandbox.',directIncrementalCostUsd:0});
put(root+'/inventory/first-freeze-audit.json',{schemaVersion:'1.0-source-verified-first-inventory-freeze',status:'passed',debateNumber:n,createdAt:new Date().toISOString(),preservedPreAudioInventory:fileRecord(root+'/inventory/pre-audio-inventory.json'),output:inventory,validation,sourceReview:fileRecord(root+'/audio/controller-resolution.json'),mechanicalReview:fileRecord(root+'/inventory/pre-audio-mechanical-review.json'),changedAfterFirstFreeze:false,modelAuthoredRatings:0,modelAuthoredTotals:0,primaryJudgmentsExecutedBeforeFreeze:0,mechanicalOnlyCompilation:true,audio:fileRecord(root+'/audio/audio-verification.json'),directIncrementalCostUsd:0});
console.log(JSON.stringify({status:'first-inventory-frozen',validation,inventory,cost:audio.cost},null,2));
