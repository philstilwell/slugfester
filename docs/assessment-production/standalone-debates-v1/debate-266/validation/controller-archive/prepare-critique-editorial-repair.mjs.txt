import fs from 'node:fs';import assert from 'node:assert/strict';import {createHash} from 'node:crypto';
const n=process.argv[2];assert.match(n,/^\d{3}$/);const read=p=>JSON.parse(fs.readFileSync(p)),hash=b=>createHash('sha256').update(b).digest('hex'),rec=p=>{const b=fs.readFileSync(p);return{path:p,sha256:hash(b),bytes:b.length};},write=(p,o)=>fs.writeFileSync(p,JSON.stringify(o,null,2)+'\n',{flag:'wx'});
const registry=read('docs/assessment-production/standalone-debates-v1/registry.json'),r=registry.debates.find(x=>x.debateNumber===n);assert(r);const root=r.root,dir=root+'/publication/critique-editorial-repair-1',pub=read(root+'/publication/output.json'),ledger=read(root+'/final-ledger/final-ledger.json'),auth=read(root+'/authorization.json');assert.equal(pub.debateId,r.debateId);assert.equal(read(root+'/score-pass/attestation.json').ordinal,1);
fs.mkdirSync(dir,{recursive:false});fs.copyFileSync(root+'/publication/output.json',dir+'/preserved-prior-output.json',fs.constants.COPYFILE_EXCL);
const abandoned=root+'/publication/rhetorical-tag-review-1';for(const p of ['pass-a','pass-b'])assert(!fs.existsSync(abandoned+'/'+p+'/output.json'),'Cannot quarantine a completed review');fs.renameSync(abandoned,dir+'/aborted-pre-repair-rhetorical-reading');
const cards=pub.candidate.sections.flatMap((s,si)=>s.exchanges.flatMap((e,ei)=>['pro','con'].flatMap(side=>e[side]?[{path:`candidate.sections[${si}].exchanges[${ei}].${side}.critique`,card:e[side]}]:[])));assert.equal(cards.length,ledger.moves.length);
write(dir+'/editorial-failure.json',{status:'rejected-editorial-specificity-despite-mechanical-length-pass',debateNumber:n,createdAt:new Date().toISOString(),preservedPriorOutput:rec(dir+'/preserved-prior-output.json'),findings:['Every critique repeats its description, then substitutes generic cyclic dimension explanations for the source-specific frozen judgment rationales.','Repeated boilerplate includes its legitimate reach therefore remains local and generic references to admissible evidence, while several limitations fail to name the actual inferential gap.','All critique leaves require source-grounded repair; descriptions, AI Contribution, identity, evidence, judgments, scores, quotations, and overall commentary remain frozen.'],authorization:'Existing bounded score-neutral publication repair authority, with current user instruction to use discretion rather than request routine approvals.',maximumFieldsPerShard:2,attemptsPerShard:1,retries:0,abortedRhetoricalReads:{outputsCreated:false,semanticReviewReleased:false,reason:'Publication critiques failed direct editorial review before rhetorical release.'}});
const plans=[];for(let i=0;i<cards.length;i+=2){const id=String(i/2+1).padStart(2,'0'),sd=dir+'/shard-'+id;fs.mkdirSync(sd);const selected=cards.slice(i,i+2),sections=new Set(selected.map(x=>ledger.moves.find(m=>m.moveId===x.card.ledgerMoveId).sectionId));
 const packet={schemaVersion:'1.0-bounded-critique-editorial-repair-input',debateNumber:n,debateId:r.debateId,motion:pub.candidate.motion,scope:pub.candidate.sourceNote,sourceEvidence:rec(root+'/final-ledger/final-ledger.json'),writableFields:selected.map(x=>({path:x.path,moveId:x.card.ledgerMoveId,original:x.card.critique,description:x.card.words,lockedScore:x.card.score,lockedMove:ledger.moves.find(m=>m.moveId===x.card.ledgerMoveId)})),sectionContext:ledger.moves.filter(m=>sections.has(m.sectionId)).map(({moveId,side,speaker,claim,warrant,inference,sourceSpan,respondsToIds,responseComponents})=>({moveId,side,speaker,claim,warrant,inference,sourceSpan,respondsToIds,responseComponents})),requirements:['Rewrite ONLY declared critique leaves, using the actual two frozen judgment rationales and source, not a canned sentence keyed to a dimension number.','Exactly four labeled sentences in order: Strongest feature:, Principal limitation:, Live burden:, Locked score:. 105–130 whitespace words and at least880characters. Aim125–129words with necessary substantive detail; count mechanically before writing. Avoid abbreviations or interior terminal punctuation.','Each sentence must name the particular argument, example, distinction, evidence, counterexample, or inferential limitation; avoid generic phrases transferable to other debates. Do not simply repeat the description. The critique must explain the move rather than narrate assessment machinery.','Preserve substantive assessment: do not add new defects, penalties, source claims, or change scores. Explain the fixed score in terms of what this move accomplished and what remains unestablished. No need to list dimension numbers.','Scope exclusions remain binding. Moderator/audience material is not evidence. Use audio supplement for relevant attributions.','One first submission per shard; no overwriting rejected bytes or placeholder. Draft and check literal strings in memory before writing. Output JSON {debateNumber,debateId,shardId,fields:[{path,moveId,value}]} exactly in declared order, plus reading-audit.json. No other writes.'],shardId:id};
 write(sd+'/input.json',packet);const plan={schemaVersion:'1.0-isolated-two-field-repair-plan',debateNumber:n,debateId:r.debateId,shardId:id,model:{slug:auth.execution.assessmentModel,label:auth.execution.recordedDisplayModel,reasoningEffort:auth.execution.reasoningEffort,authentication:auth.execution.authentication},allowedInputs:[rec(sd+'/input.json'),rec(root+'/audio/score-blind-supplement.json')],outputPath:sd+'/output.json',readingAuditPath:sd+'/reading-audit.json',writableFields:selected.map(x=>x.path),attempts:1,retries:0,forkTurns:'none',directIncrementalCostUsd:0};write(sd+'/execution-plan.json',plan);plans.push({shardId:id,path:sd+'/execution-plan.json',fields:plan.writableFields});}
write(dir+'/plan.json',{status:'frozen-before-repair',debateNumber:n,debateId:r.debateId,preservedPriorOutput:rec(dir+'/preserved-prior-output.json'),shards:plans});console.log(JSON.stringify({shards:plans.length,fields:cards.length,dir},null,2));
