import fs from 'node:fs';import assert from 'node:assert/strict';import {createHash} from 'node:crypto';
const n=process.argv[2];assert.match(n,/^\d{3}$/);const read=p=>JSON.parse(fs.readFileSync(p)),rec=p=>{const b=fs.readFileSync(p);return{path:p,sha256:createHash('sha256').update(b).digest('hex'),bytes:b.length};},write=(p,o)=>fs.writeFileSync(p,JSON.stringify(o,null,2)+'\n',{flag:'wx'});
const r=read('docs/assessment-production/standalone-debates-v1/registry.json').debates.find(x=>x.debateNumber===n);assert(r);const base=r.root+'/publication/critique-editorial-repair-1',plan=read(base+'/plan.json'),old=read(base+'/shard-16/output.json'),input=read(base+'/shard-16/input.json');assert(old.fields[0].value.includes('does not earn a likelihood advantage'));
const next=String(plan.shards.length+1).padStart(2,'0'),dir=base+'/shard-'+next;assert.equal(next,'21');fs.mkdirSync(dir);const authorization={status:'one-exceptional-wording-replacement-authorized-by-delegated-discretion',debateNumber:n,createdAt:new Date().toISOString(),userInstruction:"I'm going to bed, so just use your discretion if you encounter anything that might trigger a request for approval.",scope:[old.fields[0].path],priorOutput:rec(base+'/shard-16/output.json'),attempts:1,retries:0,reason:'The first sentence incorrectly says a low prior prevents a likelihood advantage. A low prior can offset favorable likelihood in the posterior without removing the likelihood advantage. Correct only that distinction; preserve all actual assessment judgments and scores.',noGeneralRetryWaiver:true,directIncrementalCostUsd:0};write(dir+'/authorization.json',authorization);
const packet={...input,shardId:next,writableFields:[{...input.writableFields[0],original:old.fields[0].value}],requirements:[...input.requirements,'Exceptional narrow correction of one exhausted leaf under explicit current user-delegated discretion. The original first sentence confuses likelihood advantage with posterior warrant: low prior discounts posterior support, not conditional likelihood. Change only the necessary clause in the first sentence to state this accurately. Preserve the other three sentences byte-for-byte. No new judgment, critique defect, or numerical change. One attempt only, no further replacement.'],replacementAuthorization:authorization};write(dir+'/input.json',packet);const oldPlan=read(base+'/shard-16/execution-plan.json');write(dir+'/execution-plan.json',{...oldPlan,shardId:next,allowedInputs:[rec(dir+'/input.json'),rec(r.root+'/audio/score-blind-supplement.json')],outputPath:dir+'/output.json',readingAuditPath:dir+'/reading-audit.json',writableFields:[old.fields[0].path]});
plan.shards.push({shardId:next,path:dir+'/execution-plan.json',fields:[old.fields[0].path],supersedes:{shardId:'16',field:old.fields[0].path},exceptionAuthorization:rec(dir+'/authorization.json')});write(base+'/plan-before-wording-replacement.json',read(base+'/plan.json'));fs.writeFileSync(base+'/plan.json',JSON.stringify(plan,null,2)+'\n');console.log(JSON.stringify({status:authorization.status,shardId:next,field:old.fields[0].path}));
