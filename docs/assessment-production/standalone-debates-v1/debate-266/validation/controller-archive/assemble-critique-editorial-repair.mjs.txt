import fs from 'node:fs';import assert from 'node:assert/strict';import {createHash} from 'node:crypto';import {checkPublication} from './check-publication-draft.mjs';
const n=process.argv[2];assert.match(n,/^\d{3}$/);const read=p=>JSON.parse(fs.readFileSync(p)),hash=b=>createHash('sha256').update(b).digest('hex'),rec=p=>{const b=fs.readFileSync(p);return{path:p,sha256:hash(b),bytes:b.length};},write=(p,o)=>fs.writeFileSync(p,JSON.stringify(o,null,2)+'\n',{flag:'wx'});
const registry=read('docs/assessment-production/standalone-debates-v1/registry.json'),r=registry.debates.find(x=>x.debateNumber===n);assert(r);const dir=r.root+'/publication/critique-editorial-repair-1',plan=read(dir+'/plan.json'),prior=read(dir+'/preserved-prior-output.json');assert.equal(rec(r.root+'/publication/output.json').sha256,plan.preservedPriorOutput.sha256);const candidate=structuredClone(prior),shards=[];
for(const s of plan.shards){const sd=dir+'/shard-'+s.shardId,e=read(sd+'/execution.json'),out=read(sd+'/output.json');if(e.status==='validated-with-one-rejected-leaf-preserved-for-authorized-replacement'){assert.equal(e.supersededLeaves.length,1);for(const v of e.supersededLeaves){const replacement=plan.shards.find(x=>x.shardId===v.replacementShardId);assert(replacement&&replacement.supersedes.shardId===s.shardId&&replacement.supersedes.field===v.field);assert.equal(rec(v.authorization.path).sha256,v.authorization.sha256);assert.equal(read(dir+'/shard-'+v.replacementShardId+'/execution.json').status,'passed-isolated-two-field-editorial-repair');}}else assert.equal(e.status,'passed-isolated-two-field-editorial-repair');assert.equal(e.output.sha256,rec(sd+'/output.json').sha256);assert.equal(e.input.sha256,rec(sd+'/input.json').sha256);assert(e.writableFieldCount<=2&&e.attempts===1&&e.retries===0);for(const f of out.fields){assert(s.fields.includes(f.path));if(e.supersededLeaves?.some(x=>x.field===f.path))continue;const parts=f.path.replaceAll('[','.').replaceAll(']','').split('.');let target=candidate;for(const k of parts.slice(0,-1))target=target[k];target[parts.at(-1)]=f.value;}shards.push(e);}
const validation=checkPublication(candidate,read(r.root+'/publication/input-bundle.json'));const masked=x=>{const z=structuredClone(x);for(const s of z.candidate.sections)for(const e of s.exchanges)for(const side of ['pro','con'])if(e[side])e[side].critique='[critique]';return z;};assert.deepEqual(masked(candidate),masked(prior));
write(dir+'/accepted-output.json',candidate);fs.copyFileSync(dir+'/accepted-output.json',r.root+'/publication/output.json');
write(dir+'/execution.json',{status:'passed-isolated-card-depth-repair',debateNumber:n,debateId:r.debateId,model:shards[0].model,shards,audit:{judgmentChanges:0,scoreChanges:0,moveChanges:0,tagChanges:0,maximumWritableFieldsPerShard:2,changedLeafFields:new Set(shards.flatMap(x=>x.writableFields)).size,exceptionalSingleLeafReplacementAttempts:plan.shards.filter(x=>x.supersedes).length,attemptsPerShard:1,retries:0,directIncrementalCostUsd:0},before:plan.preservedPriorOutput,after:rec(r.root+'/publication/output.json'),validation});
write(dir+'/installation.json',{status:'all-bounded-critique-fields-installed-once',debateNumber:n,before:plan.preservedPriorOutput,after:rec(r.root+'/publication/output.json'),execution:rec(dir+'/execution.json'),validation});
r.contentParity={...(r.contentParity||{}),auditPath:r.root+'/publication/content-parity-audit.json',correctionPath:r.root+'/publication/correction-critique-editorial.json',preservedOutputPath:dir+'/preserved-prior-output.json',executionPath:dir+'/execution.json'};fs.writeFileSync('docs/assessment-production/standalone-debates-v1/registry.json',JSON.stringify(registry,null,2)+'\n');console.log(JSON.stringify({status:'editorial-repairs-installed',validation},null,2));
