import fs from 'node:fs';import assert from 'node:assert/strict';import {createHash} from 'node:crypto';
const [n,id,...flags]=process.argv.slice(2);assert.match(n,/^\d{3}$/);assert.match(id,/^\d{2}$/);const read=p=>JSON.parse(fs.readFileSync(p)),hash=b=>createHash('sha256').update(b).digest('hex'),rec=p=>{const b=fs.readFileSync(p);return{path:p,sha256:hash(b),bytes:b.length};};
const r=read('docs/assessment-production/standalone-debates-v1/registry.json').debates.find(x=>x.debateNumber===n);assert(r);const dir=r.root+'/publication/critique-editorial-repair-1/shard-'+id,plan=read(dir+'/execution-plan.json'),packet=read(dir+'/input.json'),out=read(plan.outputPath),release=read(dir+'/reading-release.json');assert.equal(release.status,'authenticated-and-released-for-first-submission');assert.equal(out.debateNumber,n);assert.equal(out.debateId,r.debateId);assert.equal(out.shardId,id);assert.deepEqual(out.fields.map(x=>x.path),plan.writableFields);assert(out.fields.length<=2);
const metrics=out.fields.map((f,i)=>{assert.equal(f.moveId,packet.writableFields[i].moveId);assert.deepEqual(Object.keys(f).sort(),['moveId','path','value']);assert(typeof f.value==='string');assert.notEqual(f.value,packet.writableFields[i].original);const words=f.value.trim().split(/\s+/).length;assert(words>=105&&words<=130,`${f.moveId} words ${words}`);assert(f.value.length>=880,`${f.moveId} chars ${f.value.length}`);const labels=['Strongest feature:','Principal limitation:','Live burden:','Locked score:'],parts=f.value.split(/(?=Principal limitation:|Live burden:|Locked score:)/).map(s=>s.trim());assert.equal(parts.length,4);assert(parts.every((v,i)=>v.startsWith(labels[i])&&/[.!?]$/.test(v)));assert.equal((f.value.match(/[.!?](?=\s|$)/g)||[]).length,4);assert(!/[\u3400-\u9fff\uac00-\ud7af\ufffd]/u.test(f.value));assert(parts[3].includes(String(packet.writableFields[i].lockedScore)));return{moveId:f.moveId,words,characters:f.value.length};});
for(const x of plan.allowedInputs)assert.equal(rec(x.path).sha256,x.sha256);const session=fs.readFileSync(release.session.path),contexts=session.toString().trim().split('\n').map(JSON.parse).filter(x=>x.type==='turn_context').map(x=>({model:x.payload.model,reasoningEffort:x.payload.effort}));assert(contexts.length&&contexts.every(x=>x.model===plan.model.slug&&x.reasoningEffort===plan.model.reasoningEffort));read(plan.readingAuditPath);
const superseders=read(r.root+'/publication/critique-editorial-repair-1/plan.json').shards.filter(x=>x.supersedes?.shardId===id);
if(packet.replacementAuthorization){const oldParts=packet.writableFields[0].original.split('Principal limitation:'),newParts=out.fields[0].value.split('Principal limitation:');assert.equal(newParts[1],oldParts[1],'Exceptional replacement changed the three frozen sentences');assert(!newParts[0].includes('does not earn a likelihood advantage'));}
if(flags.includes('--freeze-after-direct-review')||flags.includes('--freeze-preserving-superseded-leaf')){if(superseders.length)assert(flags.includes('--freeze-preserving-superseded-leaf'),'Rejected leaf must be explicitly retained as superseded');else assert(!flags.includes('--freeze-preserving-superseded-leaf'));fs.writeFileSync(dir+'/execution.json',JSON.stringify({status:superseders.length?'validated-with-one-rejected-leaf-preserved-for-authorized-replacement':'passed-isolated-two-field-editorial-repair',debateNumber:n,debateId:r.debateId,shardId:id,model:plan.model,actualModelContexts:contexts,readingRelease:rec(dir+'/reading-release.json'),readingAudit:rec(plan.readingAuditPath),input:rec(dir+'/input.json'),output:rec(plan.outputPath),session:{path:release.session.path,sha256:hash(session),bytes:session.length},writableFieldCount:out.fields.length,writableFields:plan.writableFields,metrics,supersededLeaves:superseders.map(x=>({field:x.supersedes.field,replacementShardId:x.shardId,authorization:x.exceptionAuthorization})),attempts:1,retries:0,forkTurns:'none',directEditorialReview:superseders.length?'Controller accepted the unaffected leaf and retained the identified likelihood-versus-posterior wording error for its separately authorized one-leaf replacement.':'Controller read every replacement and compared its inferential specificity with frozen source and primary rationales before freezing.',directIncrementalCostUsd:0},null,2)+'\n',{flag:'wx'});}
console.log(JSON.stringify({status:'valid-bounded-fields',shardId:id,metrics,fields:out.fields},null,2));
