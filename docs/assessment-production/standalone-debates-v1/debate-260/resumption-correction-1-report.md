# Debate 260 correction and audio checkpoint

The user-authorized single inventory correction passed. The final frozen inventory contains 40 original moves (20 per side) across seven source-grounded sections. Counts, capacity, source quotations, classification, and reply links pass the current unchanged validator. All previous failures and raw reviewer outputs remain preserved. No selected argument, substantive evidence, weight, importance, or burden link was changed.

The correction audit records two exact controller normalizations: removal of an accidental copied suffix and correction of the reviewer's mistaken destination-section count/rationale. No additional model repair was run.

The final inventory SHA-256 is `9b17d989ef771a3378d292b48195f4e13d54c3cd886a0d1cdf783a56935e4ece`. Its manifest and score-blind judgment packet are frozen. Primary judgments have not begun.

Twenty-five audio checks were frozen. The first two succeeded and were reviewed, at exact combined direct cost **$0.1279475**. There are no failed or transport-uncertain paid calls. The remaining 23 clips have never been submitted.

Observed charges imply approximately **$1.30 total**, invalidating the earlier $0.44 indicative estimate and $0.93 allowance. The installed add-debate skill requires stopping when the full estimate exceeds its standing $1 cap. A cumulative **$2 cap** is proposed, with a conservatively recalculated $1.7568691 full-plan allowance, but is not yet authorized. Preserve the successful responses and all frozen clip bytes. If authorized, append a revised cost plan for the unattempted clips; do not overwrite the original plan, repeat successful calls, or alter inventory substance.

Two zero-write controller setup defects were fixed before successful execution: absolute installed-skill paths were initially passed to a relative-path helper; and the audio preparer initially read speaker identities from `inventory.sides` instead of the actual `inventory.routes`. Neither failed setup made a paid call or wrote assessment outputs.

All four temporary controllers were byte-exact gzip archived, round-trip checked, and removed from the scripts directory. Audio and reference files remain in the ignored recovery cache. No temporary browser, server, or paid process remains. No public site files, commit, push, or pull request were created. Existing unrelated work remains untouched.

Resume from `docs/assessment-production/standalone-debates-v1/debate-260/audio/cost-reforecast-pause.json` after an explicit cap decision. Do not rebuild the valid inventory or either successful audio result.
