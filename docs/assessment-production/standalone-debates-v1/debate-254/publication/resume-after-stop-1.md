# Debate 254 publication checkpoint

The current source ownership recovery, all 22 audio checks, both composite primary passes, anonymous adjudication, resolved ledger and single score calculation have passed. Official collective scores are pro 69, con 85; stability passes and the computable equal-active-speaker diagnostic does not change the leader. All four leave-one-speaker-out diagnostics are not computable, so do not describe those as successful robustness tests. Never calculate a replacement official score.

The initial publication was rejected for word overruns, interchangeable critique scaffolding, padded descriptions, and three meta-writing AI Contribution bodies. Preserve `initial-output.json`, `initial-validation.json`, and `initial-editorial-rejection.json`. This is not accepted or publishable prose.

`repairs/plan.json` authorizes 24 disjoint repairs, at most two leaf fields each and one attempt each. Only 01–03 were dispatched. Their actual agents, packets, outputs, hashes and validation are recorded in `repairs/execution-checkpoint-1.json`.

- 01: description passes; critique is 145 words, exceeding the unchanged 130-word maximum. Only `sections.0.exchanges.0.pro.critique` is exhausted and requires new explicit authorization. Preserve the valid description and failed critique exactly.
- 02: both fields pass; critique 122 words, 905 characters.
- 03: both fields pass; critique 127 words, 994 characters.
- 04–24: prepared but never dispatched. Do not count preparation as an attempt.

The active stop is `stop-1.json`. Do not dispatch further repairs, publish, commit, or push before resolving the exhausted field under new authority. A resumed repair should use a hard in-memory word/character count before its single final write. Retain all successful fields. Do not relax any content limit or replace assessments to solve an editorial failure.

After authorized recovery, validate and mechanically assemble only the declared fields into a new prose artifact, authenticate all original and repair outputs, and perform substantive whole-record review. Check critique six-word repetition against the ordinary 15% mean / 25% maximum limits; a numeric word mean does not excuse generic exposition. Check both AI Contribution sides for direct, distinct reasoning and normal punctuation.

Still unfinished: publication-prose execution authentication; two blind fallacy/bias reviews and complete-union adjudication; final tag application and audit; independent 25-debate content parity; complete team publication adapter/auditor and generic standalone routing; actual production candidate validation; generated site output; transcript-corpus living inventory update; desktop/mobile rendering and all four profiles/ranking exclusions; normal checks and final preflight; topic-branch commit/push; protected-main pull request, required green check, merge, Site Quality and Pages verification. No site publication or Git commit has occurred.

`scripts/prepare-team-tag-reviews.mjs`, the tag validators and their synthetic test are prepared but have not been executed against this debate. They require a validated `publication/prose-execution.json`. `scripts/audit-standalone-team-debate.mjs` still supports only source/inventory modes and needs revision-aware full-stage routing. The generic standalone auditor recognizes the team profile but does not yet dispatch to a complete team audit. `validateTeamSiteLedgerAdapter` still needs publication/tag/parity/rendering provenance checks. Do not claim end-to-end team publication support is finished.

Work only in the existing `codex/team-debate-gcszn0dvflm` checkout at `.assessment-cache/team-debate-worktree`. The original repository checkout and its old Debate 238 edits are out of scope. All historical standalone audits 196–253 passed earlier in this run. There were no new paid calls during this recovery; existing successful audio usage is estimated at $1.27772 within the separately authorized $2 cap, with an earlier rejected request's cost uncertainty recorded separately.
