import fs from 'node:fs';
import assert from 'node:assert/strict';
import {spawnSync} from 'node:child_process';
import {fileRecord} from '../scripts/lib/assessment-production-standalone-debate-v1.mjs';
const args=process.argv.slice(2),n=args[args.indexOf('--debate')+1],id=args[args.indexOf('--id')+1];assert(args.includes('--debate')&&args.includes('--id'));assert.match(n,/^\d{3,}$/);assert.match(id,/^[a-z0-9]+(?:-[a-z0-9]+)*$/);
const root=`docs/assessment-production/standalone-debates-v1/debate-${n}`,read=p=>JSON.parse(fs.readFileSync(p)),put=(p,o)=>fs.writeFileSync(p,JSON.stringify(o,null,2)+'\n',{flag:'wx'});
const initial=read(root+'/source/acquisition-plan.json'),recovery=read(root+'/source/authorized-source-recovery-1.json'),input=read(recovery.inputs.path),audio=read(root+'/source/audio-acquisition.json'),bp=read(root+'/source/boundary-verification-plan-1.json');
assert.equal(initial.videoId,recovery.videoId);assert.equal(initial.videoId,audio.videoId);assert.equal(initial.debateNumber,n);
const verify=r=>assert.equal(fileRecord(r.path).sha256,r.sha256),cache=`.assessment-cache/captions/${initial.videoId}`,norm=recovery.sourceOutputRoot+'/normalization-inputs';
for(const p of ['events.json','transcript.txt','manifest.json','openai-diarized-chunks.json'])assert(!fs.existsSync(cache+'/'+p));assert(!fs.existsSync(norm));fs.mkdirSync(norm);
verify(audio.media);const raw=[],results=[],changes=[],chunks=[];
for(const c of input.clips){
 const base=(c.index===recovery.exceptionalInputIndex?recovery.recoveryOutputRoot:recovery.sourceOutputRoot)+`/chunk-${String(c.index).padStart(3,'0')}`;
 const r=read(base+'.result.json');assert.equal(r.exitCode,0);verify(r.output);raw.push(r.output);results.push({record:fileRecord(base+'.result.json'),result:r});
 const j=read(r.output.path);assert(j.segments?.length);chunks.push(j);
}
const empty=chunks[0].segments.filter(s=>!String(s.text).trim());assert.equal(empty.length,1);changes.push({kind:'remove-empty-nonspeech-event',chunk:0,segments:empty,reason:'No source words are removed; the original empty response segment remains in raw storage.'});chunks[0].segments=chunks[0].segments.filter(s=>String(s.text).trim());
const b2=bp.clips.find(c=>c.index===2),b5=bp.clips.find(c=>c.index===5);
for(const c of [b2,b5]){const r=read(c.base+'.result.json');assert.equal(r.exitCode,0);verify(r.output);results.push({record:fileRecord(c.base+'.result.json'),result:r});}
const v2=read(b2.base+'.json'),v5=read(b5.base+'.json');
assert.equal(chunks[2].segments.at(-1).id,121);assert.equal(chunks[2].segments.at(-2).id,120);assert.equal(chunks[3].segments[0].id,0);
assert(v2.segments[2].text.includes('who don\'t take it literally are'));
changes.push({kind:'verified-cross-boundary-sentence-replacement',affected:[{chunk:2,segmentIds:[120,121]},{chunk:3,segmentIds:[0]}],verification:fileRecord(b2.base+'.json'),verificationSegmentIds:[2],reason:'Original last segment exceeds its physical input and hallucinates a repeated phrase. The new longer-context audio result supplies one complete sentence at its own unaltered source timestamps; no timestamp clamping or editorial wording.'});
chunks[2].segments=chunks[2].segments.filter(s=>![120,121].includes(s.id));chunks[3].segments=chunks[3].segments.filter(s=>s.id!==0);
chunks[2].segments.push({...v2.segments[2],id:'verified-boundary-002-2',start:b2.startSeconds+v2.segments[2].start-600,end:b2.startSeconds+v2.segments[2].end-600});
assert.equal(chunks[5].segments.at(-1).id,113);assert.deepEqual(chunks[6].segments.slice(0,3).map(s=>s.id),[0,1,2]);assert(v5.segments[3].text.includes('emphasize creed'));
changes.push({kind:'verified-cross-boundary-sentence-replacement',affected:[{chunk:5,segmentIds:[113]},{chunk:6,segmentIds:[0,1,2]}],verification:fileRecord(b5.base+'.json'),verificationSegmentIds:[3,4],reason:'The retained retry ends with an incomplete out-of-bounds reply. The independent clip supplies the creed/confirmation sentences and the following introduction, matching the next raw chunk. Keep its own original timestamps and words; retain all rejected raw bytes.'});
chunks[5].segments=chunks[5].segments.filter(s=>s.id!==113);chunks[6].segments=chunks[6].segments.filter(s=>![0,1,2].includes(s.id));
for(const s of v5.segments.filter(s=>[3,4].includes(s.id)))chunks[5].segments.push({...s,id:`verified-boundary-005-${s.id}`,start:b5.startSeconds+s.start-1500,end:b5.startSeconds+s.end-1500});
const derived=chunks.map((j,i)=>{const p=norm+`/chunk-${String(i).padStart(3,'0')}.json`;put(p,j);return fileRecord(p);});
const auditPath=root+'/source/normalization-provenance.json';
put(auditPath,{schemaVersion:'1.0-source-normalization-provenance',createdAt:new Date().toISOString(),debateNumber:n,debateId:id,videoId:initial.videoId,controller:fileRecord('tmp/normalize-current-source.mjs'),normalizer:fileRecord('scripts/normalize-openai-diarized-transcript.mjs'),rawInputs:raw,derivedInputs:derived,changes,wordsOtherwiseUnchanged:true,timestampsOtherwiseUnchanged:true,derivedCrossBoundarySegmentsAreVerificationEvidenceNotOriginalChunkClaims:true,rawSourcePreserved:true,models:[{model:'gpt-4o-transcribe-diarize',chunkIndices:[0]},{model:'whisper-1',chunkIndices:input.clips.slice(1).map(c=>c.index),verificationClips:bp.clips.map(c=>c.base+'.json')}],speakerAttribution:'Only chunk 000 has anonymous automatic diarization. Later chunks contain no automatic speaker labels; named attribution must be independently established from the speaker-labelled publisher transcript and contextual turn boundaries. No direct listening is claimed.',results});
const r=spawnSync('node',['scripts/normalize-openai-diarized-transcript.mjs','--video-id',initial.videoId,'--debate-id',id,'--title',initial.title,'--source-url',initial.canonicalUrl,'--audio-source-url',initial.canonicalUrl,'--chunk-dir',norm,'--chunk-seconds','300','--channel',initial.channel,'--model','mixed-openai-timestamped-source'],{encoding:'utf8'});assert.equal(r.status,0,r.stderr);console.log(r.stdout);
const manifestPath=cache+'/manifest.json',manifest=read(manifestPath),events=read(cache+'/events.json'),physicalDuration=Number(audio.probe.format.duration)*1000;
manifest.modelDetails=read(auditPath).models;manifest.normalizationProvenance=fileRecord(auditPath);manifest.physicalSourceDurationMs=physicalDuration;manifest.sourceDurationRoundedSeconds=initial.durationSeconds;manifest.speakerLabelPolicy=read(auditPath).speakerAttribution;manifest.originalUploadEnding='The original upload and official publisher text both end during Sacks final audience-question reply. This is a source cutoff, not an acquisition failure; all audience questions are excluded from assessment.';
fs.writeFileSync(manifestPath,JSON.stringify(manifest,null,2)+'\n');
assert(events.every((e,i)=>Number.isInteger(e.startMs)&&e.startMs>=0&&Number.isInteger(e.durationMs)&&e.durationMs>=0&&e.startMs+e.durationMs<=physicalDuration&&e.text.trim()&&(!i||e.startMs>=events[i-1].startMs)));
for(const [p,h] of [['events.json',manifest.normalizedEventsSha256],['transcript.txt',manifest.transcriptSha256],['openai-diarized-chunks.json',manifest.rawTranscriptSha256]])assert.equal(fileRecord(cache+'/'+p).sha256,h);
const gaps=events.flatMap((e,i)=>i&&e.startMs-events[i-1].startMs-events[i-1].durationMs>4000?[{priorEvent:i-1,nextEvent:i,startMs:events[i-1].startMs+events[i-1].durationMs,endMs:e.startMs,gapMs:e.startMs-events[i-1].startMs-events[i-1].durationMs}]:[]);
const failed=read(recovery.sourceOutputRoot+'/chunk-005.result.json'),known=results.reduce((s,r)=>s+r.result.knownSuccessfulCallCostUsd,0),maximum=known+failed.maximumPossiblePaidCostUsd;
put(root+'/source/normalized-source-audit.json',{status:'normalized-awaiting-final-source-scope-review',createdAt:new Date().toISOString(),debateNumber:n,debateId:id,videoId:initial.videoId,files:{events:fileRecord(cache+'/events.json'),transcript:fileRecord(cache+'/transcript.txt'),manifest:fileRecord(manifestPath),raw:fileRecord(cache+'/openai-diarized-chunks.json')},provenance:fileRecord(auditPath),physicalDurationMs:physicalDuration,firstEventMs:events[0].startMs,maxEndMs:Math.max(...events.map(e=>e.startMs+e.durationMs)),eventCount:events.length,spokenWordCount:events.reduce((s,e)=>s+e.text.split(/\s+/).length,0),gapsOverFourSeconds:gaps,knownSuccessfulCallCostUsd:Number(known.toFixed(6)),maximumPossiblePaidCostUsd:Number(maximum.toFixed(6)),failedCall:fileRecord(recovery.sourceOutputRoot+'/chunk-005.result.json'),billingConfirmed:false,fullWorkflowUpperEstimateUsd:.9192,sourceGatePassed:false});
console.log(JSON.stringify({normalized:true,eventCount:events.length,gaps,knownSuccessfulCallCostUsd:known,maximumPossiblePaidCostUsd:maximum}));
