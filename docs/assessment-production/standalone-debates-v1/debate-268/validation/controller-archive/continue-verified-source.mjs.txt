import fs from 'node:fs';
import assert from 'node:assert/strict';
import {spawnSync} from 'node:child_process';
import {fileRecord} from '../scripts/lib/assessment-production-standalone-debate-v1.mjs';
const args=process.argv.slice(2),mode=args[0],n=args[args.indexOf('--debate')+1];assert(args.includes('--debate'));assert.match(n,/^\d{3,}$/);
const root=`docs/assessment-production/standalone-debates-v1/debate-${n}`,read=p=>JSON.parse(fs.readFileSync(p)),put=(p,o)=>fs.writeFileSync(p,JSON.stringify(o,null,2)+'\n',{flag:'wx'}),planPath=root+'/source/verified-source-continuation.json';
const priorPath=root+'/source/authorized-source-recovery-1.json',prior=read(priorPath),inputs=read(prior.inputs.path),boundaryPath=root+'/source/boundary-verification-plan-1.json',boundary=read(boundaryPath);
const verify=r=>assert.equal(fileRecord(r.path).sha256,r.sha256);
if(mode==='freeze'){
 const retryPath=prior.recoveryOutputRoot+'/chunk-005.result.json',retry=read(retryPath);assert.equal(retry.exitCode,0);assert.deepEqual(retry.qualityIssues,['Segment outside frozen input bounds']);verify(retry.output);
 const checks=boundary.clips.map(c=>{const resultPath=c.base+'.result.json',r=read(resultPath);assert.equal(r.exitCode,0);verify(r.output);const j=read(r.output.path);assert(j.segments.every(s=>s.start>=0&&s.end>=s.start&&s.end<=c.durationSeconds));return{input:c.input,rawSource:c.rawRecord,verifiedOutput:r.output,result:fileRecord(resultPath),startSeconds:c.startSeconds,absoluteSegments:j.segments.map(({start,end,text})=>({startMs:Math.round((c.startSeconds+start)*1000),endMs:Math.round((c.startSeconds+end)*1000),text}))};});
 const indices=prior.allowedInputIndices.filter(i=>i!==prior.exceptionalInputIndex);for(const i of indices)assert(!fs.existsSync(`${prior.sourceOutputRoot}/chunk-${String(i).padStart(3,'0')}.started.json`));
 put(planPath,{schemaVersion:'1.0-verified-boundary-source-continuation',createdAt:new Date().toISOString(),debateNumber:n,videoId:prior.videoId,priorPlan:fileRecord(priorPath),preservedRetryResult:fileRecord(retryPath),boundaryPlan:fileRecord(boundaryPath),controller:fileRecord('tmp/continue-verified-source.mjs'),inputs:prior.inputs,wrapper:prior.wrapper,helper:prior.helper,sourceOutputRoot:prior.sourceOutputRoot,model:prior.model,allowedInputIndices:indices,attemptsPerInput:1,automaticRetries:0,usdPerMinute:.006,fullWorkflowUpperEstimateUsd:.9192,checks,review:{finding:'Both overruns are chunk-boundary transcription artifacts. The independent 30-second clips contain coherent complete wording within physical bounds and agree with the official publisher text. Original invalid segments remain quarantined, not clamped or relabeled valid.',boundary15MinuteDisposition:'Verified segment 895–904s supplies the entire Chief Rabbi / literal reading sentence without the invented repeated tail. It will replace the affected split sentence in a derivative normalization input, preserving all raw files.',boundary30MinuteDisposition:'Verified segments 1798.04–1814.84s continue the creed/confirmation/community reply beyond the original boundary. Match the next unattempted chunk before assembling a source-exact replacement.',authorizationEffect:'Continue once through only the already authorized, unattempted audio inputs. This is not a further full-chunk retry or a passed complete-source gate.',sourceGatePassed:false,assessmentMayStart:false}});console.log(JSON.stringify({frozen:true,unattemptedInputIndices:indices,sourceGatePassed:false}));
}else if(mode==='run'){
 const p=read(planPath);for(const r of [p.controller,p.priorPlan,p.preservedRetryResult,p.boundaryPlan,p.inputs,p.wrapper,p.helper])verify(r);for(const c of p.checks){verify(c.verifiedOutput);verify(c.result);}
 const index=Number(args[args.indexOf('--chunk')+1]);assert(p.allowedInputIndices.includes(index));for(const prev of p.allowedInputIndices.filter(i=>i<index)){const r=read(`${p.sourceOutputRoot}/chunk-${String(prev).padStart(3,'0')}.result.json`);assert.equal(r.exitCode,0);assert(r.qualityPassed);}
 const c=inputs.clips[index];verify(c.input);const base=`${p.sourceOutputRoot}/chunk-${String(index).padStart(3,'0')}`,out=base+'.json';for(const suffix of ['.json','.started.json','.result.json','.stdout.log','.stderr.log'])assert(!fs.existsSync(base+suffix));
 const cost=Math.ceil(c.durationSeconds)*p.usdPerMinute/60;put(base+'.started.json',{videoId:p.videoId,chunk:index,model:p.model,startedAt:new Date().toISOString(),input:c.input,plan:fileRecord(planPath),maximumPossibleAttemptCostUsd:cost});
 const r=spawnSync('python3',[p.wrapper.path,c.input.path,'--language','en','--out',out],{encoding:'utf8',timeout:195000,maxBuffer:1000000,env:{...process.env,PYTHONDONTWRITEBYTECODE:'1'}});fs.writeFileSync(base+'.stdout.log',r.stdout??'',{flag:'wx'});fs.writeFileSync(base+'.stderr.log',r.stderr??'',{flag:'wx'});
 const issues=[];if(r.status===0&&fs.existsSync(out)){const j=read(out);if(!j.segments?.length)issues.push('No timed segments');else{if(!j.segments.every(s=>s.start>=0&&s.end>=s.start&&s.end<=c.durationSeconds+1))issues.push('Segment outside frozen input bounds');if(!(j.segments[0].start<15&&j.segments.at(-1).end>c.durationSeconds-15))issues.push('Endpoint coverage requires inspection');}}else issues.push('Paid request failed; no automatic retry authorized');
 const result={videoId:p.videoId,chunk:index,model:p.model,completedAt:new Date().toISOString(),exitCode:r.status,signal:r.signal,attempts:1,retries:0,knownSuccessfulCallCostUsd:r.status===0?cost:null,maximumPossiblePaidCostUsd:cost,pricingBasis:'Frozen input duration rounded up to the next second at $0.006/minute; invoice not verified',output:fs.existsSync(out)?fileRecord(out):null,billingConfirmed:false,qualityPassed:issues.length===0,qualityIssues:issues};put(base+'.result.json',result);console.log(JSON.stringify(result));assert.equal(r.status,0,'Paid attempt failed; stop without retry');assert.equal(issues.length,0,'Source-quality issue; inspect before further calls');
}else throw new Error('Unknown mode');
