import fs from 'node:fs';import assert from 'node:assert/strict';import{createHash}from'node:crypto';
const args=process.argv.slice(2),n=args[args.indexOf('--debate')+1];assert(args.includes('--debate'));assert.match(n,/^\d{3,}$/);
const read=p=>JSON.parse(fs.readFileSync(p,'utf8')),rec=p=>{const b=fs.readFileSync(p);return{path:p,sha256:createHash('sha256').update(b).digest('hex'),bytes:b.length};},put=(p,v)=>fs.writeFileSync(p,JSON.stringify(v,null,2)+'\n',{flag:'wx'});
const r=read('docs/assessment-production/standalone-debates-v1/registry.json').debates.find(x=>x.debateNumber===n);assert(r);
const root=r.root,base=root+'/publication/exceptional-field-repair-1',review=read(root+'/publication/field-repair-1/controller-editorial-review.json'),bundle=read(root+'/publication/input-bundle.json'),ledger=read(bundle.ledgerPath),a=read(root+'/authorization.json');
assert.equal(review.status,'blocked-on-two-exhausted-field-editorial-corrections');assert.equal(review.holds.length,2);assert(!fs.existsSync(root+'/publication/output.json'));assert(!fs.existsSync(base));
for(const x of read(root+'/publication/execution-plan.json').allowedInputs)assert.equal(rec(x.path).sha256,x.sha256);
for(const h of review.holds)assert.equal(rec(h.originalRepairOutput.path).sha256,h.originalRepairOutput.sha256);
fs.mkdirSync(base);
const authorization={schemaVersion:'1.0-specific-two-field-repair-exception',status:'authorized-before-extra-attempt',debateNumber:n,debateId:r.debateId,authorizedAt:new Date().toISOString(),userReply:'Yes.',approvedRequest:'May I make one additional two-field repair, leaving all evidence and scores unchanged? Added cost: $0. Fallacy/bias review and publication checks would then follow; nothing has been published yet.',reason:rec(root+'/publication/field-repair-1/controller-editorial-review.json'),fields:review.holds.map(h=>h.field),additionalAttemptsPerField:1,automaticFurtherRetries:false,maximumWritableLeafFields:2,other122ProseFieldsImmutable:true,sourceAndScoresImmutable:true,directIncrementalCostEstimateUsd:0,paidCallsAuthorized:0};put(base+'/authorization.json',authorization);
const packet={schemaVersion:'1.0-exceptional-two-field-publication-repair',debateNumber:n,debateId:r.debateId,authorization:rec(base+'/authorization.json'),fields:review.holds.map(h=>({path:h.field,moveId:h.moveId,originalText:h.originalText,issue:h.issue,allowedChange:h.permittedProposedChange,lockedMove:ledger.moves.find(m=>m.moveId===h.moveId)})),rules:['Read all allowed inputs literally and completely in bounded untruncated chunks; report reading checkpoint and await controller release before writing.','Exactly one additional attempt for the two declared critique strings; preserve every other field and every unaffected sentence byte-for-byte. First field permits only eastward wind -> east wind. Second field permits only its Live burden sentence to change.','For the second field explain only the unresolved charity measurement burden: coverage of religious and secular causes does not establish church-giving inclusion or its effect on the association. Do not claim Sacks excluded church donations, attribute controller instructions to him, add a fallacy or penalty, or change the existing assessment.','Each full critique must stay105–130words and>=880characters, exactly four labeled sentences with normal punctuation. Author the new sentence as natural literal prose; no automatic padding, truncation, or generic filler.','Use only declared inputs and output paths, no outside research, other agents, calculator, or credentials. Shell commands remove OPENAI_API_KEY and ANTHROPIC_API_KEY. Code may validate and serialize exact prose only.','After release, validate the complete exact literal output in memory with the declared checker before the sole apply_patch submission of output and reading-audit. Never overwrite submitted output or retry a failed submission.'],outputContract:{status:'complete-authorized-two-field-repair',debateNumber:n,debateId:r.debateId,fields:review.holds.map(h=>({path:h.field,text:'The complete critique string, preserving all sentences outside the authorized change.'}))}};
put(base+'/packet.json',packet);
const checker='.assessment-cache/debate-'+n+'-check-exceptional-publication-repair.mjs';
const allowedInputs=[rec(base+'/packet.json'),rec(bundle.transcriptPath),rec(bundle.audioSupplementPath),rec('docs/assessment-production-workflow.md'),rec('docs/reassessment-rubric-v2.1.md'),rec(checker)];
put(base+'/execution-plan.json',{schemaVersion:'1.0-exceptional-publication-repair-execution-plan',status:'frozen-before-single-authorized-exceptional-attempt',debateNumber:n,debateId:r.debateId,model:{slug:a.execution.assessmentModel,displayLabel:a.execution.recordedDisplayModel,reasoningEffort:a.execution.reasoningEffort,authentication:a.execution.authentication},allowedInputs,packetPath:base+'/packet.json',outputPath:base+'/output.json',readingAuditPath:base+'/reading-audit.json',authorization:rec(base+'/authorization.json'),attempts:1,retries:0,forkTurns:'none',directIncrementalCostUsd:0});
console.log(JSON.stringify({status:'exceptional-two-field-packet-ready',debateNumber:n,fields:authorization.fields,additionalAttemptsPerField:1}));
