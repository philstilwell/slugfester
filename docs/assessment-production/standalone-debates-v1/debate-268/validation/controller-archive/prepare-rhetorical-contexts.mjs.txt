import fs from 'node:fs';import assert from 'node:assert/strict';import {createHash} from 'node:crypto';
import {referenceDefinitions} from '../src/data/references.js';
const args=process.argv.slice(2),n=args[args.indexOf('--debate')+1];assert(args.includes('--debate'));assert.match(n,/^\d{3,}$/);const read=p=>JSON.parse(fs.readFileSync(p)),hash=b=>createHash('sha256').update(b).digest('hex'),rec=p=>{const b=fs.readFileSync(p);return {path:p,sha256:hash(b),bytes:b.length};},write=(p,o)=>fs.writeFileSync(p,JSON.stringify(o,null,2)+'\n',{flag:'wx'});
const r=read('docs/assessment-production/standalone-debates-v1/registry.json').debates.find(r=>r.debateNumber===n);assert(r);
const dir=r.root+'/publication/rhetorical-tag-review-1',pubPath=r.root+'/publication/output.json',pub=read(pubPath),ledger=read(r.root+'/final-ledger/final-ledger.json'),a=read(r.root+'/authorization.json');
assert.equal(read(r.root+'/score-pass/attestation.json').ordinal,1);assert.equal(pub.debateId,r.debateId);
fs.mkdirSync(dir,{recursive:true});fs.copyFileSync(pubPath,dir+'/preserved-prior-output.json',fs.constants.COPYFILE_EXCL);
const defs=Object.entries(referenceDefinitions).flatMap(([type,rows])=>Object.entries(rows).map(([slug,d])=>({definitionKey:type+'|'+slug,type,slug,label:d.label,definition:d.definition,url:d.externalUrl})));
write(dir+'/catalog-snapshot.json',{schemaVersion:'1.0-local-rhetorical-catalog-snapshot',source:rec('src/data/references.js'),definitions:defs});
const c=pub.candidate,moves=c.sections.flatMap(s=>s.exchanges.flatMap(e=>['pro','con'].flatMap(side=>{if(!e[side])return[];const card=e[side],m=ledger.moves.find(m=>m.moveId===card.ledgerMoveId);assert(m);assert.equal(card.tags.length,0);const {moveId,sectionId,speaker,moveKind,sourceSpan,claim,warrant,inference,burdenContact,respondsToIds,responseComponents}=m;assert(claim&&warrant&&inference);return[{moveId,sectionId,side,speaker,moveKind,sourceSpan,claim,warrant,inference,burdenContact,respondsToIds,responseComponents,publicationDescription:card.words,publicationCritique:card.critique.replace(/Locked score:[\s\S]*$/,'Locked score: [withheld for blind rhetorical review].')}];})));
assert.equal(moves.length,ledger.moves.length);assert.equal(new Set(moves.map(m=>m.moveId)).size,moves.length);
const overallBlunders=['pro','con'].flatMap(side=>c.overall[side].blunders.map((b,i)=>{assert.equal(b.links.length,0);return{blunderId:'overall-'+side+'-blunder-'+(i+1),side,speaker:c.sides[side].speaker,text:b.text};}));
const packet={schemaVersion:'1.0-score-blind-rhetorical-review-packet',debateNumber:n,debateId:r.debateId,motion:c.motion,controllerOverrides:[],referenceDefinitions:defs,moves,overallBlunders,completeTimestampedTranscript:read(r.root+'/manifest.json').sourceLocks.indexedTranscript.path,audioContextPath:r.root+'/audio/score-blind-supplement.json',sourceScope:read(r.root+'/source/reader-scope-disclosure.json'),editorApprovedScope:read(r.root+'/source/editor-scope.json'),reviewRules:[
 'Read every declared input literally before review. Consider every local definition against each complete move and live reply. Do not infer a fallacy merely from weak evidence, an unfinished mechanism, a contested premise, non-deductive inference, or a missing alternative comparison.',
 'Record source-specific reasoning for every move and every overall blunder, including no-tag decisions. Include every serious candidate considered, both accepted and rejected, in candidateReviews or overallBlunderCandidateReviews. The acceptedTags arrays are exact accepted subsets. Do not manufacture implausible candidates just to fill a table.',
 'Each accepted tag requires a material source defect already represented in the frozen critique. Tags do not change scores. A source caveat or qualification may defeat an apparent label; review complete context and audio corrections before acceptance. Never equate disagreement with a named fallacy. Two labels for one defect require distinct catalog-defined failures.',
 'No quotas or forced side symmetry; zero accepted labels can be justified only by specific complete review. Keep output reviewer identity separate; do not access the other review, scores, published tags, earlier debates or tag frequencies.',
 'Use exact catalog label/type/url. Context8–35words, rationale at least12words. Candidate key is moveId|type|label or blunderId|type|label. Candidate rows include identifier,candidateKey,type,label,url,context,rationale,decision accepted or rejected.',
 'Self-check first complete draft in memory using the declared checker; never submit a placeholder or generic no-tag review. Literal unsaved draft components may be emitted for controller inspection/compilation; do not overwrite any submitted output. One attempt; no automatic retries.'
 ],outputContract:{schemaVersion:'1.0-independent-rhetorical-review',status:'complete-blind-move-complete-review',debateNumber:n,debateId:r.debateId,reviewedMoveIds:moves.map(m=>m.moveId),reviewedOverallBlunderIds:overallBlunders.map(b=>b.blunderId),moveReviews:'One per move in exact packet order: moveId, allDefinitionsConsidered (all exact definitionKeys), candidateKeys (complete serious candidate set), source-specific rationale≥12words.',overallBlunderReviews:'One per blunder in exact packet order, same fields with blunderId.',candidateReviews:'Complete serious move candidate table, accepted and rejected.',overallBlunderCandidateReviews:'Complete serious overall blunder candidate table, accepted and rejected.',acceptedTags:'Exact accepted subset of candidateReviews.',acceptedOverallBlunderLinks:'Exact accepted subset of overallBlunderCandidateReviews.',audit:{model:a.execution.assessmentModel,reasoningEffort:a.execution.reasoningEffort,attempts:1,retries:0,directIncrementalCostUsd:0,existingTagsUnavailable:true,scoresUnavailable:true,otherReviewUnavailable:true,judgmentChanges:0,scoreChanges:0}}};
write(dir+'/packet.json',packet);
const allowed=[rec(dir+'/packet.json'),rec(dir+'/catalog-snapshot.json'),rec(packet.completeTimestampedTranscript),rec(packet.audioContextPath),rec('src/data/references.js'),rec('.assessment-cache/debate-'+n+'-check-rhetorical-draft.mjs')];
for(const pass of ['pass-a','pass-b']){fs.mkdirSync(dir+'/'+pass);write(dir+'/'+pass+'/execution-plan.json',{schemaVersion:'1.0-isolated-rhetorical-review-execution-plan',status:'frozen-before-model-execution',debateNumber:n,debateId:r.debateId,model:{slug:a.execution.assessmentModel,displayLabel:a.execution.recordedDisplayModel,reasoningEffort:a.execution.reasoningEffort,authentication:a.execution.authentication},allowedInputs:allowed,outputPath:dir+'/'+pass+'/output.json',readingAuditPath:dir+'/'+pass+'/reading-audit.json',packetPath:dir+'/packet.json',attempts:1,retries:0,forkTurns:'none',readingGateRequired:true,directIncrementalCostUsd:0});}
console.log(JSON.stringify({status:'two-fresh-blind-packets-ready',moves:moves.length,blunders:overallBlunders.length,definitions:defs.length},null,2));
