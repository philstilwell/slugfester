import fs from 'node:fs';
import assert from 'node:assert/strict';
import {spawnSync} from 'node:child_process';
import {fileRecord} from '../scripts/lib/assessment-production-standalone-debate-v1.mjs';
const args=process.argv.slice(2),mode=args[0],n=args[args.indexOf('--debate')+1];assert(args.includes('--debate'));assert.match(n,/^\d{3,}$/);
const root=`docs/assessment-production/standalone-debates-v1/debate-${n}`,read=p=>JSON.parse(fs.readFileSync(p)),put=(p,o)=>fs.writeFileSync(p,JSON.stringify(o,null,2)+'\n',{flag:'wx'}),initialPath=root+'/source/acquisition-plan.json',initial=read(initialPath),inputs=read(root+'/source/transcription-inputs.json'),amendedPath=root+'/source/fixed-price-continuation-plan.json';
if(mode==='freeze'){
 const first=read(initial.sourceOutputRoot+'/chunk-000.result.json');assert.equal(first.exitCode,0);assert(first.usage.output_tokens>initial.pricing.documentedMaximumOutputTokens);
 const remaining=inputs.clips.slice(1);for(const c of remaining)assert(!fs.existsSync(`${initial.sourceOutputRoot}/chunk-${String(c.index).padStart(3,'0')}.started.json`));
 const sourceCost=remaining.reduce((s,c)=>s+Math.ceil(c.durationSeconds)*.006/60,0),reserve=.45,total=first.maximumPossiblePaidCostUsd+sourceCost+reserve;assert(total<=initial.pricing.fullDebateWorkflowConservativeUpperEstimateUsd&&total<=1);
 put(amendedPath,{schemaVersion:'1.0-fixed-price-remaining-source-plan',status:'frozen-before-unattempted-inputs',createdAt:new Date().toISOString(),debateNumber:n,videoId:initial.videoId,initialPlan:fileRecord(initialPath),retainedSuccessfulFirstCall:fileRecord(initial.sourceOutputRoot+'/chunk-000.result.json'),scopeUnchanged:true,reason:'First successful diarized response reported 8077 output tokens, exceeding the model page stated 2000 maximum; the prior per-request upper assumption is not reliable for internal audio chunking. Preserve that result; do not repeat it. Use the existing fixed-price OpenAI Whisper timestamp route only for still-unattempted audio inputs.',model:'whisper-1',responseFormat:'verbose_json',speakerLabelsAutomatic:false,speakerAttributionPlan:'Match time-aligned source against the official speaker-labelled publisher transcript; below-high selected moves still require separately frozen audio verification.',sourceOutputRoot:initial.sourceOutputRoot,remainingInputIndices:remaining.map(c=>c.index),inputs:fileRecord(root+'/source/transcription-inputs.json'),wrapper:fileRecord('tmp/whisper-current-once.py'),helper:fileRecord('scripts/transcribe-whisper-timestamps.py'),attemptsPerInput:1,automaticRetries:0,pricing:{source:'https://developers.openai.com/api/docs/models/whisper-1',verifiedAt:new Date().toISOString(),usdPerMinute:.006,knownSuccessfulCallCostUsd:first.knownSuccessfulCallCostUsd,remainingSourceUpperEstimateUsd:sourceCost,reserveVerificationCalls:3,reservePerVerificationCallUsd:.15,verificationReserveUsd:reserve,fullDebateWorkflowConservativeUpperEstimateUsd:total,standingCapUsd:1,estimateReportedBeforeContinuation:true,billingConfirmed:false}});
 console.log(JSON.stringify({frozen:true,remainingSourceUsd:sourceCost,fullWorkflowEstimateUsd:total}));
}else if(mode==='run'){
 const p=read(amendedPath);assert.equal(fileRecord(initialPath).sha256,p.initialPlan.sha256);assert.equal(fileRecord(p.inputs.path).sha256,p.inputs.sha256);assert.equal(fileRecord(p.wrapper.path).sha256,p.wrapper.sha256);assert.equal(fileRecord(p.helper.path).sha256,p.helper.sha256);
 const index=Number(args[args.indexOf('--chunk')+1]);assert(p.remainingInputIndices.includes(index));const c=inputs.clips[index],base=`${p.sourceOutputRoot}/chunk-${String(index).padStart(3,'0')}`,out=base+'.json',started=base+'.started.json';assert(!fs.existsSync(out)&&!fs.existsSync(started));assert.equal(fileRecord(c.input.path).sha256,c.input.sha256);
 const cost=Math.ceil(c.durationSeconds)*p.pricing.usdPerMinute/60;
 put(started,{videoId:p.videoId,chunk:index,model:p.model,startedAt:new Date().toISOString(),input:c.input,plan:fileRecord(amendedPath),maximumPossibleAttemptCostUsd:cost});
 const r=spawnSync('python3',[p.wrapper.path,c.input.path,'--language','en','--out',out],{encoding:'utf8',timeout:195000,maxBuffer:1000000});fs.writeFileSync(base+'.stdout.log',r.stdout??'',{flag:'wx'});fs.writeFileSync(base+'.stderr.log',r.stderr??'',{flag:'wx'});
 const o={videoId:p.videoId,chunk:index,model:p.model,completedAt:new Date().toISOString(),exitCode:r.status,signal:r.signal,attempts:1,retries:0,knownSuccessfulCallCostUsd:r.status===0?cost:null,maximumPossiblePaidCostUsd:cost,pricingBasis:'Frozen original input duration rounded up to the next second at $0.006/minute; invoice not verified',output:fs.existsSync(out)?fileRecord(out):null,billingConfirmed:false};put(base+'.result.json',o);console.log(JSON.stringify(o));assert.equal(r.status,0,'Failed paid source attempt; stop without retry');
 const j=read(out);assert(j.segments?.length);assert(j.segments.every(s=>s.start>=0&&s.end>=s.start&&s.end<=c.durationSeconds+1));assert(j.segments[0].start<15&&j.segments.at(-1).end>c.durationSeconds-15,'Coverage requires inspection before another call');
}else throw new Error('Unknown mode');
