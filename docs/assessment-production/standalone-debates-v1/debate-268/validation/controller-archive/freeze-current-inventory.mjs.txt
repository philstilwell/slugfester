import fs from 'node:fs';
import assert from 'node:assert/strict';
import {fileRecord,sha256,validateStandaloneInventory} from '../scripts/lib/assessment-production-standalone-debate-v1.mjs';
import {validateEditorScopedSource} from '../scripts/lib/assessment-editor-scoped-source.mjs';
const args=process.argv.slice(2),n=args[args.indexOf('--debate')+1];assert(args.includes('--debate'));assert.match(n,/^\d{3,}$/);
const read=p=>JSON.parse(fs.readFileSync(p)),put=(p,o)=>fs.writeFileSync(p,JSON.stringify(o,null,2)+'\n',{flag:'wx'}),verify=r=>assert.equal(fileRecord(r.path).sha256,r.sha256);
const rec=read('docs/assessment-production/standalone-debates-v1/registry.json').debates.find(r=>r.debateNumber===n),root=rec.root,dir=root+'/inventory',manifest=read(root+'/manifest.json'),a=read(root+'/authorization.json'),inv=read(dir+'/pre-audio-inventory.json'),mechanical=read(dir+'/pre-audio-mechanical-review.json'),correction=read(dir+'/correction-1-assembly-audit.json'),review=read(dir+'/source-review.json'),wording=read(dir+'/wording-verification-evidence.json'),scope=read(root+'/source/audio-source-verification.json');
assert.equal(review.status,'passed-complete-source-and-bounded-correction-review');verify(review.inventoryCandidate);assert.equal(review.inventoryCandidate.sha256,fileRecord(dir+'/pre-audio-inventory.json').sha256);assert.equal(correction.output.sha256,mechanical.draft.sha256);verify(correction.output);verify(correction.originalDraft);verify(correction.correction);
const events=read('.assessment-cache/captions/'+rec.videoId+'/events.json'),validation=validateStandaloneInventory(inv,events),scopeValidation=validateEditorScopedSource({authorization:a,sourceLock:read(root+'/source/source-lock.json'),inventory:inv});
const triggered=validation.belowHighAttributionMoveIds;assert.deepEqual(review.requiredAudioChecks.map(c=>c.moveId),triggered);assert(review.requiredAudioChecks.every(c=>c.materialMeaningResolved&&c.retainedQuotationOwnershipResolved));
const contexts=[];
for(const stem of ['', 'correction-1-']){const release=read(dir+'/'+stem+'reading-release.json');assert.equal(release.status,'authenticated-and-released-for-first-submission');assert.equal(release.outputExistedAtRelease,false);assert(release.inputs.every(r=>r.literalCompleteReadAuthenticated));for(const r of release.inputs)verify(r);const bytes=fs.readFileSync(release.session.path),rows=bytes.toString().trim().split('\n').map(JSON.parse),models=rows.filter(r=>r.type==='turn_context').map(r=>({model:r.payload.model,reasoningEffort:r.payload.effort}));assert(models.length&&models.every(m=>m.model===manifest.modelSettings.model&&m.reasoningEffort===manifest.modelSettings.reasoningEffort));contexts.push({agentPath:release.agentPath,readingRelease:fileRecord(dir+'/'+stem+'reading-release.json'),session:{path:release.session.path,sha256:sha256(bytes),bytes:bytes.length,kind:'prefix-snapshot-after-output'},modelContexts:models});}
for(const p of ['judgments/pass-a/output.json','judgments/pass-b/output.json','score-pass/output.json'])assert(!fs.existsSync(root+'/'+p));
for(const r of Object.values(manifest.sourceLocks))verify(r);assert.equal(inv.motion,a.identity.motion);put(dir+'/inventory.json',inv);const inventory=fileRecord(dir+'/inventory.json');
fs.mkdirSync(root+'/audio',{recursive:true});
const cost={knownSuccessfulCallCostUsd:wording.knownSuccessfulCallCostUsd,maximumPossibleTotalCostUsd:wording.maximumPossiblePaidCostUsd,maximumPossibleTotalCostEstimateUsd:wording.maximumPossiblePaidCostUsd,directCostUsd:wording.knownSuccessfulCallCostUsd,uncertainPaidAttempts:scope.uncertainPaidAttempts,successfulCalls:16,paidCallAttempts:17,authorizedExceptionalRetries:1,billingDashboardConfirmed:false,basis:'Successful audio acquisition calls plus two boundary verifications and one pre-freeze wording verification; maximum additionally includes the earlier transport-uncertain five-minute call. Estimates, not reconciled invoice charges.',frozenEstimateUsd:a.cost.fullWorkflowConservativeUpperEstimateUsd,capUsd:a.cost.standingPaidTranscriptionMaximumUsd};assert(cost.maximumPossibleTotalCostUsd<=cost.frozenEstimateUsd&&cost.frozenEstimateUsd<=cost.capUsd);
put(root+'/audio/audio-verification.json',{schemaVersion:'1.0-standalone-audio-verification',protocolId:a.protocolId,status:'complete',debateNumber:n,debateId:rec.debateId,videoId:rec.videoId,inventory,scopeAudioEvidence:fileRecord(root+'/source/audio-source-verification.json'),preFreezeWordingEvidence:fileRecord(dir+'/wording-verification-evidence.json'),triggeredMoveIds:triggered,checks:review.requiredAudioChecks,preFreezeClarifications:review.preFreezeClarifications,cost,audit:{requiredAttributionChecks:triggered.length,completedAttributionChecks:review.requiredAudioChecks.length,unresolvedAttributionChecks:0,paidCallAttempts:17,paidRetries:1,retryExplicitlyAuthorized:true,sourceCaptionBytesUnchanged:true,finalInventoryFrozenAfterAudio:true,directListeningPerformed:false}});
put(root+'/audio/score-blind-supplement.json',{schemaVersion:'1.0-score-blind-audio-source-supplement',debateNumber:n,debateId:rec.debateId,inventory,sourceVerification:fileRecord(root+'/audio/audio-verification.json'),method:'Independent short-clip automatic transcription, not direct listening. Official named transcript and frozen scoped turn attribution support identity. Canonical source remains unchanged.',clarifications:review.preFreezeClarifications,scopeRule:'Only the explicitly approved self-contained main-conversation selection is eligible. Excluded host interventions and dependent replies, introductions and audience Q&A supply no evidence, omission penalty or concession. Do not impose unadopted burdens on a critic.',frozenBeforePrimaryJudgments:true});
put(dir+'/session-authentication.json',{schemaVersion:'1.0-inventory-session-authentication',status:'authenticated-original-plus-bounded-correction-before-first-freeze',debateNumber:n,debateId:rec.debateId,contexts,originalDraft:correction.originalDraft,firstWorkerFailurePreserved:fileRecord(dir+'/failure-preservation.json'),originalSelfRevisionAccepted:false,authorizedCorrection:fileRecord(dir+'/correction-1-assembly-audit.json'),sourceReviewedDraft:mechanical.draft,controllerSourceReview:fileRecord(dir+'/source-review.json'),output:inventory,isolation:'Fresh original inventory and separate fresh bounded correction contexts; protocol-restricted allowlists, not operating-system filesystem isolation.',directIncrementalCostUsd:0});
put(dir+'/first-freeze-audit.json',{schemaVersion:'1.0-source-verified-first-inventory-freeze',status:'passed',createdAt:new Date().toISOString(),debateNumber:n,debateId:rec.debateId,output:inventory,validation,scopeValidation,sourceReview:fileRecord(dir+'/source-review.json'),mechanicalReview:fileRecord(dir+'/pre-audio-mechanical-review.json'),changedAfterFirstFreeze:false,modelAuthoredRatings:0,modelAuthoredTotals:0,primaryJudgmentsExecutedBeforeFreeze:0,audio:fileRecord(root+'/audio/audio-verification.json'),directIncrementalCostUsd:0});
console.log(JSON.stringify({status:'first-inventory-frozen',validation,inventory,cost},null,2));
