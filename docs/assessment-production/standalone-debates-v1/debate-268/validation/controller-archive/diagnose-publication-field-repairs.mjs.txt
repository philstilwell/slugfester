import fs from 'node:fs';
import assert from 'node:assert/strict';
import {createHash} from 'node:crypto';
import {checkRepair} from './check-publication-field-repair.mjs';
import {checkPublication} from './check-publication-draft.mjs';
const args=process.argv.slice(2),n=args[args.indexOf('--debate')+1];assert(args.includes('--debate'));assert.match(n,/^\d{3,}$/);
const read=p=>JSON.parse(fs.readFileSync(p,'utf8')),rec=p=>{const b=fs.readFileSync(p);return{path:p,sha256:createHash('sha256').update(b).digest('hex'),bytes:b.length};};
const r=read('docs/assessment-production/standalone-debates-v1/registry.json').debates.find(x=>x.debateNumber===n);assert(r);
const dir=r.root+'/publication',base=dir+'/field-repair-1',plan=read(base+'/plan.json'),bundle=read(dir+'/input-bundle.json'),ledger=read(bundle.ledgerPath),candidate=structuredClone(bundle.lockedCandidateSkeleton),seen=[],groups=[];
assert(!fs.existsSync(dir+'/output.json'));
for(const item of plan.groups){
 assert.equal(rec(item.path).sha256,item.sha256);
 const p=read(item.path),audit=read(p.outputPath.replace('/output.json','/execution.json')),o=read(p.outputPath);
 assert.equal(audit.status,'passed-authenticated-first-field-repair');assert.equal(rec(p.outputPath).sha256,audit.output.sha256);
 const validation=checkRepair(o,read(p.packetPath));groups.push({groupId:o.groupId,output:rec(p.outputPath),execution:rec(p.outputPath.replace('/output.json','/execution.json')),validation});
 for(const s of o.shards)for(const f of s.fields){assert(!seen.includes(f.path));seen.push(f.path);const keys=f.path.replace(/^candidate\./,'').replace(/\[(\d+)\]/g,'.$1').split('.');let target=candidate;for(const k of keys.slice(0,-1)){assert(k in target);target=target[k];}assert.equal(target[keys.at(-1)],'');target[keys.at(-1)]=f.text;}
}
assert.deepEqual(seen,plan.fields);
const quotes=['pro','con'].map(side=>{const text=candidate.quotes[side].text,matches=ledger.moves.filter(m=>m.side===side&&m.quoteEligibleExactSpans.some(s=>s.includes(text)));assert.equal(matches.length,1);const m=matches[0];return{side,moveId:m.moveId,speaker:candidate.sides[side].speaker,text,quoteEligibleExactSpan:m.quoteEligibleExactSpans.find(s=>s.includes(text)),sourcePath:bundle.ledgerPath,transcriptPath:bundle.transcriptPath,...Object.fromEntries(['startEvent','endEvent','startMs','endMs'].map(k=>[k,m.sourceSpan[k]])),exactMatch:true};});
const nv=(side,field,sourceMoveIds,explanation)=>({side,field,path:'candidate.logicalExtension.'+side+'.'+field,hypothetical:true,sourceMoveIds,explanation});
const noveltyMap=[
 nv('pro','finalArgument',['sacks-science-compatible-roles','sacks-scientific-incompatibility-rule','sacks-putnam-community-evidence','sacks-shared-integrity-and-dignity-of-science'],'Combines the positive case into a bounded partnership that accepts empirical correction without presenting compatibility or community benefits as proof of supernatural claims.'),
 nv('pro','newArguments[0]',['sacks-scientific-incompatibility-rule','dawkins-asks-for-classification-rule'],'Develops scientific correction into a credibility condition: genuine rejection of readings demonstrates constraint without claiming that surviving interpretations are thereby verified.'),
 nv('pro','newArguments[1]',['sacks-meaning-outside-system','dawkins-football-does-not-establish-cosmic-purpose'],'Retains the distinction between mechanism and significance while limiting the football comparison to human moral uptake rather than inferring an external cosmic author.'),
 nv('pro','newArguments[2]',['sacks-putnam-community-evidence','dawkins-community-mechanism-is-plausible','dawkins-secular-community-substitute'],'Adds an institutional account of sustaining costly commitments while expressly allowing secular replacement and withholding any inference to doctrinal truth.'),
 nv('con','finalArgument',['dawkins-purpose-questions-limited','dawkins-creator-is-presupposed','dawkins-asks-for-classification-rule','dawkins-moral-common-ground-with-supernatural-divide'],'Unifies the critical case around independent warrant while preserving practical cooperation and distinguishing an unwarranted explanatory partnership from impossible coexistence.'),
 nv('con','newArguments[0]',['dawkins-football-does-not-establish-cosmic-purpose','dawkins-creator-is-presupposed','sacks-scientific-incompatibility-rule'],'Introduces reciprocal error correction as a partnership test, extending the unresolved purpose and creator warrants without denying the conceded interpretive rule.'),
 nv('con','newArguments[1]',['sacks-putnam-community-evidence','dawkins-community-mechanism-is-plausible','dawkins-secular-community-substitute'],'Converts the speculative secular substitute into a comparative institutional test that identifies mechanisms and does not assume that an effective replacement already exists.'),
 nv('con','newArguments[2]',['sacks-transcendence-supports-dignity-and-hope','dawkins-moral-common-ground-with-supernatural-divide'],'Distinguishes normative standards from measurable objects and offers secular justificatory routes, challenging the inference from scientific evidential limits to necessary transcendence.')
];
const o={...bundle.outputRequirements,candidate,sourceQuoteAudit:quotes,noveltyMap,audit:bundle.outputRequirements.audit,editorialDisposition:'Diagnostic assembly only: two critique fields require an additionally authorized repair; this is not an accepted publication output.'};
const validation=checkPublication(o,bundle);
const put=(p,v)=>{fs.writeFileSync(p,JSON.stringify(v,null,2)+'\n',{flag:'wx'});return rec(p);};
const diagnosticCandidate=put(base+'/assembled-diagnostic-output.json',o);
put(base+'/controller-novelty-map.json',{status:'passed-direct-source-specific-novelty-review',debateNumber:n,debateId:r.debateId,aiRepairOutputSha256:rec(base+'/ai-contribution/output.json').sha256,entries:noveltyMap,directReview:{bothSidesReadTogether:true,allThesesPremisesConclusionsAndNewArgumentsRead:true,distinctInferentialContributions:true,formulaicLaunchersFound:false,ordinaryPunctuationPreserved:true,notes:['The affirmative section is expressly a bounded practical and interpretive partnership; it does not silently claim that the football analogy proves cosmic purpose.','The negative section allows cooperation while demanding independent explanatory warrant, avoiding a caricature that scientific naturalism precludes shared moral practice.','The six reinforcing arguments respectively address empirical constraint, moral uptake, institutional continuity, reciprocal correction, functional replaceability, and the category distinction between norms and measurements. Their openings and paragraph progressions follow different substantive claims rather than interchangeable provenance frames.']}});
const holds=[
 {field:'candidate.sections[2].exchanges[0].pro.critique',moveId:'sacks-red-sea-science-and-story-purpose',groupId:'cards-1',shardId:'008',source:ledger.moves.find(x=>x.moveId==='sacks-red-sea-science-and-story-purpose').sourceSpan,issue:'The critique says eastward wind, but the source says east wind. An east wind comes from the east; eastward ordinarily describes motion toward the east. Retain the source wording rather than introducing the opposite directional gloss.',permittedProposedChange:'Replace only eastward wind with east wind; preserve the rest of this critique byte-for-byte.',assessmentSubstanceChange:false},
 {field:'candidate.sections[4].exchanges[1].pro.critique',moveId:'sacks-charity-covers-religious-and-secular-causes',groupId:'cards-3',shardId:'028',sourceSupplement:rec(r.root+'/audio/score-blind-supplement.json'),issue:'The Live burden sentence tells Sacks not to make an inference that the audio supplement expressly forbids. This leaks controller instructions into reader-facing criticism and risks implying that he made an exclusion claim; the locked assessment explicitly withholds that attribution.',permittedProposedChange:'Replace only the Live burden sentence with a reader-facing explanation that the breadth clarification leaves church-giving inclusion and its effect on the reported association unresolved. Preserve the other three sentences byte-for-byte and add no defect, penalty, or claim of exclusion.',assessmentSubstanceChange:false}
 ];
for(const h of holds){const group=read(base+'/'+h.groupId+'/output.json'),found=group.shards.find(s=>s.fields.some(f=>f.path===h.field));assert(found);h.shardId=found.shardId;h.originalRepairOutput=rec(base+'/'+h.groupId+'/output.json');h.originalText=found.fields.find(f=>f.path===h.field).text;}
const cards=candidate.sections.flatMap(s=>s.exchanges.flatMap(e=>['pro','con'].flatMap(side=>e[side]?[e[side]]:[]))),wc=s=>s.trim().split(/\s+/).length;
const report={schemaVersion:'1.0-publication-field-repair-controller-review',status:'blocked-on-two-exhausted-field-editorial-corrections',debateNumber:n,debateId:r.debateId,recordedAt:new Date().toISOString(),repairPlan:rec(base+'/plan.json'),diagnosticCandidate,groups,validation,metrics:{cards:cards.length,descriptionMeanWords:cards.reduce((sum,c)=>sum+wc(c.words),0)/cards.length,descriptionMinimumWords:Math.min(...cards.map(c=>wc(c.words))),critiqueMinimumWords:Math.min(...cards.map(c=>wc(c.critique))),critiqueMaximumWords:Math.max(...cards.map(c=>wc(c.critique))),critiqueMinimumCharacters:Math.min(...cards.map(c=>c.critique.length))},review:{all124RepairFieldsRead:true,sourceSpecificCardCritiquesReviewed:true,overviewReviewed:true,aiContributionReviewed:true,sourceQuotesMechanicallyAuthenticated:true,remainingEditorialHolds:holds.length,heldFieldsAltered:false,other122FieldsRemainUnchanged:true},holds,authority:{ordinaryFirstFieldRepairsConsumed:true,additionalAttemptsAuthorized:false,neededException:'One additional bounded attempt for exactly the two identified critique leaves, preserving all other publication fields, source evidence, inventory, judgments, adjudication and scores.',directIncrementalCostEstimateUsd:0,paidCallsNeeded:0},publicationStatus:{formalOutputExists:false,rhetoricalReviewsNotStarted:true,siteDataChanged:false,commitCreated:false,pushed:false,pullRequestCreated:false},frozenScores:{pro:read(bundle.scorePath).overall.pro.score,con:read(bundle.scorePath).overall.con.score},scoreRecalculated:false};
put(base+'/controller-editorial-review.json',report);
assert(!fs.existsSync(dir+'/output.json'));
console.log(JSON.stringify({status:report.status,validation,metrics:report.metrics,heldFields:holds.map(x=>({field:x.field,shardId:x.shardId})),frozenScores:report.frozenScores}));
