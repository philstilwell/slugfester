import fs from 'node:fs';
import assert from 'node:assert/strict';
import {spawnSync} from 'node:child_process';
import {createHash} from 'node:crypto';
import {fileRecord} from '../scripts/lib/assessment-production-standalone-debate-v1.mjs';
const args=process.argv.slice(2),n=args[args.indexOf('--debate')+1];assert(args.includes('--debate'));const read=p=>JSON.parse(fs.readFileSync(p));
const r=read('docs/assessment-production/standalone-debates-v1/registry.json').debates.find(x=>x.debateNumber===n);assert(r);const root=r.root,release=read(root+'/inventory/reading-release.json'),session=fs.readFileSync(release.session.path),rows=session.toString().trim().split('\n').map(JSON.parse);
const calls=rows.filter(x=>x.payload?.type==='custom_tool_call'&&x.payload.input?.includes('*** Add File:')&&x.payload.input.includes('/inventory/draft-parts.json'));
assert.equal(calls.length,1);const call=calls[0],input=call.payload.input,match=input.match(/^const patch = ("(?:[^"\\]|\\.)*");\n/);assert(match);
const patch=JSON.parse(match[1]),lines=patch.split('\n');assert.equal(lines[0],'*** Begin Patch');assert.equal(lines[1],'*** Add File: '+process.cwd()+'/'+root+'/inventory/draft-parts.json');assert.equal(lines.at(-1),'*** End Patch');assert(lines.slice(2,-1).every(s=>s.startsWith('+')));
const original=lines.slice(2,-1).map(s=>s.slice(1)).join('\n')+'\n';JSON.parse(original);
const first=root+'/inventory/rejected-original-draft.json',later=root+'/inventory/quarantined-self-revision.json';fs.writeFileSync(first,original,{flag:'wx'});fs.writeFileSync(later,fs.readFileSync(root+'/inventory/draft-parts.json'),{flag:'wx'});
const failure=spawnSync('node',['tmp/assemble-current-inventory.mjs','--debate',n,'--draft='+first],{encoding:'utf8'});assert.notEqual(failure.status,0);const log=root+'/inventory/rejected-original-validation.log';fs.writeFileSync(log,(failure.stdout||'')+(failure.stderr||''),{flag:'wx'});
const outputs=[first,later].map(p=>{const j=read(p);return{file:fileRecord(p),actualMoves:j.moves.length,totals:{pro:j.moves.filter(m=>m.side==='pro').length,con:j.moves.filter(m=>m.side==='con').length},reported:j.selectionBalanceAudit.totals};});
const record={schemaVersion:'1.0-pre-freeze-inventory-failure-preservation',createdAt:new Date().toISOString(),debateNumber:n,debateId:r.debateId,status:'invalid-original-and-unauthorized-self-revision-quarantined',firstSubmissionRecoveredExactlyFromPreservedToolCall:true,firstSubmissionCallId:call.payload.call_id,firstSubmissionOrdinal:call.ordinal,session:{path:release.session.path,sha256:createHash('sha256').update(session).digest('hex'),bytes:session.length,kind:'prefix-snapshot-containing-first-add-patch-and-later-self-revision'},originalRecoveredBytes:Buffer.byteLength(original),independentEarlierReadObservedOriginalByteLength:61813,byteLengthMatchesEarlierRead:Buffer.byteLength(original)===61813,outputs,validatorFailure:fileRecord(log),problems:['Original summary miscounts its own move array.','Original Red Sea move contains a substantive opponent interjection.','Original Dawkins classification-rule move incorporates Sacks argument and quotation as Dawkins evidence.','Worker changed the first written draft before controller authorization and without preserving original bytes; that self-revision is not accepted.','Revised count summary remains wrong.'],inventoryFrozen:false,primaryJudgmentsStarted:false,scorePasses:0,recoveryDisposition:'Use a fresh isolated score-blind, explicitly field-bounded correction of the original draft; retain both versions, this incident, and validator failure. No first-submission overwrite is accepted as valid evidence.'};
fs.writeFileSync(root+'/inventory/failure-preservation.json',JSON.stringify(record,null,2)+'\n',{flag:'wx'});console.log(JSON.stringify({status:record.status,outputs,originalBytes:record.originalRecoveredBytes,bytesMatch:record.byteLengthMatchesEarlierRead},null,2));
