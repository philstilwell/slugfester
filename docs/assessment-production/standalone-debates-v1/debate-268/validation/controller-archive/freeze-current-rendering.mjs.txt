import fs from 'node:fs';import assert from 'node:assert/strict';import {spawnSync} from 'node:child_process';
import {fileRecord} from '../scripts/lib/assessment-production-standalone-debate-v1.mjs';
const args=process.argv.slice(2),n=args[args.indexOf('--debate')+1];assert(args.includes('--debate'));assert.match(n,/^\d{3,}$/);const read=p=>JSON.parse(fs.readFileSync(p)),r=read('docs/assessment-production/standalone-debates-v1/registry.json').debates.find(x=>x.debateNumber===n);assert(r);
const root=r.root,dir=root+'/rendering',notes=read(dir+'/controller-observations.json'),publication=fileRecord(root+'/publication/output.json'),c=read(publication.path).candidate;
assert.equal(notes.status,'passed-direct-desktop-and-mobile-review');assert.equal(notes.publicationSha256,publication.sha256);
assert(Number.isInteger(notes.previewPort)&&notes.previewPort>1024&&notes.previewPort<65536);
assert.equal(spawnSync('lsof',['-nP','-iTCP:'+notes.previewPort,'-sTCP:LISTEN']).status,1,'Owned preview server must be closed');
assert.deepEqual(notes.viewports,[{width:1440,height:1000},{width:390,height:844}]);
for(const key of ['allLockedCardsPresent','correctTitleMotionSourcePositionsScoresByline','acceptedTagsAndOverallLinksRendered','aiDefaultCollapsedPointerEnterSpacePassedBothViewports','critiquePointerEnterSpaceFocusPassedBothViewports','mobileCritiqueDoesNotInterceptDistantControl','biographyLeftDesktopAboveMobile','bothSpeakerPortraitsLoaded','homeSearchTopicsSitemapBothProfilesContainDebate','rankingsComparisonContainsBothSpeakers','approvedScopeDisclosureVisible','visualInspectionPassed'])assert.equal(notes.checks[key],true,key);
for(const key of ['runtimeFailures','failedResources','horizontalOverflowFailures','emptyArgumentCards','temporaryBrowsersRemaining','temporaryServersRemaining'])assert.equal(notes.audit[key],0,key);
assert(notes.screenshotPaths.length>=4);assert(notes.snapshotPaths.length>=2);assert(notes.observations.length>=4);
const dest=dir+'/browser-evidence';fs.mkdirSync(dest,{recursive:true});
const copy=p=>{const target=dest+'/'+p.split('/').at(-1);fs.copyFileSync(p,target,fs.constants.COPYFILE_EXCL);return fileRecord(target);};
const screenshots=notes.screenshotPaths.map(copy),snapshots=notes.snapshotPaths.map(copy),cards=c.sections.flatMap(s=>s.exchanges.flatMap(e=>[e.pro,e.con].filter(Boolean)));
const audit={...notes.audit,viewports:2,screenshots:screenshots.length,argumentCardsPerViewport:cards.length,acceptedTagsRendered:cards.reduce((a,x)=>a+x.tags.length,0),acceptedOverallLinksRendered:['pro','con'].reduce((a,side)=>a+c.overall[side].blunders.reduce((b,x)=>b+x.links.length,0),0),directIncrementalCostUsd:0};
const o={schemaVersion:'1.0-standalone-production-rendering-audit',protocolId:'assessment-production-standalone-debate-v1',status:'passed-standalone-production-rendering',debateNumber:n,debateId:r.debateId,completedAt:new Date().toISOString(),publication,controllerNotes:fileRecord(dir+'/controller-observations.json'),viewports:notes.viewports,screenshots,snapshots,console:fileRecord(notes.consolePath),network:fileRecord(notes.networkPath),checks:notes.checks,observations:notes.observations,audit};
const put=(p,v)=>fs.writeFileSync(p,JSON.stringify(v,null,2)+'\n',{flag:'wx'});
put(dir+'/rendering-audit.json',o);put(dir+'/post-rhetorical-tag-audit-1.json',{schemaVersion:'1.0-post-rhetorical-tag-rendering-audit',status:'passed-post-rhetorical-tag-rendering-audit',debateNumber:n,debateId:r.debateId,completedAt:o.completedAt,publication,renderingEvidence:fileRecord(dir+'/rendering-audit.json'),viewports:o.viewports,audit});
console.log(JSON.stringify({status:o.status,audit},null,2));
