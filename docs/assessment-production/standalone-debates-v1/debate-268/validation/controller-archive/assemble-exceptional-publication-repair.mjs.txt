import fs from 'node:fs';import assert from 'node:assert/strict';import{createHash}from'node:crypto';
import{checkExceptionalRepair}from'./check-exceptional-publication-repair.mjs';
import{checkPublication}from'./check-publication-draft.mjs';
const args=process.argv.slice(2),n=args[args.indexOf('--debate')+1];assert(args.includes('--debate'));assert.match(n,/^\d{3,}$/);
const read=p=>JSON.parse(fs.readFileSync(p,'utf8')),rec=p=>{const b=fs.readFileSync(p);return{path:p,sha256:createHash('sha256').update(b).digest('hex'),bytes:b.length};},put=(p,v)=>fs.writeFileSync(p,JSON.stringify(v,null,2)+'\n',{flag:'wx'});
const r=read('docs/assessment-production/standalone-debates-v1/registry.json').debates.find(x=>x.debateNumber===n);assert(r);
const dir=r.root+'/publication',base=dir+'/exceptional-field-repair-1',first=dir+'/field-repair-1',p=read(base+'/execution-plan.json'),release=read(base+'/reading-release.json'),packet=read(p.packetPath),repair=read(p.outputPath),notes=read(base+'/controller-editorial-acceptance.json'),review=read(first+'/controller-editorial-review.json');
assert(!fs.existsSync(dir+'/output.json'));assert.equal(release.outputExistedAtRelease,false);assert(release.inputs.every(x=>x.literalCompleteReadAuthenticated));
for(const x of p.allowedInputs)assert.equal(rec(x.path).sha256,x.sha256);
assert.equal(notes.status,'passed-direct-two-field-source-faithfulness-review');assert.equal(notes.outputSha256,rec(p.outputPath).sha256);
const fieldValidation=checkExceptionalRepair(repair,packet),session=rec(release.session.path),rows=fs.readFileSync(session.path,'utf8').trim().split('\n').map(JSON.parse),contexts=rows.filter(r=>r.type==='turn_context').map(r=>({model:r.payload.model,reasoningEffort:r.payload.effort}));assert(contexts.length&&contexts.every(c=>c.model===p.model.slug&&c.reasoningEffort===p.model.reasoningEffort));
assert.equal(rec(review.diagnosticCandidate.path).sha256,review.diagnosticCandidate.sha256);
const original=read(review.diagnosticCandidate.path),output=structuredClone(original),plan=read(first+'/plan.json');
for(const g of review.groups){assert.equal(rec(g.output.path).sha256,g.output.sha256);assert.equal(rec(g.execution.path).sha256,g.execution.sha256);}
for(const f of repair.fields){const keys=f.path.replace(/^candidate\./,'').replace(/\[(\d+)\]/g,'.$1').split('.');let target=output.candidate;for(const k of keys.slice(0,-1))target=target[k];assert.equal(target[keys.at(-1)],packet.fields.find(x=>x.path===f.path).originalText);target[keys.at(-1)]=f.text;}
const diffs=[];function compare(a,b,path='candidate'){if(typeof a==='string'||a===null||typeof a!=='object'){if(a!==b)diffs.push(path);return;}assert.deepEqual(Object.keys(a),Object.keys(b));for(const k of Object.keys(a))compare(a[k],b[k],path+(Array.isArray(a)?'['+k+']':'.'+k));}compare(original.candidate,output.candidate);assert.deepEqual(diffs,p.authorization?packet.fields.map(f=>f.path):[]);
delete output.editorialDisposition;
output.publicationRecovery={initialFailure:rec(dir+'/initial-failure/diagnostic.json'),initialSubmittedOutputs:0,firstRepairPlan:rec(first+'/plan.json'),firstRepairGroups:review.groups.length,firstRepairShards:plan.shards,exceptionalAuthorization:rec(base+'/authorization.json'),exceptionalRepair:rec(p.outputPath),exceptionalFields:diffs,exceptionalAttemptsPerField:1,unauthorizedFurtherRetries:0,mechanicalAssemblyOnly:true};
const validation=checkPublication(output,read(dir+'/input-bundle.json'));
const execution={schemaVersion:'1.0-authenticated-user-authorized-exceptional-publication-repair',status:'passed-authenticated-authorized-two-field-repair',debateNumber:n,debateId:r.debateId,model:p.model,actualModelContexts:contexts,agentPath:release.agentPath,session:{...session,kind:'prefix-snapshot-after-output'},executionPlan:rec(base+'/execution-plan.json'),authorization:rec(base+'/authorization.json'),readingRelease:rec(base+'/reading-release.json'),readingAudit:rec(p.readingAuditPath),output:rec(p.outputPath),controllerEditorialAcceptance:rec(base+'/controller-editorial-acceptance.json'),fields:diffs,maximumWritableLeafFields:2,attempts:1,retries:0,fieldValidation,judgmentChanges:0,scoreChanges:0,directIncrementalCostUsd:0};
put(base+'/execution.json',execution);put(dir+'/output.json',output);
put(dir+'/execution.json',{schemaVersion:'1.2-isolated-publication-with-authorized-exception',status:'passed-authenticated-isolated-publication',debateNumber:n,debateId:r.debateId,model:p.model,initialContext:{executionPlan:rec(dir+'/execution-plan.json'),readingRelease:rec(dir+'/reading-release.json'),failure:rec(dir+'/initial-failure/diagnostic.json'),submittedOutputs:0},repairPlan:rec(first+'/plan.json'),repairContexts:review.groups,firstRepairEditorialReview:rec(first+'/controller-editorial-review.json'),exceptionalRepairExecution:rec(base+'/execution.json'),output:rec(dir+'/output.json'),validation,initialAttempts:1,firstRepairAttemptsPerShard:1,firstRepairShards:plan.shards,userAuthorizedAdditionalFieldAttempts:2,unauthorizedRetriesOfExhaustedRepairs:0,mechanicalAssembly:true,scoreChanges:0,judgmentChanges:0,directIncrementalCostUsd:0});
put(base+'/assembly-audit.json',{status:'passed-authorized-two-field-mechanical-assembly',debateNumber:n,debateId:r.debateId,before:review.diagnosticCandidate,after:rec(dir+'/output.json'),execution:rec(base+'/execution.json'),publicationExecution:rec(dir+'/execution.json'),changedFields:diffs,unchangedOtherProseFields:122,unaffectedSentencesUnchanged:true,sourceQuoteAuditUnchanged:true,noveltyMapUnchanged:true,validation,judgmentChanges:0,scoreChanges:0});
console.log(JSON.stringify({status:'publication-assembled-after-authorized-two-field-repair',changedFields:diffs,validation}));
