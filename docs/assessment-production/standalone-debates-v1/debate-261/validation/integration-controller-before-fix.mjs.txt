import assert from 'node:assert/strict';
import {readFileSync,writeFileSync,existsSync} from 'node:fs';
import {debates} from '../src/data/debates.js';
import {fileRecord,serializedJson,validateStandaloneCandidate} from './lib/assessment-production-standalone-debate-v1.mjs';
const read=p=>JSON.parse(readFileSync(p)),registryPath='docs/assessment-production/standalone-debates-v1/registry.json',route=read(registryPath).debates.find(d=>d.debateNumber==='261'),root=route.root,pub=read(`${root}/publication/output.json`),source=read(`${root}/source/source-lock.json`),bio=read(`${root}/source/profile-background-review.json`),corpusPath='docs/calibration/v2.1/corpus-transcript-audit.json',corpus=read(corpusPath);
assert.equal(route.status,'publication-and-rhetorical-review-frozen-awaiting-production');assert.equal(read(route.rhetoricalTagReview.auditPath).status,'passed-rhetorical-tag-review');assert.equal(read(`${root}/publication/correction-1-depth.json`).status,'applied-once-and-frozen');assert.equal(debates.some(d=>d.number===route.debateNumber||d.id===route.debateId||new URL(d.youtubeUrl).searchParams.get('v')===route.videoId),false);assert.equal(Math.max(...debates.map(d=>Number(d.number)))+1,Number(route.debateNumber));assert.equal(pub.candidate.id,route.debateId);assert.equal(pub.candidate.number,route.debateNumber);validateStandaloneCandidate(pub.candidate,read(`${root}/score-pass/output.json`));
const dataPath='src/data/debates.js',data=readFileSync(dataPath,'utf8'),anchor='\n];\n\naddMissingAiContributions(debates);';assert.equal(data.split(anchor).length,2);const next=data.replace(anchor,`,\n${JSON.stringify(pub.candidate,null,2).split('\n').map(l=>'  '+l).join('\n')}${anchor}`);
const biosPath='src/data/interlocutor-bios.js',bios=readFileSync(biosPath,'utf8'),bioAnchor='const entries = [\n';assert.equal(bios.split(bioAnchor).length,2);assert.equal(bios.includes('["Al Sharpton"'),false);assert.equal(bio.speaker,'Al Sharpton');assert.equal(bio.boundaries.excludedFromJudgmentPackets,true);assert.equal(bio.boundaries.excludedFromScoresAndRankings,true);const bioLine=JSON.stringify([bio.speaker,bio.biography,bio.source,bio.reviewDate]);const nextBios=bios.replace(bioAnchor,bioAnchor+'  '+bioLine+',\n');
const manifest=read(`${root}/manifest.json`);assert.deepEqual(fileRecord(manifest.sourceLocks.transcript.path),manifest.sourceLocks.transcript);assert.deepEqual(fileRecord(manifest.sourceLocks.events.path),manifest.sourceLocks.events);const transcriptStorage=`.assessment-cache/captions/${route.videoId}/transcript.txt`,eventsStorage=`.assessment-cache/captions/${route.videoId}/events.json`;assert.equal(fileRecord(transcriptStorage).sha256,source.hashes.transcriptSha256);assert.equal(fileRecord(eventsStorage).sha256,source.hashes.normalizedEventsSha256);assert.equal(corpus.entries.some(e=>e.debateNumber===route.debateNumber||e.debateId===route.debateId||e.videoId===route.videoId),false);const oldEntries=structuredClone(corpus.entries),cap=read(`.assessment-cache/captions/${route.videoId}/manifest.json`);
corpus.entries.push({debateNumber:route.debateNumber,debateId:route.debateId,videoId:route.videoId,status:'available',transcriptStorage,eventsStorage,track:cap.track,extractionMethod:source.acquisition.method,audioSourceUrl:null,model:null,wordCount:source.content.wordCount,eventCount:source.content.eventCount,retrievedAt:source.acquisition.retrievedAt,transcriptSha256:source.hashes.transcriptSha256,normalizedEventsSha256:source.hashes.normalizedEventsSha256});assert.ok(corpus.entries.at(-1).track?.languageCode);assert.deepEqual(corpus.entries.slice(0,-1),oldEntries);corpus.corpusDebates=debates.length+1;corpus.locallySavedTranscripts=corpus.entries.filter(e=>e.status==='available').length;corpus.unavailableTranscripts=corpus.entries.filter(e=>e.status==='unavailable').length;corpus.acquisitionRate=Number((corpus.locallySavedTranscripts/corpus.corpusDebates).toFixed(4));corpus.auditAsOf=source.acquisition.retrievedAt;
const auditPath=`${root}/publication/production-integration.json`;assert.equal(existsSync(auditPath),false);const before=[dataPath,biosPath,corpusPath].map(fileRecord);writeFileSync(dataPath,next);writeFileSync(biosPath,nextBios);writeFileSync(corpusPath,serializedJson(corpus));writeFileSync(auditPath,serializedJson({schemaVersion:'1.0-standalone-production-integration',status:'integrated-awaiting-generated-pages-and-production-validation',debateNumber:route.debateNumber,debateId:route.debateId,publication:fileRecord(`${root}/publication/output.json`),biographySource:fileRecord(`${root}/source/profile-background-review.json`),before,after:[dataPath,biosPath,corpusPath].map(fileRecord),audit:{priorDebatesPreserved:true,priorCorpusEntriesPreserved:true,otherBiographiesPreserved:true,avatarChanges:0,judgmentChanges:0,scoreChanges:0,moveChanges:0}}),{flag:'wx'});console.log(JSON.stringify({status:'integrated',record:fileRecord(auditPath)}));
