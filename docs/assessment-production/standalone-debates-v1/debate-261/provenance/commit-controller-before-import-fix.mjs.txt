import assert from 'node:assert/strict';
import fs from 'node:fs';
import {spawnSync} from 'node:child_process';
import {gzipSync,gunzipSync} from 'node:zlib';
import {fileRecord,serializedJson} from './lib/assessment-production-standalone-debate-v1.mjs';
const registryPath='docs/assessment-production/standalone-debates-v1/registry.json';
const json=p=>JSON.parse(fs.readFileSync(p));const registry=json(registryPath),route=registry.debates.find(x=>x.debateNumber==='261'),R=route.root;
assert.equal(route.videoId,'HPYxA8dYLBY');assert.equal(route.status,'published-and-frozen');
const git=(...args)=>{const x=spawnSync('git',args,{encoding:'utf8',maxBuffer:30*1024*1024});assert.equal(x.status,0,x.stderr);return x.stdout;};
assert.equal(git('branch','--show-current').trim(),'codex/debate-261');
const oldModule=await import(`data:text/javascript;base64,${Buffer.from(git('show','HEAD:src/data/debates.js')).toString('base64')}`);
const currentModule=await import('../src/data/debates.js');assert.equal(currentModule.debates.length,261);assert.deepEqual(currentModule.debates.slice(0,-1),oldModule.debates);
assert.deepEqual(registry.debates.slice(0,-1),JSON.parse(git('show',`HEAD:${registryPath}`)).debates);
const corpusPath='docs/calibration/v2.1/corpus-transcript-audit.json';assert.deepEqual(json(corpusPath).entries.slice(0,-1),JSON.parse(git('show',`HEAD:${corpusPath}`)).entries);
const stripAsset=s=>s.replace(/\?v=[a-f0-9]{16}/g,'?v=GENERATED');assert.equal(stripAsset(fs.readFileSync('src/app.js','utf8')),stripAsset(git('show','HEAD:src/app.js')));
const manifest=json(`${R}/manifest.json`);let locked=0;for(const group of ['controlLocks','sourceLocks'])for(const rec of Object.values(manifest[group])){assert.equal(fileRecord(rec.path).sha256,rec.sha256,rec.path);locked++;}
const labels=['production-validation','source-corpus-validation','focused-standalone','all-standalone','generated-pages-check','diff-whitespace-check','full-repository-check-with-local-sources','site-preflight-first'];
for(const l of labels)assert.equal(json(`${R}/validation/${l}.json`).exitCode,0,l);
assert.equal(json(`${R}/provenance/validation-runtime-cleanup.json`).status,'removed-owned-links-original-targets-preserved');
const walk=d=>fs.readdirSync(d,{withFileTypes:true}).flatMap(x=>x.isDirectory()?walk(`${d}/${x.name}`):[`${d}/${x.name}`]);
const sources=fs.readdirSync('scripts').filter(f=>/^tmp-.*-261\.(mjs|py)$/.test(f)).sort().map(f=>`scripts/${f}`);
const archives=sources.map(p=>{const bytes=fs.readFileSync(p),encoded=gzipSync(bytes).toString('base64');assert.deepEqual(gunzipSync(Buffer.from(encoded,'base64')),bytes);return {original:fileRecord(p),encoding:'gzip-base64',content:encoded};});
const archivePath=`${R}/provenance/controller-archive.json`;assert.equal(fs.existsSync(archivePath),false);fs.writeFileSync(archivePath,serializedJson({schemaVersion:'1.0-exact-controller-archive',debateNumber:route.debateNumber,archives}),{flag:'wx'});
const suspicious=[];for(const p of [...walk(R),...sources]){const s=fs.readFileSync(p,'utf8');if(/\b(?:sk-proj-[A-Za-z0-9_-]{30,}|gh[pousr]_[A-Za-z0-9]{30,}|github_pat_[A-Za-z0-9_]{35,})\b/.test(s)||/-----BEGIN (?:RSA |EC |OPENSSH )?PRIVATE KEY-----/.test(s))suspicious.push(p);}
assert.deepEqual(suspicious,[],'Potential credential detected; stop before staging.');
fs.writeFileSync(`${R}/validation/final-local-gates.json`,serializedJson({status:'passed-prepublication-local-gates',debateNumber:route.debateNumber,completedAt:new Date().toISOString(),gates:labels.map(l=>fileRecord(`${R}/validation/${l}.json`)),frozenLocksReverified:locked,first260DebatesUnchanged:true,previousRegistryRecordsUnchanged:true,previousTranscriptAuditEntriesUnchanged:true,appChangesOnlyGeneratedAssetVersion:true,credentialScanMatches:0,finalImmediatePrecommitPreflightStillRequired:true,remotePublicationPending:true}),{flag:'wx'});
const state=json(`${R}/resume-status.json`);state.status='local-gates-passed-ready-for-immediate-precommit-preflight';state.updatedAt=new Date().toISOString();state.productionLedger=fileRecord(route.productionLedger.path);state.cleanup.controllersPreservedForArchival=false;state.cleanup.controllerArchive=archivePath;state.resumeSequence=['All assessment, publication, rhetorical, adapter and local gates are complete. Never rebuild or rescore them.','Run final immediate precommit preflight, stage only this debate and generated site output, commit topic branch, replay standalone audit from a clean checkout, fetch and push topic branch, open PR and merge only after required check passes.','Verify origin/main and successful Site Quality and Pages deployment. Stop without starting another debate.'];fs.writeFileSync(`${R}/resume-status.json`,serializedJson(state));
fs.appendFileSync(`${R}/report.md`,'\n## Local publication gates passed\n\nThe adapter is frozen and matches the unchanged 76–84 result. Production validation, all-standalone replay, corpus checks, content parity, rhetorical audits, historical campaign/calibration checks, scope fixtures, generated-page checks and the 17-test site preflight all passed. The first full check failed only because this separate checkout lacked access to historical ignored audio; temporary links restored the existing sources, all 522 transcript/event hashes matched, and the unchanged full check passed. All 261 temporary links were removed without changing their targets. No test or performance limit was changed.\n\nThe original 260 debate records, prior registry records and prior transcript-audit entries remain unchanged. The only app.js difference is the generated asset-version identifier. Credential scan found no credential patterns. All temporary controllers have exact recoverable compressed archives; rendering screenshots and command records remain durable evidence. The final immediate pre-commit preflight and protected-branch remote publication are the remaining steps.\n');
console.log(JSON.stringify({status:'ready-for-controller-cleanup',controllers:sources,archive:fileRecord(archivePath),locks:locked}));
