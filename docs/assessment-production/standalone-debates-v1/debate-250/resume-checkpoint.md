# Debate 250: resumed after audio budget approval

Current status: the user approved the $3 budget. `audio/cost-authorization-exception.json` authenticates that approval to the frozen plan. Both isolated primary assessments completed and validated, producing only two burden-eligibility documentation disputes. Sequential one-attempt audio checks are in progress; inspect the per-clip attempt records before doing anything with audio. Never repeat a started or completed attempt. The older pause snapshot below is preserved as history, not the current authority state.

Video: https://www.youtube.com/watch?v=q6-57NNo5fA

James Anderson and Tom Jump, “Is there reason to believe in God?” Event January 24, 2020; supplied upload November 9, 2023. Source identity evidence preserves that distinction. Both supplied and original-linked video IDs were checked for duplicates.

Working copy: `/Users/philstilwell/Documents/SLUGFESTER-debate-250`, branch `codex/debate-250`, initial main baseline `02ede16a2eefb116e719a61dc72932c55b118fff`. All older work remains untouched. Nothing has been committed, pushed or published for this debate.

## Completed

- Complete public English ASR transcript acquired, fully read, and hash-locked: 2,266 events, 5,178-second nominal recording. No material timestamp gaps; no substantive third advocate.
- Fresh isolated GPT-5.6 Sol/low score-blind inventory completed. Controller independently reviewed all selected excerpts, sections, burdens, reply links and coverage. Seven sections, 30 moves, 15 per side, no fourth rows.
- Raw model bytes retained. Before controller acceptance, one reply target was corrected and substantive exact quotations substituted for introductory fragments. The refinement record authenticates every change; source spans, attribution, importance, sections and burdens are unchanged.
- Canonical inventory and primary judgment packet frozen. Both primary judgments remain unstarted; no scores exist.
- English audio acquired at no direct cost. Two opening speaker references and all 30 below-high-attribution clips prepared and hash-locked. No paid calls made; no unknown transport cost.

## Approval needed

The frozen `audio/plan.json` requires 30 one-attempt sequential speaker checks with `gpt-4o-transcribe-diarize`. Indicative duration estimate is $0.2522479, while the conservative planning budget is $3.00 because speaker-labeled output is billed by tokens and varies in detail. This exceeds the skill's standing $1.00 allowance. `audio/cost-disclosure.json` records the request; it is **not authorization**. No other debate's approval applies.

If the user authorizes the requested budget, authenticate that approval against the existing audio plan before the first paid call. Do not reacquire source, recut successful clips, regenerate the accepted inventory, or overwrite any current evidence. Read the current skill/runbook and verify locks when resuming. Implement a guarded no-retry audio execution controller from the prior accepted pattern, deriving identity, clips and counts from this plan. Check actual cumulative usage before each remaining call, preserving uncertain outcomes and stopping for any new authority needed.

Then complete the two isolated primary judgments, deterministic disagreements, all required audio reviews before adjudication, anonymous adjudication, single score pass, isolated publication, two blind fallacy/bias reviews and adjudication, content parity, production validations, rendering and protected-main PR publication. Run `npm run site:preflight` immediately before commit; merge only after the required check is green, then verify main and Pages workflows and live publication.

Temporary controllers currently remain for safe continuation. No browser tabs or servers were opened. Only the inventory subagent ran and it is finished. Clean up owned temporary controllers after archiving their exact bytes at successful completion.
