{
  "type": "object",
  "additionalProperties": false,
  "required": [
    "debateNumber",
    "decisions"
  ],
  "properties": {
    "debateNumber": {
      "type": "string",
      "const": "229"
    },
    "decisions": {
      "type": "array",
      "minItems": 12,
      "maxItems": 12,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "candidateKey",
          "decision",
          "rationale",
          "context"
        ],
        "properties": {
          "candidateKey": {
            "type": "string"
          },
          "decision": {
            "enum": [
              "accepted",
              "rejected"
            ]
          },
          "rationale": {
            "type": "string"
          },
          "context": {
            "type": [
              "string",
              "null"
            ]
          }
        }
      }
    }
  }
}
