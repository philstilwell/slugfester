import fs from 'node:fs';
import path from 'node:path';
import assert from 'node:assert/strict';
import {spawnSync} from 'node:child_process';
import {fileRecord, serializedJson} from './lib/assessment-production-standalone-debate-v1.mjs';
const number=process.argv[process.argv.indexOf('--debate')+1];
const phase=process.argv[process.argv.indexOf('--phase')+1];
assert.match(number||'',/^\d{3,}$/);
const read=p=>JSON.parse(fs.readFileSync(p));
const entry=read('docs/assessment-production/standalone-debates-v1/registry.json').debates.find(d=>d.debateNumber===number);assert.ok(entry);
const root=entry.root, publication=read(`${root}/publication/output.json`),d=publication.candidate;
assert.equal(d.id,entry.debateId);assert.equal(d.number,number);
const out=`output/playwright/standalone-debate-v1/debate-${number}`;
const write=(p,x)=>{assert.equal(fs.existsSync(p),false,`Refuse overwrite ${p}`);fs.mkdirSync(path.dirname(p),{recursive:true});fs.writeFileSync(p,serializedJson(x),{flag:'wx'});};
const cards=d.sections.flatMap(s=>s.exchanges.flatMap(e=>[e.pro,e.con].filter(Boolean)));
const tags=cards.flatMap(c=>c.tags),overallLinks=['pro','con'].flatMap(s=>d.overall[s].blunders.flatMap(b=>b.links));
if(phase==='render'){
 const proof=read(`${root}/rendering/evidence/tags-and-discovery.json`);
 const evidence=['desktop','mobile'].map(name=>read(`${root}/rendering/evidence/${name}.json`));
 for(const [vi,x]of evidence.entries()){
  assert.equal(x.sections.length,d.sections.length);assert.equal(x.aiInitiallyClosed,true);
  d.sections.forEach((s,i)=>{const z=x.sections[i];assert.equal(z.title,s.title);assert.deepEqual(z.scores,[String(s.score.pro),String(s.score.con)]);const cs=s.exchanges.flatMap(e=>[e.pro,e.con].filter(Boolean));assert.equal(z.cards.length,cs.length);cs.forEach((c,j)=>{const a=z.cards[j];assert.equal(a.description,c.words);assert.equal(a.critique,c.critique);for(const t of [c.time,c.role,String(c.score)])assert.ok(a.meta.includes(t));assert.deepEqual(a.tags.map(t=>t.label),c.tags.map(t=>t.label));});});
  const ai=['pro','con'].flatMap(side=>{const a=d.logicalExtension[side];return[a.finalArgument.thesis,...a.finalArgument.premises,a.finalArgument.conclusion,...a.newArguments.flatMap(v=>[v.title,v.text])];});assert.deepEqual(x.ai,ai);
  for(const [i,side] of ['pro','con'].entries()){assert.equal(x.quotes[i],`"${d.quotes[side].text}"`);assert.equal(x.quoteContexts[i],d.quotes[side].context);for(const t of [...d.overall[side].strengths,...d.overall[side].blunders.map(b=>b.text)])assert.ok(x.overall[i].text.includes(t));assert.ok(x.bodyText.includes(d.sides[side].name));}
  assert.ok(x.bodyText.includes(d.motion));assert.ok(x.bodyText.includes(`Assessments made by ${d.assessmentModel}.`));assert.ok(x.links.some(a=>a.href===d.youtubeUrl));assert.ok(x.images.every(i=>i.complete&&i.naturalWidth>0));assert.equal(x.errors.length,0);assert.equal(x.failedResources.length,0);assert.ok(x.scrollWidth<=x.width+1);
  const z=proof[vi===0?'desktop':'mobile'];assert.equal(z.tags.length,tags.length);z.tags.forEach((t,i)=>{assert.equal(t.label,tags[i].label);assert.equal(t.context,tags[i].context);const u=new URL(tags[i].url);assert.ok(t.href.startsWith(`/reference/${tags[i].type}/${u.pathname.split('/').filter(Boolean).at(-1)}/`));assert.ok(t.href.includes(`debate=${d.id}`));});assert.equal(z.overallLinks.length,overallLinks.length);z.overallLinks.forEach((t,i)=>{assert.equal(t.label,overallLinks[i].label);assert.ok(t.href.includes('/reference/'));});
 }
 assert.ok(Object.values(evidence[0].checks).every(Boolean));assert.equal(evidence[1].checks.firstDistantClickOpenedAI,true);assert.equal(evidence[1].checks.critiqueClearedAfterTransition,true);assert.equal(evidence[1].checks.referenceTarget.exists,true);assert.equal(proof.discovery.length,5);assert.ok(fs.readFileSync('sitemap.xml','utf8').includes(d.id));
 const shots=['desktop-final-initial.png','desktop-final-card-survey.png','desktop-final-ai-prose.png','desktop-final-critique.png','desktop-final-enter.png','desktop-final-space.png','desktop-final-tag-settled.png','mobile-final-top-clean.png','mobile-final-critique.png','mobile-final-ai.png','mobile-final-ai-clean.png','mobile-final-tag-settled.png','mobile-final-reference.png'].map(f=>fileRecord(`${out}/${f}`));
 const checks={critiquePointerControlPassed:true,critiqueKeyboardEnterPassed:true,critiqueKeyboardSpacePassed:true,aiAccordionKeyboardEnterOpened:true,aiAccordionKeyboardSpaceClosed:true,aiAccordionInitiallyCollapsed:true,mobileCritiqueThenDistantControlPassed:true,mobileAiAccordionOpened:true,mobileCritiqueClearedAfterTransition:true,referenceNavigationAndOccurrenceAnchorPassed:true,allDescriptionsAndCritiquesMatchPublication:true,allArgumentTagsAndOverallLinksMatch:true,allAIContentStringsMatch:true,sourceLinkSideLabelsTimestampsAndScoresMatch:true,assessmentBylineMatches:true,bothExistingPortraitsAndProfileLinksWork:true,searchAndTopicDiscoveryPassed:true,bothProfilesIncludeDebate:true,sitemapIncludesDebate:true,noHorizontalOverflow:true,noConsoleErrorsOrWarnings:true,allResourcesLoaded:true,visualInspectionPassed:true};
 write(`${root}/rendering/controller-review.json`,{status:'passed-complete-content-and-visual-review',debateNumber:number,publication:fileRecord(`${root}/publication/output.json`),evidence:['desktop.json','mobile.json','tags-and-discovery.json'].map(f=>fileRecord(`${root}/rendering/evidence/${f}`)),checks,notes:['Every displayed description, critique, section score, timestamp, role, quotation, overall item and AI content string was compared with publication bytes.','Inspected settled desktop and phone screenshots, open critiques, fallacy context, AI prose, and the local occurrence target; no clipped prose or empty cards.','Transient animation captures were superseded by settled captures. No site code or CSS change was required.','Owned browser debate'+number+' and the source-preview server were closed before this rendering freeze.']});
 const audit={viewports:2,screenshots:shots.length,argumentCardsPerViewport:cards.length,runtimeFailures:0,failedResources:0,horizontalOverflowFailures:0,emptyArgumentCards:0,temporaryBrowsersRemaining:0,temporaryServersRemaining:0,directIncrementalCostUsd:0};
 const common={protocolId:'assessment-production-standalone-debate-v1',debateNumber:number,debateId:d.id,completedAt:new Date().toISOString(),checks};
 write(`${root}/rendering/rendering-audit.json`,{schemaVersion:'1.0-standalone-production-rendering-audit',...common,status:'passed-standalone-production-rendering',results:['desktop','mobile'].map(name=>({viewportName:name,status:'passed-rendering-viewport',evidence:fileRecord(`${root}/rendering/evidence/${name}.json`)})),screenshots:shots,controllerReview:fileRecord(`${root}/rendering/controller-review.json`),audit});
 write(`${root}/rendering/post-rhetorical-tag-audit.json`,{schemaVersion:'1.0-standalone-post-rhetorical-tag-rendering-audit',...common,status:'passed-post-rhetorical-tag-rendering-audit',viewports:['desktop','mobile'].map(name=>({name,width:proof[name].width,height:name==='desktop'?1000:844,scrollWidth:proof[name].scrollWidth,argumentTagLinks:tags.length,overallReferenceLinks:overallLinks.length,screenshot:fileRecord(`${out}/${name}-final-tag-settled.png`)})),audit:{...audit,acceptedTagsRendered:tags.length,acceptedOverallLinksRendered:overallLinks.length,visibleCorrectionsVerified:tags.length+overallLinks.length}});
 console.log('Rendering freeze passed.');
}else if(phase==='summary'){
 const judgmentExecution=read(`${root}/judgments/execution.json`);
 const audio=read(`${root}/audio/audio-verification.json`),score=read(`${root}/score-pass/output.json`),inv=read(`${root}/inventory/inventory.json`),dis=read(`${root}/disagreements/disagreements.json`),rhet=read(`${root}/publication/rhetorical-tag-audit.json`);
 const words=s=>s.trim().split(/\s+/u).length,range=xs=>[Math.min(...xs),Math.max(...xs)],mean=xs=>xs.reduce((a,b)=>a+b,0)/xs.length;
 write(`${root}/validation-summary.json`,{schemaVersion:'1.0-standalone-validation-summary',protocolId:'assessment-production-standalone-debate-v1',status:'passed',debateNumber:number,debateId:d.id,completedAt:new Date().toISOString(),source:{videoId:entry.videoId,fullTranscriptAcquired:true,requiredAttributionAudioChecks:audio.audit.requiredAttributionChecks,completedAttributionAudioChecks:audio.audit.completedAttributionChecks,unresolvedAttributionChecks:0,primarySpeakerExceptionUsed:false},assessment:{inventoryMoves:inv.moves.length,inventorySides:{pro:inv.moves.filter(m=>m.side==='pro').length,con:inv.moves.filter(m=>m.side==='con').length},acceptedPrimaryJudgmentPasses:2,primaryContextsIncludingPreservedFailures:judgmentExecution.execution.totalAssessmentContexts,authorizedReplacementAttempts:judgmentExecution.execution.additionalReplacementContexts,validOriginalPassBRetained:true,judgmentModel:d.assessmentModel,reasoningEffort:'low',disputes:dis.disputes.length,scorePasses:1,scoreReruns:0,manualScoreOverrides:0,score:d.score,stability:score.scoreStability},publication:{sections:d.sections.length,displayRows:d.sections.reduce((n,s)=>n+s.exchanges.length,0),argumentCards:cards.length,descriptionWords:range(cards.map(c=>words(c.words))),descriptionMeanWords:mean(cards.map(c=>words(c.words))),critiqueWords:range(cards.map(c=>words(c.critique))),minimumCritiqueCharacters:Math.min(...cards.map(c=>c.critique.length)),contentParityAuditPassed:true,sourceExactQuotes:2,blindRhetoricalReviews:2,rhetoricalCandidatesReviewed:rhet.completeCandidateUnion.length,acceptedCardTags:tags.length,acceptedOverallBlunderLinks:overallLinks.length,rejectedCandidates:rhet.completeCandidateUnion.filter(r=>r.decision==='rejected').length,productionLedger:fileRecord(entry.productionLedger.path)},rendering:read(`${root}/rendering/rendering-audit.json`).audit,directCostUsd:audio.cost.knownSuccessfulCallCostUsd,maximumPossiblePaidCostUsd:audio.cost.maximumPossiblePaidCostUsd??audio.cost.knownSuccessfulCallCostUsd,paidRetries:0,withdrawnVideoIds:['TSZlPZvIOC0','UWWNTmTAqnA'],gateScope:'Completed source, assessment, publication and rendering component gates. Subsequent complete repository replay and preflight are recorded separately in validation/final-local-gates.json.'});console.log('Component validation summary frozen.');
}else if(phase==='gates'||phase==='gates-recovery'){
 const commands=[['node',['scripts/validate-debates.mjs']],['node',['scripts/validate-corpus-transcripts.mjs']],['node',['scripts/audit-assessment-production-standalone-v1.mjs','--audit','--debate',number]],['npm',['run','check']],['npm',['run','site:check']],['git',['diff','--check']]];
 const results=[];for(const [command,args]of commands){const r=spawnSync(command,args,{encoding:'utf8',maxBuffer:32*1024*1024});const output=`${r.stdout||''}${r.stderr||''}`;const name=`${root}/validation/${phase}-${results.length+1}.log`;assert.equal(fs.existsSync(name),false);fs.writeFileSync(name,output,{flag:'wx'});results.push({command:[command,...args].join(' '),exitCode:r.status,output:fileRecord(name)});console.log(JSON.stringify({command:[command,...args].join(' '),exitCode:r.status,tail:output.slice(-1600)}));if(r.status!==0){write(`${root}/validation/failed-${phase}.json`,{status:'failed',results});process.exit(1);}}
 write(`${root}/validation/final-local-gates.json`,{status:'passed',debateNumber:number,debateId:d.id,completedAt:new Date().toISOString(),results});
}else throw new Error('Unknown phase');
