import fs from 'node:fs';
import path from 'node:path';
import assert from 'node:assert/strict';
import {fileRecord,serializedJson,canonicalJson} from './lib/assessment-production-standalone-debate-v1.mjs';
import {referenceDefinitions} from '../src/data/references.js';
import {debates} from '../src/data/debates.js';
const arg=k=>process.argv[process.argv.indexOf(k)+1],number=arg('--debate'),phase=arg('--phase');assert.match(number||'',/^\d{3,}$/);
const read=p=>JSON.parse(fs.readFileSync(p)),record=fileRecord;
const entry=read('docs/assessment-production/standalone-debates-v1/registry.json').debates.find(r=>r.debateNumber===number);assert.ok(entry);
const root=entry.root,rr=`${root}/publication/rhetorical-tag-review-1`,publicationPath=`${root}/publication/output.json`;
const write=(p,v)=>{assert.equal(fs.existsSync(p),false,`Refuse overwrite ${p}`);fs.mkdirSync(path.dirname(p),{recursive:true});fs.writeFileSync(p,serializedJson(v),{flag:'wx'});};
const words=s=>String(s??'').trim().split(/\s+/u).filter(Boolean).length;
const allCards=d=>d.sections.flatMap(s=>s.exchanges.flatMap(e=>[e.pro,e.con].filter(Boolean)));
const key=r=>`${r.moveId??r.blunderId}|${r.type}|${r.label}`;
const definition=r=>{assert.ok(['fallacy','bias'].includes(r.type));const d=Object.values(referenceDefinitions[r.type]).find(d=>d.label===r.label);assert.ok(d,`Unknown label ${r.label}`);assert.equal(r.url,d.externalUrl);return d;};
const model={label:'5.6 Sol',slug:'gpt-5.6-sol',reasoningEffort:'low',authentication:'ChatGPT subscription'};
const checkRow=(r,ids,idKey)=>{assert.ok(ids.includes(r[idKey]));definition(r);assert.ok(['accepted','rejected'].includes(r.decision));assert.ok(words(r.context)>=8&&words(r.context)<=35,`${key(r)} context length`);assert.ok(words(r.rationale)>=12,`${key(r)} rationale length`);};
if(phase==='freeze'){
 const publication=read(publicationPath),candidate=publication.candidate,inventory=read(`${root}/inventory/inventory.json`);
 assert.equal(candidate.id,entry.debateId);const byId=new Map(inventory.moves.map(m=>[m.moveId,m]));
 const cards=allCards(candidate);assert.equal(cards.length,inventory.moves.length);for(const c of cards)assert.deepEqual(c.tags,[]);
 write(`${rr}/reference-definitions.json`,referenceDefinitions);
 const moves=cards.map(c=>{const m=byId.get(c.ledgerMoveId);assert.ok(m);return {moveId:m.moveId,side:m.side,speaker:m.speaker,sectionId:m.sectionId,sourceSpan:m.sourceSpan,claim:m.claim,inference:m.inferentialBridge,respondsToIds:m.respondsToIds,responseComponents:m.responseComponents,burdenContact:m.burdenContact,description:c.words,critiqueWithoutLockedScore:c.critique.split('Locked score:')[0].trim()};});
 const overallBlunders=['pro','con'].flatMap(side=>candidate.overall[side].blunders.map((b,i)=>{assert.deepEqual(b.links,[]);return {blunderId:`overall-${side}-blunder-${i+1}`,side,speaker:candidate.sides[side].speaker,text:b.text};}));
 const instructions={reviewEveryMoveInPublicationOrder:true,reviewEveryOverallBlunder:true,applyEveryCatalogDefinition:true,recordAllSeriousCandidatesIncludingAcceptancesAndRejections:true,doNotInventTagQuota:true,doNotTagMereWeaknessDisagreementCitationsOrAnalogy:true,identifyObservableDefinitionMechanism:true,distinguishFallacyFromBias:true,avoidDuplicatingOneMechanismWithTwoTagsUnlessDistinct:true,scoresAndWinnerUnavailable:true,otherReviewerAndOtherDebatesUnavailable:true,contextWords:[8,35],rationaleMinimumWords:12};
 const outputContract={schemaVersion:'1.1',status:'complete-and-schema-valid',pass:'pass-a or pass-b',reviewedMoveIds:moves.map(m=>m.moveId),reviewedOverallBlunderIds:overallBlunders.map(b=>b.blunderId),candidateReviews:'[{moveId,type,label,url,context,decision:accepted|rejected,rationale}] for every serious move candidate',overallBlunderCandidateReviews:'[{blunderId,type,label,url,context,decision:accepted|rejected,rationale}] for every serious overall candidate',acceptedTags:'exact accepted subset of candidateReviews',acceptedOverallBlunderLinks:'exact accepted subset of overallBlunderCandidateReviews',audit:{allMovesReviewed:true,allOverallBlundersReviewed:true,all13DefinitionsConsidered:true,noTagQuota:true,scoreBlind:true}};
 write(`${rr}/packet.json`,{schemaVersion:'1.1-blind-rhetorical-packet',protocolId:'assessment-production-standalone-debate-v1',status:'frozen-after-score-and-critique-lock',debateNumber:number,debateId:entry.debateId,motion:candidate.motion,sourceTranscript:record(`.assessment-cache/captions/${entry.videoId}/transcript.txt`),sides:candidate.sides,catalog:record(`${rr}/reference-definitions.json`),moves,overallBlunders,instructions,outputContract});
 write(`${rr}/execution-plan.json`,{status:'frozen-before-two-blind-reviews',debateNumber:number,debateId:entry.debateId,model,forkTurns:'none',freshContexts:2,allowedInputs:[`${rr}/packet.json`,`${rr}/reference-definitions.json`,`.assessment-cache/captions/${entry.videoId}/transcript.txt`,`${root}/audio/audio-verification.json`].map(p=>record(p)),passes:['a','b'].map(s=>({pass:`pass-${s}`,agentTask:`/root/rhetorical_${number}_${s}`,outputPath:`${rr}/pass-${s}-output.json`,attempts:1,retries:0})),controllerOverrides:{},directIncrementalCostUsd:0});
 console.log(JSON.stringify({debateNumber:number,moves:moves.length,overallBlunders:overallBlunders.length}));
}else if(phase==='union'){
 const packet=read(`${rr}/packet.json`),plan=read(`${rr}/execution-plan.json`);
 for(const r of plan.allowedInputs)assert.equal(record(r.path).sha256,r.sha256);assert.deepEqual(plan.controllerOverrides,{});
 const mid=packet.moves.map(m=>m.moveId),bid=packet.overallBlunders.map(b=>b.blunderId);
 const passes=plan.passes.map(p=>{const x=read(p.outputPath);assert.equal(x.schemaVersion,'1.1');assert.equal(x.status,'complete-and-schema-valid');assert.equal(x.pass,p.pass);assert.deepEqual(x.reviewedMoveIds,mid);assert.deepEqual(x.reviewedOverallBlunderIds,bid);x.candidateReviews.forEach(r=>checkRow(r,mid,'moveId'));x.overallBlunderCandidateReviews.forEach(r=>checkRow(r,bid,'blunderId'));assert.equal(new Set([...x.candidateReviews,...x.overallBlunderCandidateReviews].map(key)).size,x.candidateReviews.length+x.overallBlunderCandidateReviews.length);assert.deepEqual(x.acceptedTags,x.candidateReviews.filter(r=>r.decision==='accepted'));assert.deepEqual(x.acceptedOverallBlunderLinks,x.overallBlunderCandidateReviews.filter(r=>r.decision==='accepted'));for(const k of Object.keys(packet.outputContract.audit))assert.equal(x.audit[k],true);return x;});
 const union=new Map();for(const x of passes)for(const r of [...x.candidateReviews,...x.overallBlunderCandidateReviews]){const k=key(r);if(!union.has(k))union.set(k,{candidateKey:k,moveId:r.moveId??null,blunderId:r.blunderId??null,type:r.type,label:r.label,url:r.url,definition:definition(r).definition,reviews:[]});union.get(k).reviews.push({decision:r.decision,context:r.context,rationale:r.rationale});}
 const order=[...mid,...bid],candidates=[...union.values()].sort((a,b)=>order.indexOf(a.moveId??a.blunderId)-order.indexOf(b.moveId??b.blunderId)||a.type.localeCompare(b.type)||a.label.localeCompare(b.label));
 write(`${rr}/pass-validation.json`,{status:'passed-complete-ordered-blind-reviews',debateNumber:number,moveCount:mid.length,blunderCount:bid.length,passes:plan.passes.map((p,i)=>({pass:p.pass,output:record(p.outputPath),moveCandidates:passes[i].candidateReviews.length,overallCandidates:passes[i].overallBlunderCandidateReviews.length})),unionCount:candidates.length,acceptedCandidatesIncluded:true,sourcePacket:record(`${rr}/packet.json`)});
 write(`${rr}/adjudication-packet.json`,{schemaVersion:'1.1-anonymous-rhetorical-adjudication-packet',status:'frozen-anonymous-union',debateNumber:number,debateId:entry.debateId,motion:packet.motion,sourceTranscript:packet.sourceTranscript,definitions:referenceDefinitions,moves:packet.moves,overallBlunders:packet.overallBlunders,candidates,requiredCandidateKeys:candidates.map(c=>c.candidateKey),outputContract:{schemaVersion:'1.1',status:'complete-and-schema-valid',exactResolutionCount:candidates.length,minItems:candidates.length,maxItems:candidates.length,resolutions:'[{candidateKey,decision:accepted|rejected,context:8-35 words,rationale:at least12words}] in exact requiredCandidateKeys order',audit:{allCandidatesResolved:true,scoresUnavailable:true,anonymousReviews:true,noTagQuota:true}},instructions:{anonymousReviews:true,everyCandidateExactlyOnce:true,oneReviewerCandidatesIncluded:true,noQuota:true,applyExactCatalogDefinition:true,sourceSpecificMechanismRequired:true,distinguishBiasFromFallacy:true,noInventedCandidates:true,scoresUnavailable:true}});
 write(`${rr}/adjudication-execution-plan.json`,{status:'frozen-before-anonymous-adjudication',debateNumber:number,debateId:entry.debateId,model,freshContext:true,forkTurns:'none',agentTask:`/root/rhetorical_${number}_adjudication`,allowedInputs:[`${rr}/adjudication-packet.json`,packet.sourceTranscript.path,`${root}/audio/audio-verification.json`].map(p=>record(p)),outputPath:`${rr}/adjudication-output.json`,candidateCount:candidates.length,attempts:1,retries:0,directIncrementalCostUsd:0});
 console.log(JSON.stringify({debateNumber:number,unionCandidates:candidates.length,passes:passes.map(p=>({pass:p.pass,moveCandidates:p.candidateReviews.length,accepted:p.acceptedTags.length}))}));
}else if(phase==='apply'){
 const packet=read(`${rr}/adjudication-packet.json`),resolved=read(`${rr}/adjudication-output.json`),review=read(`${rr}/controller-review.json`);
 assert.equal(review.status,'passed-source-and-exact-definition-review');assert.equal(review.output.sha256,record(`${rr}/adjudication-output.json`).sha256);assert.equal(review.allCandidatesRead,true);assert.deepEqual(review.controllerOverrides,{});
 assert.equal(resolved.status,'complete-and-schema-valid');assert.deepEqual(resolved.resolutions.map(r=>r.candidateKey),packet.requiredCandidateKeys);
 const rows=resolved.resolutions.map((r,i)=>{assert.ok(['accepted','rejected'].includes(r.decision));assert.ok(words(r.rationale)>=12);assert.ok(words(r.context)>=8&&words(r.context)<=35);const c=packet.candidates[i];definition(c);return {...c,decision:r.decision,context:r.context,rationale:r.rationale};});
 const publication=read(publicationPath),before=structuredClone(publication),cards=allCards(publication.candidate),mid=cards.map(c=>c.ledgerMoveId);
 const candidateReviews=rows.filter(r=>r.moveId).map(({reviews,definition,...r})=>r),overallBlunderCandidateReviews=rows.filter(r=>r.blunderId).map(({reviews,definition,...r})=>r);
 const acceptedTags=mid.flatMap(moveId=>candidateReviews.filter(r=>r.moveId===moveId&&r.decision==='accepted').map(r=>({moveId,label:r.label,type:r.type,url:r.url,context:r.context,rationale:r.rationale})));
 const acceptedOverallBlunderLinks=overallBlunderCandidateReviews.filter(r=>r.decision==='accepted').map(r=>({blunderId:r.blunderId,type:r.type,label:r.label,url:r.url,context:r.context,rationale:r.rationale}));
 write(`${rr}/final-definition-check.json`,{status:'passed-exact-local-definition-and-critique-check',debateNumber:number,debateId:entry.debateId,catalog:record(`${rr}/reference-definitions.json`),adjudication:record(`${rr}/adjudication-output.json`),controllerReview:record(`${rr}/controller-review.json`),controllerOverrides:{},completeCandidateUnion:rows,acceptedTags,acceptedOverallBlunderLinks});
 const plan=read(`${rr}/execution-plan.json`),aplan=read(`${rr}/adjudication-execution-plan.json`);
 for(const r of [...plan.allowedInputs,...aplan.allowedInputs])assert.equal(record(r.path).sha256,r.sha256);
 write(`${rr}/execution.json`,{schemaVersion:'1.0-standalone-rhetorical-review-execution',protocolId:'assessment-production-standalone-debate-v1',status:'passed-two-blind-reviews-adjudication-and-final-definition-check',debateNumber:number,debateId:entry.debateId,model,isolation:{blindReviewContexts:2,freshAnonymousAdjudicator:true,forkTurns:'none',otherReviewOutputsUnavailableToEachBlindReviewer:true,otherDebatesAndTagFrequenciesUnavailableToModels:true},sourcePacket:record(`${rr}/packet.json`),catalogSnapshot:record(`${rr}/reference-definitions.json`),contexts:[...plan.passes.map(p=>({role:`blind-review-${p.pass.at(-1)}`,agentTask:p.agentTask,input:record(`${rr}/packet.json`),output:record(p.outputPath),attempts:1,retries:0})),{role:'anonymous-adjudication',agentTask:aplan.agentTask,input:record(`${rr}/adjudication-packet.json`),output:record(aplan.outputPath),attempts:1,retries:0}],audit:{blindReviews:2,retries:0,unionCandidates:rows.length},directIncrementalCostUsd:0,completedAt:new Date().toISOString()});
 const baseline=debates.filter(d=>Number(d.number)>=171&&Number(d.number)<=195),stats=baseline.map(d=>{const m=allCards(d);return {number:d.number,moves:m.length,tagged:m.filter(c=>c.tags.length).length,tags:m.reduce((s,c)=>s+c.tags.length,0)};}),sum=k=>stats.reduce((s,v)=>s+v[k],0),sorted=stats.map(s=>s.tags).sort((a,b)=>a-b);
 const baselineComparison={debateCount:stats.length,debateNumbers:stats.map(s=>s.number),moves:sum('moves'),taggedArguments:sum('tagged'),tags:sum('tags'),taggedArgumentRate:Number((sum('tagged')/sum('moves')).toFixed(3)),perDebateTagMinimum:sorted[0],perDebateTagMedian:sorted[Math.floor(sorted.length/2)],perDebateTagMaximum:sorted.at(-1),zeroTagDebates:stats.filter(s=>!s.tags).map(s=>s.number),interpretation:'The independent corpus supplies a consistency check, not a target. Every local catalog definition was considered for each source-grounded move; conditional criticism and mere underdevelopment are not automatically fallacies.'};
 write(`${root}/publication/rhetorical-tag-audit.json`,{schemaVersion:'1.0-standalone-rhetorical-tag-audit',protocolId:'assessment-production-standalone-debate-v1',status:'passed-rhetorical-tag-review',debateNumber:number,debateId:entry.debateId,reviewedMoveIds:mid,independentReview:{executionPath:`${rr}/execution.json`,executionSha256:record(`${rr}/execution.json`).sha256},baselineComparison,completeCandidateUnion:rows,candidateReviews,overallBlunderCandidateReviews,acceptedTags,acceptedOverallBlunderLinks,audit:{judgmentChanges:0,scoreChanges:0,moveChanges:0,acceptedTagCount:acceptedTags.length,rejectedCandidateCount:candidateReviews.filter(r=>r.decision==='rejected').length,overallBlunderRejectedCandidateCount:overallBlunderCandidateReviews.filter(r=>r.decision==='rejected').length}});
 const preservedPath=`${root}/publication/preserved-output-before-rhetorical-tag-review.json`;fs.copyFileSync(publicationPath,preservedPath,fs.constants.COPYFILE_EXCL);assert.equal(record(preservedPath).sha256,record(publicationPath).sha256);
 const changed=[];publication.candidate.sections.forEach((s,si)=>s.exchanges.forEach((e,ei)=>['pro','con'].forEach(side=>{const c=e[side];if(!c)return;const tags=acceptedTags.filter(t=>t.moveId===c.ledgerMoveId).map(({moveId,rationale,...t})=>t);if(tags.length){assert.deepEqual(c.tags,[]);c.tags=tags;changed.push(`candidate.sections[${si}].exchanges[${ei}].${side}.tags`);}})));
 for(const side of ['pro','con'])publication.candidate.overall[side].blunders.forEach((b,i)=>{const links=acceptedOverallBlunderLinks.filter(t=>t.blunderId===`overall-${side}-blunder-${i+1}`).map(t=>({label:t.label,url:t.url}));if(links.length){assert.deepEqual(b.links,[]);b.links=links;changed.push(`candidate.overall.${side}.blunders[${i}].links`);}});
 const stripped=structuredClone(publication);allCards(stripped.candidate).forEach(c=>c.tags=[]);for(const side of ['pro','con'])stripped.candidate.overall[side].blunders.forEach(b=>b.links=[]);assert.equal(canonicalJson(stripped),canonicalJson(before));
 const stage=`${publicationPath}.tag-stage`;write(stage,publication);fs.renameSync(stage,publicationPath);
 write(`${root}/publication/correction-2-rhetorical-tags.json`,{schemaVersion:'1.0-rhetorical-tag-correction',protocolId:'assessment-production-standalone-debate-v1',status:'applied-once-and-frozen',debateNumber:number,debateId:entry.debateId,preservedPriorOutput:record(preservedPath),applicationShards:Array.from({length:Math.ceil(changed.length/2)},(_,i)=>({ordinal:i+1,fields:changed.slice(i*2,i*2+2),maximumFields:2,deterministicApplication:true})),audit:{judgmentChanges:0,scoreChanges:0,moveChanges:0,critiqueChanges:0,tagChanges:acceptedTags.length,overallBlunderLinkChanges:acceptedOverallBlunderLinks.length,maximumWritableFieldsPerShard:2},evidence:{after:record(publicationPath),rhetoricalTagAudit:record(`${root}/publication/rhetorical-tag-audit.json`),finalDefinitionCheck:record(`${rr}/final-definition-check.json`)}});
 console.log(JSON.stringify({acceptedTags:acceptedTags.length,acceptedOverallBlunderLinks:acceptedOverallBlunderLinks.length,rejected:rows.filter(r=>r.decision==='rejected').length,scoreChanges:0}));
}else throw new Error('Unknown phase');
