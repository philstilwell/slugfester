import fs from 'node:fs';
import path from 'node:path';
import assert from 'node:assert/strict';
import {execFileSync} from 'node:child_process';
import {fileRecord,serializedJson,validateStandaloneAdjudication} from './lib/assessment-production-standalone-debate-v1.mjs';
import {debates} from '../src/data/debates.js';
const arg=k=>process.argv[process.argv.indexOf(k)+1],number=arg('--debate'),phase=arg('--phase');assert.match(number||'',/^\d{3,}$/);
const read=p=>JSON.parse(fs.readFileSync(p)),record=fileRecord;
const entry=read('docs/assessment-production/standalone-debates-v1/registry.json').debates.find(r=>r.debateNumber===number);assert.ok(entry);
const root=entry.root,auth=read(`${root}/authorization.json`),identity=auth.identity;
assert.equal(identity.debateId,entry.debateId);assert.equal(identity.videoId,entry.videoId);
const write=(p,v)=>{assert.equal(fs.existsSync(p),false,`Refuse overwrite ${p}`);fs.mkdirSync(path.dirname(p),{recursive:true});fs.writeFileSync(p,serializedJson(v),{flag:'wx'});};
const command=mode=>execFileSync(process.execPath,['scripts/audit-assessment-production-standalone-v1.mjs',mode,'--debate',number],{encoding:'utf8',maxBuffer:5*1024*1024});
if(phase==='adjudication'){
 const outputPath=`${root}/adjudication/output.json`,planPath=`${root}/adjudication/execution-plan.json`,plan=read(planPath);
 for(const r of plan.allowedInputs)assert.equal(record(r.path).sha256,r.sha256);
 const validation=validateStandaloneAdjudication(read(outputPath),read(`${root}/disagreements/disagreements.json`));
 assert.equal(validation.disputes,plan.disputeCount);
 assert.equal(read(`${root}/audio/audio-verification.json`).audit.unresolvedAttributionChecks,0);
 write(`${root}/adjudication/execution.json`,{status:'complete-and-schema-valid',debateNumber:number,debateId:entry.debateId,model:{slug:plan.model,label:plan.displayModel,reasoningEffort:plan.reasoningEffort,authentication:plan.authentication},agentTask:plan.agentTask,freshContext:true,forkTurns:'none',plan:record(planPath),output:record(outputPath),attempts:1,retries:0,directIncrementalCostUsd:0,validation,completedAt:new Date().toISOString()});
 console.log(command('--assemble-ledger'));
 console.log(command('--score-once'));
}else if(phase==='packet'){
 const inventory=read(`${root}/inventory/inventory.json`),scores=read(`${root}/score-pass/output.json`),source=read(`${root}/source/source-lock.json`),audio=read(`${root}/audio/audio-verification.json`);
 assert.equal(scores.debateId,entry.debateId);assert.equal(inventory.motion,identity.motion);
 const sec=ms=>{const n=Math.floor(ms/1000);return n>=3600?`${Math.floor(n/3600)}:${String(Math.floor(n%3600/60)).padStart(2,'0')}:${String(n%60).padStart(2,'0')}`:`${Math.floor(n/60)}:${String(n%60).padStart(2,'0')}`;};
 const counts=inventory.moves.length;
 const sample=debates.filter(d=>Number(d.number)<Number(number)).sort((a,b)=>Number(a.number)-Number(b.number)).slice(-25).map(d=>{const cards=d.sections.flatMap(s=>s.exchanges.flatMap(e=>[e.pro,e.con].filter(Boolean)));return {number:d.number,mean:cards.reduce((sum,c)=>sum+c.words.trim().split(/\s+/u).length,0)/cards.length};});
 assert.equal(sample.length,25);const sorted=sample.map(d=>d.mean).sort((a,b)=>a-b),median=sorted[Math.floor(sorted.length/2)],minimum=Math.max(20,median-1);
 write(`${root}/publication/recent-description-benchmark.json`,{status:'frozen-before-publication',debateNumber:number,debateId:entry.debateId,source:record('src/data/debates.js'),sample,medianMeanWords:median,minimumMeanWords:minimum,maximumShareBelow20Words:0.25,notes:'Independent recent production descriptions are a numeric style benchmark only; no precedent assessment prose is supplied to the publication worker. Formal all-standalone-excluding parity is a separate gate.'});
 const template={number,id:entry.debateId,date:new Intl.DateTimeFormat('en-CA',{timeZone:'America/New_York',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date()),title:`${identity.pro.speaker} vs ${identity.con.speaker}: Classical Theism and Monotheism (${identity.eventYear})`,label:identity.label,topicCategory:identity.topicCategory,youtubeUrl:identity.canonicalUrl,duration:'1 hr 43 min',motion:identity.motion,assessmentModel:'5.6 Sol',assessmentRubric:'Slugfester Reassessment Rubric v2',sourceNote:`Complete automatic English captions cover this Intellectual Catholicism exchange, uploaded on ${identity.uploadDate}; the exact recording date is not established. The assessed window is ${sec(source.participants.assessedDebateWindowMs.start)}–${sec(source.participants.assessedDebateWindowMs.end)}. Suan Sonna's moderator logistics are unscored, and only criticisms Joe Schmid adopts or answers are assessed; absent Ryan Mullins is not a third advocate. Condensed summaries are distinguished from short exact source quotations. The frozen inventory contains ${counts} moves, with all ${audio.checks.length} required audio-attribution checks completed.`,scoringNote:`These AI-generated scores estimate argumentative performance in this transcript, not the truth of a worldview. They were computed exactly once from a frozen ${counts}-move ledger after two accepted isolated primary judgments and anonymous adjudication. One failed second assessment was preserved and replaced with user authorization. Publication prose and subsequent fallacy and cognitive-bias review cannot alter the numerical result.`,sides:Object.fromEntries(['pro','con'].map(side=>[side,{name:identity[side].position,speaker:identity[side].speaker,color:side==='pro'?'teal':'coral'}])),score:{pro:scores.overall.pro.score,con:scores.overall.con.score,winner:scores.winner}};
 const publicationLayout=scores.sections.map(s=>{const selected=inventory.moves.filter(m=>m.sectionId===s.sectionId);const p=s.sides.pro.moves,c=s.sides.con.moves;return {sectionId:s.sectionId,title:s.title,timebox:`${sec(Math.min(...selected.map(m=>m.sourceSpan.startMs)))}–${sec(Math.max(...selected.map(m=>m.sourceSpan.endMs)))}`,score:{pro:s.sides.pro.score,con:s.sides.con.score},rows:Array.from({length:Math.max(p.length,c.length)},(_,i)=>[p[i]?.moveId??null,c[i]?.moveId??null])};});
 const moveScore=new Map(scores.sections.flatMap(s=>['pro','con'].flatMap(side=>s.sides[side].moves.map(m=>[m.moveId,m.score]))));
 const moveDisplayControls=Object.fromEntries(inventory.moves.map(m=>[m.moveId,{ledgerMoveId:m.moveId,time:sec(m.sourceSpan.startMs),score:moveScore.get(m.moveId),role:m.moveKind==='constructive'?(m.importance===3?'Load-bearing constructive':'Supporting constructive'):(m.importance===3?'Major direct reply':'Supporting reply')} ]));
 const controls={argumentDescriptionTargetWords:[24,28],argumentDescriptionAllowedWords:[8,55],argumentMeanMinimum:minimum,maximumShareBelow20Words:0.25,critiqueAllowedWords:[105,130],critiqueMinimumCharacters:880,critiqueSentenceLabels:['Strongest feature:','Principal limitation:','Live burden:','Locked score:'],summaryWords:[18,28],overallStrengthsPerSide:3,overallBlundersPerSide:3,overallItemTargetWords:[18,30],newArgumentsPerSide:3,premisesPerSide:5,representativeQuoteWords:[3,18],quoteContextWords:[18,45],tagsAndBlunderLinksMustRemainEmptyUntilRhetoricalReview:true};
 const evidenceLocks=Object.fromEntries([['inventory',`${root}/inventory/inventory.json`],['finalLedger',`${root}/final-ledger/final-ledger.json`],['scores',`${root}/score-pass/output.json`],['scoreAttestation',`${root}/score-pass/attestation.json`],['source',`${root}/source/source-lock.json`],['audio',`${root}/audio/audio-verification.json`],['transcript',`.assessment-cache/captions/${entry.videoId}/transcript.txt`]].map(([k,p])=>[k,record(p)]));
 write(`${root}/publication/packet.json`,{schemaVersion:'1.1-standalone-publication-packet',protocolId:'assessment-production-standalone-debate-v1',status:'frozen-score-locked-input',debateNumber:number,debateId:entry.debateId,evidenceLocks,template,publicationLayout,moveDisplayControls,publicationControls:controls,immutable:{identity:true,motion:true,sections:true,moves:true,scores:true}});
 const contract={schemaVersion:'1.0-standalone-publication-prose-contract',requiredTopLevelFields:['schemaVersion','protocolId','status','debateNumber','debateId','candidate','noveltyMap','sourceQuoteAudit','audit'],requiredSchemaVersion:'1.0-standalone-publication-output',moveIdsInRequiredPublicationOrder:publicationLayout.flatMap(s=>s.rows.flatMap(r=>r.filter(Boolean))),candidateShape:{fixedFields:'Copy every packet.template field byte-for-byte.',summary:'18–28 word source-specific comparative summary.',quotes:{pro:'{text,context}',con:'{text,context}'},sections:'Copy publicationLayout identities/title/timebox/score and convert rows to exchanges [{pro:card or null,con:card or null}]; cards {ledgerMoveId,time,role,words,score,critique,tags:[]} using exact moveDisplayControls. Preserve every move and per-side order.',overall:'Each side {score:locked overall score,strengths:[3 strings],blunders:[3 {text,links:[]}]}',logicalExtension:'Each side {finalArgument:{thesis,premises:[5 strings],conclusion,notice},newArguments:[3 {title,text}]}.'},instructions:{...controls,critiquesExactlyFourLabeledSentences:true,critiqueRationalesMustBeSourceSpecific:true,thesisMinimumWords:12,premiseMinimumWords:12,conclusionMinimumWords:15,newArgumentAllowedWords:[45,130],newArgumentTargetWords:[90,120],ordinaryPunctuationRequired:true,noGenericBoilerplate:true,quoteTextMustBeSourceExact:true,noveltyMapMustDiscloseEveryAIGeneratedArgument:true,noveltyMapSourceMoveIdsRequired:true,sourceQuoteAuditMustVerifyBothQuotes:true,noManualScoreChanges:true},noveltyMapShape:'8 entries total: each side finalArgument plus newArguments[0], [1], [2]. Each {side,field,path: candidate.logicalExtension.<side>.<field>,sourceMoveIds:[],explanation,hypothetical:true}.',sourceQuoteAuditShape:'2 entries: {side,speaker,moveId,text,sourcePath: final ledger path,transcriptPath,startEvent,endEvent,startMs,endMs,quoteEligibleExactSpan,exactMatch:true}; quote text must be exact substring of that move quoteEligibleExactSpans.'};
 write(`${root}/publication/contract.json`,contract);
 const allowedInputs=[`${root}/publication/packet.json`,`${root}/publication/contract.json`,...Object.values(evidenceLocks).map(r=>r.path),'docs/assessment-production-workflow.md','docs/reassessment-rubric-v2.1.md','docs/assessment-workflow-v4.2.21.17.41.md','scripts/lib/assessment-production-standalone-debate-v1.mjs'];
 write(`${root}/publication/execution-plan.json`,{status:'frozen-before-execution',debateNumber:number,debateId:entry.debateId,model:{slug:'gpt-5.6-sol',label:'5.6 Sol',reasoningEffort:'low',authentication:'ChatGPT subscription'},agentTask:`/root/publication_${number}`,allowedInputs:allowedInputs.map(p=>record(p)),freshContext:true,forkTurns:'none',attemptLimit:1,retries:0,directIncrementalCostUsd:0,writablePaths:[`${root}/publication/raw-prose-output-1.json`]});
 console.log(JSON.stringify({debateNumber:number,moves:counts,sections:publicationLayout.length,meanMinimum:minimum,score:template.score,date:template.date}));
}else throw new Error('Unknown phase');
