import fs from 'node:fs';import assert from 'node:assert/strict';import {createHash} from 'node:crypto';
const n=process.argv[2];assert.match(n,/^\d{3}$/);const read=p=>JSON.parse(fs.readFileSync(p)),r=read('docs/assessment-production/standalone-debates-v1/registry.json').debates.find(x=>x.debateNumber===n);assert(r);const dir=r.root+'/publication/rhetorical-tag-review-1',packet=read(dir+'/packet.json'),adj=read(dir+'/adjudication/output.json');assert.equal(read(dir+'/adjudication/execution.json').status,'passed-authenticated-rhetorical-adjudication');assert.deepEqual(packet.controllerOverrides,[]);
const specifications=[
 {moveId:'value-sensitive-theism-outpredicts-naturalism',type:'fallacy',label:'Argument from ignorance',rationale:'The source proposes an indifference assumption from naturalism’s lack of value-directed purpose, then contrasts purposive theism; it does not establish truth merely from failure to disprove a claim or falsity from failure to prove it. The frozen critique already rejects the underived measures and overstatement about successor theories. Those defects remain, but extending the catalog definition to every insufficiently supported probability assumption would overlabel this inference.'},
 {moveId:'three-creation-options-preserve-theistic-prediction',type:'bias',label:'Scope neglect',rationale:'The source gives three coarse creation categories and an underived one-tenth weight, without an established magnitude or quantity to which judgment demonstrably fails to respond proportionally. The frozen critique identifies missing representation-invariant weighting and possible category incompleteness. The adjudication’s appeal to potentially vast interiors does not establish their probability mass, so the exact quantitative scope-neglect definition is not sufficiently demonstrated.'}
];
const overrides=specifications.map(s=>{const candidateKey=s.moveId+'|'+s.type+'|'+s.label,row=adj.resolutions.find(x=>x.candidateKey===candidateKey),m=packet.moves.find(x=>x.moveId===s.moveId),d=packet.referenceDefinitions.find(x=>x.type===s.type&&x.label===s.label);assert(row&&m&&d);assert.equal(row.decision,'accepted');return {...s,candidateKey,adjudicatedDecision:'accepted',decision:'rejected',definition:d.definition,url:d.url,sourceSpan:{startEvent:m.sourceSpan.startEvent,endEvent:m.sourceSpan.endEvent},frozenCritique:m.publicationCritique};});
const o={schemaVersion:'1.0-controller-exact-definition-check',status:'passed-source-critique-and-exact-definition-review',debateNumber:n,debateId:r.debateId,reviewedCandidateKeys:adj.reviewedCandidateKeys,adjudicationSha256:createHash('sha256').update(fs.readFileSync(dir+'/adjudication/output.json')).digest('hex'),initialControllerOverrides:[],overrides,acceptedRationale:'The source explicitly compares speculative physical possibilities with unfamiliar supernatural agency and makes familiarity the reason for a larger prior penalty. The narrow ambiguity-effect label and corresponding overall link describe that specific move, not the speaker’s psychology or broader worldview. Its frozen critique already identifies the unsupported conversion from familiarity to prior probability.',baselineInterpretation:'All thirty-two moves and four overall blunders were reviewed against all thirteen local definitions by both isolated reviewers, with fresh anonymous decisions on all thirty-five union candidates. The low retained tag count reflects the qualified, technically substantive exchange and exact-definition exclusions, not a target rate or exemption: missing measures, disputed analogies, and unsupported magnitudes remain fully criticized even when no catalog label fits. No side balance was imposed.',audit:{fullUnionReviewed:true,sourceAndCritiqueReviewed:true,rawAdjudicationPreserved:true,rejectOnlyCorrections:overrides.length,judgmentChanges:0,scoreChanges:0,critiqueChanges:0,directIncrementalCostUsd:0}};
fs.writeFileSync(dir+'/controller-definition-check.json',JSON.stringify(o,null,2)+'\n',{flag:'wx'});console.log(JSON.stringify({status:o.status,union:adj.reviewedCandidateKeys.length,rejectOnlyCorrections:overrides.length}));
