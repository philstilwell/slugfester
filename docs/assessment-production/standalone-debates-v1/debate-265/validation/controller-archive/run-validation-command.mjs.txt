import fs from 'node:fs';import assert from 'node:assert/strict';import {spawnSync} from 'node:child_process';import {createHash} from 'node:crypto';
const [n,label,command,...args]=process.argv.slice(2);assert.match(n,/^\d{3}$/);assert.match(label,/^[a-z0-9-]+$/);assert(command);const r=JSON.parse(fs.readFileSync('docs/assessment-production/standalone-debates-v1/registry.json')).debates.find(x=>x.debateNumber===n);assert(r);const dir=r.root+'/validation';fs.mkdirSync(dir,{recursive:true});const log=dir+'/'+label+'.log',record=dir+'/'+label+'.json';assert(!fs.existsSync(log)&&!fs.existsSync(record));const startedAt=new Date().toISOString(),result=spawnSync(command,args,{encoding:'utf8',maxBuffer:64*1024*1024}),output=(result.stdout??'')+(result.stderr??'');fs.writeFileSync(log,output,{flag:'wx'});const evidence={schemaVersion:'1.0-validation-command-evidence',debateNumber:n,debateId:r.debateId,command:[command,...args],startedAt,completedAt:new Date().toISOString(),exitCode:result.status,signal:result.signal,error:result.error?.message??null,status:result.status===0?'passed':'failed',log:{path:log,bytes:Buffer.byteLength(output),sha256:createHash('sha256').update(output).digest('hex')},directIncrementalCostUsd:0};fs.writeFileSync(record,JSON.stringify(evidence,null,2)+'\n',{flag:'wx'});console.log(JSON.stringify(evidence,null,2));console.log(output.slice(-6000));process.exitCode=result.status??1;
