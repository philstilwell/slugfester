import fs from 'node:fs';import assert from 'node:assert/strict';import {createHash} from 'node:crypto';import {execFileSync} from 'node:child_process';
const root='docs/assessment-production/standalone-debates-v1/debate-265';
const read=p=>JSON.parse(fs.readFileSync(p,'utf8'));
const record=p=>{const b=fs.readFileSync(p);return{path:p,sha256:createHash('sha256').update(b).digest('hex'),bytes:b.length};};
const a=read(root+'/authorization.json'),inv=read(root+'/inventory/pre-audio-inventory.json'),source=read(root+'/source/original-audio-provenance.json'),refs=read(root+'/audio/speaker-references.json');
assert.equal(inv.debateId,a.identity.debateId);assert.equal(a.identity.videoId,source.videoId);
const mediaMs=Math.round(source.acquisition.durationSeconds*1000),moves=inv.moves.filter(m=>m.attributionConfidence!=='high');
const ranges=moves.map(m=>({startMs:Math.max(0,m.sourceSpan.startMs-6000),endMs:Math.min(mediaMs,m.sourceSpan.endMs+6000),moveIds:[m.moveId]}));
const merged=[];for(const x of ranges){const p=merged.at(-1);if(p&&x.startMs<=p.endMs+20000){p.endMs=Math.max(p.endMs,x.endMs);p.moveIds.push(...x.moveIds);}else merged.push({...x});}
const clips=[];for(const x of merged){let start=x.startMs;while(start<x.endMs){const end=Math.min(x.endMs,start+180000);clips.push({startMs:start,endMs:end,durationSeconds:(end-start)/1000,moveIds:x.moveIds.filter(id=>{const m=moves.find(m=>m.moveId===id);return m.sourceSpan.startMs<end&&m.sourceSpan.endMs>start;})});if(end===x.endMs)break;start=end-2000;}}
const dir='.assessment-cache/captions/'+a.identity.videoId+'/verification/clips';fs.mkdirSync(dir,{recursive:true});
for(const [i,c]of clips.entries()){c.clipId='clip-'+String(i+1).padStart(2,'0');const path=dir+'/'+c.clipId+'.mp3';execFileSync('/opt/homebrew/bin/ffmpeg',['-nostdin','-n','-hide_banner','-loglevel','error','-ss',String(c.startMs/1000),'-i',source.files.audio.path,'-t',String(c.durationSeconds),'-vn','-ac','1','-ar','16000','-b:a','64k',path]);Object.assign(c,record(path));}
const sec=clips.reduce((s,c)=>s+c.durationSeconds,0),referenceSeconds=24*clips.length;
const fullMediaEstimate=source.acquisition.durationSeconds/60*.006;
const conservativeEstimate=(sec+referenceSeconds)/60*.025;
assert.ok(conservativeEstimate<1);assert.ok(clips.length*.08<=1);
const plan={schemaVersion:'1.0-frozen-bounded-audio-verification-plan',status:'frozen-before-paid-verification',createdAt:new Date().toISOString(),debateNumber:a.identity.debateNumber,debateId:a.identity.debateId,videoId:a.identity.videoId,inventoryCandidate:record(root+'/inventory/pre-audio-inventory.json'),sourceMedia:source.files.audio,sourceOriginalAudio:true,sourceLanguage:'en',purpose:'Resolve every selected below-high wording or speaker boundary before primary judging; retain unchanged original captions and separately authenticate audio-derived confirmations. Context padding is not additional scoring evidence.',selectedMoveIds:moves.map(m=>m.moveId),model:'gpt-4o-transcribe-diarize',responseFormat:'diarized_json',chunkingStrategy:'auto',prompt:null,knownSpeakerReferences:refs.knownSpeakerReferences,clips,pricing:{verifiedDate:'2026-09-23',modelSource:'https://developers.openai.com/api/docs/models/gpt-4o-transcribe-diarize',estimateSource:'https://developers.openai.com/api/docs/pricing',inputUsdPerMillionTokens:2.5,outputUsdPerMillionTokens:10,ordinaryTranscriptionEstimatedUsdPerMinute:.006,fullMediaTranscriptionEstimateUsd:fullMediaEstimate,fullMediaTranscriptionPlanned:false,verificationAudioSeconds:sec,referenceAudioSeconds:referenceSeconds,ordinaryDurationEstimateUsd:(sec+referenceSeconds)/60*.006,conservativePlanningRateUsdPerMinute:.025,conservativeRateReason:'Diarized segment output and repeated speaker-reference processing can be substantially more expensive than the ordinary-transcription duration estimate; use a larger planning allowance and reconcile each response token usage.',estimatedCompleteDebatePaidWorkUsd:conservativeEstimate,knownCostBeforeCallsUsd:0,uncertainAttemptReserveUsd:.08,maximumPlannedUncertainExposureUsd:clips.length*.08,standingCapUsd:1,billingConfirmed:false},attemptsPerClip:1,retries:0,maximumClipSeconds:180,clipSplitOverlapSeconds:2,timeoutSeconds:180,executionOutputRoot:root+'/audio/responses',transcribeSkill:'/Users/philstilwell/.codex/skills/transcribe/scripts/transcribe_diarize.py',directListeningPerformed:false};
fs.writeFileSync(root+'/audio/plan.json',JSON.stringify(plan,null,2)+'\n',{flag:'wx'});
console.log(JSON.stringify({clips:clips.length,checks:moves.length,seconds:sec,referenceSeconds,ordinaryFullMediaEstimate:fullMediaEstimate,completeDebateWorkEstimate:conservativeEstimate,maximumPlannedUncertainExposure:clips.length*.08},null,2));
