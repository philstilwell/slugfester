import fs from 'node:fs';import assert from 'node:assert/strict';import {createHash} from 'node:crypto';import {checkRhetorical} from './check-rhetorical-draft.mjs';
const [n,...flags]=process.argv.slice(2);assert.match(n,/^\d{3}$/);const read=p=>JSON.parse(fs.readFileSync(p)),hash=b=>createHash('sha256').update(b).digest('hex'),rec=p=>{const b=fs.readFileSync(p);return{path:p,sha256:hash(b),bytes:b.length};},write=(p,o)=>fs.writeFileSync(p,JSON.stringify(o,null,2)+'\n',{flag:'wx'}),r=read('docs/assessment-production/standalone-debates-v1/registry.json').debates.find(x=>x.debateNumber===n);assert(r);const dir=r.root+'/publication/rhetorical-tag-review-1/adjudication',release=read(dir+'/reading-release.json'),p=read(dir+'/input.json'),plan=read(dir+'/execution-plan.json');assert(!fs.existsSync(plan.outputPath));
const rows=fs.readFileSync(release.session.path,'utf8').trim().split('\n').map(JSON.parse),parts=[];
for(const row of rows)for(const b of row.payload?.type==='custom_tool_call_output'?row.payload.output:[])try{const x=JSON.parse(b.text);if(x.draftPart==='rhetorical-adjudication')parts.push({ordinal:row.ordinal,callId:row.payload.call_id,timestamp:row.timestamp,sha256:hash(b.text),literal:x});}catch{}
if(!parts.length){console.log('Draft not yet emitted');process.exit(0);}for(const part of parts.slice(1))assert.equal(part.literal.supersedesPrevious,true);
const resolutions=parts.at(-1).literal.resolutions;assert(Array.isArray(resolutions));const c=p.outputContract,output={schemaVersion:c.schemaVersion,status:c.status,debateNumber:n,debateId:r.debateId,reviewedCandidateKeys:p.candidates.map(x=>x.candidateKey),resolutions,audit:c.audit},validation=checkRhetorical(output,p);
if(flags.includes('--freeze')){write(dir+'/literal-draft-components.json',{schemaVersion:'1.0-literal-unsaved-rhetorical-components',debateNumber:n,debateId:r.debateId,session:rec(release.session.path),parts});write(plan.outputPath,output);write(dir+'/mechanical-first-submission.json',{schemaVersion:'1.0-mechanical-rhetorical-first-submission',status:'single-first-submission-compiled-from-authenticated-adjudicator-literals',debateNumber:n,debateId:r.debateId,readingRelease:rec(dir+'/reading-release.json'),draftComponents:rec(dir+'/literal-draft-components.json'),output:rec(plan.outputPath),compiler:rec('.assessment-cache/compile-rhetorical-adjudication.mjs'),validation,controllerAuthoredJudgments:false,submittedOutputs:1,preWriteSelfChecksAreNotReplacementAttempts:true,judgmentChanges:0,scoreChanges:0,directIncrementalCostUsd:0});}
console.log(JSON.stringify({status:flags.includes('--freeze')?'first-submission-frozen':'unsaved-draft-validated',validation},null,2));
