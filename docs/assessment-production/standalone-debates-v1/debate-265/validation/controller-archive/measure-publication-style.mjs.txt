import fs from 'node:fs';import assert from 'node:assert/strict';import {debates} from '../src/data/debates.js';
const n=process.argv[2];assert.match(n,/^\d{3}$/);const registry=JSON.parse(fs.readFileSync('docs/assessment-production/standalone-debates-v1/registry.json')),r=registry.debates.find(r=>r.debateNumber===n);assert(r);const c=JSON.parse(fs.readFileSync(r.root+'/publication/output.json')).candidate;
const strings=x=>typeof x==='string'?[x]:Array.isArray(x)?x.flatMap(strings):x&&typeof x==='object'?Object.values(x).flatMap(strings):[],wc=s=>s.trim().split(/\s+/).filter(Boolean).length,tokens=s=>s.toLowerCase().replace(/[^a-z0-9/]+/g,' ').trim().split(/\s+/),grams=s=>{const t=tokens(s),out=[];for(let i=0;i<=t.length-6;i++)out.push(t.slice(i,i+6).join(' '));return out;};
const all=[...debates.filter(d=>d.number!==n),c],profiles=all.map(d=>{const text=strings(d.logicalExtension).join(' '),words=wc(text),commas=(text.match(/,/g)||[]).length;return{debateNumber:d.number,words,commas,commaDensity:commas/words};}).filter(p=>p.words>=300),densities=profiles.map(p=>p.commaDensity).sort((a,b)=>a-b),median=densities[Math.floor(densities.length/2)],threshold=median/4;
const excluded=new Set(registry.debates.map(r=>r.debateNumber)),independent=debates.filter(d=>+d.number<+n&&!excluded.has(d.number)).slice(-25),recent=debates.filter(d=>+d.number<+n).slice(-25),targetGrams=new Set(grams(strings(c.logicalExtension).join(' ')));
const compare=rows=>rows.map(d=>({debateNumber:d.number,sharedSixWordSequences:[...new Set(grams(strings(d.logicalExtension).join(' ')))].filter(g=>targetGrams.has(g))})).filter(d=>d.sharedSixWordSequences.length);
const openings=['pro','con'].flatMap(side=>{const e=c.logicalExtension[side];return[{side,field:'thesis',opening:e.finalArgument.thesis.split(/\s+/).slice(0,12).join(' ')},{side,field:'conclusion',opening:e.finalArgument.conclusion.split(/\s+/).slice(0,12).join(' ')},...e.newArguments.map((x,i)=>({side,field:'newArguments['+i+']',title:x.title,opening:x.text.split(/\s+/).slice(0,12).join(' '),words:wc(x.text)}))];});
console.log(JSON.stringify({schemaVersion:'1.0-publication-style-measurements',status:'measurements-complete-direct-editorial-review-required',debateNumber:n,debateId:r.debateId,punctuation:{debatesReviewed:profiles.length,corpusMedianDensity:median,severeDriftThreshold:threshold,target:profiles.find(p=>p.debateNumber===n),flaggedDebates:profiles.filter(p=>p.commaDensity<threshold).map(p=>p.debateNumber)},independentWindow:independent.map(d=>d.number),recentWindow:recent.map(d=>d.number),independentSixWordOverlaps:compare(independent),recentSixWordOverlaps:compare(recent),openings,introductoryProvenancePhrases:(strings(c.logicalExtension).join(' ').match(/AI-generated extension|AI-developed extension|possible extension|AI reconstruction/gi)||[]).length},null,2));
