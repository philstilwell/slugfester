import fs from 'node:fs';import assert from 'node:assert/strict';import {createHash} from 'node:crypto';
const n=process.argv[2];assert.match(n,/^\d{3}$/);const read=p=>JSON.parse(fs.readFileSync(p));
const r=read('docs/assessment-production/standalone-debates-v1/registry.json').debates.find(x=>x.debateNumber===n);assert.ok(r);const root=r.root;
const rec=p=>{const b=fs.readFileSync(p);return{path:p,sha256:createHash('sha256').update(b).digest('hex'),bytes:b.length};};
const a=read(root+'/authorization.json'),packet=read(root+'/adjudication/packet.json'),audio=read(root+'/audio/audio-verification.json');
assert.equal(packet.debateId,r.debateId);assert.equal(packet.status,'frozen-anonymous-dispute-only-packet');assert.equal(audio.status,'complete');assert.equal(audio.audit.unresolvedAttributionChecks,0);
assert.equal(fs.existsSync(root+'/score-pass/output.json'),false,'Adjudication must remain score-blind');
const inputs=[rec(root+'/adjudication/packet.json'),rec(root+'/inventory/inventory.json'),rec('.assessment-cache/captions/'+r.videoId+'/indexed-transcript.txt'),rec(root+'/audio/score-blind-supplement.json'),rec('docs/assessment-production-workflow.md'),rec('docs/reassessment-rubric-v2.1.md'),{...rec('scripts/lib/assessment-production-standalone-debate-v1.mjs'),allowedLines:[{start:903,end:946}]}];
const p={schemaVersion:'1.0-frozen-dispute-only-execution-plan',status:'frozen-before-model-execution',debateNumber:n,debateId:r.debateId,model:{slug:a.execution.assessmentModel,displayLabel:a.execution.recordedDisplayModel,reasoningEffort:a.execution.reasoningEffort,authentication:a.execution.authentication},allowedInputs:inputs,disputeCount:packet.disputes.length,outputPath:root+'/adjudication/output.json',readingAuditPath:root+'/adjudication/reading-audit.json',attempts:1,retries:0,forkTurns:'none',directIncrementalCostUsd:0,requirements:[
 'Fresh anonymous adjudicator. Read every declared input literally and completely (validator only allowed lines). Transcript chunks at most380lines; inventory/packet bounded untruncated chunks. Send reading checkpoint and wait for authenticated release before decisions.',
 'Only anonymous dispute options are visible. Do not access either primary output or pass identity, other debates, calculated totals, winner labels, publication, biographies, external research, credentials, paidAPI or subagents. Use env -u OPENAI_API_KEY -u ANTHROPIC_API_KEY in shell commands. Do not call any calculator.',
 'Resolve exactly every packet dispute once in packet order. Choose only option-a or option-b for the disputed field; never invent a new rating or change source, inventory, weights or links. Supply independently reasoned source-specific rationale at least40characters for each choice. No evaluation outside the listed disputes.',
 'Respect the frozen audio supplement and full live replies. Never penalize caption corruption; no direct listening claimed. Distinguish confirmation from posterior probability, and a skeptical challenge from a positive naturalistic burden according to the locked motion.',
 'Use validator schema1.0-standalone-dispute-adjudication; protocol assessment-production-standalone-debate-v1; complete-and-schema-valid; reviewerRole isolated-dispute-only-adjudicator; assessmentModel5.6 Sol; reasoningEffort low; exact debateNumber/debateId. Required isolation booleans from validator. resolutions entries contain disputeId, selectedOption, rationale.',
 'Single first submission, no automatic retry. Self-check literal draft before first output write. Use apply_patch and only declared output/reading-audit paths; do not overwrite submitted output. No arbitrary deadline.'
 ]};
fs.writeFileSync(root+'/adjudication/execution-plan.json',JSON.stringify(p,null,2)+'\n',{flag:'wx'});console.log(JSON.stringify({debateNumber:n,disputes:p.disputeCount,plan:rec(root+'/adjudication/execution-plan.json')},null,2));
