import fs from 'node:fs';import assert from 'node:assert/strict';import {createHash} from 'node:crypto';
const [n,mode]=process.argv.slice(2);assert.match(n,/^\d{3}$/);assert(['components','register'].includes(mode));const read=p=>JSON.parse(fs.readFileSync(p)),registryPath='docs/assessment-production/standalone-debates-v1/registry.json',registry=read(registryPath),r=registry.debates.find(r=>r.debateNumber===n);assert(r);const root=r.root,rec=p=>{const b=fs.readFileSync(p);return{path:p,sha256:createHash('sha256').update(b).digest('hex'),bytes:b.length};},write=(p,o)=>fs.writeFileSync(p,JSON.stringify(o,null,2)+'\n',{flag:'wx'});
if(mode==='components'){
 const c=read(root+'/publication/output.json').candidate,s=read(root+'/score-pass/output.json'),audio=read(root+'/audio/audio-verification.json'),inv=read(root+'/inventory/inventory.json'),render=read(root+'/rendering/rendering-audit.json'),tags=read(r.rhetoricalTagReview.auditPath),parity=read(root+'/publication/content-parity-audit.json'),style=read(root+'/publication/ai-contribution-editorial-audit.json'),manifest=read('.assessment-cache/captions/'+r.videoId+'/manifest.json');
 assert.equal(render.status,'passed-standalone-production-rendering');assert.equal(render.audit.runtimeFailures,0);assert.equal(render.audit.horizontalOverflowFailures,0);assert.equal(render.audit.temporaryBrowsersRemaining,0);assert.equal(render.audit.temporaryServersRemaining,0);assert.equal(parity.status,'passed-content-parity-audit');assert.equal(style.status,'passed-direct-nonformulaic-prose-and-punctuation-review');assert.equal(tags.status,'passed-rhetorical-tag-review');
 const events=read('.assessment-cache/captions/'+r.videoId+'/events.json');
 write(root+'/validation-summary.json',{schemaVersion:'1.0-standalone-validation-summary',protocolId:'assessment-production-standalone-debate-v1',status:'passed',debateNumber:n,debateId:r.debateId,source:{videoId:r.videoId,timedEvents:events.length,spokenWords:events.reduce((a,e)=>a+e.text.trim().split(/\s+/).filter(Boolean).length,0),method:'Complete English automatic captions and twelve bounded audio-derived verification transcriptions for thirteen triggered moves',audioListenedTo:false,fullDebateAudioVerified:false,primarySpeakerExceptionUsed:false,requiredAttributionChecks:audio.audit.requiredAttributionChecks,completedAttributionChecks:audio.audit.completedAttributionChecks,unresolvedMaterialMeaningChecks:audio.audit.unresolvedAttributionChecks},assessment:{inventoryMoves:inv.moves.length,inventorySides:Object.fromEntries(['pro','con'].map(side=>[side,inv.moves.filter(m=>m.side===side).length])),acceptedPrimaryJudgmentPasses:2,disputes:read(root+'/disagreements/disagreements.json').disputes.length,scorePasses:1,scoreReruns:0,manualScoreOverrides:0,score:{pro:s.overall.pro.score,con:s.overall.con.score,winner:s.winner}},publication:{sections:c.sections.length,displayRows:parity.metrics.rows,argumentCards:parity.metrics.moves,descriptionMeanWords:parity.metrics.argumentWordsMean,critiqueMeanWords:parity.metrics.critiqueWordsMean,contentParityAuditPassed:true,acceptedCardTags:tags.acceptedTags.length,acceptedOverallBlunderLinks:tags.acceptedOverallBlunderLinks.length,anonymousCandidates:tags.completeCandidateUnion.length,rejectedCandidates:tags.completeCandidateUnion.filter(x=>x.decision==='rejected').length,controllerDefinitionCorrections:tags.controllerDefinitionCorrections?.length??0,nonformulaicAIProsePassed:true,punctuationIntegrityPassed:true},recoveries:['The original iCloud-backed checkout was left untouched; an isolated complete clone of the same repository carries this debate.', 'Initial source metadata and one audio-verified attribution boundary were corrected before inventory and judgment freeze, with prior bytes retained.', 'The same isolated publication writer completed unsaved literal draft components; one mechanically compiled first submission preserves all prior components and passed full contract checks before submission.', 'Truncated reading blocks received no authentication credit; complete literal rereads occurred before the affected model outputs were released.'],rendering:render.audit,directCostUsd:audio.cost.knownSuccessfulCallCostUsd,maximumPossiblePaidCostUsd:audio.cost.maximumPossibleTotalCostUsd,paidRetries:audio.cost.retries,gateScope:'Passed source, assessment, publication, rhetorical review, content parity, and rendering component gates. Full repository, final precommit, clean-checkout, PR, and deployment checks remain mandatory and are recorded separately. Registry status denotes a frozen production entry prepared for publication, not an assertion that its PR is already merged.'});
 r.contentParity={auditPath:root+'/publication/content-parity-audit.json'};
}else{
 assert.equal(read(root+'/validation-summary.json').status,'passed');const ledger=read(r.productionLedger.path);assert.equal(ledger.debateId,r.debateId);assert.equal(ledger.status,'frozen-standalone-site-ledger-adapter');r.productionLedger=rec(r.productionLedger.path);r.status='published-and-frozen';
}
fs.writeFileSync(registryPath,JSON.stringify(registry,null,2)+'\n');console.log(JSON.stringify({status:mode+'-complete',debateNumber:n}));
