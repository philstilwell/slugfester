import fs from 'node:fs';import assert from 'node:assert/strict';import {spawnSync} from 'node:child_process';import {createHash} from 'node:crypto';
const n=process.argv[2];assert.match(n,/^\d{3}$/);const read=p=>JSON.parse(fs.readFileSync(p)),r=read('docs/assessment-production/standalone-debates-v1/registry.json').debates.find(r=>r.debateNumber===n);assert(r);const dir=r.root+'/publication',rec=p=>{const b=fs.readFileSync(p);return{path:p,sha256:createHash('sha256').update(b).digest('hex'),bytes:b.length};},write=(p,o)=>fs.writeFileSync(p,JSON.stringify(o,null,2)+'\n',{flag:'wx'});
const run=args=>{const x=spawnSync(process.execPath,args,{encoding:'utf8'});assert.equal(x.status,0,x.stderr);return JSON.parse(x.stdout);};
const parity=run(['scripts/audit-assessment-production-standalone-content-parity.mjs','--debate',n,'--candidate',dir+'/output.json','--print']);
const rationales={
 motionWords:'The exact authorized question already specifies cosmic fine-tuning, strong evidence, theism, and naturalism; adding words would alter the frozen question without clarifying its burden.',
 rows:'Sixteen paired rows preserve thirty-two independently scored moves across likelihoods, theory replacement, entropy, purposes, embodiment, and final physics; the balanced fourth row was justified before judgment.',
 moves:'The long exchange contains distinct probability, entropy, purpose, embodiment, multiverse, and theory-replacement arguments and replies; all thirty-two source-locked moves remain represented exactly once.',
 strengthWordsMean:'Each strength names a concrete comparative achievement and its limitation or consequence, including entropy disconfirmation and parameter measures; the extra words supply specificity rather than generic praise.',
 extensionThesisWordsMean:'Each compact thesis states its direction and governing condition: robust value-directed prediction for the affirmative, justified measures on both worldviews for the skeptic; the following premises develop those conditions separately.',
 extensionPremiseWordsMean:'The ten premises divide measure choice, purpose convergence, successor weighting, and matched granularity into distinct inferential steps; each states a complete condition without repeating the richer reinforcing arguments.',
 extensionConclusionWordsMean:'The conclusions explicitly distinguish substantial comparative evidence from an unresolved likelihood, carrying the stated conditional limitations forward without repeating all five premises or claiming proof of either worldview.'
};
for(const o of parity.relativeOutliers){assert(rationales[o.metric],'Unreviewed outlier '+o.metric);o.rationale=rationales[o.metric];}
write(dir+'/content-parity-audit.json',parity);
const style=run(['.assessment-cache/measure-publication-style.mjs',n]);assert.equal(style.punctuation.flaggedDebates.length,0);assert.equal(style.introductoryProvenancePhrases,0);assert.equal(style.independentSixWordOverlaps.length,0);assert.equal(style.recentSixWordOverlaps.length,0);
write(dir+'/ai-contribution-editorial-audit.json',{...style,status:'passed-direct-nonformulaic-prose-and-punctuation-review',publication:rec(dir+'/output.json'),directReview:{bothSidesReadTogether:true,allThesesPremisesConclusionsAndReinforcingArgumentsRead:true,openingAndParagraphArchitectureReviewed:true,swappableScaffoldsFound:false,sourceSpecificNoveltyMapReviewed:true,punctuationIntact:true,notes:[
 'The affirmative develops robustness across measures, convergent reasons for valuable worlds, and graded successor-theory weighting; the skeptic develops representation invariance, predictive specificity, and comparison of complete packages. These are distinct responses to the frozen debate, not interchangeable prompts for further research.',
 'The conditional first argument, value-convergence argument, and theory-weighting argument have different inferential structures. Skeptical passages separately diagnose redescription, retrospective flexibility, and mismatched comparison levels. Shared probability vocabulary and genuine logical parallelism are not treated as boilerplate.',
 'Pro premise one is explicitly conditional rather than a claim that parameter robustness has already been established. The new material remains a disclosed hypothetical contribution, not evidence attributed to either speaker or input to scoring.',
 'Lists, subordinate clauses, contrasts, semicolons, and colon punctuation were read directly. The complete corpus has no severe punctuation-drift flag; no punctuation was added to meet a quota.',
 'All thirty-two card descriptions include the actual claim and inferential purpose. Critiques distinguish limited objections from stronger positive burdens, preserve frozen scores, and contain no shared six-word boilerplate at the current threshold.'
 ]},audit:{judgmentChanges:0,scoreChanges:0,moveChanges:0,tagsChanged:0,directIncrementalCostUsd:0}});
console.log(JSON.stringify({status:'editorial-and-parity-audits-frozen',metrics:parity.metrics,relativeOutliers:parity.relativeOutliers.map(o=>o.metric),styleFlags:style.punctuation.flaggedDebates.length},null,2));
