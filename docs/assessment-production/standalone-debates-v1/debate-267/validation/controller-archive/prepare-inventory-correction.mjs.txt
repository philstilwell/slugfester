import fs from 'node:fs';
import assert from 'node:assert/strict';
import {fileRecord} from '../scripts/lib/assessment-production-standalone-debate-v1.mjs';
const args=process.argv.slice(2),n=args[args.indexOf('--debate')+1];assert(args.includes('--debate'));
const read=p=>JSON.parse(fs.readFileSync(p)),r=read('docs/assessment-production/standalone-debates-v1/registry.json').debates.find(x=>x.debateNumber===n);assert(r);
const root=r.root,dir=root+'/inventory/correction-1',failure=read(root+'/inventory/first-draft-failure.json'),draft=read(failure.draft.path),auth=read(root+'/authorization.json');
assert.equal(fileRecord(failure.draft.path).sha256,failure.draft.sha256);assert(!fs.existsSync(root+'/inventory/inventory.json'));assert(!fs.existsSync(root+'/score-pass/output.json'));
fs.mkdirSync(dir,{recursive:true});const write=(p,v)=>fs.writeFileSync(p,JSON.stringify(v,null,2)+'\n',{flag:'wx'});
const groups=[
 ['hitchens-victim-perspective-and-present-danger',['endEvent']],
 ['hitchens-rival-miracles-discrimination-problem',['quoteEligibleExactSpans.0']],
 ['hitchens-hume-comparative-probability',['endEvent','attributionConfidence','attributionRationale','audioVerificationReason']],
 ['wilson-ancients-knew-ordinary-natural-course',['quoteEligibleExactSpans.1']],
 ['hitchens-virgin-birth-is-recurring-religious-pattern',['quoteEligibleExactSpans.1','sectionId','burdenContact.bridgeId','burdenContact.tier','burdenContact.rationale']],
 ['wilson-christianity-displaced-demonism',['endEvent','attributionConfidence','attributionRationale','audioVerificationReason']],
 ['wilson-naturalism-unmoved-by-amalekites',['attributionConfidence','attributionRationale','audioVerificationReason']]
];
const paths=groups.flatMap(([id,leaves])=>{const i=draft.moves.findIndex(m=>m.moveId===id);assert(i>=0);return leaves.map(leaf=>({path:`moves.${i}.${leaf}`,moveId:id}));});
paths.push({path:'publicationCapacityAudit.sections.3.rationale',moveId:null});
const shards=[];for(let i=0;i<paths.length;i+=2)shards.push({shardId:'shard-'+String(shards.length+1).padStart(2,'0'),writableLeaves:paths.slice(i,i+2),outputPath:dir+'/shard-'+String(shards.length+1).padStart(2,'0')+'.json',attempts:1,retries:0});
assert.equal(draft.sections[3].sectionId,'miracles-testimony-and-rival-claims');
const transcript=read(root+'/manifest.json').sourceLocks.indexedTranscript;
const p={schemaVersion:'1.0-authorized-pre-scoring-inventory-correction',status:'frozen-before-exceptional-correction',createdAt:new Date().toISOString(),debateNumber:n,debateId:r.debateId,userAuthorization:'Yes to one bounded pre-scoring correction attempt, preserving the approved scope and source evidence; the rejected first draft and all source locks remain unchanged.',originalDraft:failure.draft,failure:fileRecord(root+'/inventory/first-draft-failure.json'),model:{slug:auth.execution.assessmentModel,displayLabel:auth.execution.recordedDisplayModel,reasoningEffort:auth.execution.reasoningEffort,authentication:auth.execution.authentication},shards,attemptsPerShard:1,retries:0,directIncrementalCostUsd:0,outputPath:dir+'/completion.json',readingAuditPath:dir+'/reading-audit.json',requirements:[
 'Read this plan, the complete original draft and failure record, the complete scoped indexed transcript, original inventory packet, and declared validator lines literally before drafting. Report a reading checkpoint and wait for authenticated release. Do not read outside these inputs or follow provenance paths into excluded recording content.',
 'One user-authorized bounded correction round, with each declared output shard containing at most two leaf replacements. Each shard is submitted once to its separate path. No second revision, recursive retry, rebuilding inventory, new move, changed claim/warrant/inference/importance/weight/reply target, or scored judgment. Use API-key-free shell commands and apply_patch for output files. No additional agents or paid calls.',
 'Each shard JSON: schemaVersion 1.0-inventory-leaf-correction, debateNumber, debateId, shardId, originalDraftSha256, replacements array in exact writable-leaf order. Each replacement has path, oldValue (exact original leaf), newValue, rationale (source-specific, at least 40 characters). It is permissible to retain oldValue only when source review establishes it already correct. No undeclared replacement.',
 'Correct source endings to include complete assertions: the victim-perspective conclusion ends with toxic in event1121; the Hume qualifier completes at1660; the Christianity/Enlightenment reply completes Christian faith at1236. Retain original timestamps through event indices. Do not expand any other source window. These events are eligible and do not enter the replay.',
 'Three quotations omit caption fillers/repetitions. Restore exact contiguous caption strings for the declared quote leaves, 3–18 words, preserving their meaning. Do not silently clean up captions or change the other quotations. The two other initially invalid quotes become exact when their end events are extended; leave their bytes unchanged.',
 'The Hume and naturalism-unmoved-by-Amalekites passages include substantive opponent speech at a boundary. Mark their attribution medium, explain whose substantive proposition is selected and the boundary concern, and require the already-authorized audio verification. Review the newly completed Christianity passage for its short exchange at the end; medium is appropriate if the full attribution remains uncertain. Do not claim audio has been heard.',
 'Review the recurring-virgin-birth/counterfeit argument with the actual miracle-testimony exchange it answers. Correct its semantic section and burden-contact leaves to the existing source-grounded miracle route if warranted. Preserve its claim, warrant, inference, importance, source endpoints and response links. Explain any retained assignment specifically. A move must not be placed in another section merely to avoid a fourth row.',
 'If the correction makes the miracles section four con versus three pro cards, author the declared capacity rationale explaining why all four distinct con moves are nonmergeable within that coherent section. The controller will independently compute every count, flag, threshold, and fourth-row condition after applying source-authorized leaves. Do not attempt to author numerical audit corrections in the model shards.',
 'Completion JSON is only a receipt: status complete-single-authorized-correction, debateNumber, debateId, shardIds in plan order, sourceReviewComplete true, noScoresAuthored true, originalDraftUnchanged true. Reading audit should list literal complete ranges actually read. No biography research, other debates, scores, publication, Git history, credentials, external sources or previous agent contexts.'
 ],allowedInputs:[fileRecord(failure.draft.path),fileRecord(root+'/inventory/first-draft-failure.json'),transcript,fileRecord(root+'/inventory/packet.json'),{...fileRecord('scripts/lib/assessment-production-standalone-debate-v1.mjs'),allowedLines:[{start:1,end:461}]}]};
write(dir+'/execution-plan.json',p);console.log(JSON.stringify({debateNumber:n,dir,shards:shards.length,leaves:paths.length,plan:fileRecord(dir+'/execution-plan.json')},null,2));
