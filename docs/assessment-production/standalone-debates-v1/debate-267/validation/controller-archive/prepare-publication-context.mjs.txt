import fs from 'node:fs';import assert from 'node:assert/strict';import {createHash} from 'node:crypto';
import {debates} from '../src/data/debates.js';
const args=process.argv.slice(2),n=args[args.indexOf('--debate')+1];assert(args.includes('--debate'));assert.match(n,/^\d{3,}$/);
const read=p=>JSON.parse(fs.readFileSync(p,'utf8')),hash=b=>createHash('sha256').update(b).digest('hex');
const rec=p=>{const b=fs.readFileSync(p);return{path:p,sha256:hash(b),bytes:b.length};};
const registry=read('docs/assessment-production/standalone-debates-v1/registry.json'),r=registry.debates.find(x=>x.debateNumber===n);assert.ok(r);const root=r.root;
const a=read(root+'/authorization.json'),inv=read(root+'/inventory/inventory.json'),ledger=read(root+'/final-ledger/final-ledger.json'),scores=read(root+'/score-pass/output.json'),att=read(root+'/score-pass/attestation.json'),audio=read(root+'/audio/audio-verification.json');
assert.equal(att.ordinal,1);assert.equal(att.maximumPermitted,1);assert.equal(att.output.sha256,rec(root+'/score-pass/output.json').sha256);assert.equal(scores.debateId,r.debateId);assert.equal(inv.motion,a.identity.motion);
const wc=s=>s.trim().split(/\s+/).length,nonTargets=new Set(registry.debates.map(x=>x.debateNumber));
const reference=debates.filter(d=>+d.number<+n&&!nonTargets.has(d.number)).slice(-25);assert.equal(reference.length,25);
const refMeans=reference.map(d=>{const cards=d.sections.flatMap(s=>s.exchanges.flatMap(e=>[e.pro,e.con].filter(Boolean)));return cards.reduce((x,c)=>x+wc(c.words),0)/cards.length;}).sort((a,b)=>a-b);
const refMedian=Number(refMeans[12].toFixed(1)),minMean=Math.max(20,refMedian-1);
const stamp=ms=>{const s=Math.floor(ms/1000);return Math.floor(s/60)+':'+String(s%60).padStart(2,'0');};
const identity=a.identity,byId=new Map(ledger.moves.map(m=>[m.moveId,m]));
const skeleton={number:n,id:r.debateId,date:new Date().toISOString().slice(0,10),title:identity.title,label:identity.label,topicCategory:identity.topicCategory,youtubeUrl:identity.canonicalUrl,duration:'',motion:identity.motion,assessmentModel:a.execution.recordedDisplayModel,assessmentRubric:'Slugfester Reassessment Rubric v2',sourceNote:'',scoringNote:'',sides:Object.fromEntries(['pro','con'].map(side=>[side,{name:identity[side].position,speaker:identity[side].speaker,color:side==='pro'?'teal':'coral'}])),score:{pro:scores.overall.pro.score,con:scores.overall.con.score,winner:scores.winner},summary:'',quotes:{pro:{text:'',context:''},con:{text:'',context:''}},sections:[],overall:{},logicalExtension:{}};
// Duration is derived from source, not inherited from any earlier debate.
const seconds=read(root+'/source/source-lock.json').content.sourceDurationMs/1000;
skeleton.duration=Math.floor(seconds/3600)+' hr '+Math.floor((seconds%3600)/60)+' min';
for(const section of scores.sections){const moves=ledger.moves.filter(m=>m.sectionId===section.sectionId),sides=section.sides;
 const card=m=>({ledgerMoveId:m.moveId,time:stamp(byId.get(m.moveId).sourceSpan.startMs),score:m.score,role:'',words:'',critique:'',tags:[]});
 const exchanges=Array.from({length:Math.max(sides.pro.moves.length,sides.con.moves.length)},(_,i)=>Object.fromEntries(['pro','con'].flatMap(side=>sides[side].moves[i]?[[side,card(sides[side].moves[i])]]:[])));
 skeleton.sections.push({sectionId:section.sectionId,title:section.title,timebox:stamp(Math.min(...moves.map(m=>m.sourceSpan.startMs)))+'–'+stamp(Math.max(...moves.map(m=>m.sourceSpan.endMs))),score:{pro:sides.pro.score,con:sides.con.score},exchanges});
}
for(const side of ['pro','con']){skeleton.overall[side]={score:scores.overall[side].score,strengths:['','',''],blunders:[{text:'',links:[]},{text:'',links:[]}]};skeleton.logicalExtension[side]={finalArgument:{thesis:'',premises:['','','','',''],conclusion:''},newArguments:[{title:'',text:''},{title:'',text:''},{title:'',text:''}]};}
const packet={schemaVersion:'1.0-isolated-publication-input',debateNumber:n,debateId:r.debateId,outputPath:root+'/publication/output.json',readingAuditPath:root+'/publication/reading-audit.json',ledgerPath:root+'/final-ledger/final-ledger.json',scorePath:root+'/score-pass/output.json',transcriptPath:read(root+'/manifest.json').sourceLocks.indexedTranscript.path,requiredReaderDisclosure:read(root+'/source/reader-scope-disclosure.json').requiredReaderDisclosure,audioSupplementPath:root+'/audio/score-blind-supplement.json',lockedCandidateSkeleton:skeleton,argumentMinimumMean:minMean,referenceDepth:{referenceNumbers:reference.map(d=>d.number),medianArgumentMean:refMedian,targetCardWords:[22,28]},sourceDisposition:{completeAutomaticEnglishCaptions:true,verifiedAudioClips:audio.cost.successfulCalls,verifiedMoves:audio.checks.length,verificationMethod:'Independent audio-derived transcription with known-speaker references and controller comparison, not direct listening; complete video was not retranscribed.',sourceCaptionBytesUnchanged:true,sourceQuotePolicy:'Use exact quoteEligibleExactSpans only; do not correct caption wording inside direct quotation.',scope:'Only the explicitly approved openings and direct exchange; the physical upload replay is counted once. The introduction, audience Q&A and later closings are excluded.',editorApprovedScope:inv.editorApprovedScope,assessedWindow:a.identity.editorApprovedScope.assessedWindow,scopeVerificationAudioClips:read(root+'/source/audio-source-verification.json').boundaryCalls.length},outputRequirements:{schemaVersion:'1.0-standalone-publication-output',protocolId:'assessment-production-standalone-debate-v1',status:'complete-publication-prose-candidate',debateNumber:n,debateId:r.debateId,audit:{model:a.execution.assessmentModel,reasoningEffort:a.execution.reasoningEffort,directIncrementalCostUsd:0,cardCount:ledger.moves.length,sectionCount:ledger.sections.length,tagsDeferred:true,blunderLinksDeferred:true,scoresChanged:false,publicationAttempt:1}},additionalOutputFields:{sourceQuoteAudit:'Two entries in pro/con order: side, moveId, speaker, text, quoteEligibleExactSpan, sourcePath=ledgerPath, transcriptPath, exact frozen startEvent/endEvent/startMs/endMs, exactMatch:true.',noveltyMap:'In pro then con order, one finalArgument entry and one per newArguments[0], [1], [2]. Each has side, field, path=candidate.logicalExtension.SIDE.FIELD, hypothetical:true, sourceMoveIds (valid IDs or empty for genuinely new reasoning), explanation of at least12 words.'},requirements:[
 'Read all declared evidence and instructions literally, in untruncated chunks, before writing; report a reading checkpoint and wait for authenticated controller release. Full final ledger includes both primary rationales and resolved dimensions: use them faithfully rather than inferring criticism from numbers alone.',
 'The sourceNote must include requiredReaderDisclosure byte-for-byte, and distinguish exact caption quotations from condensed descriptions. Add the actual additional audio-verification disposition without claiming direct listening. Do not credit, penalize, quote, or infer any excluded branch or audience Q&A. Preserve the exact explanatory motion and critical versus constructive burden distinctions. The scores cover only this disclosed excerpt.',
 'Fill only the empty reader-facing strings in the skeleton. Do not alter identities, exact question, weights, sections, move counts/order/links, ratings, or scores. Tags and blunder links remain empty until fresh rhetorical review. Do not run any calculator.',
 'Produce a complete single first publication draft; no arbitrary deadline. Each description should state claim, reason/comparison, and implication, targeting22–28words. Summary18–28words, never over35.',
 'Each critique must contain105–130words AND at least880characters, exactly four labeled and terminally punctuated sentences: Strongest feature:, Principal limitation:, Live burden:, Locked score:. Target about118–125words with substantive specificity rather than padding; make each sentence specific to this move. The last sentence explains the actual locked score without new penalties. No extra periods from abbreviations. Use ordinary English punctuation.',
 'Self-check all numerical, structural, quote and prose constraints in memory before first output submission. You may send literal draft components in tool output to the controller for mechanical compilation if transport requires it, but do not create undeclared draft files or overwrite submitted output. The checker may inspect frozen scores but never calculate them.',
 'Write three concrete overall strengths and two material limitations per side, each tied to the actual weighted record; do not inflate weaker performance or treat an overall result as worldview truth. Aim roughly25–40words per item where that supports complete reasoning.',
 'The AI Contribution is separate unscored reasoning, disclosed at section level by the site. Write direct substantive thesis/premises/conclusion and three distinct new arguments per side. Thesis≥12words, five premises≥12words each, conclusion≥15words; each new argument45–130words (target70–100) and title2–8words. Do not use provenance launchers such as AI-developed extension or possible extension, or repeated a-model-would scaffolds. Read both sides together for mirrored templates. Do not claim immunity to criticism.',
 'Novelty map must explain how every extension item strengthens or repairs specific live argument(s), and distinguish genuinely new reasoning. Never attribute this added reasoning to the speakers.',
 'Use exact clean source quotations3–18words, target6–14, from eligible spans of the correct side. Quote context12–55words; no malformed ASR. Source note must disclose complete captions plus targeted audio-derived verification honestly and distinguish condensed cards from exact quotations. Scoring note≥18words must explicitly call scores AI-generated estimates of transcript performance, not truth, and state AI Contribution does not affect them.',
 'Review all locked critiques for boilerplate: recurring six-word phrases in at least25% of cards may cover no more than15% on average or25% in any card. Passing counts does not excuse interchangeable criticism or formulaic extensions. Output only declared publication/reading-audit paths using apply_patch; no other debate data, external research, credentials, paid APIs or agents.'
 ]};
const dir=root+'/publication';fs.mkdirSync(dir,{recursive:true});
const out=dir+'/input-bundle.json';fs.writeFileSync(out,JSON.stringify(packet,null,2)+'\n',{flag:'wx'});
const allowed=[rec(out),rec(packet.ledgerPath),rec(packet.scorePath),rec(packet.transcriptPath),rec(packet.audioSupplementPath),rec('docs/assessment-production-workflow.md'),rec('docs/reassessment-rubric-v2.1.md'),rec('.assessment-cache/check-publication-draft.mjs')];
fs.writeFileSync(dir+'/execution-plan.json',JSON.stringify({schemaVersion:'1.0-isolated-publication-execution-plan',status:'frozen-before-model-execution',debateNumber:n,debateId:r.debateId,model:{slug:a.execution.assessmentModel,displayLabel:a.execution.recordedDisplayModel,reasoningEffort:a.execution.reasoningEffort,authentication:a.execution.authentication},allowedInputs:allowed,outputPath:packet.outputPath,readingAuditPath:packet.readingAuditPath,forkTurns:'none',attempts:1,retries:0,directIncrementalCostUsd:0},null,2)+'\n',{flag:'wx'});
console.log(JSON.stringify({debateNumber:n,output:rec(out),inputs:allowed.length,cards:ledger.moves.length,minimumDescriptionMean:minMean},null,2));
