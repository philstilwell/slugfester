import fs from 'node:fs';
import path from 'node:path';
import assert from 'node:assert/strict';
import {execFileSync} from 'node:child_process';
import {debates} from '../src/data/debates.js';
import {fileRecord,STANDALONE_ROOT,STANDALONE_PROTOCOL_ID} from '../scripts/lib/assessment-production-standalone-debate-v1.mjs';
import {validateEditorScopedSource} from '../scripts/lib/assessment-editor-scoped-source.mjs';
const read=p=>JSON.parse(fs.readFileSync(p));
const put=(p,x)=>{assert(!fs.existsSync(p),`Refusing overwrite: ${p}`);fs.mkdirSync(path.dirname(p),{recursive:true});fs.writeFileSync(p,typeof x==='string'?x:JSON.stringify(x,null,2)+'\n',{flag:'wx'});};
const git=(...x)=>execFileSync('git',x,{encoding:'utf8'}).trim();
const config=read(process.argv[2]),number=config.debateNumber,video=config.videoId,root=`${STANDALONE_ROOT}/debate-${number}`,cache=`.assessment-cache/captions/${video}`;
assert.equal(git('branch','--show-current'),`codex/debate-${number}`);
assert.equal(git('status','--porcelain'),'');
assert.equal(git('rev-parse','HEAD'),git('rev-parse','main'));assert.equal(git('rev-parse','HEAD'),git('rev-parse','origin/main'));
const registryPath=`${STANDALONE_ROOT}/registry.json`,registry=read(registryPath),caption=read(`${cache}/manifest.json`),events=read(`${cache}/events.json`);
assert.equal(Number(number),Math.max(...debates.map(d=>Number(d.number)))+1);
assert(!registry.debates.some(d=>d.videoId===video||d.debateId===config.debateId||d.debateNumber===number));
assert(!debates.some(d=>d.id===config.debateId||JSON.stringify(d).includes(video)));
assert.equal(caption.videoId,video);assert.equal(caption.sourceUrl,config.canonicalUrl);assert.equal(caption.title,config.sourceTitle);
for(const [f,h] of [['events.json',caption.normalizedEventsSha256],['transcript.txt',caption.transcriptSha256],['captions.srv3.xml',caption.rawCaptionSha256]])assert.equal(fileRecord(`${cache}/${f}`).sha256,h);
const approval=read(config.sourceScopeAuthorizationPath),wave=read(config.waveformCheckPath),audioPlan=read(config.boundaryPlanPath),at=new Date().toISOString();
assert.equal(approval.videoId,video);assert.equal(wave.videoId,video);assert.equal(wave.checks.length,29);assert(wave.checks.every(c=>c.normalizedCorrelation>.99&&c.offsetSeconds===583.915125));
assert.equal(fileRecord(wave.media.path).sha256,wave.media.sha256);
const calls=audioPlan.clips.map(c=>{const p=`${audioPlan.outputRoot}/${c.id}.result.json`,result=read(p);assert.equal(result.exitCode,0);assert.equal(result.videoId,video);assert(result.knownSuccessfulCallCostUsd>=0);return {...c,result:fileRecord(p),output:result.output,costUsd:result.knownSuccessfulCallCostUsd};});
const paid=calls.reduce((s,c)=>s+c.costUsd,0);
const window={...config.approvedWindow,durationMs:config.approvedWindow.endMs-config.approvedWindow.startMs};
const excluded={intervalId:'verified-upload-replay',...config.replayExclusion,durationMs:config.replayExclusion.endMs-config.replayExclusion.startMs,reason:'The upload replays the already retained miracle and testimony exchange. Twenty-nine waveform comparisons prove a 583.915125-second repeat offset with correlations above 0.99. This is duplicate media, not a new argumentative performance; preserve it as source evidence but do not score it twice.',dependentRepliesReviewed:true,noCreditOrPenalty:true,kind:'duplicate-media-replay'};
const disclosure='This assessment covers only the opening presentations and direct exchange, approximately 11:46–1:13:22, with the final complete reply included. Audience Q&A, the later closing statements, and the introduction are not assessed. A verified replay at approximately 59:26–1:09:10 is counted only once; timestamps refer to the original upload. Scores describe this limited excerpt, not the complete event.';
const scope={protocolId:'editor-approved-moderator-exclusion-v1',scopeType:'explicitly-approved-main-exchange-with-replay-deduplication',explicitDebateSpecificApproval:true,videoId:video,userApproval:`The user answered Yes to: ${approval.approvedProposal}`,originalRecordingIsStrictlyDyadic:false,automaticPrimarySpeakerExceptionClaimed:false,readerDisclosureRequired:true,requiredReaderDisclosure:disclosure,assessedWindow:window,excludedIntervals:[excluded],excludedDurationMs:excluded.durationMs,excludedShareOfAssessedWindow:excluded.durationMs/window.durationMs,boundaryBasis:'Opening diarized audio places Wilson at 11:47.2; the enclosing caption begins 11:46.519. The final substantive response ends at 1:13:20.862; its complete enclosing caption ends 1:13:22.400. No audience question or later closing is included. The protocol identifier is the existing general editor-scope validator; the in-window exclusion here is verified duplicate media, not moderator advocacy.',outsideWindowDisposition:'Complete introduction, audience Q&A, and both later closings are unscored by explicit user approval.',captionTailPolicy:config.readinessCaution};
const identity=Object.fromEntries(Object.entries(config).filter(([k])=>!['sourceScopeAuthorizationPath','boundaryPlanPath','waveformCheckPath','approvedWindow','replayExclusion','readinessCaution'].includes(k)));
identity.dyadic=true;identity.editorApprovedScope=scope;
const model={model:'gpt-5.6-sol',displayLabel:'5.6 Sol',reasoningEffort:'low',authentication:'ChatGPT subscription'};
const authorization={schemaVersion:'1.0-standalone-debate-authorization',protocolId:STANDALONE_PROTOCOL_ID,status:'authorized-and-frozen',authorizedAt:at,userAuthorization:scope.userApproval,repositoryBaseline:{branch:git('branch','--show-current'),head:git('rev-parse','HEAD'),localMain:git('rev-parse','main'),originMain:git('rev-parse','origin/main'),clean:true,isolationReason:'Continue the already-created clean topic branch in the non-iCloud working copy; original source work retained.'},identity,execution:{assessmentModel:model.model,recordedDisplayModel:model.displayLabel,reasoningEffort:model.reasoningEffort,authentication:model.authentication,primaryJudgments:2,primaryContextsFreshAndIsolated:true,adjudicationContextFreshAndIsolated:true,publicationContextFreshAndIsolated:true,oneDeterministicScorePass:true,modelAuthoredTotalsPermitted:false,manualScoreChangesPermitted:false},cost:{publicCaptionAcquisitionCostUsd:0,localAndSubscriptionIncrementalCostUsd:0,paidTranscriptionUsed:true,knownPaidCostEstimateUsd:paid,maximumPossiblePaidCostEstimateUsd:paid,standingPaidTranscriptionMaximumUsd:1,fullWorkflowConservativeUpperEstimateUsd:audioPlan.fullDebateWorkflowConservativeUpperEstimateUsd,fullEstimateRequiredBeforePaidCall:true,otherPaidServicesPermitted:false},repairPolicy:{judgmentSubstanceMutableAfterAcceptance:false,resolvedLedgerMutableAfterScorePass:false,calculatedScoresMutable:false,publicationRepairMaximumWritableFields:2,attemptsPerPublicationRepairPacket:1,recursiveAutomaticRetryPermitted:false},historicalBoundary:{standalonePostCampaignAddition:true,campaignBatch:null,batch18Permitted:false,frozenCampaignEvidenceWritable:false,frozenCalibrationEvidenceWritable:false,frozenClosureEvidenceWritable:false},allowedTrackedPaths:[root,registryPath,`docs/assessment-ledgers/${identity.debateId}.json`,'docs/calibration/v2.1/corpus-transcript-audit.json','src/data/debates.js','src/data/interlocutors.js','src/data/interlocutor-bios.js','generated site output for this addition']};
const caution='English automatic captions contain recognition errors, especially proper names, and overlapping display tails. Do not penalize caption artifacts or silently improve quoted words. The physical replay is not a second move. Score only the approved scoped source. Hitchens adopts naturalism but is not assigned a complete positive theory wherever he only challenges a theistic inference. No excluded Q&A or closing may supply a premise, burden, concession, omission, or score effect.';
const sourceLock={schemaVersion:'1.0-standalone-source-lock',protocolId:STANDALONE_PROTOCOL_ID,status:'complete-and-hash-locked',identity:{canonicalUrl:identity.canonicalUrl,videoId:video,title:caption.title,channel:caption.channel,eventYear:identity.eventYear,dateBasis:'Westminster official media page identifies this event and links its recording dated 2008-10-30. The YouTube upload is 2011-11-30, not the event date.',dateSource:'https://wm.wts.edu/listen/hitchens-wilson-debate-apologetics-in-action---aesthetics-and-the-existence-of-god',speakerIdentityBasis:'Named opponent introduction, full transcript review by controller and isolated source reviewer, plus timestamp-preserving boundary audio transcription.'},acquisition:{method:caption.extractionMethod,language:caption.track.languageCode,trackName:caption.track.name,captionKind:caption.track.kind,retrievedAt:caption.retrievedAt,paidTranscriptionUsed:true},content:{eventCount:events.length,wordCount:caption.wordCount,spokenWordCount:events.reduce((s,e)=>s+e.text.trim().split(/\s+/).length,0),firstStartMs:events[0].startMs,maxEndMs:Math.max(...events.map(e=>e.startMs+e.durationMs)),sourceDurationMs:caption.durationSeconds*1000,eventsChronological:events.every((e,i)=>!i||e.startMs>=events[i-1].startMs),eventsNonempty:events.every(e=>e.text.trim()),timestampCoverageComplete:true,retainedSubstantiveWindowComplete:true,coverageNote:disclosure,captionCaution:caution},participants:{pro:identity.pro.speaker,con:identity.con.speaker,moderators:[identity.moderator],substantiveAdvocatesInOriginal:'Full event contains audience advocacy; complete-event dyadic assessment not claimed.',substantiveAdvocatesInAssessedScope:2,moderatorsAdvancedIndependentArgument:false,dyadicGatePassed:true,originalRecordingStrictlyDyadic:false,dyadicGateBasis:'Explicitly approved openings/direct-exchange excerpt only; no host advocacy within eligible source.',completeTranscriptReviewedByController:true,assessedDebateWindowMs:{start:window.startMs,end:window.endMs},editorApprovedScope:scope},duplicateAudit:{identityDuplicateFound:false,videoIdDuplicateFound:false,checkedProductionDebates:debates.length,checkedRegistryRecords:registry.debates.length,repositoryHead:git('rev-parse','HEAD')},files:{rawCaptions:fileRecord(`${cache}/captions.srv3.xml`),events:fileRecord(`${cache}/events.json`),transcript:fileRecord(`${cache}/transcript.txt`),captionManifest:fileRecord(`${cache}/manifest.json`)}};
const scoped=events.map((e,eventIndex)=>({eventIndex,...e})).filter(e=>e.startMs>=window.startMs&&e.startMs+e.durationMs<=window.endMs&&!(e.startMs<excluded.endMs&&e.startMs+e.durationMs>excluded.startMs));
validateEditorScopedSource({authorization,sourceLock,inventory:{editorApprovedScope:scope,assessedDebateWindowMs:{start:window.startMs,end:window.endMs},moves:[]}});
put(`${root}/authorization.json`,authorization);put(`${root}/source/source-lock.json`,sourceLock);put(`${root}/source/editor-scope.json`,scope);put(`${root}/source/reader-scope-disclosure.json`,{debateNumber:number,debateId:identity.debateId,requiredReaderDisclosure:disclosure});
put(`${root}/source/audio-source-verification.json`,{videoId:video,verifiedAt:at,waveformCheck:wave,boundaryPlan:audioPlan,boundaryCalls:calls,directListeningClaimed:false,knownSuccessfulCallCostUsd:paid,maximumPossiblePaidCostUsd:paid,invoiceVerified:false,partialDownloadDisposition:'The initial public-Android full download timed out; incomplete bytes remain preserved in ignored storage. Existing installed yt-dlp obtained the complete original audio with no paid service or new installation.',sourceReview:fileRecord(`.assessment-cache/source-screen-${video}-report.md`)});
put(`${cache}/scoped-events.json`,scoped);put(`${cache}/scoped-indexed-transcript.txt`,scoped.map(e=>`[${String(e.eventIndex).padStart(4,'0')}] ${e.startMs}-${e.startMs+e.durationMs}ms ${e.text}`).join('\n')+'\n');
const old=read(`${STANDALONE_ROOT}/debate-${Number(number)-1}/manifest.json`).controlLocks;
for(const r of Object.values(old))assert.equal(fileRecord(r.path).sha256,r.sha256);
const controlLocks={...Object.fromEntries(Object.entries(old).map(([k,v])=>[k,fileRecord(v.path)])),editorScopeValidator:fileRecord('scripts/lib/assessment-editor-scoped-source.mjs')};
const manifest={schemaVersion:'1.0-standalone-debate-manifest',protocolId:STANDALONE_PROTOCOL_ID,status:'inputs-frozen-before-judgment',debateNumber:number,debateId:identity.debateId,videoId:video,sourceLocks:{...sourceLock.files,authorization:fileRecord(`${root}/authorization.json`),committedSourceLock:fileRecord(`${root}/source/source-lock.json`),indexedTranscript:fileRecord(`${cache}/scoped-indexed-transcript.txt`),scopedEvents:fileRecord(`${cache}/scoped-events.json`),editorScope:fileRecord(`${root}/source/editor-scope.json`),audioSourceVerification:fileRecord(`${root}/source/audio-source-verification.json`)},controlLocks,modelSettings:model,scoringControls:{dimensionDisagreementThreshold:8,moveScoreDisagreementThreshold:4,burdenAdjustmentDisagreementThreshold:2,oneScorePass:true,scoreProtocolId:'standalone-v1-single-deterministic-score-pass'},inventoryControls:{schemaVersion:'1.2-standalone-score-blind-inventory',uniqueSemanticSectionIdsRequired:true,selectionBalanceAuditRequired:true,publicationCapacityAuditRequired:true,maximumSelectedMovesPerSidePerSection:4,fourthRowPreauthorizationRequired:true,speakerAttributionReviewRequired:true,sourceExcerptTargetMaximumCharacters:2200,longerCompleteArgumentRequiresRationale:true},frozenAt:at};put(`${root}/manifest.json`,manifest);
const requirements=[
`Read the entire packet, all ${scoped.length} scoped indexed transcript lines, and validator lines 1–461 literally in bounded untruncated output before drafting. Report read ranges to the controller and WAIT for release before any draft.`,
'Inventory only. No ratings, totals, critiques, rhetorical tags, winner, outside research, other debates, Git history, credentials, paid calls or other agent contexts. One initial inventory attempt. Use API-key-free shell commands.',
'Freeze exactly the supplied motion and side identities. Wilson argues Christian theism grounds truth, goodness, beauty and rationality; Hitchens proposes secular inquiry/cultural value and challenges theistic inferences. Do not assign the critic an unadopted burden of a complete naturalistic explanation.',
'The full event was screened by the controller; the eligible excerpt is the complete source for this assessment. Do not consult excluded introduction, Q&A, closings or replay. Caption errors are not argument defects. Mark meaningful wording/attribution uncertainty medium or low and explain the audio need, never claim audio was heard.',
'Select source-led 4–7 semantic sections weighted to 100 and 8–48 moves, with at most four per side per section. Three rows are ordinary; every fourth requires explicit independently complete, nonmergeable argument rationale. Do not manufacture balance or drop material content.',
'Each side needs 3–7 burden bridges with exactly one motion bridge, at least one central and one subsidiary. Importance 1–3 and weights must be source-relevance based, never predictions of strength.',
'Use original eventIndex values, not scoped array positions. A selected source passage must consist of contiguous original indices, wholly inside the approved scope and outside the replay. Keep the smallest complete claim-and-reason passage; target 30–90 words and at most 2200 characters unless source-specific necessity is explained. Avoid substantive opponent interjections inside a claimed single-speaker span.',
'Replies link to earlier selected moves; components identify the particular claim answered. Coverage audit must account for every substantive eligible phase, repetition, concession and omission without using unseen excluded content.',
'Provide independent selectionBalanceAudit and publicationCapacityAudit, with exact recomputable side counts, thresholds 3 total/2 section for balance explanation, and all fourth-row rationales. The controller will independently recompute and validate everything before locking.',
'Draft parts only: routes, sections, moves, coverageAudit, speakerAttributionReview, audits and scope objects. Each move includes moveId, side, speaker, sectionId, moveKind, claim, warrant, inference, startEvent, endEvent, quoteEligibleExactSpans (exact 3–18-word strings), attributionConfidence, attributionRationale, audioVerificationReason, importance, importanceRationale, burdenContact (bridgeId,tier,rationale), respondsToIds, responseComponents (targetMoveId,component,rationale). Controller materializes the exact sourceSpan.',
'Use apply_patch for authorized files. Do not write inventory.json; controller assembles after independent source and capacity checks. Do not import other model work or produce scores.'
];
const packet={schemaVersion:'1.0-score-blind-inventory-packet',identity,model,captionCaution:caution,assessedDebateWindowMs:{start:window.startMs,end:window.endMs},editorApprovedScope:scope,requirements,allowedInputs:[fileRecord(`${cache}/scoped-indexed-transcript.txt`),fileRecord(`${cache}/scoped-events.json`)],contractPath:'scripts/lib/assessment-production-standalone-debate-v1.mjs',contractAllowedLines:[{start:1,end:461}],draftPath:`${root}/inventory/draft-parts.json`,readingOutputPath:`${root}/inventory/reading-audit.json`,inventoryShape:{schemaVersion:'1.2-standalone-score-blind-inventory',protocolId:STANDALONE_PROTOCOL_ID,status:'complete-and-frozen',debateNumber:number,debateId:identity.debateId,assessmentModel:model.displayLabel,motion:identity.motion,audit:{calculatedTotalsAbsent:true,completeTranscriptReviewed:true,allSpansSourceExact:true,originalRecordingReviewedByInventoryWorker:false,approvedScopeFullyReviewed:true}}};
put(`${root}/inventory/packet.json`,packet);put(`${root}/inventory/execution-plan.json`,{status:'frozen-before-model',debateNumber:number,debateId:identity.debateId,createdAt:at,model,isolation:'Fresh built-in subagent with fork_turns none; declared-input protocol isolation, not an operating-system sandbox.',attempts:1,retries:0,allowedInputs:[fileRecord(`${root}/inventory/packet.json`),...packet.allowedInputs,{...fileRecord(packet.contractPath),allowedLineRanges:packet.contractAllowedLines}],allowedOutputPaths:[packet.draftPath,packet.readingOutputPath],directIncrementalCostUsd:0});
registry.debates.push({debateNumber:number,debateId:identity.debateId,videoId:video,root,validationProfile:'semantic-balanced-capacity-v2',status:'authorized-source-locked-inventory-pending',editorScopePath:`${root}/source/editor-scope.json`,readerScopeDisclosurePath:`${root}/source/reader-scope-disclosure.json`,productionLedger:{path:`docs/assessment-ledgers/${identity.debateId}.json`}});
fs.writeFileSync(registryPath,JSON.stringify(registry,null,2)+'\n');
console.log(JSON.stringify({debateNumber:number,root,scopedEventCount:scoped.length,window,excluded,paidCostUsd:paid,packet:fileRecord(`${root}/inventory/packet.json`)},null,2));
