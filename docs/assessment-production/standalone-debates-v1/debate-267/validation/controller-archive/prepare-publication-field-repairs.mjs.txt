import fs from 'node:fs';import assert from 'node:assert/strict';import {createHash} from 'node:crypto';
const args=process.argv.slice(2),n=args[args.indexOf('--debate')+1];assert(args.includes('--debate'));assert.match(n,/^\d{3,}$/);const read=p=>JSON.parse(fs.readFileSync(p)),rec=p=>{const b=fs.readFileSync(p);return{path:p,sha256:createHash('sha256').update(b).digest('hex'),bytes:b.length};},put=(p,o)=>fs.writeFileSync(p,JSON.stringify(o,null,2)+'\n',{flag:'wx'});
const r=read('docs/assessment-production/standalone-debates-v1/registry.json').debates.find(x=>x.debateNumber===n);assert(r);const root=r.root,base=root+'/publication/field-repair-1',bundle=read(root+'/publication/input-bundle.json'),ledger=read(root+'/final-ledger/final-ledger.json'),a=read(root+'/authorization.json');
assert(!fs.existsSync(root+'/publication/output.json'));assert.equal(read(root+'/publication/initial-failure/diagnostic.json').status,'failed-with-no-submitted-publication');assert.equal(a.repairPolicy.publicationRepairMaximumWritableFields,2);assert.equal(a.repairPolicy.attemptsPerPublicationRepairPacket,1);
const cards=bundle.lockedCandidateSkeleton.sections.flatMap((s,si)=>s.exchanges.flatMap((e,ei)=>['pro','con'].flatMap(side=>e[side]?[{...e[side],side,path:`candidate.sections[${si}].exchanges[${ei}].${side}`}]:[]))),groups=[];
const field=(path,kind,min,max,more={})=>({path,kind,minimumWords:min,maximumWords:max,...more});
for(let i=0;i<cards.length;i+=8){const cs=cards.slice(i,i+8),fields=cs.flatMap(c=>[field(c.path+'.words','description',20,40,{moveId:c.ledgerMoveId}),field(c.path+'.critique','critique',105,130,{moveId:c.ledgerMoveId,minimumCharacters:880})]).concat(cs.map(c=>field(c.path+'.role','role',1,5,{moveId:c.ledgerMoveId})));groups.push({groupId:'cards-'+(groups.length+1),fields,moveIds:cs.map(c=>c.ledgerMoveId)});}
const general=[field('candidate.summary','summary',18,28),field('candidate.sourceNote','sourceNote',20,300),field('candidate.scoringNote','scoringNote',18,100)];
for(const side of ['pro','con']){general.push(field(`candidate.quotes.${side}.text`,'quote',3,18,{side}),field(`candidate.quotes.${side}.context`,'context',12,55));for(let i=0;i<3;i++)general.push(field(`candidate.overall.${side}.strengths[${i}]`,'overall',20,50));for(let i=0;i<2;i++)general.push(field(`candidate.overall.${side}.blunders[${i}].text`,'overall',20,50));}
groups.push({groupId:'overview',fields:general,moveIds:ledger.moves.map(m=>m.moveId)});
const ai=[];for(const side of ['pro','con']){ai.push(field(`candidate.logicalExtension.${side}.finalArgument.thesis`,'ai',12,65));for(let i=0;i<5;i++)ai.push(field(`candidate.logicalExtension.${side}.finalArgument.premises[${i}]`,'ai',12,60));ai.push(field(`candidate.logicalExtension.${side}.finalArgument.conclusion`,'ai',15,75));for(let i=0;i<3;i++)ai.push(field(`candidate.logicalExtension.${side}.newArguments[${i}].title`,'ai',2,8),field(`candidate.logicalExtension.${side}.newArguments[${i}].text`,'ai',65,130));}
groups.push({groupId:'ai-contribution',fields:ai,moveIds:ledger.moves.map(m=>m.moveId)});
let ordinal=0;const plans=[];fs.mkdirSync(base,{recursive:true});
for(const group of groups){const dir=base+'/'+group.groupId;fs.mkdirSync(dir);const shards=[];for(let i=0;i<group.fields.length;i+=2)shards.push({shardId:String(++ordinal).padStart(3,'0'),maximumWritableLeafFields:2,attempts:1,retries:0,fields:group.fields.slice(i,i+2)});
const selected=new Set(group.moveIds),packet={schemaVersion:'1.0-isolated-publication-field-repair-packet',debateNumber:n,debateId:r.debateId,groupId:group.groupId,shards,identity:{title:a.identity.title,motion:a.identity.motion,pro:a.identity.pro,con:a.identity.con},requiredReaderDisclosure:bundle.requiredReaderDisclosure,evidence:{routes:ledger.routes,sections:ledger.sections,moves:ledger.moves.filter(m=>selected.has(m.moveId)),scores:read(root+'/score-pass/output.json'),cardMapping:cards.filter(c=>selected.has(c.ledgerMoveId)),sourceDisposition:bundle.sourceDisposition},requirements:[
'This is the first and only bounded repair for these unavailable leaf fields after the original publication context produced no submitted output. Each shard has at most two declared leaf fields and one attempt. No field outside this packet may be authored or changed.',
'Read every declared input literally and completely in bounded untruncated chunks. Send reading checkpoint and wait for controller release before authoring. Do not inspect other writing groups, failed draft generators, other debates, credentials, external research or agents. Use API-key-free shell commands.',
'Author all reader-facing strings as complete, natural, specific literal prose. Never synthesize prose by truncating source sentences, concatenating ledger rationales, appending arbitrary words, automated synonym substitution, numeric word-padding, or shared canned scaffolds. Use code only to count, validate, serialize, and map frozen metadata. Do not insert ledger move IDs in prose.',
'Every critique105–130words AND at least880characters, exactly four complete labeled sentences: Strongest feature:, Principal limitation:, Live burden:, Locked score:. Labels count as two words each. Aim120–125words. Explain actual inference, evidence, limitation, remaining burden and fixed score using the locked rationales; no new penalties. Use normal punctuation and substantive detail, not verbosity for its own sake.',
'Description20–40words, aim24–30: state the source claim, its reason or comparison, and intended inference. Role1–5words expresses the locked move kind, not an invented accomplishment. Use qualifications faithfully, including live opposing replies and the audio supplement. No scores, tags, source spans, sections or motion changes.',
'Overview: three actual strengths and two material blunders per side from the weighted locked record. Exact clean caption quotations, ideally6–14words, from permitted spans of that side. SourceNote includes requiredReaderDisclosure exactly and honestly states complete automatic captions plus two scope and five move-verification audio-derived clips, not direct listening. ScoringNote calls scores AI-generated estimates of transcript performance, not truth, and excludes the AI Contribution from scores.',
'AI Contribution: direct philosophical prose under the site-level AI disclosure; no repeated provenance launchers, generic model-would framing, mirrored scaffolds or claims of immunity. Both sides together: thesis, five explicit premises, proportionate conclusion, three genuinely new reinforcing arguments65–130words with2–8word titles. Each new item introduces distinct reasoning, addresses actual weaknesses, and is not attributed to the speakers. Preserve ordinary punctuation.',
'After controller release, self-check the exact literal draft in memory with the declared checker before writing. Write one output bundle containing the listed shards in order, each shard.fields containing exactly path and text, plus a separate reading audit. Use apply_patch only to the two declared output paths. No submitted-output overwrite, retry, extra draft file or calculator. A failed submitted repair stops its field; do not repair it again.'
],outputContract:{status:'complete-first-bounded-field-repair',debateNumber:n,debateId:r.debateId,groupId:group.groupId,shards:'Exact packet order; each shardId and fields:[{path,text}] matching that shard. No extra writable fields.'}};
put(dir+'/packet.json',packet);const model={slug:a.execution.assessmentModel,displayLabel:a.execution.recordedDisplayModel,reasoningEffort:a.execution.reasoningEffort,authentication:a.execution.authentication},allowedInputs=[rec(dir+'/packet.json'),rec(bundle.transcriptPath),rec(bundle.audioSupplementPath),rec('docs/assessment-production-workflow.md'),rec('docs/reassessment-rubric-v2.1.md'),rec('.assessment-cache/check-publication-field-repair.mjs')];const plan={schemaVersion:'1.0-bounded-publication-repair-execution-plan',status:'frozen-before-first-repair-attempt',debateNumber:n,debateId:r.debateId,groupId:group.groupId,model,allowedInputs,outputPath:dir+'/output.json',readingAuditPath:dir+'/reading-audit.json',packetPath:dir+'/packet.json',shards:shards.map(s=>({shardId:s.shardId,fields:s.fields.map(f=>f.path),attempts:1,retries:0})),attemptsPerShard:1,retries:0,forkTurns:'none',directIncrementalCostUsd:0};put(dir+'/execution-plan.json',plan);plans.push(rec(dir+'/execution-plan.json'));}
const paths=groups.flatMap(g=>g.fields.map(f=>f.path));assert.equal(new Set(paths).size,paths.length);put(base+'/plan.json',{schemaVersion:'1.0-frozen-unavailable-publication-field-repair',status:'first-bounded-repair-planned',debateNumber:n,debateId:r.debateId,initialFailure:rec(root+'/publication/initial-failure/diagnostic.json'),frozenSkeletonSource:rec(root+'/publication/input-bundle.json'),authorization:rec(root+'/authorization.json'),groups:plans,shards:ordinal,fields:paths,maximumWritableLeafFieldsPerShard:2,attemptsPerShard:1,recursiveRetry:false,judgmentChanges:0,scoreChanges:0,sourceQuoteAudit:'Controller derives exact ownership and event metadata from the chosen eligible quotation.',noveltyMap:'Controller separately documents the source-specific mapping after directly reading all accepted AI prose; this audit is not reader-facing prose.',directIncrementalCostUsd:0});console.log(JSON.stringify({groups:plans.map(x=>x.path),shards:ordinal,fields:paths.length}));
