# Batch 9 terminal single-field continuation manual

Rewrite only `moveProse.con-contradictions-as-defeasible-costs.critique` from the preserved rejected Debate 183 con shard. The complete rejected shard is an immutable substantive base; every other field is unavailable and immutable.

Preserve the adjudicated meaning and locked score band. Write exactly four ordered labeled sentences: `Strongest feature:`, `Principal limitation:`, `Live burden:`, and `Locked score:`. Target 112–118 words, remain within 105–130 words and at least 880 characters, and end every sentence with terminal punctuation. Count words before returning.

Do not emit or alter any score, identity, source, role, summary, quote, tag, commentary, AI Extension item, move, or unlisted field. Use no outside source, other debate, failed repair output, legacy assessment, ranking, winner comparison, or other model context. Return exactly one schema-conforming JSON object and nothing else.

Only if this field passes validation may the three untouched frozen contexts—Debate 112 con and Debate 17 pro/con—run under their original packets, schemas, score locks, and host-awake guard.
