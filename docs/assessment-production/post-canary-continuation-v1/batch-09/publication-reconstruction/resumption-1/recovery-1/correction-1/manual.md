# Batch 9 final publication-timeout recursive recovery manual

This one-time recovery corrects exactly five diagnosed critique word-count overruns in two complete rejected shard outputs. The rejected output is an immutable substantive base; only the critique fields named in `packet.json` may be rewritten. A packet exposes at most two fields.

Preserve each supplied critique’s adjudicated meaning and locked score band. Write exactly four ordered labeled sentences: `Strongest feature:`, `Principal limitation:`, `Live burden:`, and `Locked score:`. Target 112–118 words, remain within 105–130 words and at least 880 characters, and end every sentence with terminal punctuation. Count the words before returning.

Do not emit or alter scores, identities, sources, roles, summaries, quotes, tags, commentary, AI Extension material, moves, or any unlisted field. Use no outside source, other debate, failed repair output, legacy assessment, ranking, winner comparison, or other model context. Return exactly one schema-conforming JSON object and nothing else.

If all three repairs validate, the five previously unattempted side-shard contexts resume from their already-frozen packets and schemas. They remain governed by the parent recovery manual, score lock, field partition, one-attempt limit, and authenticated host-awake guard.
