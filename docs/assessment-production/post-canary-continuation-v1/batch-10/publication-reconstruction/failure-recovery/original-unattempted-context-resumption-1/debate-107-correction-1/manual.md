# Debate 107 three-field publication correction

Author only the three `novelty.explanation` strings identified in `packet.json`. They explain how three locked AI-extension premises repair or extend the supplied transcript-derived move evidence. The AI-extension item text, novelty classification, and source-move mapping are immutable.

Each explanation must contain 8–35 words, target 12–18 words, state the particular relationship rather than merely repeating the classification, and end with terminal punctuation. Do not add scores, tags, quotations, arguments, premises, or new source mappings.

This is a narrow user-authorized exception to the normal two-field repair-packet limit: all three failures share one local AI-extension explanation constraint and must be returned in the fixed supplied order. The rejected explanations and the rest of the failed publication output are unavailable. Use no outside source, legacy assessment, other debate, ranking, winner, or other model context. Return exactly one schema-conforming JSON object and nothing else.
