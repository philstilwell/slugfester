I authorize creation and use of a Batch 6 standing authorization covering selection and the complete Batch 6 SLUGFESTER reassessment and publication workflow.

Begin by deterministically selecting and freezing exactly the next ten eligible debates under the current promoted post-canary selection policy. Then automatically prepare, validate, freeze, activate, execute, analyze, commit, and push every successful or preserved-failure checkpoint to `main`.

This authorization covers:

1. Score-blind source-packet preparation and discovery.
2. Candidate-census planning and candidate-sharded side selection.
3. Two isolated independent judgments per debate.
4. Deterministic disagreement extraction.
5. Local audio-source preparation, one public-source download attempt per missing source with downloader retries disabled, and local FFmpeg clip creation where required. Do not play or semantically evaluate audio.
6. Audio-verification preparation, cost estimation, and conditionally authorized transcription calls.
7. Dispute-only adjudication and bounded corrections.
8. Final-ledger assembly and exactly one deterministic repository score pass.
9. Score-locked publication reconstruction and bounded field-level repairs.
10. Compilation, finalization, rendering verification, compatibility staging, production publication, generated-derivative correction, and complete repository validation.

Preserve these controls throughout:

- Use 5.6 Sol with low reasoning effort through my ChatGPT subscription for every discovery, selection, judgment, adjudication, correction, and publication context.
- Subscription-backed model execution and local work have a direct incremental cost cap of $0.
- Use ChatGPT-subscription authentication with API keys removed from subscription-backed contexts.
- Keep every model context isolated and score-blind wherever required.
- Never expose legacy scores, winners, assessments, publication prose, tags, or unrelated debate material to score-blind model contexts.
- Hash-lock every context, schema, source, candidate, output path, validator, merge rule, ledger, and production mutation before execution.
- Use the current frozen stage-specific scheduler and concurrency.
- Give every newly frozen model context or deterministic pass exactly one attempt, with no ordinary retries, reruns, timeout extensions, rollback, or manual score adjustment.
- Permit integer-rounded ties under the active v2.2 score-stability policy.
- Models must never calculate numerical scores. Use exactly one repository score pass.
- Each judgment field may be accepted exactly once. Never reuse a rejected partial output as accepted content.
- Preserve every accepted field, source, identity, move decision, score, candidate, ledger, reference, and unrelated production record unless a frozen diagnosis proves that exact field is the cause of a failure.
- Publication repair packets may expose no more than two explicitly diagnosed writable fields each.
- Production publication may replace only the ten frozen Batch 6 debate records and publish their ten validated ledgers.
- Keep `src/data/references.js` and all unrelated production records byte-identical.
- Compatibility changes may add only the authenticated Batch 6 route required by the frozen compatibility plan. Do not otherwise change validator meaning.
- Generated files may be written only from a frozen inventory produced by an isolated generator comparison.
- Commit and push every successful checkpoint and every preserved-failure diagnosis to `main`.

For paid audio verification:

- Before the first call, freeze and report the usage-derived estimate using the current official rate.
- I grant conditional advance approval only if the total frozen estimate is no more than $1.00.
- If that condition passes, proceed automatically using `gpt-4o-transcribe-diarize` through the OpenAI Transcription API.
- Use the frozen same-debate speaker references, sequential execution, one attempt per clip, and no retries.
- Apply returned usage at the frozen official rate.
- Stop remaining paid calls after any request-level failure or usage-derived cap exceedance.
- Do not use any other paid service.

Bounded failure recovery is also authorized:

For each distinct deterministic-validation, transport, timeout, response-schema, rendering, compilation, compatibility, production-publication, or generated-derivative failure, you may automatically:

1. Preserve and diagnose the failure using only frozen records and local repository evidence.
2. Validate, freeze, commit, and push the diagnosis.
3. Prepare and hash-lock one bounded correction.
4. Execute that correction once.
5. If necessary, perform one recursive correction for the same underlying problem.
6. Replay the complete affected cohort, validate it, commit and push, and resume the standing workflow automatically.

Permitted corrections include:

- deterministic execution-harness and authenticated-hash corrections;
- response-schema and transport corrections;
- minimum field-disjoint partitioning of an oversized or timed-out context;
- resumption of only unattempted contexts;
- validation overlays that preserve original evidence byte-identically;
- replacement contexts for fields without an accepted decision;
- publication repairs exposing at most two diagnosed writable fields per packet;
- deterministic rendering, compatibility, calibration-output, and generated-derivative corrections limited to diagnosed files.

Every correction must be separately diagnosed and frozen before execution. A correction is not an ordinary retry. Do not reuse a failed partial model output as accepted content.

Continue automatically while all frozen gates pass. Routine preparation, activation, model execution, deterministic execution, bounded first or recursive correction, validation, merge, cohort replay, analysis, commit, and push require no further approval.

Stop and obtain new approval only before:

- a projected or actual paid-service cost above $1.00;
- a third failure involving the same underlying problem;
- a failed second bounded correction;
- changing an accepted source, identity, judgment, numerical score, reference, candidate field, or production target outside a frozen diagnosis;
- changing validator meaning beyond the frozen Batch 6 compatibility route;
- modifying a debate outside the ten frozen Batch 6 selections;
- an ordinary retry, rerun, timeout extension, rollback, manual score adjustment, or reuse of rejected content;
- production mutation differing from its frozen manifest;
- using a model or paid service not explicitly authorized here;
- selecting Batch 7; or
- any action outside this authorization.

When Batch 6 is complete, stop before Batch 7 selection and provide a concise report covering debates processed, model contexts, disputes, audio calls and actual cost, corrections, scores, publication validation, rendering, production changes, generated files, repository validation, commits, and the next action.