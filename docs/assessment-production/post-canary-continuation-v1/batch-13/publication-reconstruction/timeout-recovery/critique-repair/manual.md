# Batch 13 Debate 87 critique-only recovery

Read every supplied file completely and no other file. Author only the critique strings required by `packet.json` and `schema.json`. Return exactly one schema-conforming JSON object and no commentary.

Each critique must contain exactly four ordered sentences beginning, in order, with `Strongest feature:`, `Principal limitation:`, `Live burden:`, and `Locked score:`. Target 112–118 whitespace-separated words and do not exceed 125 by your own count; the repository accepts 105–130. Each critique must contain at least 880 characters, and every sentence must end with terminal punctuation.

The locked move, findings, score, role, summary words, and tags are supplied only as immutable context. Explain the supplied score band from the locked findings; never recalculate, propose, or change a score. The rejected prior critique strings are unavailable and must not be inferred. Do not change any non-target field, add tags, consult outside material, or use other debates. This is the final authorized recovery level and each critique may be accepted exactly once.
