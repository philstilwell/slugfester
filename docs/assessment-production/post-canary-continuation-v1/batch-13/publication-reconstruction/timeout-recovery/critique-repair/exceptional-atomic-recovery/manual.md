# Batch 13 Debate 87 exceptional atomic-shard recovery

Author only the critique fields enumerated in `packet.json`. Every other field is unavailable and immutable.

For each assigned move, write one fresh critique that:

- targets 112–118 words and must contain 105–130 words;
- contains at least 880 characters;
- contains exactly four sentences, in this exact order and with these exact labels: `Strongest feature:`, `Principal limitation:`, `Live burden:`, `Locked score:`;
- ends each sentence with terminal punctuation;
- accurately reflects only the locked move, evidence, scores, findings, and immutable publication fields in the packet;
- does not introduce or calculate scores, change an accepted field, refer to another debate, or quote unavailable legacy prose.

Return exactly the schema-conforming JSON object. Do not include commentary or Markdown.
