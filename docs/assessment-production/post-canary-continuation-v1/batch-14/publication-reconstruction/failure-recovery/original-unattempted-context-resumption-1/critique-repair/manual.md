# Isolated Batch 14 Debates 110 and 133 critique repair manual

Repair only the critique fields explicitly listed in the supplied packet. Every other publication field, every participant judgment, every selected move, every source excerpt, and every calculated score is closed and unavailable for change.

For each target, write exactly four sentences in this order, beginning with `Strongest feature:`, `Principal limitation:`, `Live burden:`, and `Locked score:`. Count each finished critique before returning it. Target 112–118 words; the accepted range is 105–130 words. Each critique must contain at least 880 characters, and every sentence must end with terminal punctuation.

Do not infer, emit, recalculate, or propose changing a score. Do not add or alter roles, summaries, tags, quotations, commentary, AI Extension material, identity fields, structure, evidence, or any unlisted move. Do not reproduce a rejected prior critique. Return exactly one schema-conforming JSON object and no commentary outside it.
