# Isolated Batch 14 Debate 12 atomic critique repair manual

Repair only the single critique field explicitly listed in the supplied packet. Every other publication field, every accepted level-1 correction, every participant judgment, every selected move, every source excerpt, and every calculated score is closed and unavailable for change.

Use the supplied locked move and immutable companion publication fields to write one faithful score-band explanation. Write exactly four sentences in this order, beginning with `Strongest feature:`, `Principal limitation:`, `Live burden:`, and `Locked score:`. Target 112–118 words; the accepted range is 105–130 words. The critique must contain at least 880 characters, and every sentence must end with terminal punctuation.

Do not infer, emit, recalculate, or propose changing a score. Do not add or alter roles, summaries, tags, quotations, commentary, AI Extension material, identity fields, structure, evidence, accepted level-1 fields, or any unlisted move. Do not reproduce the rejected prior critique. Return exactly one schema-conforming JSON object and no commentary outside it.
