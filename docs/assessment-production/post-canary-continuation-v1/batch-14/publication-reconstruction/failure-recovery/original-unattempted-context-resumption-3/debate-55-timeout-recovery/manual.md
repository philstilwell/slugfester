# Isolated Batch 14 Debate 55 timeout recovery manual

Author only the top-level publication fields explicitly listed in the supplied shard packet. The three shards are field-disjoint and together cover `moveProse`, `summary`, `representativeQuotes`, `overallCommentary`, and `aiExtension`. Identity, structure, selected moves, evidence, participant judgment, and every calculated score are closed and unavailable for change.

For every `moveProse` critique, write exactly four sentences in this order, beginning with `Strongest feature:`, `Principal limitation:`, `Live burden:`, and `Locked score:`. Target 112–118 words; the accepted range is 105–130 words. Each critique must contain at least 880 characters, and every sentence must end with terminal punctuation. Representative quotes must be exact contiguous source text, preferably 6–14 words and always 3–18 words.

The Overall Commentary must give at least three strengths and one weakness per side. Each AI Extension side must contain four to six premises, two to four genuinely new arguments of 45–130 words, complete novelty mappings, globally unique identifiers, the exact disclosure footer, and the correct model byline.

Do not infer, emit, recalculate, or propose changing a score. Do not author any field outside the shard’s writable list, consult another debate or recovery shard, or attribute AI material to a participant. Return exactly one schema-conforming JSON object and no commentary outside it.
