Continue the SLUGFESTER reassessment campaign with Batch 15 in `/Users/philstilwell/Documents/SLUGFESTER.com`.

Batch 14 completed at commit `cbacd97e1366e2a42ff462b3e623bcff80bfa3c3`. Begin by confirming the workspace is clean and local `main` matches `origin/main`.

Use the `reassess-slugfester-debates` skill, the repository’s current frozen workflow and controls, and Batch 14 as the latest precedent. Deterministically select and freeze the next ten eligible, previously unpublished dyadic debates. Do not select Batch 16.

This is standing authorization for the complete Batch 15 workflow: source preparation and bounded recovery; score-blind inventory; two fresh isolated 5.6 Sol/low judgments per debate; disagreement extraction; required audio verification; adjudication; final-ledger assembly; exactly one deterministic score pass; publication reconstruction and bounded field-level repair; compilation; rendering verification; compatibility staging; production publication; generated SEO and dependent-artifact correction; complete repository validation; commits; and pushes to `origin/main`.

All local and ChatGPT-subscription work is authorized at $0 direct incremental cost. Conditional sequential `gpt-4o-transcribe-diarize` verification is authorized up to $1.00 total. Report the frozen estimate before the first paid call, then continue automatically if it remains within the cap. Use one paid attempt per clip and no paid retries.

Continue automatically across successful gates without requesting separate approvals. All routine preparation, activation, execution, deterministic correction, production mutation, validation, commit, and push steps within this workflow are authorized.

Bounded diagnosed recovery is authorized. Preserve every failure, avoid blind repetition, and use the minimum fresh field-disjoint recovery shards. Allow one attempt per shard and at most two ordinary recovery levels. The existing standing authorization for an exceptional third level also applies when a failed multi-field shard atomically discards otherwise potentially valid companion fields: recover only the unavailable fields using the minimum fresh field-disjoint shards, one attempt each, no retries, and no fourth level. Exact deterministic correction of stale SEO, summaries, sitemaps, calibration analyses, tests, control labels, or similar derived files is authorized.

Never rerun the score pass, manually change calculated scores, reuse rejected prose, alter accepted fields, or select Batch 16. A controller or server startup failure that occurs before any rendering viewport is attempted may be diagnosed and corrected without counting as a viewport retry; preserve the failure record.

Fetch before every push, never force-push, and verify local and remote commit equality after every push. Close temporary browser sessions and servers and remove or safely relocate temporary artifacts.

Stop only if:

- the paid-service cap would be exceeded;
- required source identity or evidence cannot be established;
- an allowed model or recovery gate remains invalid after its authorized attempts;
- repository history has diverged and cannot be reconciled safely;
- or another genuinely substantive blocker requires new authority.

On completion, report the selected debates, final scores, paid and direct costs, recovery use, rendering and repository-validation results, production changes, final commit, local/remote equality, and cleanup status. Stop before selecting Batch 16.

