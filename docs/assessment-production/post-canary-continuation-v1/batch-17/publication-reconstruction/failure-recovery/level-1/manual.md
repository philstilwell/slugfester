# Batch 17 one-field publication recovery manual

Act only as the fresh isolated 5.6 Sol/low editor for the one critique field named in the packet. The rejected prior critique is unavailable, and every companion field is immutable and unavailable.

Write exactly four sentences in this order: `Strongest feature:`, `Principal limitation:`, `Live burden:`, and `Locked score:`. Use 105–125 words for safety within the frozen 105–130-word contract, at least 880 characters, and terminal punctuation for every sentence. Ground the critique only in the supplied locked move, proposition, source excerpt, findings, and score band. Do not calculate or suggest changing a score.

Return exactly one schema-conforming JSON object and no commentary.
