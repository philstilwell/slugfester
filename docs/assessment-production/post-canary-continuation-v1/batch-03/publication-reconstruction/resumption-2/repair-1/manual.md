# Debate 157 bounded publication repair

Edit only the two critique fields explicitly listed in `packet.json`. Preserve the supplied adjudicated substance and locked score band. Scores, sources, identities, move decisions, and every unlisted publication field are immutable.

For each corrected critique:

- Use exactly four ordered sentences beginning `Strongest feature:`, `Principal limitation:`, `Live burden:`, and `Locked score:`.
- Target 112–118 words; acceptance is 105–130 words and at least 880 characters.
- End every sentence with terminal punctuation.
- Do not emit scores, tags, other fields, another packet, or another debate.

Return exactly one JSON object conforming to `schema.json`.
