# Slugfester Structural Partition Primary A Execution v4.2.21.13.1

This stage freezes three isolated candidate-grounded Primary A contexts. Each context uses the ChatGPT subscription with API keys removed, one attempt, no retry, and no semantic correction.

The model receives the complete candidate bundle and exact sparse source context for one debate. It must use the nested structural schema, which guarantees four to six sections and one or two moves per side per section. The repository restores immutable candidate fields, rejects duplicate or wrong-side selection and invalid target chronology, renders exact evidence, and applies the unchanged v4.2.20 validator.

All three contexts must pass before Pass B packet preparation. No score, winner, audio claim, Overall Commentary, AI Extension, or publication prose is authorized.
