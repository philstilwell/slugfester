# Slugfester Hard-Route Publication Reconstruction Gate v4.2.21.17.32

Generate one isolated calibration-only public assessment for each of Debates 51, 63, 90, 153, and 165 after the adjudicated raw ledger and single deterministic score pass are locked. The model may author only the debate summary, representative quote selections and context, move summaries and critiques, post-scoring reference tags, Overall Commentary, and the separately disclosed AI Extension. Repository code owns and inserts all identity metadata, sides, chronology, sections, move IDs, timestamps, scores, and display structure.

Every selected move appears exactly once in the compiled scorecard. Critiques contain four labeled sentences—Strongest feature, Principal limitation, Live burden, and Locked score—and remain within 105–130 words. Representative quotations must be exact strings from a high-confidence locked source span or from a span whose required audio verification already passed. Tags are optional on moves; every Overall Commentary weakness must use only a genuinely applicable local reference.

The AI Extension appears immediately after Overall Commentary in the existing visually distinct, default-collapsed accordion. It is explicitly AI-generated, is not transcript content or wording attributable to either participant, cannot affect a score, strengthens both positions proportionately, contains four to six explicit premises and two to four new reinforcing arguments per side, and records novelty against locked move IDs. Rational-invulnerability language and the previously prohibited word remain absent.

Five 5.6 Sol/low contexts use ChatGPT-subscription authentication, a one-to-two concurrency ramp, one attempt, no retry or correction, an eight-minute per-context maximum, and a six-minute mean maximum. Passing structured validation must be followed by deterministic compilation, desktop and mobile rendering checks, closed/open/keyboard accordion checks, reduced-motion verification, and a final 195-debate projection no greater than fifty wall hours.
