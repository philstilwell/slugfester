# Slugfester Independent Judgment Gate v4.2.21.17

The retired partition-three inventories now enter the required adjudicated-consensus workflow. Each debate receives two independent 5.6 Sol performance judgments, Pass A and Pass B. Both receive the same score-blind locked-inventory hash but run in separate fresh contexts and cannot see the other output, candidate discovery alternatives, legacy assessments, scores, winner labels, publication prose, or control selection.

Every locked move is a required named property in each output. Reply target options are generated only from earlier opposing locked moves. The judge authors decisive response components, component contact, one structurally coherent response mode, within-class responsiveness, burden contact code, within-tier relevance, closed precision and calibration findings, charity testing, direct logical-coherence and evidence-warrant ratings, confidence, and the strict burden-residual findings. The repository derives target lists, component IDs, response class, absolute responsiveness, absolute relevance/burden, the neutral untested-charity anchor, and burden-residual exclusion.

The judge cannot author any calculated score. Each completed output must compile into the unchanged v4.2.20 raw-ledger contract and pass its full source, chronology, response, burden, precision, calibration, charity, and isolation validation without semantic repair.

After six valid outputs, deterministic field-level disagreement extraction is required. Every medium-confidence move from either pass must receive audio verification before a third isolated model adjudicates only disputed fields. Final ledgers and scores remain prohibited until that adjudication is complete.

The exact compiler replay passed for all six planned contexts. A and B share the same locked-inventory hash for each debate, all source chains replay locally, and copied input remains between 104 and 110 KB per context. This checkpoint authorizes an execution manifest, not model execution, disagreement extraction, audio calls, adjudication, or score derivation.
