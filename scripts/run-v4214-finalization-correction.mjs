#!/usr/bin/env node
import { createHash } from "node:crypto";
import { spawn } from "node:child_process";
import { access, copyFile, mkdtemp, readFile, rm, writeFile } from "node:fs/promises";
import os from "node:os";
import path from "node:path";
import { classifyTransportEventCount, extractTransportEvents } from "./lib/v385-transport.mjs";
import { assertV4 } from "./lib/v41-lean-production.mjs";
import { applyV4214Correction, V4214_ROOT } from "./lib/v4214-bounded-finalization-correction.mjs";
const manifest = JSON.parse(await readFile(`${V4214_ROOT}/execution-manifest.json`, "utf8")), codex = "/Applications/ChatGPT.app/Contents/Resources/codex", auth = path.join(os.homedir(), ".codex", "auth.json"), sha256 = (value) => createHash("sha256").update(value).digest("hex"); assertV4(manifest.status === "frozen-one-bounded-finalization-correction-authorized" && manifest.authorization.modelExecution, "v4.2.14 unauthorized"); for (const [file, digest] of Object.entries(manifest.sourceHashes)) assertV4(sha256(await readFile(file)) === digest, `source hash mismatch: ${file}`); for (const file of manifest.futureOutputPathsExcludedFromSourceHashes) await access(file).then(() => { throw new Error(`${file} already exists`); }, () => true); await access(codex); await access(auth);
function run(command, args, options, timeoutMs) { return new Promise((resolve) => { const child = spawn(command, args, { ...options, stdio: ["ignore", "pipe", "pipe"] }); let stdout = "", stderr = "", timedOut = false, force = null; child.stdout.on("data", (chunk) => { stdout += chunk; }); child.stderr.on("data", (chunk) => { stderr += chunk; }); const timer = setTimeout(() => { timedOut = true; child.kill("SIGTERM"); force = setTimeout(() => child.kill("SIGKILL"), 5000); }, timeoutMs); child.on("close", (code, signal) => { clearTimeout(timer); if (force) clearTimeout(force); resolve({ code, signal, stdout, stderr, timedOut }); }); }); }
const temporary = await mkdtemp(path.join(os.tmpdir(), "slugfester-v4214-")), home = await mkdtemp(path.join(os.tmpdir(), "slugfester-v4214-home-")), startedAt = new Date().toISOString(), started = Date.now(); let record;
try { const names = { manual: "manual.md", schema: "schema.json", packet: "packet.json" }; for (const [key, source] of Object.entries(manifest.inputs)) await copyFile(source, path.join(temporary, names[key])); await copyFile(auth, path.join(home, "auth.json")); const env = { ...process.env, CODEX_HOME: home }; delete env.OPENAI_API_KEY; delete env.OPENAI_ORG_ID; delete env.CODEX_API_KEY;
  const prompt = "Read manual.md, packet.json, and schema.json; read nothing else. Act only as the v4.2.14 bounded prose-correction editor. Rewrite exactly the two quote contexts, nine critiques, and four new AI arguments supplied in the packet to their word-count ranges while preserving their locked verdicts, scores, identities, and novelty classifications. Return exactly one schema-conforming JSON object and no commentary."; process.stdout.write("[v4.2.14-finalization-correction] starting 5.6 Sol/low Debate 103\n"); const invocation = await run(codex, ["exec", "--ephemeral", "--skip-git-repo-check", "--ignore-user-config", "--ignore-rules", "--model", manifest.model.slug, "-c", `model_reasoning_effort="${manifest.model.reasoningEffort}"`, "--disable", "plugins", "--disable", "remote_plugin", "--disable", "skill_search", "--disable", "apps", "--disable", "memories", "--disable", "multi_agent", "--disable", "browser_use", "--disable", "computer_use", "--disable", "workspace_dependencies", "--sandbox", "read-only", "--output-schema", "schema.json", "--output-last-message", "result.json", prompt], { cwd: temporary, env }, manifest.executionPolicy.timeoutMs);
  const events = extractTransportEvents(invocation.stderr), transportClassification = classifyTransportEventCount(events.length, 2, 8), exists = await access(path.join(temporary, "result.json")).then(() => true, () => false), base = { debateNumber: "103", startedAt, completedAt: new Date().toISOString(), elapsedMs: Date.now() - started, model: manifest.model.label, reasoningEffort: manifest.model.reasoningEffort, attemptCount: 1, retryCount: 0, recoverableStreamEvents: events.length, transportClassification, timedOut: invocation.timedOut, commandExitCode: invocation.code, terminationSignal: invocation.signal, authentication: "ChatGPT subscription", apiKeysRemoved: true, meteredApiCostUsd: 0, transcriptionCostUsd: 0, stdoutSha256: sha256(invocation.stdout), stderrSha256: sha256(invocation.stderr) };
  if (invocation.timedOut || invocation.code !== 0 || invocation.signal !== null || !exists) record = { ...base, status: invocation.timedOut ? "timed-out" : !exists ? "result-missing" : "transport-failed", accepted: false, proposalWritten: false };
  else { await copyFile(path.join(temporary, "result.json"), manifest.outputs.proposal); const [proposal, original, violations] = await Promise.all([readFile(manifest.outputs.proposal, "utf8").then(JSON.parse), readFile(manifest.validationInputs.originalOutput, "utf8").then(JSON.parse), readFile(manifest.validationInputs.violations, "utf8").then(JSON.parse)]); try { const corrected = applyV4214Correction(proposal, original, violations); await writeFile(manifest.outputs.corrected, `${JSON.stringify(corrected, null, 2)}\n`); const validation = await run(process.execPath, ["scripts/validate-v388-reconstruction-output.mjs", manifest.outputs.corrected, manifest.validationInputs.sourcePacket], { cwd: process.cwd(), env: process.env }, 120000), contentValid = validation.code === 0, accepted = contentValid && transportClassification !== "invalid"; record = { ...base, status: accepted ? "completed-valid" : !contentValid ? "corrected-output-validation-failed" : "transport-event-limit-exceeded", accepted, proposalWritten: true, proposalSha256: sha256(await readFile(manifest.outputs.proposal)), correctedWritten: true, correctedSha256: sha256(await readFile(manifest.outputs.corrected)), validationSummary: contentValid ? JSON.parse(validation.stdout) : null, validationMessage: contentValid ? null : `${validation.stdout}\n${validation.stderr}`.trim().slice(-8000) }; } catch (error) { record = { ...base, status: "proposal-validation-failed", accepted: false, proposalWritten: true, proposalSha256: sha256(await readFile(manifest.outputs.proposal)), validationMessage: error.stack }; } }
} finally { await rm(temporary, { recursive: true, force: true }); await rm(home, { recursive: true, force: true }); }
const execution = { schemaVersion: "4.2.14-bounded-finalization-correction-model-execution", protocolId: manifest.protocolId, status: record.accepted ? "bounded-finalization-correction-execution-passed" : "bounded-finalization-correction-execution-failed", attempts: 1, retries: 0, result: record, meteredApiCostUsd: 0, transcriptionCostUsd: 0, authorization: { analysis: true, retry: false, scoring: false } }; await writeFile(manifest.artifacts.execution, `${JSON.stringify(execution, null, 2)}\n`); console.log(JSON.stringify({ status: execution.status, elapsedMinutes: Number((record.elapsedMs / 60000).toFixed(2)), accepted: record.accepted, attempts: 1, retries: 0, meteredApiCostUsd: 0 }, null, 2));
