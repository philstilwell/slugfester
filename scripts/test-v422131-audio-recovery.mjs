#!/usr/bin/env node
import { strict as assert } from "node:assert";
import { createHash } from "node:crypto";
import { access, readFile } from "node:fs/promises";
const root = "docs/calibration/v4.2.21.3.1/audio-recovery", manifest = JSON.parse(await readFile(`${root}/execution-manifest.json`, "utf8")); assert.equal(manifest.status, "frozen-five-paid-diarized-audio-recovery-calls-authorized"); assert.equal(manifest.calls.length, 5); assert(manifest.referenceContract.references.every((reference) => reference.actualDurationSeconds >= 1.2 && reference.actualDurationSeconds <= 10)); assert.equal(manifest.executionPolicy.stopRemainingAfterRequestLevelFailure, true); assert.equal(manifest.executionPolicy.retriesMaximum, 0); assert.equal(manifest.costEstimate.newExpectedCostUsd, 0.0291); assert.equal(manifest.costEstimate.newMaximumCostUsd, 0.04); assert.equal(manifest.costEstimate.conservativeCumulativeMaximumUsd, 0.08); assert.equal(manifest.authorization.scoreDerivation, false); const sha256 = (value) => createHash("sha256").update(value).digest("hex"); for (const [file, digest] of Object.entries(manifest.sourceHashes)) assert.equal(sha256(await readFile(file)), digest, `source hash mismatch: ${file}`); for (const future of manifest.futureOutputPathsExcludedFromSourceHashes) await access(future).then(() => assert.fail(`future output exists: ${future}`), () => true); console.log(JSON.stringify({ status: "passed", callsMaximum: 5, referenceDurationsSeconds: manifest.referenceContract.references.map((reference) => reference.actualDurationSeconds), newExpectedCostUsd: manifest.costEstimate.newExpectedCostUsd, newMaximumCostUsd: manifest.costEstimate.newMaximumCostUsd, conservativeCumulativeMaximumUsd: manifest.costEstimate.conservativeCumulativeMaximumUsd, retries: 0, transcriptsExisting: 0, scoresDerived: 0 }, null, 2));
