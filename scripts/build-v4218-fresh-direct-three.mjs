#!/usr/bin/env node
import { createHash } from "node:crypto";
import { mkdir, readFile, stat, writeFile } from "node:fs/promises";
import path from "node:path";
import { assertV4 } from "./lib/v41-lean-production.mjs";
import { buildV4218SourceLedger, makeV4218PrimarySchema, validateV4218SourceLedger, V4218_EXCERPT_MAXIMUM_CHARACTERS, V4218_MODEL, V4218_OUTPUT_VERSION, V4218_PACKET_VERSION, V4218_PROTOCOL_ID, V4218_ROOT } from "./lib/v4218-fresh-direct-three.mjs";

const shouldWrite = process.argv.includes("--write"), sha256 = (value) => createHash("sha256").update(value).digest("hex");
const [sample, screening, authorization] = await Promise.all(["source-only-sample.json", "sample-screening.json"].map((file) => readFile(`${V4218_ROOT}/${file}`, "utf8").then(JSON.parse)).concat(readFile("docs/calibration/v4.2.17/no-truncation-finalization-gate/analysis.json", "utf8").then(JSON.parse)));
assertV4(sample.status === "frozen-pending-motion-only-screening" && sample.debates.length === 3 && sample.audit.priorFreshGateOverlap === 0 && !sample.selectionBoundary.legacyAssessmentContentAccessed, "v4.2.18 sample invalid");
assertV4(screening.status === "sample-screened-packet-preparation-authorized" && screening.authorization.compactSourcePacketPreparation && authorization.authorization.newDisjointFreshJudgmentGatePreparation, "v4.2.18 packet preparation unauthorized");
for (const [file, digest] of Object.entries(sample.sourceHashes)) assertV4(sha256(await readFile(file)) === digest, `sample source hash mismatch: ${file}`);
const control = [...sample.debates].sort((a, b) => sha256(`slugfester-v4.2.18-control:${a.debateId}`).localeCompare(sha256(`slugfester-v4.2.18-control:${b.debateId}`)))[0];
const contexts = [];
for (const debate of sample.debates) {
  const transcriptPath = `.assessment-cache/captions/${debate.videoId}/transcript.txt`, eventsPath = `.assessment-cache/captions/${debate.videoId}/events.json`, manifestPath = `.assessment-cache/captions/${debate.videoId}/manifest.json`;
  const [transcriptBytes, eventsBytes, manifestBytes] = await Promise.all([transcriptPath, eventsPath, manifestPath].map((file) => readFile(file)));
  const eventsDocument = JSON.parse(eventsBytes), manifest = JSON.parse(manifestBytes), events = Array.isArray(eventsDocument) ? eventsDocument : eventsDocument.events;
  assertV4(Array.isArray(events) && events.length > 0 && manifest.videoId === debate.videoId && manifest.durationSeconds < 5400, `${debate.number}: local source chain invalid`);
  assertV4(manifest.transcriptSha256 === sha256(transcriptBytes) && manifest.normalizedEventsSha256 === sha256(eventsBytes), `${debate.number}: source hash mismatch`);
  const sourceLedgerPath = `.assessment-cache/compact-ledgers/v4.2.18/debate-${debate.number}.jsonl`, sourceLedgerBytes = Buffer.from(buildV4218SourceLedger(eventsDocument));
  const ledgerValidation = validateV4218SourceLedger(sourceLedgerBytes, eventsDocument, sha256(sourceLedgerBytes));
  if (shouldWrite) { await mkdir(path.dirname(sourceLedgerPath), { recursive: true }); await writeFile(sourceLedgerPath, sourceLedgerBytes); }
  const packet = { schemaVersion: V4218_PACKET_VERSION, protocolId: V4218_PROTOCOL_ID, debateNumber: debate.number, debateId: debate.debateId, motion: debate.motion, sides: debate.sides, durationSeconds: manifest.durationSeconds, eventCount: events.length, sourceChain: { transcriptPath, transcriptSha256: sha256(transcriptBytes), eventsPath, eventsSha256: sha256(eventsBytes), localManifestPath: manifestPath, localManifestSha256: sha256(manifestBytes) }, transportChain: { format: "jsonl rows [eventIndex,startMs,durationMs,text]", sourceLedgerPath, sourceLedgerSha256: sha256(sourceLedgerBytes), sourceLedgerBytes: sourceLedgerBytes.length, sourceLedgerEventCount: ledgerValidation.eventCount, replayExactToOriginalEvents: true }, modelInputBoundary: { fullTimestampedTranscriptRequired: true, completeTimestampedSourceLedgerRequired: true, plainTranscriptDeliveredToModel: false, originalEventsFileDeliveredToModel: false, originalTranscriptAndEventsStoredAndHashLockedLocally: true, repositoryOwnedSourceTimes: true, modelSuppliedSourceMillisecondsProhibited: true, excerptMinimumTokens: 12, excerptMaximumTokens: 100, excerptMaximumCharacters: V4218_EXCERPT_MAXIMUM_CHARACTERS, minimumExcerptLexicalRecall: 0.8, minimumExcerptOrderedCoverage: 0.8, movesNestedUnderSections: false, oneChronologicalMoveInventoryRequired: true, replyTargetsMustAlreadyAppearInInventory: true, legacyAssessmentsUnavailable: true, priorArgumentInventoriesUnavailable: true, priorBurdenMapsUnavailable: true, priorSectionsAndWeightsUnavailable: true, priorRatingsAndTotalsUnavailable: true, winnerLabelsUnavailable: true, assessmentProseUnavailable: true, priorPrimaryOutputsUnavailable: true, controlSelectionUnavailable: true, boundedMoveMinimum: 8, boundedMoveMaximum: 24, sectionMinimum: 4, sectionMaximum: 6, movesPerSidePerSectionMinimum: 1, movesPerSidePerSectionMaximum: 2 } };
  const packetPath = `${V4218_ROOT}/packets/debate-${debate.number}.json`;
  if (shouldWrite) { await mkdir(path.dirname(packetPath), { recursive: true }); await writeFile(packetPath, `${JSON.stringify(packet, null, 2)}\n`); }
  contexts.push({ debateNumber: debate.number, debateId: debate.debateId, family: debate.family, durationSeconds: debate.durationSeconds, captionKind: debate.captionKind, controlSampleSelected: debate.debateId === control.debateId, packet: packetPath, sourceLedger: sourceLedgerPath, originalTranscript: transcriptPath, originalEvents: eventsPath, originalManifest: manifestPath, rawOutput: `${V4218_ROOT}/primary-outputs/debate-${debate.number}.json`, compiledOutput: `${V4218_ROOT}/primary-compiled/debate-${debate.number}.json`, sourceLedgerSha256: sha256(sourceLedgerBytes), sourceLedgerEvents: ledgerValidation.eventCount });
}
const schemaPath = `${V4218_ROOT}/primary.schema.json`;
if (shouldWrite) await writeFile(schemaPath, `${JSON.stringify(makeV4218PrimarySchema(), null, 2)}\n`);
const inputs = { rubricBase: "docs/reassessment-rubric-v4.0.md", rubricDerivedScores: "docs/reassessment-rubric-v4.0.1.md", rubricBounded: "docs/reassessment-rubric-v4.1.md", manual: `${V4218_ROOT}/manual.md`, schema: schemaPath };
const sharedBytes = (await Promise.all(Object.values(inputs).map((file) => stat(file).then((item) => item.size)))).reduce((sum, value) => sum + value, 0);
for (const context of contexts) context.compactCopiedInputBytes = sharedBytes + (await stat(context.packet)).size + (await stat(context.sourceLedger)).size;
const preparation = { schemaVersion: "4.2.18-fresh-direct-three-preparation", protocolId: V4218_PROTOCOL_ID, status: shouldWrite ? "prepared-three-fresh-direct-contexts" : "preview", calibrationOnly: true, AIOnly: true, dyadicOnly: true, directLaneOnly: true, model: { label: V4218_MODEL.label, slug: V4218_MODEL.slug, reasoningEffort: V4218_MODEL.primaryReasoningEffort, authentication: "ChatGPT subscription", meteredApiCostUsdMaximum: 0 }, sample: `${V4218_ROOT}/source-only-sample.json`, screening: `${V4218_ROOT}/sample-screening.json`, contexts, inputs, sourcePolicy: { completeLocalTranscriptChains: true, oneLosslessTimestampedLedgerPerDebate: true, originalTranscriptAndEventsStoredLocally: true, duplicatePlainTranscriptDeliveryProhibited: true, maximumDurationSecondsExclusive: 5400, excerptMaximumCharacters: V4218_EXCERPT_MAXIMUM_CHARACTERS, excerptTokenRange: [12, 100], minimumLexicalRecall: 0.8, minimumOrderedCoverage: 0.8 }, controlPolicy: { selectedDebateId: control.debateId, selectedDebateNumber: control.number, visibleToPrimaryJudge: false, purpose: "force later isolated Pass B and adjudication-path exercise" }, executionPolicy: { attemptsPerDebate: 1, retries: 0, normalizationAuthorized: false, truncationAuthorized: false, scoreDerivationAuthorized: false }, totals: { debates: contexts.length, sourceLedgerEvents: contexts.reduce((sum, item) => sum + item.sourceLedgerEvents, 0), meanCompactCopiedInputBytes: Math.round(contexts.reduce((sum, item) => sum + item.compactCopiedInputBytes, 0) / contexts.length), maximumCompactCopiedInputBytes: Math.max(...contexts.map((item) => item.compactCopiedInputBytes)), modelContextsExecuted: 0, meteredApiCostUsd: 0, transcriptionCostUsd: 0 }, authorization: { deterministicFixtures: true, primaryExecutionManifest: false, primaryModelExecution: false, scoreDerivation: false, productionMutation: false, all195Debates: false } };
if (shouldWrite) await writeFile(`${V4218_ROOT}/preparation-manifest.json`, `${JSON.stringify(preparation, null, 2)}\n`);
console.log(JSON.stringify({ status: preparation.status, outputSchemaVersion: V4218_OUTPUT_VERSION, debates: contexts.map((item) => item.debateNumber), control: preparation.controlPolicy.selectedDebateNumber, totalDurationHours: Number((contexts.reduce((sum, item) => sum + item.durationSeconds, 0) / 3600).toFixed(2)), sourceLedgerEvents: preparation.totals.sourceLedgerEvents, meanCompactCopiedInputKilobytes: Math.round(preparation.totals.meanCompactCopiedInputBytes / 1000), maximumCompactCopiedInputKilobytes: Math.round(preparation.totals.maximumCompactCopiedInputBytes / 1000), modelContextsExecuted: 0, meteredApiCostUsd: 0, transcriptionCostUsd: 0 }, null, 2));
