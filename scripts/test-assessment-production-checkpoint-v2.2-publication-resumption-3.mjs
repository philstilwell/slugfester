#!/usr/bin/env node

import assert from "node:assert/strict";
import { createHash } from "node:crypto";
import { access, readFile } from "node:fs/promises";
import path from "node:path";
import { CHECKPOINT_V22_RESUMPTION_3_DEBATES, CHECKPOINT_V22_RESUMPTION_3_ROOT } from "./lib/assessment-production-checkpoint-v2.2-publication-resumption-3.mjs";
const readJson = (file) => readFile(path.resolve(file), "utf8").then(JSON.parse), exists = (file) => access(path.resolve(file)).then(() => true, () => false), sha256 = (value) => createHash("sha256").update(value).digest("hex");
const preparation = await readJson(`${CHECKPOINT_V22_RESUMPTION_3_ROOT}/preparation-manifest.json`); assert.equal(preparation.status, "seven-untouched-production-checkpoint-v2.2-publication-contexts-prepared-and-frozen"); assert.deepEqual(preparation.contexts.map(({ debateNumber }) => debateNumber), CHECKPOINT_V22_RESUMPTION_3_DEBATES); assert.deepEqual(preparation.contexts.map(({ originalContextIndex }) => originalContextIndex), [3, 4, 5, 6, 7, 8, 9]); assert.equal(preparation.totals.moves, 136); assert.equal(preparation.policy.attemptsPerContext, 1); assert.equal(preparation.policy.retriesMaximum, 0); assert.equal(preparation.authorization.productionMutation, false);
const activationPath = `${CHECKPOINT_V22_RESUMPTION_3_ROOT}/execution-activation.json`;
if (await exists(activationPath)) { const activation = await readJson(activationPath); assert.equal(activation.genericResumptionRunner, true); assert.equal(activation.authorization.publicationModelExecution, true); assert.equal(activation.authorization.retry, false); assert.equal(activation.authorization.productionMutation, false); assert.equal(activation.model.label, "5.6 Sol"); assert.equal(activation.model.reasoningEffort, "low"); assert.equal(activation.model.authentication, "ChatGPT subscription"); for (const [file, digest] of Object.entries(activation.sourceHashes)) assert.equal(sha256(await readFile(path.resolve(file))), digest, `source hash mismatch: ${file}`); if (await exists(activation.artifacts.execution)) { const execution = await readJson(activation.artifacts.execution); assert.equal(execution.contextsPlanned, 7); assert.equal(execution.attempts, execution.contextsAttempted); assert.equal(execution.retries, 0); assert.equal(execution.correctionContexts, 0); assert.equal(execution.maximumObservedConcurrency <= 2, true); if (await exists(activation.artifacts.analysis)) { const analysis = await readJson(activation.artifacts.analysis); assert.equal(analysis.gate.retries, 0); assert.equal(analysis.authorization.productionMutation, false); if (analysis.status === "production-checkpoint-v2.2-publication-resumed-model-gate-passed") { assert.equal(analysis.gate.cohortSemanticPass, true); assert.equal(analysis.gate.cohortValidDebates, 10); assert.equal(analysis.gate.cohortMoves, 188); assert.equal(analysis.gate.cohortCritiques, 188); assert.equal(analysis.gate.cohortExactSourceQuotes, 20); assert.equal(analysis.gate.cohortOverallCommentarySides, 20); assert.equal(analysis.gate.cohortAIExtensionSides, 20); assert.equal(analysis.authorization.deterministicCompilation, true); } } } }
console.log(JSON.stringify({ status: "passed", resumptionContexts: 7, untouchedOriginalContextIndexes: [3, 4, 5, 6, 7, 8, 9], attemptsPerContext: 1, retriesMaximum: 0, maximumParallelContexts: 2, modelAuthoredScores: 0, productionMutation: false }, null, 2));
