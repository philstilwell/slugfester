#!/usr/bin/env node

import { createHash } from "node:crypto";
import { execFileSync } from "node:child_process";
import { access, readFile, writeFile } from "node:fs/promises";
import { assertV4 } from "./lib/v4-lean-production.mjs";
import { V422112_ROOT } from "./lib/v422112-simplified-discovery.mjs";

const shouldWrite = process.argv.includes("--write");
const index = process.argv.indexOf("--frozen-at");
const frozenAt = index >= 0 ? process.argv[index + 1] : null;
assertV4(frozenAt && !Number.isNaN(Date.parse(frozenAt)), "--frozen-at requires an ISO timestamp");
const preparationPath = `${V422112_ROOT}/preparation-manifest.json`;
const manifestPath = `${V422112_ROOT}/execution-manifest.json`;
const executionPath = `${V422112_ROOT}/model-execution.json`;
const analysisPath = `${V422112_ROOT}/analysis.json`;
if (shouldWrite) for (const file of [manifestPath, executionPath, analysisPath]) await access(file).then(() => { throw new Error(`${file} already exists`); }, () => true);
const preparation = JSON.parse(await readFile(preparationPath, "utf8"));
assertV4(preparation.status === "twelve-simplified-discovery-contexts-prepared-execution-manifest-authorized" && preparation.authorization.executionManifest && preparation.totals.discoveryContexts === 12 && !preparation.sourceBoundary.predecessorOutputsReadForAssessment, "simplified successor preparation unavailable");
const sha256 = (value) => createHash("sha256").update(value).digest("hex");
const sourceFiles = ["docs/assessment-workflow-v4.2.21.12.1.md", preparationPath, preparation.inputs.design, preparation.inputs.manual, "scripts/lib/v4-lean-production.mjs", "scripts/lib/v41-lean-production.mjs", "scripts/lib/v418-source-integrity.mjs", "scripts/lib/v42219-generalized-partition.mjs", "scripts/lib/v422112-simplified-discovery.mjs", "scripts/validate-v422112-discovery.mjs", "scripts/preregister-v4221121-successor-discovery.mjs", "scripts/run-v4221121-successor-discovery.mjs", "scripts/analyze-v4221121-successor-discovery.mjs", ...preparation.contexts.flatMap((context) => [context.packet, context.plan, context.fullLedger, context.originalEvents, ...context.chunks.flatMap((chunk) => [chunk.chunkLedgerPath, chunk.schemaPath])])];
const sourceHashes = {};
for (const file of [...new Set(sourceFiles)]) sourceHashes[file] = sha256(await readFile(file));
const contexts = preparation.contexts.flatMap((context) => context.chunks.map((chunk) => ({ debateNumber: context.debateNumber, debateId: context.debateId, packet: context.packet, plan: context.plan, fullLedger: context.fullLedger, originalEvents: context.originalEvents, chunkId: chunk.chunkId, coreStartEvent: chunk.coreStartEvent, coreEndEvent: chunk.coreEndEvent, contextStartEvent: chunk.contextStartEvent, contextEndEvent: chunk.contextEndEvent, chunkLedgerPath: chunk.chunkLedgerPath, chunkLedgerSha256: chunk.chunkLedgerSha256, schemaPath: chunk.schemaPath, schemaSha256: chunk.schemaSha256, copiedInputBytes: chunk.copiedInputBytes, rawOutput: chunk.rawOutput })));
const bundlePaths = preparation.contexts.flatMap((context) => [`${V422112_ROOT}/candidate-bundles/debate-${context.debateNumber}.json`, `${V422112_ROOT}/candidate-context/debate-${context.debateNumber}.jsonl`]);
const futureOutputs = [...contexts.map((context) => context.rawOutput), ...bundlePaths, executionPath, analysisPath];
const manifest = { schemaVersion: "4.2.21.12.1-simplified-discovery-execution-manifest", protocolId: preparation.protocolId, status: "frozen-twelve-simplified-discovery-contexts-authorized", frozenAt, checkpointCommit: execFileSync("git", ["rev-parse", "HEAD"], { encoding: "utf8" }).trim(), calibrationOnly: true, AIOnly: true, predecessorOutputsReused: false, model: { label: preparation.model.label, slug: preparation.model.slug, reasoningEffort: preparation.model.reasoningEffort }, costEstimate: { authentication: "ChatGPT subscription", meteredApiCostUsdMaximum: 0, transcriptionCostUsdMaximum: 0, expectedSequentialWallMinutes: [14, 24], absoluteTimeoutMinutes: 120 }, modelInputs: { manual: preparation.inputs.manual }, preparation: preparationPath, contexts, isolation: { freshTemporaryCodexHomePerContext: true, freshSourceDirectoryPerContext: true, oneChunkPerContext: true, otherChunksUnavailable: true, predecessorOutputsUnavailable: true, otherSuccessorOutputsUnavailable: true, legacyUnavailable: true, priorJudgmentsUnavailable: true, ratingsScoresWinnersUnavailable: true, publicationProseUnavailable: true }, executionPolicy: { contexts: contexts.length, attemptsPerContext: 1, retriesMaximum: 0, sequentialExecution: true, continueIndependentContextsAfterFailure: true, timeoutMs: 600000, authentication: "ChatGPT subscription", APIKeysRemoved: true, meteredApiCostUsdMaximum: 0, transcriptionCostUsdMaximum: 0 }, compilationPolicy: { allContextsMustValidate: true, silentSemanticDeduplication: false, repositoryDerivedMoveKindOnly: true, localTargetIdsAbsent: true, selectedTargetTopologyDeferredToPrimaryA: true, sparseContextFlankEvents: 12, sparseSourceRowsMayDeduplicate: true, candidateMinimumPerDebate: 8, candidateMinimumPerSide: 4, scoresDerived: false }, authorization: { modelContexts: true, deterministicValidation: true, deterministicCandidateCompilation: true, analysis: true, retry: false, semanticCorrection: false, primaryPacketPreparation: false, primaryModelExecution: false, scoreDerivation: false, productionMutation: false }, artifacts: { execution: executionPath, analysis: analysisPath, candidateBundles: bundlePaths.filter((file) => file.includes("candidate-bundles")), sparseContexts: bundlePaths.filter((file) => file.includes("candidate-context")), rawOutputs: contexts.map((context) => context.rawOutput) }, futureOutputPathsExcludedFromSourceHashes: futureOutputs, sourceHashes };
if (shouldWrite) await writeFile(manifestPath, `${JSON.stringify(manifest, null, 2)}\n`);
console.log(JSON.stringify({ status: shouldWrite ? "frozen" : "preview", contexts: contexts.length, debates: preparation.contexts.map((context) => context.debateNumber), attempts: contexts.length, retries: 0, expectedSequentialWallMinutes: manifest.costEstimate.expectedSequentialWallMinutes, authentication: manifest.costEstimate.authentication, meteredApiCostUsdMaximum: 0, transcriptionCostUsdMaximum: 0, predecessorOutputsReused: false, scoresDerived: 0 }, null, 2));
