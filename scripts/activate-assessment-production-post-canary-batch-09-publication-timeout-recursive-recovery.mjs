#!/usr/bin/env node
import { createHash } from "node:crypto";
import { execFileSync } from "node:child_process";
import { access, readFile, writeFile } from "node:fs/promises";
import path from "node:path";
import { POST_CANARY_BATCH_09_PUBLICATION_TIMEOUT_RECURSIVE_RECOVERY_PROTOCOL_ID as PROTOCOL_ID, POST_CANARY_BATCH_09_PUBLICATION_TIMEOUT_RECURSIVE_RECOVERY_ROOT as ROOT } from "./lib/assessment-production-post-canary-batch-09-publication-timeout-recursive-recovery.mjs";
import { assertV4, canonicalJson } from "./lib/v4-lean-production.mjs";
const shouldWrite = process.argv.includes("--write"); const atIndex = process.argv.indexOf("--activated-at"); const activatedAt = atIndex >= 0 ? process.argv[atIndex + 1] : null; assertV4(activatedAt && !Number.isNaN(Date.parse(activatedAt)), "--activated-at requires an ISO timestamp");
const PREPARATION = `${ROOT}/execution-preparation-manifest.json`; const ACTIVATION = `${ROOT}/execution-activation.json`; const sha256 = (value) => createHash("sha256").update(value).digest("hex"); const exists = (file) => access(path.resolve(file)).then(() => true, () => false);
assertV4(!(await exists(ACTIVATION)), "recursive recovery activation already exists"); const preparationBytes = await readFile(path.resolve(PREPARATION)); const preparation = JSON.parse(preparationBytes);
assertV4(preparation.protocolId === PROTOCOL_ID && preparation.status === "frozen-three-repair-plus-five-resumption-contexts-prepared-not-activated" && preparation.repairContexts?.length === 3 && preparation.resumptionContexts?.length === 5, "recursive recovery not prepared");
assertV4(preparation.model?.slug === "gpt-5.6-sol" && preparation.model?.reasoningEffort === "low" && preparation.model?.authentication === "ChatGPT subscription" && preparation.executionPolicy?.attemptsPerContext === 1 && preparation.executionPolicy?.retriesMaximum === 0 && preparation.executionPolicy?.timeoutExtensionsMaximum === 0 && preparation.executionPolicy?.recursiveCorrectionsAfterThisMaximum === 0 && canonicalJson(preparation.executionPolicy?.schedulerRamp) === canonicalJson([1, 2]), "model or execution controls changed");
assertV4(execFileSync(preparation.executionEnvironment.codexPath, ["--version"], { encoding: "utf8" }).trim() === preparation.executionEnvironment.codexCliVersion, "Codex command-line version changed"); for (const [file, digest] of Object.entries(preparation.sourceHashes)) assertV4(sha256(await readFile(path.resolve(file))) === digest, `source hash mismatch: ${file}`); for (const future of preparation.futureOutputPathsExcludedFromSourceHashes) if (future !== ACTIVATION) assertV4(!(await exists(future)), `future output exists: ${future}`);
const activation = { schemaVersion: "1.0-assessment-production-post-canary-batch-09-publication-timeout-recursive-recovery-activation", protocolId: PROTOCOL_ID, status: "frozen-three-repair-plus-five-resumption-contexts-activated", activatedAt, checkpointCommit: execFileSync("git", ["rev-parse", "HEAD"], { encoding: "utf8" }).trim(), productionCanary: false, batchNumber: 9, stagingOnly: true, userAuthorization: preparation.userAuthorization, preparation: { path: PREPARATION, sha256: sha256(preparationBytes) }, preservedParent: preparation.preservedParent, diagnosis: preparation.diagnosis, model: preparation.model, repairContexts: preparation.repairContexts, resumptionContexts: preparation.resumptionContexts, modelInputs: preparation.modelInputs, isolation: preparation.isolation, recoveryContract: preparation.recoveryContract, executionEnvironment: preparation.executionEnvironment, executionPolicy: preparation.executionPolicy, stopRules: preparation.stopRules, sourceHashes: { ...preparation.sourceHashes, [PREPARATION]: sha256(preparationBytes) }, futureOutputPathsExcludedFromSourceHashes: preparation.futureOutputPathsExcludedFromSourceHashes.filter((file) => file !== ACTIVATION), artifacts: preparation.artifacts, authorization: { exactThreeRepairContexts: true, exactFiveResumptionContextsAfterRepairsPass: true, modelExecution: true, deterministicValidationMergeAndCohortReplay: true, retries: false, timeoutExtensions: false, furtherRecovery: false, paidServices: false, productionMutation: false, nextBatchSelection: false }, nextAuthorizedAction: "execute-three-repair-contexts-and-only-after-all-pass-five-unattempted-resumption-contexts" };
if (shouldWrite) await writeFile(path.resolve(ACTIVATION), `${JSON.stringify(activation, null, 2)}\n`); console.log(JSON.stringify({ status: activation.status, repairContextsAuthorized: 3, resumptionContextsConditionallyAuthorized: 5, model: activation.model, schedulerRamp: [1, 2], attemptsPerContext: 1, retries: 0, noFurtherRecovery: true, hostAwakeGuard: activation.executionEnvironment.hostAwakeGuard, costUsdMaximum: 0 }, null, 2));
