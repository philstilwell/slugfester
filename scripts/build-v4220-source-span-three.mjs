#!/usr/bin/env node
import { createHash } from "node:crypto";
import { mkdir, readFile, stat, writeFile } from "node:fs/promises";
import path from "node:path";
import { assertV4 } from "./lib/v41-lean-production.mjs";
import { classifyV4219PrimaryRoute } from "./lib/v4219-primary-recovery.mjs";
import { V4220_MODEL, V4220_OUTPUT_VERSION, V4220_PACKET_VERSION, V4220_PROTOCOL_ID, V4220_ROOT, buildV4220SourcePacket, makeV4220PrimarySchema } from "./lib/v4220-source-span-rendering.mjs";

const shouldWrite = process.argv.includes("--write");
const sha256 = (value) => createHash("sha256").update(value).digest("hex");
const [sample, screening] = await Promise.all(["source-only-sample.json", "sample-screening.json"].map((file) => readFile(`${V4220_ROOT}/${file}`, "utf8").then(JSON.parse)));
assertV4(sample.status === "frozen-pending-motion-route-screening" && sample.debates.length === 3 && sample.audit.priorOrRejectedSampleOverlap === 0, "v4.2.20 sample invalid");
assertV4(screening.status === "sample-screened-packet-preparation-authorized" && screening.authorization.compactSourcePacketPreparation, "v4.2.20 packet preparation unauthorized");
for (const [file, digest] of Object.entries(sample.sourceHashes)) assertV4(sha256(await readFile(file)) === digest, `sample source hash mismatch: ${file}`);
const inputs = { rubricBase: "docs/reassessment-rubric-v4.0.md", rubricDerivedScores: "docs/reassessment-rubric-v4.0.1.md", rubricBounded: "docs/reassessment-rubric-v4.1.md", manual: `${V4220_ROOT}/manual.md`, schema: `${V4220_ROOT}/primary.schema.json` };
const schemaBytes = Buffer.from(`${JSON.stringify(makeV4220PrimarySchema(), null, 2)}\n`);
if (shouldWrite) await writeFile(inputs.schema, schemaBytes); else assertV4(sha256(await readFile(inputs.schema)) === sha256(schemaBytes), "v4.2.20 stored schema differs from generator");
const sharedInputBytes = (await Promise.all(Object.values(inputs).map((file) => stat(file).then((item) => item.size)))).reduce((sum, value) => sum + value, 0);
assertV4(sharedInputBytes === sample.routingAudit.sharedInputBytes, "v4.2.20 shared input bytes changed after selection");
const control = [...sample.debates].sort((left, right) => sha256(`slugfester-v4.2.20-control:${left.debateId}`).localeCompare(sha256(`slugfester-v4.2.20-control:${right.debateId}`)))[0];
const contexts = [];
for (const debate of sample.debates) {
  const base = `.assessment-cache/captions/${debate.videoId}`;
  const transcriptPath = `${base}/transcript.txt`, eventsPath = `${base}/events.json`, manifestPath = `${base}/manifest.json`, sourceLedgerPath = `.assessment-cache/compact-ledgers/v4.2.20/debate-${debate.number}.jsonl`;
  const [transcriptBytes, eventsBytes, manifestBytes] = await Promise.all([transcriptPath, eventsPath, manifestPath].map((file) => readFile(file)));
  const built = buildV4220SourcePacket({ debate, transcriptPath, eventsPath, manifestPath, sourceLedgerPath, transcriptBytes, eventsBytes, manifestBytes });
  const compactCopiedInputBytes = sharedInputBytes + built.packetBytes.length + built.sourceLedgerBytes.length;
  const route = classifyV4219PrimaryRoute({ sourceLedgerEvents: built.packet.eventCount, compactCopiedInputBytes });
  assertV4(route.route === "direct" && built.packet.eventCount === debate.sourceLedgerEvents && built.sourceLedgerBytes.length === debate.sourceLedgerBytes && built.packetBytes.length === debate.packetBytes && compactCopiedInputBytes === debate.compactCopiedInputBytes, `${debate.number}: frozen transport measurement changed`);
  const packetPath = `${V4220_ROOT}/packets/debate-${debate.number}.json`;
  if (shouldWrite) { await mkdir(path.dirname(sourceLedgerPath), { recursive: true }); await mkdir(path.dirname(packetPath), { recursive: true }); await writeFile(sourceLedgerPath, built.sourceLedgerBytes); await writeFile(packetPath, built.packetBytes); }
  contexts.push({ debateNumber: debate.number, debateId: debate.debateId, family: debate.family, durationSeconds: debate.durationSeconds, captionKind: debate.captionKind, route: route.route, routeEvidence: route, controlSampleSelected: debate.debateId === control.debateId, packet: packetPath, sourceLedger: sourceLedgerPath, originalTranscript: transcriptPath, originalEvents: eventsPath, originalManifest: manifestPath, rawOutput: `${V4220_ROOT}/primary-outputs/debate-${debate.number}.json`, compiledOutput: `${V4220_ROOT}/primary-compiled/debate-${debate.number}.json`, sourceLedgerSha256: built.packet.transportChain.sourceLedgerSha256, sourceLedgerEvents: built.packet.eventCount, sourceLedgerBytes: built.sourceLedgerBytes.length, packetBytes: built.packetBytes.length, sharedInputBytes, compactCopiedInputBytes });
}
const preparation = { schemaVersion: "4.2.20-source-span-three-preparation", protocolId: V4220_PROTOCOL_ID, status: shouldWrite ? "prepared-three-source-span-direct-contexts" : "preview", calibrationOnly: true, AIOnly: true, dyadicOnly: true, model: { label: V4220_MODEL.label, slug: V4220_MODEL.slug, reasoningEffort: V4220_MODEL.primaryReasoningEffort, authentication: "ChatGPT subscription", meteredApiCostUsdMaximum: 0 }, sample: `${V4220_ROOT}/source-only-sample.json`, screening: `${V4220_ROOT}/sample-screening.json`, contexts, inputs, sourcePolicy: { completeLocalTranscriptChains: true, originalTranscriptAndEventsStoredLocally: true, oneLosslessTimestampedLedgerPerDebate: true, duplicatePlainTranscriptDeliveryProhibited: true, routeUsesDuration: false, directRouteRequiresBothEventAndByteCeilings: true, modelSelectsEventSpanOnly: true, modelAuthoredEvidenceTextProhibited: true, repositoryOwnedLexicalSalienceRendering: true, compiledExcerptMaximumCharacters: 450, compiledExcerptTokenRange: [12, 90], repositoryOwnedChronology: true, futureTargetHardFailure: true, automaticTargetRepairProhibited: true, repositoryDerivedResponseClass: true, modelAuthoredAbsoluteResponsivenessProhibited: true }, controlPolicy: { selectedDebateId: control.debateId, selectedDebateNumber: control.number, visibleToPrimaryJudge: false, purpose: "force later isolated Pass B and adjudication-path exercise" }, audioPolicy: { mediumConfidenceMoveRequiresAudioVerification: true, audioVerificationOccursBeforeAdjudicationAndScoreDerivation: true }, executionPolicy: { attemptsPerDebate: 1, retries: 0, postHocCorrectionAuthorized: false, semanticNormalizationAuthorized: false, scoreDerivationAuthorized: false }, totals: { debates: contexts.length, sourceLedgerEvents: contexts.reduce((sum, item) => sum + item.sourceLedgerEvents, 0), meanCompactCopiedInputBytes: Math.round(contexts.reduce((sum, item) => sum + item.compactCopiedInputBytes, 0) / contexts.length), maximumCompactCopiedInputBytes: Math.max(...contexts.map((item) => item.compactCopiedInputBytes)), modelContextsExecuted: 0, meteredApiCostUsd: 0, transcriptionCostUsd: 0 }, authorization: { deterministicFixtures: true, primaryExecutionManifest: true, primaryModelExecution: false, scoreDerivation: false, productionMutation: false, all195Debates: false } };
if (shouldWrite) await writeFile(`${V4220_ROOT}/preparation-manifest.json`, `${JSON.stringify(preparation, null, 2)}\n`);
console.log(JSON.stringify({ status: preparation.status, outputSchemaVersion: V4220_OUTPUT_VERSION, packetSchemaVersion: V4220_PACKET_VERSION, debates: contexts.map((item) => item.debateNumber), control: preparation.controlPolicy.selectedDebateNumber, totalDurationHours: Number((contexts.reduce((sum, item) => sum + item.durationSeconds, 0) / 3600).toFixed(2)), sourceLedgerEvents: preparation.totals.sourceLedgerEvents, meanCompactCopiedInputKilobytes: Math.round(preparation.totals.meanCompactCopiedInputBytes / 1000), maximumCompactCopiedInputKilobytes: Math.round(preparation.totals.maximumCompactCopiedInputBytes / 1000), modelContextsExecuted: 0, meteredApiCostUsd: 0, transcriptionCostUsd: 0 }, null, 2));
