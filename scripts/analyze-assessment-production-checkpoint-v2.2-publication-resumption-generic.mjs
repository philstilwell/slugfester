#!/usr/bin/env node

import { createHash } from "node:crypto";
import { readFile, writeFile } from "node:fs/promises";
import path from "node:path";
import { validateCheckpointV22PublicationOutput } from "./lib/assessment-production-checkpoint-v2.2-publication-validation.mjs";
import { assertV4 } from "./lib/v4-lean-production.mjs";

const root = process.argv[2], shouldWrite = process.argv.includes("--write");
assertV4(root && !path.isAbsolute(root) && !root.includes(".."), "provide a repository-relative resumption root");
const [preparation, activation, execution] = await Promise.all(["preparation-manifest.json", "execution-activation.json", "model-execution.json"].map((file) => readFile(path.resolve(`${root}/${file}`), "utf8").then(JSON.parse)));
const sha256 = (value) => createHash("sha256").update(value).digest("hex");
assertV4(activation.genericResumptionRunner === true && execution.schemaVersion === "1.0-production-checkpoint-v2.2-generic-publication-resumption-model-execution" && execution.contextsPlanned === activation.contexts.length && execution.attempts === execution.contextsAttempted && execution.retries === 0 && execution.correctionContexts === 0 && execution.modelAuthoredScores === 0 && activation.authorization.deterministicAnalysis === true && activation.authorization.productionMutation === false, "generic resumption analysis crossed its boundary");
for (const [file, digest] of Object.entries(activation.sourceHashes)) assertV4(sha256(await readFile(path.resolve(file))) === digest, `analysis source hash mismatch: ${file}`);
async function replay(outputPath, packetPath) { const [outputBytes, packet] = await Promise.all([readFile(path.resolve(outputPath)), readFile(path.resolve(packetPath), "utf8").then(JSON.parse)]), replay = validateCheckpointV22PublicationOutput(JSON.parse(outputBytes), packet); return { outputSha256: sha256(outputBytes), ...replay }; }
const acceptedDebates = [];
for (const debate of preparation.acceptedDebates) { const [validation, replayed] = await Promise.all([readFile(path.resolve(debate.validation), "utf8").then(JSON.parse), replay(debate.output, debate.packet)]); assertV4(validation.status === "passed" && validation.mergedOutputSha256 === replayed.outputSha256, `${debate.debateNumber}: accepted repaired debate replay failed`); acceptedDebates.push({ debateNumber: debate.debateNumber, status: "accepted-after-bounded-publication-repair", accepted: true, elapsedMinutes: null, validationReplayed: true, ...replayed, modelAuthoredScores: replayed.calculatedScoresAuthoredByModel }); }
const resumptionContexts = [];
for (const context of activation.contexts) {
  const result = execution.results.find((item) => item.contextIndex === context.contextIndex);
  if (!result) { resumptionContexts.push({ contextIndex: context.contextIndex, originalContextIndex: context.originalContextIndex, debateNumber: context.debateNumber, status: "unattempted", accepted: false, validationReplayed: false, moves: null }); continue; }
  let replayed = null, replayMessage = null;
  if (result.outputWritten) { try { replayed = await replay(context.rawOutput, context.packet); } catch (error) { replayMessage = (error.stack ?? error.message).slice(-10000); } assertV4(!result.gateAcceptancePassed || replayed?.status === "passed", `${context.debateNumber}: accepted output replay failed`); assertV4(!replayed || replayed.outputSha256 === result.outputSha256, `${context.debateNumber}: output hash mismatch`); }
  if (result.validationWritten) assertV4(sha256(await readFile(path.resolve(context.validation))) === result.validationSha256, `${context.debateNumber}: validation hash mismatch`); if (result.provenanceWritten) assertV4(sha256(await readFile(path.resolve(context.provenance))) === result.provenanceSha256, `${context.debateNumber}: provenance hash mismatch`);
  resumptionContexts.push({ contextIndex: context.contextIndex, originalContextIndex: context.originalContextIndex, debateNumber: context.debateNumber, status: result.status, accepted: Boolean(result.gateAcceptancePassed && replayed?.status === "passed"), elapsedMinutes: Number((result.elapsedMs / 60000).toFixed(2)), validationReplayed: replayed?.status === "passed", replayMessage, moves: replayed?.moves ?? null, critiques: replayed?.critiques ?? null, minimumCritiqueCharacters: replayed?.minimumCritiqueCharacters ?? null, tags: replayed?.tags ?? null, quoteExactSourceMatches: replayed?.quoteExactSourceMatches ?? null, overallCommentarySides: replayed?.overallCommentarySides ?? null, noveltyItems: replayed?.noveltyItems ?? null, introducedItems: replayed?.introducedItems ?? null, newArguments: replayed?.newArguments ?? null, aiExtensionSides: replayed?.aiExtensionSides ?? null, modelAuthoredScores: replayed?.calculatedScoresAuthoredByModel ?? null, lockedScoresUnchanged: replayed?.lockedScoresUnchanged ?? null });
}
const validResumption = resumptionContexts.filter((context) => context.accepted), cohortContexts = [...acceptedDebates, ...resumptionContexts], validCohort = cohortContexts.filter((context) => context.accepted), sum = (contexts, key) => contexts.reduce((total, context) => total + context[key], 0), contract = activation.acceptanceContract;
const resumptionSemanticPass = validResumption.length === contract.resumptionValidContextsRequired && sum(validResumption, "moves") === contract.resumptionMovesRequired && sum(validResumption, "critiques") === contract.resumptionCritiquesRequired && sum(validResumption, "quoteExactSourceMatches") === contract.resumptionExactSourceQuotesRequired && sum(validResumption, "overallCommentarySides") === contract.resumptionOverallCommentarySidesRequired && sum(validResumption, "aiExtensionSides") === contract.resumptionAIExtensionSidesRequired && validResumption.every((context) => context.minimumCritiqueCharacters >= contract.minimumCritiqueCharacters && context.newArguments >= 4 && context.introducedItems >= 2 && context.modelAuthoredScores === 0 && context.lockedScoresUnchanged === true);
const cohortSemanticPass = resumptionSemanticPass && validCohort.length === contract.cohortValidDebatesRequired && sum(validCohort, "moves") === contract.cohortMovesRequired && sum(validCohort, "critiques") === contract.cohortCritiquesRequired && sum(validCohort, "quoteExactSourceMatches") === contract.cohortExactSourceQuotesRequired && sum(validCohort, "overallCommentarySides") === contract.cohortOverallCommentarySidesRequired && sum(validCohort, "aiExtensionSides") === contract.cohortAIExtensionSidesRequired;
const timingPass = execution.results.every((result) => result.elapsedMs <= activation.executionPolicy.timeoutMsPerContext && result.timedOut === false) && execution.wallElapsedMs <= activation.executionPolicy.absoluteGateTimeoutMs, passed = cohortSemanticPass && timingPass, wallElapsedMinutes = Number((execution.wallElapsedMs / 60000).toFixed(2)), maximumElapsedMinutes = validResumption.length ? Math.max(...validResumption.map((context) => context.elapsedMinutes)) : null, meanElapsedMinutes = validResumption.length ? Number((validResumption.reduce((total, context) => total + context.elapsedMinutes, 0) / validResumption.length).toFixed(2)) : null;
const analysis = {
  schemaVersion: "1.0-production-checkpoint-v2.2-generic-publication-resumption-analysis", protocolId: activation.protocolId,
  status: passed ? "production-checkpoint-v2.2-publication-resumed-model-gate-passed" : cohortSemanticPass ? "production-checkpoint-v2.2-publication-resumption-failed-timing" : "production-checkpoint-v2.2-publication-resumption-failed-validation",
  productionCanary: true, stagingOnly: true, AIOnly: true, acceptedDebates, resumptionContexts, cohortContexts,
  gate: { resumptionSemanticPass, cohortSemanticPass, timingPass, resumptionValidContexts: validResumption.length, requiredResumptionValidContexts: contract.resumptionValidContextsRequired, cohortValidDebates: validCohort.length, requiredCohortValidDebates: contract.cohortValidDebatesRequired, cohortMoves: sum(validCohort, "moves"), requiredCohortMoves: contract.cohortMovesRequired, cohortCritiques: sum(validCohort, "critiques"), requiredCohortCritiques: contract.cohortCritiquesRequired, minimumCritiqueCharacters: validCohort.length ? Math.min(...validCohort.map((context) => context.minimumCritiqueCharacters)) : null, cohortExactSourceQuotes: sum(validCohort, "quoteExactSourceMatches"), requiredCohortExactSourceQuotes: contract.cohortExactSourceQuotesRequired, cohortOverallCommentarySides: sum(validCohort, "overallCommentarySides"), requiredCohortOverallCommentarySides: contract.cohortOverallCommentarySidesRequired, cohortAIExtensionSides: sum(validCohort, "aiExtensionSides"), requiredCohortAIExtensionSides: contract.cohortAIExtensionSidesRequired, cohortNoveltyItems: sum(validCohort, "noveltyItems"), cohortIntroducedItems: sum(validCohort, "introducedItems"), cohortNewArguments: sum(validCohort, "newArguments"), wallElapsedMinutes, maximumElapsedMinutes, meanElapsedMinutes, retries: 0, correctionContexts: 0, modelAuthoredScores: 0, scorePassesExecutedThisStage: 0 },
  evidenceBoundary: activation.isolation,
  totals: { acceptedRepairedDebates: acceptedDebates.length, acceptedRepairContexts: preparation.acceptedRepairContexts, resumptionContexts: execution.contextsAttempted, cohortDebates: activation.acceptanceContract.cohortValidDebatesRequired, retries: 0, correctionContexts: 0, modelAuthoredScores: 0, meteredApiCostUsd: 0 },
  authorization: { deterministicCompilation: passed, failureDiagnosis: !passed, repairPacketPreparation: false, retry: false, correctionModelExecution: false, publicationFinalization: false, renderingVerification: false, productionMutation: false, remainingProductionBatches: false },
  nextAuthorizedAction: passed ? "prepare-and-run-deterministic-production-checkpoint-v2.2-publication-compilation" : "diagnose-generic-publication-resumption-failure-only"
};
if (shouldWrite) await writeFile(path.resolve(activation.artifacts.analysis), `${JSON.stringify(analysis, null, 2)}\n`);
console.log(JSON.stringify({ status: analysis.status, validResumptionContexts: validResumption.length, validCohortDebates: validCohort.length, cohortMoves: analysis.gate.cohortMoves, cohortCritiques: analysis.gate.cohortCritiques, minimumCritiqueCharacters: analysis.gate.minimumCritiqueCharacters, timings: { wallElapsedMinutes, maximumElapsedMinutes, meanElapsedMinutes, passed: timingPass }, retries: 0, correctionContexts: 0, modelAuthoredScores: 0, productionMutationAuthorized: false, nextAuthorizedAction: analysis.nextAuthorizedAction }, null, 2));
