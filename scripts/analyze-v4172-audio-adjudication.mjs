#!/usr/bin/env node

import { writeFile } from "node:fs/promises";
import path from "node:path";
import { assertV4, readJson } from "./lib/v41-lean-production.mjs";
import { V4172_AUDIO_ADJ_PROTOCOL_ID, V4172_AUDIO_ADJ_ROOT, validateV4172AudioAdjudicationOutput } from "./lib/v4172-audio-adjudication.mjs";
const root = process.cwd(); const shouldWrite = process.argv.includes("--write"); const [execution, output, packet, audioAudit] = await Promise.all([readJson(`${V4172_AUDIO_ADJ_ROOT}/model-execution.json`), readJson(`${V4172_AUDIO_ADJ_ROOT}/output.json`), readJson(`${V4172_AUDIO_ADJ_ROOT}/packet.json`), readJson("docs/calibration/v4.1.7/fresh-six-gate/pass-b/audio-verification.json")]); assertV4(execution.status === "audio-adjudication-execution-passed" && execution.authorization.analysis, "valid v4.1.7.2 execution unavailable"); const validation = await validateV4172AudioAdjudicationOutput(output, packet, root); const passed = validation.verified === 2 && validation.unresolved === 0; const analysis = { schemaVersion: "4.1.7.2-audio-adjudication-analysis", protocolId: V4172_AUDIO_ADJ_PROTOCOL_ID, status: passed ? "audio-attribution-adjudication-passed" : "audio-attribution-adjudication-unresolved", inheritedEndpointFailurePreserved: "docs/calibration/v4.1.7/fresh-six-gate/pass-b/audio-adjudication/model-execution.json", preservedDeterministicGate: { status: audioAudit.status, verified: 10, unresolved: 2, erasedOrReclassified: false }, adjudication: { validation, decisions: output.adjudications }, combinedAudioResult: { requiredMoves: 12, deterministicallyVerified: 10, adjudicatedVerified: validation.verified, unresolved: validation.unresolved, verificationRate: (10 + validation.verified) / 12 }, costs: { additionalPaidTranscriptionCalls: 0, additionalTranscriptionCostUsd: 0, modelComputeAuthentication: "ChatGPT subscription", meteredModelApiCostUsd: 0 }, legacyBoundary: { legacyAssessmentContentAccessed: false, legacyScoresAccessed: false, legacyWinnersAccessed: false, scoreArtifactCreated: false }, authorization: { disagreementExtraction: passed, scoreDerivation: false, legacyComparison: false, productionMutation: false, heldOutGate: false, all195Debates: false } }; const assessment = `# v4.1.7.2 audio-attribution adjudication assessment\n\nThe endpoint-compatible isolated Sol/high adjudicator returned ${validation.verified} verified and ${validation.unresolved} unresolved decisions. The original deterministic 10/12 failure and the v4.1.7.1 endpoint failure remain preserved. Combined audio attribution is ${10 + validation.verified}/12 resolved.\n\nThis stage used no paid transcription, no metered model API, and no retry. ${passed ? "Disagreement extraction is authorized; all scoring and comparison remain blocked." : "The workflow remains blocked before disagreement extraction."}\n`; if (shouldWrite) { await writeFile(path.resolve(root, V4172_AUDIO_ADJ_ROOT, "analysis.json"), `${JSON.stringify(analysis, null, 2)}\n`); await writeFile(path.resolve(root, V4172_AUDIO_ADJ_ROOT, "assessment.md"), assessment); } console.log(JSON.stringify({ status: analysis.status, verified: validation.verified, unresolved: validation.unresolved, disagreementExtractionAuthorized: analysis.authorization.disagreementExtraction, paidTranscriptionCalls: 0, meteredModelApiCostUsd: 0, legacyAccessed: false }, null, 2)); if (!passed) process.exitCode = 1;
