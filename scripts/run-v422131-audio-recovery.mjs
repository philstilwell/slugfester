#!/usr/bin/env node
import { createHash } from "node:crypto";
import { spawn } from "node:child_process";
import { access, mkdir, readFile, writeFile } from "node:fs/promises";
import path from "node:path";
import { assertV4 } from "./lib/v4-lean-production.mjs";

const stageRoot = "docs/calibration/v4.2.21.3.1/audio-recovery", manifest = JSON.parse(await readFile(`${stageRoot}/execution-manifest.json`, "utf8")), sha256 = (value) => createHash("sha256").update(value).digest("hex"); assertV4(manifest.status === "frozen-five-paid-diarized-audio-recovery-calls-authorized" && manifest.authorization.paidRecoveryExecution && !manifest.authorization.scoreDerivation, "v4.2.21.3.1 paid recovery unauthorized"); assertV4(process.env.OPENAI_API_KEY, "OPENAI_API_KEY must be set locally"); for (const [file, digest] of Object.entries(manifest.sourceHashes)) assertV4(sha256(await readFile(file)) === digest, `source hash mismatch: ${file}`); for (const future of manifest.futureOutputPathsExcludedFromSourceHashes) await access(future).then(() => { throw new Error(`future output exists: ${future}`); }, () => true);
function invoke(args) { return new Promise((resolve) => { const child = spawn("python3", args, { cwd: process.cwd(), env: process.env, stdio: ["ignore", "pipe", "pipe"] }); let stdout = "", stderr = ""; child.stdout.on("data", (chunk) => { stdout += chunk; }); child.stderr.on("data", (chunk) => { stderr += chunk; }); child.on("close", (code, signal) => resolve({ code, signal, stdout, stderr })); }); }
const results = []; let commonRequestFailure = false;
for (const call of manifest.calls) {
  if (commonRequestFailure) { results.push({ moveId: call.moveId, expectedSpeaker: call.expectedSpeaker, status: "skipped-after-request-failure", attemptCount: 0, retryCount: 0, transcriptWritten: false, durationSeconds: call.durationSeconds, estimatedCostUsd: 0 }); continue; }
  await mkdir(path.dirname(call.transcriptPath), { recursive: true }); const startedAt = new Date().toISOString(), started = Date.now(), args = ["/Users/philstilwell/.codex/skills/transcribe/scripts/transcribe_diarize.py", call.clipPath, "--model", call.model, "--response-format", call.responseFormat, "--chunking-strategy", call.chunkingStrategy, "--language", call.language, "--out", call.transcriptPath]; for (const reference of call.knownSpeakers) args.push("--known-speaker", `${reference.speaker}=${reference.localPath}`);
  process.stdout.write(`[v4.2.21.3.1-audio] starting ${call.moveId}\n`); const invocation = await invoke(args), exists = await access(call.transcriptPath).then(() => true, () => false), passed = invocation.code === 0 && invocation.signal === null && exists, failureMessage = passed ? null : `${invocation.stdout}\n${invocation.stderr}`.trim().slice(-4000); if (!passed) commonRequestFailure = true;
  const result = { moveId: call.moveId, expectedSpeaker: call.expectedSpeaker, status: passed ? "completed" : "request-failed", attemptCount: 1, retryCount: 0, startedAt, completedAt: new Date().toISOString(), elapsedMs: Date.now() - started, commandExitCode: invocation.code, terminationSignal: invocation.signal, transcriptWritten: exists, transcriptSha256: exists ? sha256(await readFile(call.transcriptPath)) : null, stdoutSha256: sha256(invocation.stdout), stderrSha256: sha256(invocation.stderr), failureMessage, durationSeconds: call.durationSeconds, estimatedCostUsd: passed ? Number((call.durationSeconds / 60 * manifest.costEstimate.pricePerMinuteUsd).toFixed(6)) : 0 }; results.push(result); process.stdout.write(`[v4.2.21.3.1-audio] ${call.moveId} ${result.status}\n`);
}
const attempted = results.filter((result) => result.attemptCount === 1), completed = results.filter((result) => result.status === "completed"), execution = { schemaVersion: "4.2.21.3.1-audio-recovery-model-execution", protocolId: manifest.protocolId, status: completed.length === manifest.calls.length ? "five-paid-diarized-audio-recovery-calls-completed" : "paid-diarized-audio-recovery-incomplete", callsPlanned: manifest.calls.length, callsAttempted: attempted.length, callsCompleted: completed.length, callsSkipped: results.filter((result) => result.attemptCount === 0).length, attempts: attempted.length, retries: 0, correctionCalls: 0, commonRequestFailure, results, estimatedSuccessfulProcessingCostUsd: Number(completed.reduce((sum, result) => sum + result.estimatedCostUsd, 0).toFixed(4)), newMaximumAuthorizedCostUsd: manifest.costEstimate.newMaximumCostUsd, conservativeCumulativeMaximumUsd: manifest.costEstimate.conservativeCumulativeMaximumUsd, meteredModelApiCostUsd: 0, authorization: { deterministicAudioAnalysis: true, retry: false, correctionCall: false, adjudicationPacketPreparation: false, scoreDerivation: false } };
await writeFile(manifest.artifacts.execution, `${JSON.stringify(execution, null, 2)}\n`); console.log(JSON.stringify({ status: execution.status, callsCompleted: execution.callsCompleted, callsAttempted: execution.callsAttempted, callsSkipped: execution.callsSkipped, retries: 0, estimatedSuccessfulProcessingCostUsd: execution.estimatedSuccessfulProcessingCostUsd, newMaximumAuthorizedCostUsd: execution.newMaximumAuthorizedCostUsd, meteredModelApiCostUsd: 0, scoresDerived: 0 }, null, 2));
