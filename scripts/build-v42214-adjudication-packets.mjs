#!/usr/bin/env node
import { mkdir, readFile, writeFile } from "node:fs/promises";
import path from "node:path";
import { assertV4 } from "./lib/v4-lean-production.mjs";
import { V4221_ROOT, buildV4221AdjudicationPacket, makeV4221AdjudicationSchema } from "./lib/v4221-pass-b-consensus.mjs";

const shouldWrite = process.argv.includes("--write"), sourceRoot = "docs/calibration/v4.2.21.2/disagreement-audio-prep", audioRoot = "docs/calibration/v4.2.21.3.1/audio-recovery", root = "docs/calibration/v4.2.21.4/adjudication";
const [sourceAnalysis, audioAnalysis, audioAudit] = await Promise.all([readFile(`${sourceRoot}/analysis.json`, "utf8").then(JSON.parse), readFile(`${audioRoot}/analysis.json`, "utf8").then(JSON.parse), readFile(`${audioRoot}/audio-verification.json`, "utf8").then(JSON.parse)]);
assertV4(sourceAnalysis.status === "deterministic-disagreements-and-audio-work-prepared" && audioAnalysis.status === "passed-all-five-medium-attribution-moves-audio-verified" && audioAnalysis.authorization.adjudicationPacketPreparation, "v4.2.21.4 adjudication preparation unauthorized");
const audioByMoveId = new Map(audioAudit.debates.flatMap((debate) => debate.moves.map((move) => [move.moveId, move]))), contexts = [];
let disputedMoves = 0, candidateSelections = 0, audioVerifiedMoves = 0;
for (const debateNumber of ["27", "188", "195"]) {
  const disagreementPath = `${sourceRoot}/disagreements/debate-${debateNumber}.json`, packetPath = `${V4221_ROOT}/packets/debate-${debateNumber}.json`, sourcePacketPath = `docs/calibration/v4.2.20/source-span-rendering/packets/debate-${debateNumber}.json`;
  const [disagreements, lockedPacket, sourcePacket] = await Promise.all([readFile(disagreementPath, "utf8").then(JSON.parse), readFile(packetPath, "utf8").then(JSON.parse), readFile(sourcePacketPath, "utf8").then(JSON.parse)]), events = JSON.parse(await readFile(sourcePacket.sourceChain.eventsPath));
  const built = buildV4221AdjudicationPacket(disagreements, lockedPacket, events, audioByMoveId), outputPacketPath = `${root}/packets/debate-${debateNumber}.json`, provenancePath = `${root}/provenance/debate-${debateNumber}.json`, outputPath = `${root}/outputs/debate-${debateNumber}.json`;
  const selections = built.packet.disputedMoves.reduce((sum, move) => sum + [move.candidates.attributionPair, move.candidates.responsePair, move.candidates.charityPair, move.candidates.assessmentConfidencePair].filter(Boolean).length + Object.keys(move.candidates.scoringFields).length, built.packet.burdenAdjustmentDisputes.length), audioCount = built.packet.disputedMoves.filter((move) => move.evidence.audioVerification !== null).length;
  if (shouldWrite) { await mkdir(path.dirname(path.resolve(outputPacketPath)), { recursive: true }); await mkdir(path.dirname(path.resolve(provenancePath)), { recursive: true }); await writeFile(path.resolve(outputPacketPath), `${JSON.stringify(built.packet, null, 2)}\n`); await writeFile(path.resolve(provenancePath), `${JSON.stringify({ schemaVersion: "4.2.21.4-adjudication-candidate-provenance", protocolId: built.packet.protocolId, debateNumber, modelInput: false, mappings: built.provenance }, null, 2)}\n`); }
  contexts.push({ debateNumber, debateId: built.packet.debateId, packet: outputPacketPath, provenance: provenancePath, output: outputPath, disputeSource: disagreementPath, lockedPacket: packetPath, sourcePacket: sourcePacketPath, originalEvents: sourcePacket.sourceChain.eventsPath, disputedMoves: built.packet.disputedMoves.length, candidateSelections: selections, audioVerifiedMoves: audioCount, packetBytes: Buffer.byteLength(`${JSON.stringify(built.packet)}\n`) }); disputedMoves += built.packet.disputedMoves.length; candidateSelections += selections; audioVerifiedMoves += audioCount;
}
const preparation = { schemaVersion: "4.2.21.4-dispute-only-adjudication-preparation", protocolId: "v4.2.21-source-span-consensus", status: shouldWrite ? "prepared-three-dispute-only-adjudication-contexts" : "preview", calibrationOnly: true, AIOnly: true, model: { label: "5.6 Sol", slug: "gpt-5.6-sol", reasoningEffort: "high" }, inputs: { rubricBase: "docs/reassessment-rubric-v4.0.md", rubricDerivedScores: "docs/reassessment-rubric-v4.0.1.md", rubricBounded: "docs/reassessment-rubric-v4.1.md", workflow: "docs/assessment-workflow-v4.2.21.md", manual: `${root}/manual.md`, schema: `${root}/adjudication.schema.json` }, contexts, evidenceBoundary: { disputedFieldsOnly: true, candidateOrderingAnonymizedPerPair: true, provenanceFilesNeverModelInputs: true, initialPassRationalesUnavailable: true, nondisputedFieldsUnavailable: true, fullInitialOutputsUnavailable: true, calculatedScoresUnavailable: true, winnersUnavailable: true, publicationProseUnavailable: true, verifiedAudioEmbeddedWhereRequired: true }, totals: { contexts: contexts.length, disputedMoves, candidateSelections, audioVerifiedMoves, modelContextsExecuted: 0, scoresDerived: 0, meteredApiCostUsd: 0, transcriptionCostUsdThisStage: 0 }, authorization: { executionManifest: true, adjudicationModelExecution: false, finalLedgerAssembly: false, scoreDerivation: false, productionMutation: false, all195Debates: false } };
if (shouldWrite) { await mkdir(path.resolve(root), { recursive: true }); await writeFile(path.resolve(root, "adjudication.schema.json"), `${JSON.stringify(makeV4221AdjudicationSchema(), null, 2)}\n`); await writeFile(path.resolve(root, "preparation-manifest.json"), `${JSON.stringify(preparation, null, 2)}\n`); }
console.log(JSON.stringify({ status: preparation.status, debates: contexts.map((context) => ({ debateNumber: context.debateNumber, disputedMoves: context.disputedMoves, candidateSelections: context.candidateSelections, audioVerifiedMoves: context.audioVerifiedMoves, packetBytes: context.packetBytes })), totals: preparation.totals, adjudicationModelExecutionAuthorized: false }, null, 2));
