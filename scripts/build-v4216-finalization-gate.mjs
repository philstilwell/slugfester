#!/usr/bin/env node
import { mkdir, readFile, stat, writeFile } from "node:fs/promises";
import { assertV4 } from "./lib/v41-lean-production.mjs";
import { closeV4216LengthSchema, V4216_DEBATES, V4216_PROTOCOL_ID, V4216_ROOT } from "./lib/v4216-finalization-gate.mjs";
const shouldWrite = process.argv.includes("--write"), authorization = JSON.parse(await readFile("docs/calibration/v4.2.15/deterministic-prose-normalization/analysis.json", "utf8")), priorManifest = JSON.parse(await readFile("docs/calibration/v3.8.8/reconstruction/schema-compatibility-recovery/execution-manifest.json", "utf8")); assertV4(authorization.authorization.threeDebateFinalizationGatePreparation && priorManifest.contexts.length === 3, "v4.2.16 gate preparation unavailable");
const sharedInputs = { workflow: "docs/assessment-workflow-v4.0.md", rubric: "docs/reassessment-rubric-v3.8.4.md", manual: `${V4216_ROOT}/manual.md` }, contexts = [];
for (const debateNumber of V4216_DEBATES) { const prior = priorManifest.contexts.find((item) => item.debateNumber === debateNumber), sourceSchema = JSON.parse(await readFile(prior.schema, "utf8")), schemaPath = `${V4216_ROOT}/schemas/debate-${debateNumber}.json`; if (shouldWrite) { await mkdir(`${V4216_ROOT}/schemas`, { recursive: true }); await writeFile(schemaPath, `${JSON.stringify(closeV4216LengthSchema(sourceSchema), null, 2)}\n`); } const inputs = { ...sharedInputs, packet: prior.packet, schema: schemaPath }, inputBytes = (await Promise.all(Object.values(inputs).map((file) => stat(file).then((entry) => entry.size)))).reduce((sum, value) => sum + value, 0); contexts.push({ debateNumber, debateId: prior.debateId, inputs, validationGold: debateNumber === "55" ? "docs/calibration/v3.8.8/reconstruction/validated-outputs/debate-55.json" : `docs/calibration/v3.8.8/reconstruction/outputs/debate-${debateNumber}.json`, rawOutput: `${V4216_ROOT}/raw/debate-${debateNumber}.json`, normalizedOutput: `${V4216_ROOT}/normalized/debate-${debateNumber}.json`, inputBytes }); }
const preparation = { schemaVersion: "4.2.16-three-debate-finalization-preparation", protocolId: V4216_PROTOCOL_ID, status: shouldWrite ? "prepared-three-retired-finalization-contexts" : "preview", developmentOnly: true, AIOnly: true, debates: V4216_DEBATES, model: { label: "5.6 Sol", slug: "gpt-5.6-sol", reasoningEffort: "low", authentication: "ChatGPT subscription", meteredApiCostUsdMaximum: 0 }, sourceBoundary: { completeSourcePacketsDelivered: 3, rawTranscriptsDuplicated: false, rawEventsDuplicated: false, participantJudgmentAlreadyClosed: true, scoresLockedFromPackets: true }, contexts, policy: { attemptsPerDebate: 1, retries: 0, correctionContexts: 0, timeoutMsPerDebate: 600000, maximumMinutesPerDebate: 4.5, maximumMeanMinutes: 4, deterministicCritiqueNormalization: true, scoresDerived: false }, totals: { contexts: contexts.length, inputBytes: contexts.reduce((sum, context) => sum + context.inputBytes, 0), modelContextsExecuted: 0, meteredApiCostUsd: 0, transcriptionCostUsd: 0 }, authorization: { executionManifest: false, modelExecution: false, newDisjointFreshJudgmentGatePreparation: false, scoring: false, productionMutation: false } };
if (shouldWrite) await writeFile(`${V4216_ROOT}/preparation-manifest.json`, `${JSON.stringify(preparation, null, 2)}\n`); console.log(JSON.stringify({ status: preparation.status, debates: preparation.debates, contexts: contexts.length, totalInputBytes: preparation.totals.inputBytes, maximumMinutesPerDebate: preparation.policy.maximumMinutesPerDebate, maximumMeanMinutes: preparation.policy.maximumMeanMinutes, correctionContexts: 0, scoresDerived: 0, meteredApiCostUsdMaximum: 0 }, null, 2));
