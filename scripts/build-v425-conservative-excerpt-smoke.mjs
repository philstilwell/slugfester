#!/usr/bin/env node

import { createHash } from "node:crypto";
import { mkdir, readFile, stat, writeFile } from "node:fs/promises";
import path from "node:path";
import { assertV4 } from "./lib/v41-lean-production.mjs";
import { V425_EXCERPT_MAXIMUM_CHARACTERS, V425_MODEL, V425_OUTPUT_VERSION, V425_PACKET_VERSION, V425_PROTOCOL_ID, V425_ROOT, makeV425PrimarySchema, validateV425SourceLedger } from "./lib/v425-conservative-excerpt.mjs";

const root = process.cwd(); const shouldWrite = process.argv.includes("--write"); const priorRoot = "docs/calibration/v4.2.4/screened-chronology-fresh-six-gate";
const [priorFailure, priorPacket] = await Promise.all([readFile(path.resolve(root, priorRoot, "primary-failure-analysis.json"), "utf8").then(JSON.parse), readFile(path.resolve(root, priorRoot, "packets/debate-131.json"), "utf8").then(JSON.parse)]);
assertV4(priorFailure.status === "failed-token-ceiling-despite-character-schema-compliance" && priorFailure.failedContext.debateNumber === "131" && priorFailure.disposition.tighterEndpointCharacterCeilingDevelopmentAuthorized, "v4.2.4 excerpt diagnostic unavailable");
const [eventsBytes, transcriptBytes, manifestBytes, ledgerBytes] = await Promise.all([readFile(priorPacket.sourceChain.eventsPath), readFile(priorPacket.sourceChain.transcriptPath), readFile(priorPacket.sourceChain.localManifestPath), readFile(priorPacket.transportChain.sourceLedgerPath)]);
const sha256 = (value) => createHash("sha256").update(value).digest("hex"); const ledgerValidation = validateV425SourceLedger(ledgerBytes, JSON.parse(eventsBytes), priorPacket.transportChain.sourceLedgerSha256);
const packet = { ...priorPacket, schemaVersion: V425_PACKET_VERSION, protocolId: V425_PROTOCOL_ID, sourceChain: { ...priorPacket.sourceChain, transcriptSha256: sha256(transcriptBytes), eventsSha256: sha256(eventsBytes), localManifestSha256: sha256(manifestBytes) }, modelInputBoundary: { ...priorPacket.modelInputBoundary, excerptMaximumCharacters: V425_EXCERPT_MAXIMUM_CHARACTERS, failedV424OutputDeliveredToModel: false, automaticExcerptTruncationAuthorized: false } };
const packetPath = `${V425_ROOT}/packet.json`; const schemaPath = `${V425_ROOT}/schema.json`; const inputs = { rubricBase: "docs/reassessment-rubric-v4.0.md", rubricDerivedScores: "docs/reassessment-rubric-v4.0.1.md", rubricBounded: "docs/reassessment-rubric-v4.1.md", manual: `${V425_ROOT}/manual.md`, schema: schemaPath };
if (shouldWrite) { await mkdir(path.resolve(root, V425_ROOT), { recursive: true }); await writeFile(path.resolve(root, packetPath), `${JSON.stringify(packet, null, 2)}\n`); await writeFile(path.resolve(root, schemaPath), `${JSON.stringify(makeV425PrimarySchema(), null, 2)}\n`); }
const copiedFiles = [...Object.values(inputs), packetPath, packet.transportChain.sourceLedgerPath]; const copiedBytes = {};
for (const file of copiedFiles) copiedBytes[file] = file === packetPath && !shouldWrite ? Buffer.byteLength(`${JSON.stringify(packet, null, 2)}\n`) : file === schemaPath && !shouldWrite ? Buffer.byteLength(`${JSON.stringify(makeV425PrimarySchema(), null, 2)}\n`) : (await stat(path.resolve(root, file))).size;
const totalCopiedInputBytes = Object.values(copiedBytes).reduce((sum, value) => sum + value, 0);
const preparation = { schemaVersion: "4.2.5-conservative-excerpt-smoke-preparation", protocolId: V425_PROTOCOL_ID, status: shouldWrite ? "prepared-retired-conservative-excerpt-smoke" : "preview", developmentOnly: true, AIOnly: true, dyadicOnly: true, debate: { debateNumber: "131", debateId: packet.debateId, durationSeconds: packet.durationSeconds, priorFailure: `${priorRoot}/primary-failure-analysis.json`, priorRawOutputExcluded: `${priorRoot}/primary-outputs/debate-131.json`, packet: packetPath, sourceLedger: packet.transportChain.sourceLedgerPath, originalTranscript: packet.sourceChain.transcriptPath, originalEvents: packet.sourceChain.eventsPath, originalManifest: packet.sourceChain.localManifestPath, rawOutput: `${V425_ROOT}/primary-output.json`, compiledOutput: `${V425_ROOT}/primary-compiled.json` }, model: { label: V425_MODEL.label, slug: V425_MODEL.slug, reasoningEffort: V425_MODEL.primaryReasoningEffort, authentication: "ChatGPT subscription", meteredApiCostUsdMaximum: 0 }, inputs, transport: { sourceLedgerReplayExact: ledgerValidation.replayExact, sourceLedgerSha256: packet.transportChain.sourceLedgerSha256, sourceLedgerEvents: ledgerValidation.eventCount, completeSourceTextDeliveredOnce: true, duplicatePlainTranscriptDelivered: false, originalEventsDelivered: false, totalCopiedInputBytes, copiedFiles, copiedBytes }, excerptChange: { priorEndpointMaximumCharacters: 600, endpointMaximumCharacters: V425_EXCERPT_MAXIMUM_CHARACTERS, tokenRangeRetained: [12, 100], lexicalRecallRetained: 0.8, orderedCoverageRetained: 0.8, automaticTruncationAuthorized: false, judgmentAnchorsChanged: false }, totals: { debates: 1, modelContextsExecuted: 0, meteredApiCostUsd: 0, transcriptionApiCalls: 0, transcriptionCostUsd: 0 }, authorization: { deterministicFixtures: true, executionManifest: false, primaryModelExecution: false, scoreDerivation: false, legacyComparison: false, freshGateSelection: false, productionMutation: false } };
if (shouldWrite) await writeFile(path.resolve(root, V425_ROOT, "preparation-manifest.json"), `${JSON.stringify(preparation, null, 2)}\n`);
console.log(JSON.stringify({ status: preparation.status, outputSchemaVersion: V425_OUTPUT_VERSION, debateNumber: "131", sourceLedgerEvents: ledgerValidation.eventCount, totalCopiedInputBytes, endpointMaximumCharacters: V425_EXCERPT_MAXIMUM_CHARACTERS, tokenRange: [12, 100], automaticTruncationAuthorized: false, modelContextsExecuted: 0, meteredApiCostUsd: 0 }, null, 2));
