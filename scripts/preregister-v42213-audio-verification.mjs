#!/usr/bin/env node
import { createHash } from "node:crypto";
import { execFileSync } from "node:child_process";
import { access, mkdir, readFile, writeFile } from "node:fs/promises";
import path from "node:path";
import { assertV4 } from "./lib/v4-lean-production.mjs";
import { V416_AUDIO_THRESHOLDS } from "./lib/v416-audio-verification.mjs";

const shouldWrite = process.argv.includes("--write"), frozenIndex = process.argv.indexOf("--frozen-at"), frozenAt = frozenIndex >= 0 ? process.argv[frozenIndex + 1] : null;
assertV4(frozenAt && !Number.isNaN(Date.parse(frozenAt)), "--frozen-at requires an ISO timestamp");
const root = process.cwd(), prepRoot = "docs/calibration/v4.2.21.2/disagreement-audio-prep", stageRoot = "docs/calibration/v4.2.21.3/audio-verification", executionPath = `${stageRoot}/model-execution.json`, auditPath = `${stageRoot}/audio-verification.json`, analysisPath = `${stageRoot}/analysis.json`, manifestPath = `${stageRoot}/execution-manifest.json`, mediaRoot = "output/transcribe/v42212-audio-verification/debate-27", ffmpeg = "/opt/homebrew/bin/ffmpeg";
if (shouldWrite) for (const file of [manifestPath, executionPath, auditPath, analysisPath]) await access(file).then(() => { throw new Error(`${file} already exists`); }, () => true);
const [analysis, sourcePreparation] = await Promise.all([readFile(`${prepRoot}/analysis.json`, "utf8").then(JSON.parse), readFile(`${prepRoot}/audio-source-preparation.json`, "utf8").then(JSON.parse)]);
assertV4(analysis.authorization.audioSourcePreparation && sourcePreparation.status === "prepared-five-local-audio-clips" && sourcePreparation.authorization.audioVerificationManifest, "v4.2.21.3 audio verification manifest unauthorized");
const referenceDirectory = path.resolve(root, mediaRoot, "references"); await mkdir(referenceDirectory, { recursive: true });
const references = [{ speaker: "Frank Turek", startSeconds: 450, durationSeconds: 12, file: path.join(referenceDirectory, "frank-turek.mp3") }, { speaker: "Alex O'Connor", startSeconds: 1405, durationSeconds: 12, file: path.join(referenceDirectory, "alex-oconnor.mp3") }];
for (const reference of references) execFileSync(ffmpeg, ["-nostdin", "-hide_banner", "-loglevel", "error", "-y", "-ss", String(reference.startSeconds), "-i", path.resolve(root, sourcePreparation.source.sourceAudio), "-t", String(reference.durationSeconds), "-vn", "-ac", "1", "-ar", "16000", "-b:a", "64k", reference.file]);
const sha256 = (value) => createHash("sha256").update(value).digest("hex");
for (const reference of references) { reference.localPath = path.relative(root, reference.file); reference.sha256 = sha256(await readFile(reference.file)); delete reference.file; }
const calls = sourcePreparation.clips.map((clip) => ({ debateNumber: clip.debateNumber, debateId: clip.debateId, moveId: clip.moveId, expectedSpeaker: clip.expectedSpeaker, verificationExcerpt: clip.verificationExcerpt, clipPath: clip.clipPath, clipSha256: clip.clipSha256, durationSeconds: clip.durationSeconds, transcriptPath: `${mediaRoot}/transcripts/${clip.moveId}.transcript.json`, model: "gpt-4o-transcribe-diarize", responseFormat: "diarized_json", chunkingStrategy: "auto", language: "en", knownSpeakers: references.map((reference) => ({ speaker: reference.speaker, localPath: reference.localPath, sha256: reference.sha256 })) }));
const pricePerMinuteUsd = 0.006, estimatedCostUsd = calls.reduce((sum, call) => sum + call.durationSeconds, 0) / 60 * pricePerMinuteUsd;
const sourceFiles = [`${prepRoot}/analysis.json`, `${prepRoot}/audio-work-items.json`, `${prepRoot}/audio-source-preparation.json`, "scripts/lib/v416-audio-verification.mjs", "scripts/preregister-v42213-audio-verification.mjs", "scripts/run-v42213-audio-verification.mjs", "scripts/analyze-v42213-audio-verification.mjs", "scripts/test-v42213-audio-verification.mjs", "/Users/philstilwell/.codex/skills/transcribe/scripts/transcribe_diarize.py", sourcePreparation.source.sourceAudio, ...calls.map((call) => call.clipPath), ...references.map((reference) => reference.localPath)];
const sourceHashes = {}; for (const file of [...new Set(sourceFiles)]) sourceHashes[file] = sha256(await readFile(file));
const manifest = { schemaVersion: "4.2.21.3-audio-verification-execution-manifest", protocolId: "v4.2.21-source-span-consensus", status: "frozen-five-paid-diarized-audio-calls-authorized", frozenAt, checkpointCommit: execFileSync("git", ["rev-parse", "HEAD"], { encoding: "utf8" }).trim(), calibrationOnly: true, model: "gpt-4o-transcribe-diarize", calls, thresholds: V416_AUDIO_THRESHOLDS, costEstimate: { pricingSource: "https://platform.openai.com/docs/pricing", pricePerMinuteUsd, clipMinutes: Number((calls.reduce((sum, call) => sum + call.durationSeconds, 0) / 60).toFixed(4)), estimatedCostUsd: Number(estimatedCostUsd.toFixed(4)), maximumCostUsd: 0.04, ChatGPTSubscriptionApplicable: false, OpenAIApiBillingRequired: true }, executionPolicy: { calls: 5, attemptsPerCall: 1, retriesMaximum: 0, sequentialExecution: true, continueIndependentCallsAfterFailure: true, chunkingStrategy: "auto", responseFormat: "diarized_json", knownSpeakerReferences: 2, transcriptsSavedLocally: true }, authorization: { paidTranscriptionExecution: true, deterministicAudioAnalysis: true, retry: false, correctionCall: false, adjudicationPacketPreparation: false, adjudicationModelExecution: false, scoreDerivation: false, productionMutation: false }, stopRules: { sourceHashMismatchBlocks: true, preexistingTranscriptBlocks: true, failedTranscriptPreserved: true, retryAuthorized: false, correctionAuthorized: false }, artifacts: { execution: executionPath, audit: auditPath, analysis: analysisPath, transcripts: calls.map((call) => call.transcriptPath) }, futureOutputPathsExcludedFromSourceHashes: [...calls.map((call) => call.transcriptPath), executionPath, auditPath, analysisPath], sourceHashes };
if (shouldWrite) { await mkdir(path.resolve(root, stageRoot), { recursive: true }); await writeFile(path.resolve(root, manifestPath), `${JSON.stringify(manifest, null, 2)}\n`); }
console.log(JSON.stringify({ status: shouldWrite ? "frozen" : "preview", calls: calls.length, clipMinutes: manifest.costEstimate.clipMinutes, model: manifest.model, estimatedCostUsd: manifest.costEstimate.estimatedCostUsd, maximumCostUsd: manifest.costEstimate.maximumCostUsd, ChatGPTSubscriptionApplicable: false, retries: 0, scoreDerivationAuthorized: false }, null, 2));
