#!/usr/bin/env node
import { createHash } from "node:crypto";
import { execFileSync } from "node:child_process";
import { access, readFile, writeFile } from "node:fs/promises";
import { assertV4 } from "./lib/v41-lean-production.mjs";
const ROOT = "docs/calibration/v4.2.13/compact-finalization-smoke", shouldWrite = process.argv.includes("--write"), frozenIndex = process.argv.indexOf("--frozen-at"), frozenAt = frozenIndex >= 0 ? process.argv[frozenIndex + 1] : null; assertV4(frozenAt && !Number.isNaN(Date.parse(frozenAt)), "--frozen-at requires ISO timestamp");
const manifestPath = `${ROOT}/execution-manifest.json`, executionPath = `${ROOT}/model-execution.json`, analysisPath = `${ROOT}/analysis.json`; if (shouldWrite) for (const file of [manifestPath, executionPath, analysisPath]) await access(file).then(() => { throw new Error(`${file} already exists`); }, () => true);
const preparation = JSON.parse(await readFile(`${ROOT}/preparation-manifest.json`, "utf8")); assertV4(preparation.status === "prepared-one-retired-finalization-smoke", "v4.2.13 preparation unavailable");
const sourceFiles = ["docs/assessment-workflow-v4.2.13.md", ...Object.values(preparation.inputs), ...Object.values(preparation.validationInputs), `${ROOT}/preparation-manifest.json`, "scripts/lib/v385-transport.mjs", "scripts/lib/v388-reconstruction.mjs", "scripts/lib/v41-lean-production.mjs", "scripts/validate-v388-reconstruction-output.mjs", "scripts/build-v4213-finalization-smoke.mjs", "scripts/test-v4213-finalization-smoke.mjs", "scripts/preregister-v4213-finalization-smoke.mjs", "scripts/run-v4213-finalization-smoke.mjs", "scripts/analyze-v4213-finalization-smoke.mjs", "src/data/references.js"];
const sha256 = (value) => createHash("sha256").update(value).digest("hex"), sourceHashes = {}; for (const file of [...new Set(sourceFiles)]) sourceHashes[file] = sha256(await readFile(file));
const manifest = { schemaVersion: "4.2.13-compact-finalization-execution-manifest", protocolId: preparation.protocolId, status: "frozen-one-retired-finalization-smoke-authorized", frozenAt, checkpointCommit: execFileSync("git", ["rev-parse", "HEAD"], { encoding: "utf8" }).trim(), developmentOnly: true, AIOnly: true, model: preparation.model, inputs: preparation.inputs, validationInputs: preparation.validationInputs, output: preparation.output, isolation: { freshTemporaryCodexHome: true, freshSourceDirectory: true, priorReconstructionUnavailable: true, legacyAssessmentUnavailable: true, otherDebatesUnavailable: true, productionObjectsUnavailable: true }, executionPolicy: { contexts: 1, attempts: 1, retries: 0, timeoutMs: preparation.policy.timeoutMs, runtimeThresholdMinutes: preparation.policy.runtimeThresholdMinutes, authentication: "ChatGPT subscription", APIKeysRemoved: true, meteredApiCostUsdMaximum: 0, transcriptionCostUsdMaximum: 0 }, authorization: { modelExecution: true, deterministicValidation: true, analysis: true, scoring: false, productionMutation: false }, artifacts: { execution: executionPath, analysis: analysisPath }, futureOutputPathsExcludedFromSourceHashes: [preparation.output, executionPath, analysisPath], sourceHashes };
if (shouldWrite) await writeFile(manifestPath, `${JSON.stringify(manifest, null, 2)}\n`); console.log(JSON.stringify({ status: shouldWrite ? "frozen" : "preview", debateNumber: "103", attempts: 1, retries: 0, timeoutMinutes: preparation.policy.timeoutMs / 60000, runtimeThresholdMinutes: preparation.policy.runtimeThresholdMinutes, meteredApiCostUsdMaximum: 0 }, null, 2));
