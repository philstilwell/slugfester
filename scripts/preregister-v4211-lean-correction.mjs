#!/usr/bin/env node
import { createHash } from "node:crypto";
import { execFileSync } from "node:child_process";
import { access, readFile, writeFile } from "node:fs/promises";
import { assertV4 } from "./lib/v41-lean-production.mjs";
import { V4211_ROOT } from "./lib/v4211-lean-correction.mjs";
const shouldWrite = process.argv.includes("--write"), frozenIndex = process.argv.indexOf("--frozen-at"), frozenAt = frozenIndex >= 0 ? process.argv[frozenIndex + 1] : null; assertV4(frozenAt && !Number.isNaN(Date.parse(frozenAt)), "--frozen-at requires ISO timestamp");
const manifestPath = `${V4211_ROOT}/execution-manifest.json`, executionPath = `${V4211_ROOT}/model-execution.json`, analysisPath = `${V4211_ROOT}/analysis.json`; if (shouldWrite) for (const file of [manifestPath, executionPath, analysisPath]) await access(file).then(() => { throw new Error(`${file} already exists`); }, () => true);
const preparation = JSON.parse(await readFile(`${V4211_ROOT}/preparation-manifest.json`, "utf8")); assertV4(preparation.status === "prepared-one-lean-structural-correction" && preparation.totals.inputReductionFraction > 0.9, "v4.2.11 preparation invalid");
const sourceFiles = ["docs/assessment-workflow-v4.2.11.md", ...Object.values(preparation.inputs), ...Object.values(preparation.validationInputs), `${V4211_ROOT}/preparation-manifest.json`, "scripts/lib/v4-lean-production.mjs", "scripts/lib/v41-lean-production.mjs", "scripts/lib/v417-fresh-validation.mjs", "scripts/lib/v418-source-integrity.mjs", "scripts/lib/v419-schema-bounded-source.mjs", "scripts/lib/v42-compact-transport.mjs", "scripts/lib/v421-compact-fresh.mjs", "scripts/lib/v422-chronology-first.mjs", "scripts/lib/v423-chronology-fresh.mjs", "scripts/lib/v424-screened-chronology-fresh.mjs", "scripts/lib/v425-conservative-excerpt.mjs", "scripts/lib/v426-retired-completion.mjs", "scripts/lib/v427-bounded-correction.mjs", "scripts/lib/v4211-lean-correction.mjs", "scripts/build-v4211-lean-correction.mjs", "scripts/test-v4211-lean-correction.mjs", "scripts/preregister-v4211-lean-correction.mjs", "scripts/run-v4211-lean-correction.mjs", "scripts/analyze-v4211-lean-correction.mjs"];
const sha256 = (value) => createHash("sha256").update(value).digest("hex"), sourceHashes = {}; for (const file of [...new Set(sourceFiles)]) sourceHashes[file] = sha256(await readFile(file));
const manifest = { schemaVersion: "4.2.11-lean-correction-execution-manifest", protocolId: preparation.protocolId, status: "frozen-one-lean-structural-correction-authorized", frozenAt, checkpointCommit: execFileSync("git", ["rev-parse", "HEAD"], { encoding: "utf8" }).trim(), developmentOnly: true, AIOnly: true, model: preparation.model, inputs: preparation.inputs, validationInputs: preparation.validationInputs, outputs: preparation.outputs, isolation: { freshTemporaryCodexHome: true, freshSourceDirectory: true, transcriptUnavailable: true, sourceExcerptsUnavailable: true, ratingsUnavailable: true, otherCorrectionUnavailable: true, legacyUnavailable: true, scoresUnavailable: true }, executionPolicy: { contexts: 1, attempts: 1, retries: 0, timeoutMs: preparation.policy.timeoutMs, authentication: "ChatGPT subscription", APIKeysRemoved: true, meteredApiCostUsdMaximum: 0, transcriptionCostUsdMaximum: 0 }, authorization: { modelExecution: true, fullValidation: true, runtimeProjection: true, scoring: false, productionMutation: false }, artifacts: { execution: executionPath, analysis: analysisPath, ...preparation.outputs }, futureOutputPathsExcludedFromSourceHashes: [...Object.values(preparation.outputs), executionPath, analysisPath], sourceHashes };
if (shouldWrite) await writeFile(manifestPath, `${JSON.stringify(manifest, null, 2)}\n`); console.log(JSON.stringify({ status: shouldWrite ? "frozen" : "preview", debateNumber: "106", attempts: 1, retries: 0, timeoutMinutes: preparation.policy.timeoutMs / 60000, inputBytes: preparation.totals.inputBytes, inputReductionFraction: preparation.totals.inputReductionFraction, meteredApiCostUsdMaximum: 0 }, null, 2));
