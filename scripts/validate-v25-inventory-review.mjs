#!/usr/bin/env node

import { createHash } from "node:crypto";
import { readFile } from "node:fs/promises";
import path from "node:path";

const [reviewArgument, gateArgument = "docs/calibration/v2.5/held-out-gate/gate-manifest.json"] = process.argv.slice(2);
if (!reviewArgument) { console.error("Usage: node scripts/validate-v25-inventory-review.mjs <review.json> [gate.json]"); process.exit(1); }
function assert(condition, message) { if (!condition) throw new Error(message); }
function sha256(value) { return createHash("sha256").update(value).digest("hex"); }
function exactKeys(value, expected, label) { assert(value && typeof value === "object" && !Array.isArray(value), `${label} must be object`); assert(JSON.stringify(Object.keys(value).sort()) === JSON.stringify([...expected].sort()), `${label} keys differ`); }
const reviewSource = await readFile(path.resolve(reviewArgument), "utf8"); const review = JSON.parse(reviewSource); const gateSource = await readFile(path.resolve(gateArgument), "utf8"); const gate = JSON.parse(gateSource); const debate = gate.sample.debates.find((item) => item.debateId === review.debateId); assert(debate, "review debate not preregistered");
exactKeys(review, ["schemaVersion", "workflowVersion", "gateId", "debateId", "debateNumber", "reviewedAt", "reviewerModel", "calibrationOnly", "isolation", "source", "findings", "repairs", "thirdReviewTrigger", "audit"], "review");
assert(review.schemaVersion === "2.5-inventory-review" && review.workflowVersion === gate.workflowVersion && review.gateId === gate.gateId && review.debateNumber === debate.number && review.reviewerModel === "5.6 Sol" && review.calibrationOnly === true, "review identity mismatch");
exactKeys(review.isolation, ["method", "legacyMaterialAccessed", "developmentExamplesAccessed", "annotationPassesAvailable", "statement"], "isolation"); assert(review.isolation.method === "fresh-independent-v2.5-inventory-review" && review.isolation.legacyMaterialAccessed === false && review.isolation.developmentExamplesAccessed === false && review.isolation.annotationPassesAvailable === false, "review isolation failed");
exactKeys(review.source, ["draftInventoryPath", "draftInventorySha256", "reviewedInventoryPath", "reviewedInventorySha256", "transcriptSha256", "eventsSha256", "manifestSha256", "workflowSha256", "inventorySchemaSha256", "gateManifestSha256"], "source");
const [draft, reviewed, transcript, events, manifest, workflow, schema] = await Promise.all([readFile(path.resolve(review.source.draftInventoryPath), "utf8"), readFile(path.resolve(review.source.reviewedInventoryPath), "utf8"), readFile(path.resolve(`.assessment-cache/captions/${debate.videoId}/transcript.txt`), "utf8"), readFile(path.resolve(`.assessment-cache/captions/${debate.videoId}/events.json`), "utf8"), readFile(path.resolve(`.assessment-cache/captions/${debate.videoId}/manifest.json`), "utf8"), readFile(path.resolve("docs/assessment-workflow-v2.5.md"), "utf8"), readFile(path.resolve("docs/calibration/v2.5/atomic-inventory-schema.json"), "utf8")]);
assert(review.source.draftInventorySha256 === sha256(draft) && review.source.reviewedInventorySha256 === sha256(reviewed) && review.source.transcriptSha256 === sha256(transcript) && review.source.eventsSha256 === sha256(events) && review.source.manifestSha256 === sha256(manifest) && review.source.workflowSha256 === sha256(workflow) && review.source.inventorySchemaSha256 === sha256(schema) && review.source.gateManifestSha256 === sha256(gateSource), "review source hash mismatch");
const findings = new Map(); for (const finding of review.findings) { exactKeys(finding, ["findingId", "category", "moveId", "description", "disposition"], `finding ${finding.findingId}`); assert(!findings.has(finding.findingId), `duplicate finding`); findings.set(finding.findingId, finding); }
const triggerCategories = new Set(); const repairs = new Set(); for (const repair of review.repairs) { exactKeys(repair, ["findingId", "change", "triggerCategory"], `repair ${repair.findingId}`); assert(findings.has(repair.findingId) && !repairs.has(repair.findingId), `invalid repair`); repairs.add(repair.findingId); if (repair.triggerCategory !== null) triggerCategories.add(repair.triggerCategory); }
for (const finding of review.findings.filter((item) => item.disposition === "repaired")) assert(repairs.has(finding.findingId), `missing repair ${finding.findingId}`);
exactKeys(review.thirdReviewTrigger, ["required", "categories", "rationale"], "thirdReviewTrigger"); assert(review.thirdReviewTrigger.required === (triggerCategories.size > 0), "third review trigger boolean mismatch"); assert(JSON.stringify([...review.thirdReviewTrigger.categories].sort()) === JSON.stringify([...triggerCategories].sort()), "third review categories mismatch");
exactKeys(review.audit, ["sourceFidelityViolations", "atomicityViolations", "targetPacketViolations", "burdenRouteViolations", "samplingViolations", "speakerAttributionViolations", "unresolvedFindings", "reviewedInventoryValidatorPassed"], "audit"); for (const key of Object.keys(review.audit).filter((key) => key !== "reviewedInventoryValidatorPassed")) assert(review.audit[key] === 0, `${key} must be zero`); assert(review.audit.reviewedInventoryValidatorPassed === true, "reviewed inventory validator not passed");
console.log(JSON.stringify({ status: "passed", debateId: review.debateId, findingCount: review.findings.length, repairCount: review.repairs.length, thirdReviewRequired: review.thirdReviewTrigger.required, triggerCategories: review.thirdReviewTrigger.categories, reviewSha256: sha256(reviewSource) }, null, 2));
