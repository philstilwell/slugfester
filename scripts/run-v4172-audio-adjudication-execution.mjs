#!/usr/bin/env node

import { createHash } from "node:crypto";
import { spawn } from "node:child_process";
import { access, copyFile, mkdtemp, readFile, rm, writeFile } from "node:fs/promises";
import os from "node:os";
import path from "node:path";
import { classifyTransportEventCount, extractTransportEvents } from "./lib/v385-transport.mjs";
import { assertV4 } from "./lib/v41-lean-production.mjs";
import { V4172_AUDIO_ADJ_ROOT } from "./lib/v4172-audio-adjudication.mjs";
const root = process.cwd(); const manifest = JSON.parse(await readFile(path.resolve(root, `${V4172_AUDIO_ADJ_ROOT}/execution-manifest.json`), "utf8")); const codex = "/Applications/ChatGPT.app/Contents/Resources/codex"; const authSource = path.join(os.homedir(), ".codex", "auth.json"); const sha256 = (value) => createHash("sha256").update(value).digest("hex");
assertV4(manifest.status === "frozen-endpoint-compatible-audio-adjudication-authorized" && manifest.authorization.modelExecution, "v4.1.7.2 audio adjudication unauthorized");
for (const [file, hash] of Object.entries(manifest.sourceHashes)) assertV4(sha256(await readFile(path.resolve(root, file))) === hash, `source hash mismatch: ${file}`);
for (const future of manifest.futureOutputPathsExcludedFromSourceHashes) await access(path.resolve(root, future)).then(() => { throw new Error(`future output already exists: ${future}`); }, () => true); await access(codex); await access(authSource);
function run(command, args, options, timeoutMs) { return new Promise((resolve) => { const child = spawn(command, args, { ...options, stdio: ["ignore", "pipe", "pipe"] }); let stdout = ""; let stderr = ""; let timedOut = false; let forceTimer = null; child.stdout.on("data", (chunk) => { stdout += chunk; }); child.stderr.on("data", (chunk) => { stderr += chunk; }); const timer = setTimeout(() => { timedOut = true; child.kill("SIGTERM"); forceTimer = setTimeout(() => child.kill("SIGKILL"), 5000); }, timeoutMs); child.on("close", (code, signal) => { clearTimeout(timer); if (forceTimer) clearTimeout(forceTimer); resolve({ code, signal, stdout, stderr, timedOut }); }); }); }
const temporary = await mkdtemp(path.join(os.tmpdir(), "slugfester-v4172-audio-adj-")); const temporaryCodexHome = await mkdtemp(path.join(os.tmpdir(), "slugfester-v4172-home-audio-adj-")); const startedAt = new Date().toISOString(); const started = Date.now(); let record;
try { const copies = [[manifest.context.workflow, "workflow.md"], [manifest.context.manual, "manual.md"], [manifest.context.schema, "schema.json"], [manifest.context.packet, "packet.json"], ...manifest.context.rawDiarizedTranscripts.map((source, index) => [source, `audio-transcript-${index + 1}.json`])]; for (const [source, target] of copies) await copyFile(path.resolve(root, source), path.join(temporary, target)); await copyFile(authSource, path.join(temporaryCodexHome, "auth.json")); const environment = { ...process.env, CODEX_HOME: temporaryCodexHome }; delete environment.OPENAI_API_KEY; delete environment.OPENAI_ORG_ID; delete environment.CODEX_API_KEY; const prompt = `Read workflow.md, manual.md, schema.json, packet.json, audio-transcript-1.json, and audio-transcript-2.json completely and no other files. Act only as the isolated 5.6 Sol/high audio-attribution adjudicator for Debate 91. Decide only whether the expected speaker authored each locked core proposition. Cite exact diarized segment indexes. Ratings, scores, legacy data, other debates, and publication prose are unavailable. Return exactly one schema-conforming JSON object and no commentary.`; const invocation = await run(codex, ["exec", "--ephemeral", "--skip-git-repo-check", "--ignore-user-config", "--ignore-rules", "--model", manifest.model.slug, "-c", `model_reasoning_effort=\"${manifest.model.reasoningEffort}\"`, "--disable", "plugins", "--disable", "remote_plugin", "--disable", "skill_search", "--disable", "apps", "--disable", "memories", "--disable", "multi_agent", "--disable", "browser_use", "--disable", "computer_use", "--disable", "workspace_dependencies", "--sandbox", "read-only", "--output-schema", "schema.json", "--output-last-message", "result.json", prompt], { cwd: temporary, env: environment }, manifest.executionPolicy.perInvocationTimeoutMs); const events = extractTransportEvents(invocation.stderr); const classification = classifyTransportEventCount(events.length, 2, 8); const base = { debateNumber: manifest.context.debateNumber, disputedMoves: manifest.context.disputedMoves, model: manifest.model.label, reasoningEffort: manifest.model.reasoningEffort, attemptCount: 1, retryCount: 0, startedAt, completedAt: new Date().toISOString(), elapsedMs: Date.now() - started, timedOut: invocation.timedOut, terminationSignal: invocation.signal, commandExitCode: invocation.code, authentication: "ChatGPT subscription", apiKeysRemoved: true, meteredApiCostUsd: 0, paidTranscriptionCalls: 0, recoverableStreamEvents: events.length, transportClassification: classification, stdoutSha256: sha256(invocation.stdout), stderrSha256: sha256(invocation.stderr), stderrTail: invocation.stderr.trim().slice(-6000) || null };
  if (invocation.timedOut || invocation.code !== 0 || invocation.signal !== null) record = { ...base, status: invocation.timedOut ? "timed-out" : "transport-failed", outputWritten: false, deterministicValidationPassed: false, gateAcceptancePassed: false };
  else { await copyFile(path.join(temporary, "result.json"), path.resolve(root, manifest.context.output)); const validation = await run(process.execPath, ["scripts/validate-v4172-audio-adjudication-output.mjs", manifest.context.output, manifest.context.packet], { cwd: root, env: process.env }, 120000); const valid = validation.code === 0 && classification !== "invalid"; record = { ...base, status: valid ? `completed-valid-${classification}` : validation.code !== 0 ? "output-validation-failed" : "transport-event-limit-exceeded", outputWritten: true, outputSha256: sha256(await readFile(path.resolve(root, manifest.context.output))), deterministicValidationPassed: validation.code === 0, gateAcceptancePassed: valid, validationSummary: validation.code === 0 ? JSON.parse(validation.stdout) : null, validationMessage: validation.code === 0 ? null : `${validation.stdout}\n${validation.stderr}`.trim().slice(-6000) }; }
} finally { await rm(temporary, { recursive: true, force: true }); await rm(temporaryCodexHome, { recursive: true, force: true }); }
const execution = { schemaVersion: "4.1.7.2-audio-adjudication-model-execution", protocolId: manifest.protocolId, status: record.gateAcceptancePassed ? "audio-adjudication-execution-passed" : "audio-adjudication-execution-failed", contexts: 1, attempts: 1, retries: 0, meteredApiCostUsd: 0, paidTranscriptionCalls: 0, result: record, authorization: { analysis: record.gateAcceptancePassed, furtherRetry: false, disagreementExtraction: false, scoreDerivation: false, legacyComparison: false, productionMutation: false } }; await writeFile(path.resolve(root, manifest.artifacts.execution), `${JSON.stringify(execution, null, 2)}\n`); console.log(JSON.stringify({ status: execution.status, elapsedMinutes: Number((record.elapsedMs / 60000).toFixed(2)), attempts: 1, retries: 0, deterministicValidationPassed: record.deterministicValidationPassed, meteredApiCostUsd: 0, paidTranscriptionCalls: 0 }, null, 2)); assertV4(record.gateAcceptancePassed, "v4.1.7.2 audio adjudication failed; downstream work is blocked");
