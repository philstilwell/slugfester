#!/usr/bin/env node
import { readFile, writeFile } from "node:fs/promises";
import { assertV4, canonicalJson } from "./lib/v41-lean-production.mjs";
import { compileV426PrimaryOutput, evaluateV426PrimaryTiming, validateV426PrimaryOutput } from "./lib/v426-retired-completion.mjs";

const ROOT = "docs/calibration/v4.2.10/integrated-long-context-primary", shouldWrite = process.argv.includes("--write");
const [manifest, execution, discovery, anchorExecution, priorExecution, correctionExecution, continuationExecution] = await Promise.all([readFile(`${ROOT}/execution-manifest.json`, "utf8").then(JSON.parse), readFile(`${ROOT}/model-execution.json`, "utf8").then(JSON.parse), readFile("docs/calibration/v4.2.9.2/adaptive-long-context-continuation/model-execution.json", "utf8").then(JSON.parse), readFile("docs/calibration/v4.2.5/conservative-excerpt-smoke/model-execution.json", "utf8").then(JSON.parse), readFile("docs/calibration/v4.2.6/conservative-excerpt-retired-completion/model-execution.json", "utf8").then(JSON.parse), readFile("docs/calibration/v4.2.7/bounded-primary-correction/model-execution.json", "utf8").then(JSON.parse), readFile("docs/calibration/v4.2.8/correction-aware-retired-continuation/model-execution.json", "utf8").then(JSON.parse)]);
assertV4(execution.authorization.analysis, "v4.2.10 analysis unauthorized");
let validation = null, compilerReplayPassed = false;
if (execution.result.accepted) { const [output, compiled, packet, eventsBytes, ledgerBytes] = await Promise.all([readFile(manifest.output, "utf8").then(JSON.parse), readFile(manifest.compiledOutput, "utf8").then(JSON.parse), readFile(manifest.inputs.packet, "utf8").then(JSON.parse), readFile(manifest.source.originalEvents), readFile(manifest.source.fullLedger)]); validation = validateV426PrimaryOutput(output, packet, JSON.parse(eventsBytes), eventsBytes, ledgerBytes); compilerReplayPassed = canonicalJson(compileV426PrimaryOutput(output, packet, JSON.parse(eventsBytes))) === canonicalJson(compiled); assertV4(compilerReplayPassed, "v4.2.10 compiler replay failed"); }
let timing = null, correctionAdjustedProjection = null, runtimePassed = false;
if (execution.result.accepted) {
  const successfulDiscoveryMs = 120366 + discovery.results.reduce((sum, result) => sum + result.elapsedMs, 0), debate99ElapsedMs = successfulDiscoveryMs + execution.result.elapsedMs;
  const observations = [anchorExecution.result, priorExecution.results[0], ...continuationExecution.results.filter((result) => result.debateNumber !== "99")].map((result) => ({ debateNumber: result.debateNumber, gateAcceptancePassed: true, elapsedMs: result.elapsedMs, recoverableStreamEvents: result.recoverableStreamEvents ?? 0 }));
  observations.splice(3, 0, { debateNumber: "99", gateAcceptancePassed: true, elapsedMs: debate99ElapsedMs, recoverableStreamEvents: execution.result.recoverableStreamEvents });
  assertV4(observations.length === 6, "six timing observations required");
  timing = evaluateV426PrimaryTiming(observations);
  const correctionMinutes = correctionExecution.result.elapsedMs / 60000, centralCorrectionHours = (1 / 6) * correctionMinutes * 195 / 60, conservativeCorrectionHours = 0.25 * correctionMinutes * 1.25 * 195 / 60;
  correctionAdjustedProjection = { debate99SuccessfulLaneMinutes: Number((debate99ElapsedMs / 60000).toFixed(2)), central: { baseHours: timing.centralProjection.hours.total, correctionHours: Number(centralCorrectionHours.toFixed(2)), totalHours: Number((timing.centralProjection.hours.total + centralCorrectionHours).toFixed(2)) }, conservative: { baseHours: timing.conservativeProjection.hours.total, correctionHours: Number(conservativeCorrectionHours.toFixed(2)), totalHours: Number((timing.conservativeProjection.hours.total + conservativeCorrectionHours).toFixed(2)) } };
  runtimePassed = timing.timingEligible && correctionAdjustedProjection.central.totalHours <= 52 && correctionAdjustedProjection.conservative.totalHours <= 60;
}
const passed = execution.result.accepted && runtimePassed;
const analysis = { schemaVersion: "4.2.10-integrated-primary-analysis", protocolId: manifest.protocolId, status: !execution.result.accepted ? "integrated-primary-failed" : passed ? "retired-six-reliability-passed-fresh-gate-preparation-authorized" : "retired-six-reliability-failed-correction-adjusted-runtime", debateNumber: "99", developmentOnly: true, distributedCoverage: { completeSourceDiscovery: true, integratedJudgeFullLedgerAccess: false, validatedCandidateContexts: manifest.sparseContext.deliveredEvents, limitationVisible: true }, validation, compilerReplayPassed, timing, correctionAdjustedProjection, totals: { retiredDebatesFinallyValidated: execution.result.accepted ? 6 : 5, integratedPrimaryAttempts: 1, integratedPrimaryRetries: 0, scoresDerived: 0, meteredApiCostUsd: 0, transcriptionCostUsd: 0 }, authorization: { newDisjointFreshGatePreparation: passed, integratedPrimaryRetry: false, scoreDerivation: false, productionMutation: false, all195Debates: false } };
if (shouldWrite) await writeFile(manifest.artifacts.analysis, `${JSON.stringify(analysis, null, 2)}\n`);
console.log(JSON.stringify({ status: analysis.status, accepted: execution.result.accepted, compilerReplayPassed, debate99SuccessfulLaneMinutes: correctionAdjustedProjection?.debate99SuccessfulLaneMinutes ?? null, projected195CentralHours: correctionAdjustedProjection?.central.totalHours ?? null, projected195ConservativeHours: correctionAdjustedProjection?.conservative.totalHours ?? null, freshGatePreparationAuthorized: passed, scoresDerived: 0, meteredApiCostUsd: 0 }, null, 2));
