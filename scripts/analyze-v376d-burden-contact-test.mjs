#!/usr/bin/env node

import { readFile, writeFile } from "node:fs/promises";
import path from "node:path";
import { sha256 } from "./lib/v36-decision-cards.mjs";
import { assert } from "./lib/v376d-burden-contact.mjs";
import { V376D_EXECUTION_MANIFEST, adjudicationV376DOption, matchesV376DProvisionalReference, readV376DJson, v376dSemanticWinner } from "./lib/v376d-execution.mjs";

const root = process.cwd(), shouldWrite = process.argv.includes("--write"), read = (file) => readFile(path.resolve(root, file), "utf8"), manifestText = await read(V376D_EXECUTION_MANIFEST), manifest = JSON.parse(manifestText), initialText = await read(manifest.artifacts.initialExecution), initial = JSON.parse(initialText), disagreementText = await read(manifest.artifacts.initialDisagreements), disagreement = JSON.parse(disagreementText), adjudicationText = await read(manifest.artifacts.adjudicationExecution), adjudication = JSON.parse(adjudicationText), sealedText = await read(manifest.sealedOptionMap.path), sealed = JSON.parse(sealedText), adjudicationMapText = await read(manifest.artifacts.adjudicationOptionMap), adjudicationMap = JSON.parse(adjudicationMapText);
const adjudicationOutputs = {};
for (const context of disagreement.adjudicationContexts) { try { adjudicationOutputs[context.debateNumber] = await readV376DJson(root, context.output); } catch { adjudicationOutputs[context.debateNumber] = null; } }
const finalBundles = disagreement.comparisons.map((comparison) => {
  const votes = [comparison.passA, comparison.passB]; let passC = null;
  if (!comparison.agreed) { const choice = adjudicationOutputs[comparison.debateNumber]?.bundles?.find((item) => item.bundleId === comparison.bundleId); if (choice) { passC = adjudicationV376DOption(adjudicationMap, comparison.debateNumber, comparison.bundleId, choice.optionId).semanticTuple; votes.push(passC); } }
  const winner = v376dSemanticWinner(votes);
  return { bundleId: comparison.bundleId, debateNumber: comparison.debateNumber, votes: { passA: comparison.passA, passB: comparison.passB, passC }, resolved: Boolean(winner), finalSemanticTuple: winner?.value ?? null, supportingVotes: winner?.votes ?? 0, matchesProvisionalReference: winner ? matchesV376DProvisionalReference(sealed, comparison.bundleId, winner.value) : null };
});
const t = manifest.thresholds;
const structural = { initialContextsCompleted: initial.contextsCompleted === t.validInitialContexts, initialContextsValid: initial.validOutputContexts === t.validInitialContexts, adjudicationContextsValid: adjudication.validOutputContexts === disagreement.adjudicationContexts.length, preInferenceSchemaRejections: initial.preInferenceSchemaRejections + adjudication.preInferenceSchemaRejections === 0, modelOutputRetries: initial.totalRetries + adjudication.totalRetries === 0, streamRecoveries: initial.sameRequestStreamRecoveries + adjudication.sameRequestStreamRecoveries === 0, invalidInitialBundles: initial.invalidBundleCount === t.initialInvalidBundlesMaximum, audioVerificationComplete: manifest.audioPolicy.requiredAudioVerifications === manifest.audioPolicy.completedAudioVerifications, scoringFields: initial.scoringFieldCount + adjudication.scoringFieldCount === t.scoringFieldsMaximum, meteredApiCost: initial.meteredApiCostUsd + adjudication.meteredApiCostUsd === 0, transcriptionCost: initial.transcriptionCostUsd + adjudication.transcriptionCostUsd === 0 };
const semantic = { bundleCoverage: finalBundles.length === t.compositeCases, minimumInitialAgreement: disagreement.counts.agreements >= t.initialCompositeAgreementsMinimum, maximumInitialDisagreement: disagreement.counts.disagreements <= t.initialDisagreementsMaximum, finalTwoVoteConsensus: finalBundles.filter((item) => item.resolved && item.supportingVotes >= 2).length === t.finalTwoVoteBundlesRequired, unresolvedBundles: finalBundles.filter((item) => !item.resolved).length === t.unresolvedBundlesMaximum };
const passed = Object.values(structural).every(Boolean) && Object.values(semantic).every(Boolean);
assert(finalBundles.length <= t.compositeCases, "analysis exceeds frozen composite universe");
const finalContactCounts = { none: finalBundles.filter((item) => item.finalSemanticTuple?.burdenContact === null).length, support: finalBundles.filter((item) => item.finalSemanticTuple?.burdenContact?.polarity === "support").length, attack: finalBundles.filter((item) => item.finalSemanticTuple?.burdenContact?.polarity === "attack").length, motion: finalBundles.filter((item) => item.finalSemanticTuple?.burdenContact?.tier === "motion").length, central: finalBundles.filter((item) => item.finalSemanticTuple?.burdenContact?.tier === "central").length, subsidiary: finalBundles.filter((item) => item.finalSemanticTuple?.burdenContact?.tier === "subsidiary").length };
const analysis = { schemaVersion: "3.7.6-case-disjoint-burden-contact-analysis", analyzedAt: adjudication.completedAt, status: passed ? "case-disjoint-burden-contact-pass" : "case-disjoint-burden-contact-fail", warning: "This AI-only retired case-disjoint test measures repeatability of composite burden-contact choices. It does not establish human ground truth, scoring validity, assessment-prose validity, or production readiness.", sources: { executionManifestSha256: sha256(manifestText), initialExecutionSha256: sha256(initialText), initialDisagreementsSha256: sha256(disagreementText), adjudicationExecutionSha256: sha256(adjudicationText), sealedOptionMapSha256: sha256(sealedText), adjudicationOptionMapSha256: sha256(adjudicationMapText) }, results: { initial: disagreement.counts, final: { compositeCases: finalBundles.length, resolved: finalBundles.filter((item) => item.resolved).length, matchesProvisionalReference: finalBundles.filter((item) => item.matchesProvisionalReference === true).length, differsFromProvisionalReference: finalBundles.filter((item) => item.matchesProvisionalReference === false).length, contactCounts: finalContactCounts, decisions: finalBundles } }, gates: { structural, semantic }, passed, decision: { heldOutBurdenContactIntegrationGatePreregistrationAuthorized: passed, heldOutAccessAuthorized: false, benchmarkMutationAuthorized: false, largerModelBatchAuthorized: false, numericalParticipantScoringAuthorized: false, assessmentProseAuthorized: false, productionMutationAuthorized: false } };
const analysisText = `${JSON.stringify(analysis, null, 2)}\n`;
if (shouldWrite) await writeFile(path.resolve(root, manifest.artifacts.analysis), analysisText);
console.log(analysisText);
